# Fetched sources

Primary sources pulled during the research rounds and kept as fetched, so a claim that
cites one can be checked against the bytes we actually read rather than against whatever
the page says today. Nothing here is generated; nothing here is a dependency.

| File | What it is | Cited for |
|---|---|---|
| `nng-progressive-disclosure.html` | Nielsen Norman Group, *Progressive Disclosure* | The attention and disclosure rules in the plan's design section — evidence opens on demand rather than being dumped |
| `browser-company-arc-letter-2025.html` | The Browser Company, *Letter to Arc members 2025* | A shipped product publicly stepping back from its own surface; read while weighing the form-factor decision |
| `cheerpx-licensing.html` | CheerpX licensing terms | Read while considering in-browser execution; not used — the plan refuses arbitrary code execution |
| `aider-polyglot-benchmark-results.yml` | Aider polyglot benchmark, full result set | Model-capability comparison during the AI-layer and routing work |
| `npm-frontmatter-extensibility-registry.json` | npm registry document for `@frontmatter/extensibility` | The name-collision finding: the `@frontmatter` scope belongs to the incumbent |
| `d.pdf` | A 19 KB PDF fragment, one object, no page tree and no producer metadata | **Unidentified.** Fetched 2026-08-30 and never referenced by any document. Kept only because it was in the tree; delete it if nobody claims it |

Two fetches that returned nothing usable were not kept: a DPDP page that answered with a
404, and an arXiv query that answered `Rate exceeded.` Both are noted here so a later
reader does not go looking for them.
