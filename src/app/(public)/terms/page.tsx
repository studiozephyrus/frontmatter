import { PendingLegalPage } from "../pending-legal-page";

export const metadata = { title: "Terms · frontmatter" };

export default function TermsPage() {
  return (
    <PendingLegalPage
      title="Terms of service"
      summary="The agreement between you and Studio Zephyrus for the use of frontmatter, including the age floor, published pages and the grievance officer."
    />
  );
}
