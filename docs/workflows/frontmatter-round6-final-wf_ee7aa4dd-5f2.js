// SPAWN-GATE: q1=y q2=y q3=y
export const meta = {
  name: 'frontmatter-round6-final',
  description: 'Final research: core-concept, markdown-as-low-compute-app, B2B ticket-deflection, INR pricing, build-reference bibliography, screen inventory',
  phases: [{ title: 'Final research', detail: '6 agents on the last unresearched angles' }],
}
const SCHEMA = {
  type: 'object',
  required: ['report'],
  properties: {
    report: { type: 'string' },
    key_findings: { type: 'array', items: { type: 'string' } },
  },
}
const GUARD = 'HARD RULES: research only, RULE 4 — no destructive ops, no writes outside $TMPDIR, no outbound messages, never print secrets. Tag every claim [fetched] (primary opened via curl to an allowlisted host: api.github.com, github.com, raw.githubusercontent.com, registry.npmjs.org, api.npmjs.org) / [SS] (WebSearch summary) / [inference]. WebFetch may be gate-refused; fall back to WebSearch. Report only observed numbers with their source.'
const CTX = 'PRODUCT: "frontmatter" — a markdown-native editor + renderer + AI-protocol product. NOT a Notion-style project manager; NOT just a markdown editor. The bet: plain .md + YAML frontmatter as the single substrate, with heavy in-built AI and custom RENDERING (a file becomes a board / decision card / calendar / site / dashboard), where the AI works in the same file the human does — provably without corrupting a byte (a byte-preserving splice writer + a cross-engine degradation certificate already exist). Settled and NOT to re-litigate: no new markdown format (extensions are profiles over valid CommonMark that degrade gracefully). Prior rounds already covered competitor landscape, pain taxonomy, editor-feature landscape, capture loop, review loop, AI-journey-home category, foreign-vault fidelity, and format/protocol posture. Your job is ONLY the new angle assigned below.'

phase('Final research')
const r = await parallel([
  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 1 — THE FOUNDATIONAL CORE CONCEPT (the thing that makes frontmatter a category, not a feature list). The founder wants "foundational concepts we can develop — a core feature," explicitly NOT Notion-PM and NOT just an editor. Research the prior art for "a document that behaves like a small application without a runtime / without heavy compute," and extract candidate CORE PRIMITIVES frontmatter could own as CONVENTIONS (never new syntax): (1) TiddlyWiki — the 20-year master of self-contained logic-in-one-file (tiddlers, transclusion, filters, single-file wiki); read its model, what made it endure, what kept it niche. (2) org-mode (TODO-state-on-the-headline, agenda as a COMPUTED view never stored, tables-as-spreadsheet with TBLFM, babel) — which primitives are timeless. (3) Notion formulas / Coda / Airtable — the spreadsheet-logic-in-a-doc concept, and exactly where it becomes lock-in bloat to AVOID. (4) Observable / Quarto / Jupyter — reactive/executable docs, as the heavy anti-pattern. (5) Dataview / Datacore / Obsidian Bases — query-over-frontmatter as a computed view. (6) The "living document" idea (Ink&Switch, Peritext, malleable software / end-user programming literature). DELIVER: 3-5 candidate core concepts stated as ONE crisp primitive each (e.g. "the typed, rendered, self-describing document"; "the deterministic view over frontmatter"; "the reversible/append-only decision record"), each with prior-art evidence, the degradation story, and a one-line "why this is a category, not a feature." Then ONE recommendation: frontmatter\u2019s single foundational concept that everything hangs off. Return via structured output.', { label: 'core-concept', phase: 'Final research', schema: SCHEMA }),

  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 2 — MARKDOWN AS A LOW-COMPUTE APP SUBSTRATE (the founder exact ask: "how we can very trickily use Markdown for stuff that doesn\u2019t require much computation — text rendering, logic-based things"). Map precisely what app-like behavior is achievable with plain markdown + YAML frontmatter + DETERMINISTIC client-side rendering, no server, no LLM call at render time: (1) forms/inputs from task-list checkboxes and their write-back; (2) state machines from frontmatter enums (status draft->review->done) driving kanban columns / decision states; (3) tables as tiny spreadsheets (client-side sum/count/filter — how far before it needs a real engine? study Dataview inline-field + query, TiddlyWiki filters, Obsidian Bases formulas); (4) conditional / computed rendering from frontmatter flags; (5) dashboards = queries over a folder of frontmatter computed in the browser; (6) the fenced-code-block dispatch trick (a fence whose info-string names a host renderer plus args — hand the AI a data slot, not a canvas); (7) charts/diagrams from declarative specs (Mermaid, a small figure-generator vocabulary) vs anything needing a runtime. Build the COMPUTATION BUDGET MAP: a table of behavior -> renders deterministically client-side? / needs a server? / needs an LLM? — with the exact mechanism, so the plan can promise app-like surfaces while staying cheap. Note the hard boundary (spatial canvas data, real DB queries, multi-user state). Cite repos/specs. Return via structured output.', { label: 'low-compute-app', phase: 'Final research', schema: SCHEMA }),

  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 3 — THE B2B WEDGE (founder thesis: a business runs its dashboards/docs/ops from ONE markdown file, the AI generates and updates it, support-ticket load drops, and the business gets useful structured data). Research: (1) ticket-deflection economics — what companies pay for self-serve docs/KB that reduce support load; the "good docs reduce tickets" pitch (Intercom/Zendesk KB, GitBook, Mintlify, Document360, Helpjuice) and any real deflection numbers/case studies. (2) The internal-docs / runbook / status-page / changelog / ADR / onboarding-doc market — the specific DOCUMENT TYPES that are the B2B wedge (each is low-compute text+logic, exactly frontmatter shape). (3) Which BUSINESS TYPES have the everything-in-one-place, text+light-logic, agent-updatable shape: dev-tool startups, agencies/studios, consultancies, SMB SaaS, solo operators. (4) The data story: what structured data a business gets when its ops live in typed frontmatter (dashboards, rollups, audit trail) — and the compliance/audit angle (provenance, who/what/when). (5) B2B pricing anchors per seat and the procurement unlock (SSO/audit). DELIVER: 3-4 concrete B2B ICPs, each with the hook document type, the ticket-deflection or data story, the buyer, and a seat price anchor; plus the honest read on B2B-vs-D2C sequencing for a solo founder. Return via structured output.', { label: 'b2b-wedge', phase: 'Final research', schema: SCHEMA }),

  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 4 — PRICING, INR-FIRST (founder draft is Free / 299-per-month / 699-per-month in Indian rupees; 299 INR is about US$3.6, 699 INR about US$8.4 at ~83 INR/$). Research and pressure-test: (1) Is 299/699 right for the Indian prosumer/developer/writer market? What do Indians actually pay for productivity/dev/writing SaaS — the real INR bands (99 / 199 / 299 / 499 / 999), Razorpay/UPI/GST realities, the Netflix 149-199 mobile-plan anchor, JetBrains/Setapp India pricing, Indian indie-SaaS case studies. (2) The AI-COGS collision: 299 INR (~$3.6) with bundled LLM inference — does the margin survive? reconcile against the free-tier-zero-hosted-credits + BYO-key + metered-model rules from prior rounds. (3) The WORLD version: the USD/EUR tiers mirroring 299/699 without underselling (prior rounds found a $4-8 prosumer band; Obsidian $4 Sync, HackMD $5/seat — confirm current). (4) Regional/PPP mechanics (processor-level geo-pricing, Paddle/LemonSqueezy/Dodo/Razorpay MoR, the finding that UPI availability matters more than the discount). (5) Which market to target FIRST — India-first vs global-first for THIS product, with evidence (market size, WTP, payment friction, the studio being India-based). DELIVER: a recommended tier table for BOTH India (INR) and world (USD), each row justified; the margin reconciliation; and a first-market recommendation. Show the currency math. Return via structured output.', { label: 'pricing-inr', phase: 'Final research', schema: SCHEMA }),

  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 5 — THE BUILD-REFERENCE BIBLIOGRAPHY (founder: "any codebases, projects, references, resources — anything we should refer to / touch / check"). Produce the ranked, license-flagged list of exactly what to READ and what PATTERNS to take when building frontmatter. For each: repo/spec, what to take, license posture (copy? patterns only? avoid?), rough effort, and why. Cover: (1) editor substrate — CodeMirror 6 + first-party extensions (@codemirror/merge, @codemirror/lint, @codemirror/collab, lang-markdown, lezer-markdown), the Replit CM6 extension family, codemirror-copilot, y-codemirror.next; (2) markdown pipeline — remark/unified, micromark, mdast-util-*, remark-directive, github-slugger, the lezer-markdown tree for span addressing; (3) render precedents — Milkdown/Crepe, Marp, Mermaid (kanban/timeline), the Obsidian Kanban file-format parser, TiddlyWiki filter engine, Dataview query parser; (4) protocol SDKs — @modelcontextprotocol/sdk, @agentclientprotocol/sdk, Agent Trace spec; (5) fidelity/anchoring prior art — Hypothesis match-quote.ts, approx-string-match, diff-match-patch (license!), W3C TextQuoteSelector; (6) local-first / conventions references — Reflect Open, obsidian-second-brain AI-first note conventions, Letta MemFS, OKF spec, agentskills spec, llms.txt v2. Verify liveness/stars/license via api.github.com where possible. Flag GPL/AGPL/copyleft repos as pattern-only. DELIVER: the grouped bibliography table + a "read these 8 first" shortlist + a "licenses to never link" list. Return via structured output.', { label: 'build-refs', phase: 'Final research', schema: SCHEMA }),

  () => agent(GUARD + '\n' + CTX + '\n\nANGLE 6 — THE COMPLETE SCREEN / PAGE / INTERACTION INVENTORY (build on prior design findings; the founder mockups show a DASHBOARD with search + blank/templates row + recents grid; an EDITOR with left project tree, tabs, Live/Edit/Split/Read toggle, formatting bar, right rail with Outline/Tags/History/Comment/AI-Edit, an AI writing section at the bottom; a PRICING model Free/Pro/Max on NextJS+GitHub+Vercel+Firebase+GAuth). Research shipped-app patterns (Notion, Craft, Bear, Linear, Obsidian, Cursor, Granola, GitBook via WebSearch; reference Mobbin-style real shipped-app flows) and specify EVERY screen the product needs, each as: purpose, key elements above the fold, the primary interaction, the empty state, the AI affordance on it, and the one convention to copy + the one anti-pattern to avoid. Screens to cover at minimum: onboarding/first-run (value-before-auth), dashboard/home (HOME.md-as-home), the editor (all four modes), each custom-render view (kanban/decision/calendar/site), the review-loop/suggestion surface, the AI panel + inline diff, publish/share flow, version-history/local-history, doc-health/diagnostics, settings, the pricing modal, and the B2B/team dashboard view. DELIVER: the screen-by-screen inventory (a section per screen) + a global interaction-grammar summary (navigation, keyboard, how AI is summoned everywhere). Return via structured output.', { label: 'screens', phase: 'Final research', schema: SCHEMA }),
])
const ok = r.filter(Boolean)
log('round 6: ' + ok.length + '/6 returned')
return { count: ok.length, reports: ok }