// SPAWN-GATE: q1=y q2=y q3=y
export const meta = {
  name: 'frontmatter-round3-hands-on',
  description: 'Hands-on round: foreign-vault corpus widening, competitor source teardowns, Mobbin design sweep',
  phases: [{ title: 'Hands-on research', detail: '4 agents: run oracles on foreign vaults, read competitor source, mine design patterns' }],
}

const SCHEMA = {
  type: 'object',
  required: ['report'],
  properties: {
    report: { type: 'string', description: 'Full structured markdown report, every claim tagged [measured]/[fetched]/[SS]/[inference] with paths/URLs' },
    key_findings: { type: 'array', items: { type: 'string' } },
  },
}

const GUARD = `HARD RULES: research only. NO writes inside /Users/sagnikmitra/Desktop/GitHub/frontmatter or any other repo working tree; all clones/downloads/scratch files go under $TMPDIR only. No git commit/push anywhere, no outbound messages, no destructive ops (RULE 4). Allowlisted network hosts for curl/git: github.com, api.github.com, raw.githubusercontent.com, codeload.github.com, registry.npmjs.org, api.npmjs.org, api.mobbin.com. Never print secrets. Tag every claim: [measured] = you executed it, [fetched] = you opened the source, [SS] = search summary, [inference]. Report only observed numbers.`

phase('Hands-on research')
const results = await parallel([
  () => agent(`${GUARD}\n\nTASK — FOREIGN-VAULT CORPUS WIDENING (kills the single-author weakness W1 the research record itself flags). The splice writer's 907/907 and the corpus blind-spot findings were measured on ONE author's vault. Do: (1) find 4-6 PUBLIC markdown vaults/digital gardens on GitHub by DIFFERENT authors (api.github.com search: topic:obsidian-vault, topic:digital-garden, 'obsidian vault' in name — pick real personal vaults with 100+ md files, diverse authors/languages); (2) shallow-clone each into $TMPDIR (git clone --depth 1 https://github.com/OWNER/REPO); (3) SHAPE SCAN each: count files with (a) UTF-8 BOM first bytes EF BB BF, (b) CR-only line endings, (c) YAML frontmatter opening, (d) non-ASCII top-level frontmatter keys (outside [A-Za-z0-9_.$-]), (e) '...' document-end markers, (f) CRLF — write a small python3 script reading raw bytes; (4) ROUND-TRIP ORACLE on each frontmatter-bearing file: from /Users/sagnikmitra/Desktop/GitHub/frontmatter run 'node --import ./scripts/ts-resolve.mjs' with a small $TMPDIR runner that imports spliceFrontmatterValue via ./scripts/load-splice.mjs (read that file first for the import shape), performs set public_slug='audit-test' then restore-or-delete (copy the logic pattern from scripts/fm-roundtrip-audit.mjs — read it), byte-compares; report per-vault identical/changed/threw/refused counts; (5) also run the 20260828 shape scan against /Users/sagnikmitra/.claude/skills-src (474 files, second author per the record) as the named-but-never-run second corpus. DELIVER: per-vault table (author, files, shapes found, round-trip results), the verdict on whether 907/907-class behavior holds on foreign multi-author data, and any NEW failure shapes discovered (with reproducing file paths in $TMPDIR). This is LR#68 territory: a failure found here is a finding, not an embarrassment. Return via structured output.`, { label: 'foreign-corpus', phase: 'Hands-on research', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nTASK — OPENKNOWLEDGE DEEP TEARDOWN (the #1 competitor: inkeep/open-knowledge, GPL-3.0, ~100 releases/week). Do actual source reading, not metadata: (1) fetch the npm tarball metadata (registry.npmjs.org/@inkeep/open-knowledge) — current stable version, dist.tarball URL; download + extract the tarball into $TMPDIR (npm pack @inkeep/open-knowledge --pack-destination $TMPDIR or curl the dist.tarball — registry host is allowlisted); ALSO/alternatively clone the GitHub repo shallow into $TMPDIR; (2) READ the source for: (a) editor architecture (which editor engine — ProseMirror/CodeMirror/custom? WYSIWYG model?), (b) how they WRITE markdown back — do they regenerate from an AST (lossy) or splice? Find the serializer and test it if runnable: does a no-op edit preserve bytes (YAML key order, quotes, comments)? This is THE question — their fidelity posture vs ours; (c) the comments/anchoring implementation state (packages/server/src/comments/anchor.ts lineage — what changed since the 261-line version); (d) the ACP/agent layer surface (what agents can do); (e) rendering: which markdown pipeline, which extensions, any degradation handling; (3) their vault/file model: single wiki? folders? git integration depth? (4) license boundaries: what GPL-3.0 means for us (can't copy code; patterns are free). DELIVER: the teardown card — architecture, the byte-fidelity verdict with test evidence if executable, what they structurally cannot do without a rewrite, and the 3 sharpest differentiation claims frontmatter can make against them that source evidence supports. Return via structured output.`, { label: 'openknowledge-teardown', phase: 'Hands-on research', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nTASK — HUBBLE.MD + FRONT MATTER CMS SOURCE TEARDOWNS (two lighter cards, same method). A) bholmesdev/hubble.md ("the best notepad for you and your agents", 1,441 stars): shallow-clone into $TMPDIR, read source: editor engine, storage model (local files? where?), the dual-surface design (React UI for humans / plain md for agents — how do they keep them consistent?), write-path fidelity (AST-regenerate or preserve?), agent integration (skills? MCP? file conventions?), collab/sync story, license. B) estruyf/vscode-front-matter (Front Matter CMS, the name-collision senior user): clone or raw-read key source: what it actually DOES to frontmatter (does its UI regenerate YAML? — test or trace the writer code; if it mangles YAML like Obsidian's processFrontMatter, that is a live differentiation fact against the product that OWNS our name), its data model (frontmatter.json config, content-types), its dependency on VS Code. DELIVER: two teardown cards with the same fields as the OpenKnowledge card (architecture / write-fidelity verdict with code evidence / structural gaps / differentiation claims), plus one paragraph: what these three teardowns jointly say about where the write-path fidelity moat really stands. Return via structured output.`, { label: 'hubble-fmcms-teardown', phase: 'Hands-on research', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nTASK — DESIGN-PATTERN SWEEP VIA MOBBIN (the design-research avenue nobody has run; api.mobbin.com is allowlisted and Mobbin MCP tools exist — load them via ToolSearch: select:mcp__mobbin__search_screens,mcp__mobbin__search_flows,mcp__mobbin__search_sections). Research the UI patterns for the surfaces the master plan commits to: (1) HOME/dashboard surfaces in productivity/notes apps — search screens for recents grids, template galleries, daily-note hubs, empty states in note apps (Notion, Craft, Bear class); what do best-in-class home surfaces show above the fold; (2) EDITOR chrome — minimal markdown/writing editor layouts, mode toggles, formatting bars: how do premium writing apps keep chrome invisible; (3) AI-in-editor patterns — inline AI affordances, suggestion review UIs, accept/reject flows, AI provenance/attribution displays if any exist; (4) KANBAN/board views in document tools; (5) publish/share flows (one-toggle publish patterns). For each: 3-5 concrete referenced patterns with mobbin_url citations, what convention vs taste, and a specific recommendation for frontmatter's mockups (the founder's wireframes show: dashboard with blank+templates row + recents grid; editor with tree/tabs/mode-toggle/right-rail; pricing modal). Budget: keep image loads tight (limit 6 per query, escalate only on miss). If Mobbin tools are unreachable, fall back to [SS] pattern research and say so. DELIVER: the pattern cards + recommendations mapped to each mockup surface. Return via structured output.`, { label: 'mobbin-design', phase: 'Hands-on research', schema: SCHEMA }),
])

const ok = results.filter(Boolean)
log(`round 3: ${ok.length}/4 agents returned`)
return { count: ok.length, reports: ok }