---
id: S10
title: S10. Problems
mode: reference
tier: canonical
status: specified
spec: problems/panel
verified_against: 0af3c90
updated: 2026-09-18
owner: sagnik
---

# S10. Problems

> A checker and a formatter in the box, so nobody installs eleven tools to find a broken link. The
> founders said build what is there, and added one rule about mixing machine work with opinion.

> **Ids invented here.** `14-COMPONENT-INVENTORY.md`, `16-COPY-DECK.md`,
> `17-ERROR-AND-REFUSAL-CATALOGUE.md`, `19-ACCEPTANCE-CRITERIA.md`,
> `55-MEASUREMENT-AND-EVENTS.md` and `56-OPEN-DECISIONS.md` are being written in parallel and did
> not exist when this file was written. Ids follow `65-CONVENTIONS.md` section 3 on a stated
> scheme derived from the screen number: components `C100` to `C109`, errors `E100` to `E109`,
> acceptance `A100` to `A109`. A component owned by another screen is cited by that screen's id.
> Copy ids are namespaced `K.s10.*`, so they cannot collide.

> **Error ids are provisional, and they clash.** `17-ERROR-AND-REFUSAL-CATALOGUE.md` now exists and
> is the home for every error id. It allocates by **category**, not by screen: engine `E001` to
> `E025`, validation `E030` to `E041`, permission `E050` to `E060`, quota `E070` to `E079`, model
> `E080` to `E087`, provider `E090` to `E097`, network `E100` to `E104`, conflict `E110` to `E117`.
> The `E` ids below were allocated from this screen's own block before that register existed, so
> many of them name a different error there. **Read them as descriptions, not as keys**, and
> reconcile each one into its category block. Every row states what the error is, so the mapping is
> mechanical. Component, acceptance and copy ids are unaffected.

## Purpose

Show what is wrong with the open document, keep a deterministic finding apart from an opinion, and
charge nothing for the half that needs no model.

## The filter, and how the founders' instruction landed here

`docs/mvp0/SCREEN-CHANGES-2026-09-18.md:80` asked for two toggles, given in the review's own words
as human reviews and AI reviews. `:296` then generalised it:

> Toggles everywhere, on both S10 and S20. Wherever human and AI work sit together, split them with
> a toggle rather than combining them into one list.

**The general rule is the instruction; the labels are per screen.** On S20 the split is People
against AI and agents, because that screen holds work by both. On this screen nobody is reviewing
anything, so the same rule splits the two kinds of finding instead.

**The filter is All, Checks, Writing.** `docs/mvp0/SCREENS.md:129` states it and gives the reason: a
deterministic finding and a model's opinion are never mixed into one list.

Segment | What is in it | Blocks | Costs
All | everything, in document order | no | nothing beyond what the two below cost
Checks | structural findings, each one true or false | no | **nothing, ever**
Writing | advisory notes on the prose | **never** | see D28

## Entry and exit

Direction | Route or control | Goes to | Notes
Arrive | the problem count pill in `C043` | the panel opens in the right rail | still S04, a panel not a route
Arrive | the Problems collapsible in `C049` | the same | one of the rail's rows
Arrive, phone | More in the bottom bar, then Problems | `C109`, the right drawer | a drawer, not a page
Leave, a finding | click a row in `C102` | the caret at that line in the document | the line is scrolled to, never rewritten
Leave, Rules | `C107` | the rules editor for this project | rules live in the project, as a file
Leave, Fix all safe | `C106` | applies only the fixes that cannot be wrong | see below
Leave, close | the collapsible, or Escape | the rail returns to the outline | findings are recomputed on reopen

## Anatomy

Region | Component | What it holds | Notes
Problems panel | `C100` | The filter, the list, and the cost note | `.rsec.grow` in the right rail
Filter segment | `C101` | All, Checks, Writing, each with its count | `.fseg`; `filterseg()` at `docs/mvp0/screens/gen.mjs:716`
Problem row | `C102` | A dot, a title, one line of why, and a line number | `.prob`
Severity dot | `C103` | Error, warning or advisory | `.dot` with `.err`, `.warn` or `.info`
Line reference | `C104` | The line, as `L` and a number | `.ln`; a link into the document
Cost note | `C105` | The sentence that says the structural checks are free | under the list, always visible
Fix all safe | `C106` | One control, applying only unambiguous fixes | `.railfoot`, paired with Rules
Rules | `C107` | Opens the project's rule set | a file in the project, not a hidden setting
Count pill | `C108` | The number of findings, on the toolbar | `.pill` in `C043`
Phone drawer | `C109` | The same panel as the right drawer | `pdrawer(..., 'right')` at `docs/mvp0/screens/gen.mjs:752`

### The checks that ship, and what each costs

The plan's reason for shipping a checker at all is that markdownlint has 12,180,750 installs on
Visual Studio Code and Prettier has 71,626,148, so a checker and a formatter are expected rather
than impressive.

Check | Kind | Severity | Needs a model | Cost
Link goes nowhere | structural | error | no | nothing
Heading level skips | structural | warning | no | nothing
Image has no alt text | structural | warning | no | nothing
Table row width does not match the header | structural | warning | no | nothing
Spelling, against a project dictionary | structural | warning | no | nothing
Front matter schema | structural | warning | no | nothing
A sentence far longer than the rest | advisory | advisory | see D28 | see D28

- **Spelling and the front matter schema are in because they are the two most-installed checks in
  the world and neither needs a model.** That is the plan's own reasoning and it is why they are not
  a later feature.
- The drawn example carries five findings, four Checks and one Writing, each one true of the
  document beside it. A panel that shows a finding the document does not have is worse than no
  panel.

### The sales line, and why it is on the screen

**Structural checks run on the device and cost nothing.** The plan states the alternative: eleven
separate tools, four package managers and nine configuration files to get this elsewhere, or open
the file.

`C105` carries that fact in one sentence. It is on the screen rather than on a marketing page,
because the moment a person is looking at a list of problems is the moment the claim is checkable.

### Fix all safe

Safe means the fix cannot be wrong. A fix qualifies only when the correct replacement is derivable
from the document alone.

Finding | Safe to fix automatically | Why
Table row width | yes | the header defines the width
Heading level skip | yes | the missing level is determined by its neighbours
Trailing whitespace and fence hygiene | yes | formatting, with no meaning
Image alt text | **no** | only the author knows what the image shows
Broken link | **no** | the intended target is a guess
Spelling | **no** | a name is not a misspelling
A long sentence | **no** | it is an opinion

- **Fix all safe states the count before it acts**, and applies the fixes as one splice batch with
  one undo step.
- If nothing is safe, it says so rather than doing nothing silently, which is `E102`.

## Data contract

What | Direction | Port or store | Shape | When
Open file bytes | in | `DocumentReader.open(path)` | `{bytes, version, headHash}` | on mount
Findings | in | `CheckRunner.run(bytes, rules)` | findings with byte ranges and severities | on parse, debounced
Link targets | in | `GraphReader.resolve(link)` | a path, or nothing | per link finding
Project dictionary | in | a file in the project | a word list | on mount, and when it changes
Front matter schema | in | a file in the project | a schema | on mount
Rules | in and out | a file in the project | the rule set | read on mount, written from `C107`
A fix | out | `DocumentWriter.splice()` | a byte range and its replacement | on Fix all safe, or a row's own fix

- **`CheckRunner` runs on the device.** No document bytes leave the machine for the Checks segment,
  which is what makes it free and what makes it work offline.
- **The rules are a file in the project**, so they travel with the documents, can be reviewed in a
  pull request, and are not a hidden account setting.
- The dictionary is a file too, for the same reason. A name added on one machine is a name
  everywhere.

## Actions

Action | Trigger | What happens | Failure behaviour | Event id
Open the panel | the count pill, or the collapsible | runs the checks and lists them | a check threw `E100`; the document is too large `E107` | `problems.opened`
Change the filter | tap `C101` | re-filters in place, no re-run | none | `problems.filter.changed`
Open a finding | click a row | the caret at that line | the range moved under it, so the row is recomputed rather than jumping wrongly | `problems.finding.opened`
Fix one | a row's own fix, where it has one | one splice | the range moved `E103` | `problems.fix.applied`
Fix all safe | `C106` | states the count, then one batch splice, one undo step | nothing is safe `E102`; a range moved `E103` | `problems.fixall.run`
Open Rules | `C107` | the project's rule file | the rules file is invalid `E101`; an unknown rule `E108` | `problems.rules.opened`
Add a word to the dictionary | a spelling row's own action | appends to the project dictionary | the dictionary could not be read `E104` | `problems.dictionary.added`
Re-run | an edit to the document | recomputes, debounced | as above | `problems.run`

## States

State | When | What is shown | What the person can do
first-run | the first time the panel opens | the list, plus `C105` | read, fix, open Rules
empty | no findings | a plain line saying the document is clean, with the filter still visible | keep writing
loading | the first run on a large document | the filter, and the list area reserved | keep writing; checks never block typing
partial | Checks done, Writing not | the Checks findings, and the Writing count pending | filter to Checks and work
offline | no network | **everything in Checks works** | fix; Writing is unavailable and says so
unauthorised | the role cannot edit | findings shown, fixes disabled | read, export
over-cap | over the credit cap | Checks unaffected; Writing disabled with the reason | fix the structural findings, which are free
AI-unavailable | the chain is down | Checks unaffected; the Writing segment says so | the same
conflict | the document has an unresolved conflict | the panel opens; fixes are disabled | resolve on S31 first
error | a check threw | that check's findings are absent and `E100` names which check | the other checks still ran
degraded | the dictionary or the schema is missing | those checks are skipped and say so, rather than flooding the list | add the file, or ignore

**A failed check never removes the other checks' findings.** One broken rule does not blank the
panel.

## Copy

`K.s10.filter.all`, `K.s10.filter.checks`, `K.s10.filter.writing`, `K.s10.costnote`,
`K.s10.fixallsafe`, `K.s10.fixallsafe.count`, `K.s10.fixallsafe.none`, `K.s10.rules`,
`K.s10.clean`, `K.s10.check.brokenlink`, `K.s10.check.headingskip`, `K.s10.check.noalt`,
`K.s10.check.tableshape`, `K.s10.check.spelling`, `K.s10.check.frontmatter`,
`K.s10.writing.longsentence`, `K.s10.adddictionary`, `K.s10.offline.writing`,
`K.s10.check.failed`.

`K.s10.costnote` is the sales line. It says the structural checks run on the device and cost
nothing, and that the writing note is advisory and never blocks.

## Keyboard and focus

- The panel opens with focus on the filter, because the first decision is which half to look at.
- Up and down move through findings. Enter opens the focused one in the document.
- Left and right move between the three filter segments.
- Escape closes the panel and returns focus to the caret.
- There is no shortcut that applies a fix without a visible confirmation of what it will do.
- The full map is `15-INTERACTION-AND-KEYBOARD.md`.

## Responsive

- The panel becomes the right drawer, over a backdrop, reached from More in the bottom bar.
- The filter stays. It is three short words and it is the founders' instruction, so it is not the
  thing that gets dropped for width.
- Fix all safe stays in the drawer's foot; Rules moves into the drawer's own overflow.
- A finding's line number stays visible, because it is how a person finds the place on a small
  screen.

## Desktop differences

- The checks run on the device in both builds. The desktop build has no document-size ceiling, so
  `E107` is a web-only state.
- The Writing segment can run on a local model on the desktop, so it is available offline there.

## Acceptance

Id | What it asserts | Test id
A100 | The filter renders exactly three segments: All, Checks, Writing | T-S10-01
A101 | A structural finding and an advisory note never appear in one unfiltered group without the filter present | T-S10-02
A102 | Running the Checks segment sends no document bytes off the device | T-S10-03
A103 | Running the Checks segment writes no ledger entry and decrements no credit | T-S10-04
A104 | Every finding in the drawn example is true of the document beside it | T-S10-05
A105 | A Writing note never blocks a save, an export or a publish | T-S10-06
A106 | Fix all safe states the count before applying, and applies as one undo step | T-S10-07
A107 | Fix all safe never fixes alt text, a broken link or a spelling | T-S10-08
A108 | A check that throws removes only its own findings | T-S10-09
A109 | Checks run with the network disabled | T-S10-10

A103 is the one that proves the sales line. It needs a red proof against a build that routes a
structural check through the model chain.

## What this screen must never do

- **Never mix a deterministic finding with an opinion in one undifferentiated list.** That is the
  founders' rule, generalised on 18 September to every screen where the two kinds sit together.
- **Never charge a credit for a structural check.**
- **Never send document bytes off the device to run a structural check.**
- **Never let a writing note block a save, an export, a publish or a commit.**
- **Never auto-fix something only the author can know**: alt text, a link target, a proper noun.
- **Never apply a batch of fixes without stating the count first.**
- **Never blank the panel because one check failed.**
- **Never hide the rules in an account setting.** They are a file in the project.
- **Never invent a finding.** Every row is true of the open document at the line it names.
- **Never use an emoji as a severity marker.** The dot is a shape and a colour, with text beside it.

## Open questions

Id | Question | Where it came from
D28 | Is the Writing segment model-backed or rules-based, and if it is model-backed, what does it cost? | `docs/mvp0/SCREENS.md:129` calls it a model's opinion, while the plan's reasoning cites our own rules-based gate as the precedent for advisory-never-blocking. The two imply different costs, and `C105` cannot be written honestly until this is settled
D29 | Does Fix all safe include formatting, or is the formatter a separate control? | the plan's section 6 names a checker and a formatter as separate things, and this screen draws one control
D30a | Where does a per-project rule file live, and what is it called? | the plan says rules travel with the project and names no path

**On D30a the id carries a suffix letter**, because `D30` is already taken by S14. `65-CONVENTIONS.md`
section 1 allows inserting with a suffix letter and forbids renumbering.

## Limits of this file

- **Not assessed:** the formatter. It is named in the plan's section 6 and is not drawn on this
  screen.
- **Not assessed:** the rules editor's own surface.
- **Could not be verified:** the two install counts. They are quoted from
  `docs/mvp0/PRODUCT-PLAN.md` rather than re-fetched from the marketplace in this session, and the
  plan's own rule is to re-derive a number at write time. `UNVERIFIED:` as of today.
- **What would falsify this file:** a decision that the Writing segment costs a credit, which would
  make `C105` wrong as written and would need the cost stated beside the segment.
