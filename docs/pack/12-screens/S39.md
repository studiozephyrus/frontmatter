---
id: S39
title: S39. Sheet
mode: reference
tier: canonical
status: specified
spec: sheets/grid
verified_against: cb7c16f
updated: 2026-09-20
owner: sagnik
---

# S39. Sheet

> **Where the ids come from.** Every `F`, `C`, `E`, `A`, `D` and `K` id below lives in its register:
> `10-FEATURE-REGISTER.md`, `14-COMPONENT-INVENTORY.md`, `17-ERROR-AND-REFUSAL-CATALOGUE.md`,
> `19-ACCEPTANCE-CRITERIA.md`, `56-OPEN-DECISIONS.md` and `16-COPY-DECK.md`. This file first carried
> placeholders; they were allocated on 19 September, and `tools/new-ids-allocation.md` maps each one.

> **What this screen draws.** The format, the formulas and the refusals belong to
> `68-SHEETS-SPEC.md`. This file says only how a person meets them. Where the two disagree,
> `68` wins and this file is the one to fix.

## Purpose

Work with a table in my document the way I would in a spreadsheet, and still have a plain
markdown table in the file that every other tool can read.

## The rules this screen carries

- **A sheet is a table in a document, not a new file type.** The grid edits a GFM pipe table. Its
  formulas live in an `fm-sheet@1` block directly under the table (`68-SHEETS-SPEC.md` sections 2.1
  and 3).
- **Formulas name columns, never cells.** The formula bar reads `= Qty * Price`. There is no `B2`
  anywhere on the screen, because an address like that goes stale when a row is added
  (`68-SHEETS-SPEC.md` section 4, and section 10's never-build list).
- **Viewing never writes.** Sort, filter, row numbers, column widths and the frozen header all
  change the view and never the file (`68-SHEETS-SPEC.md` section 6.1). The toolbar says so in one
  line, so nobody has to wonder.
- **Typing into a cell changes that cell's bytes and nothing else.** The diff for one edit is one
  line (`68-SHEETS-SPEC.md` section 5.1).
- **A summary is drawn and never written.** The `sum` under the grid comes from a `foot.` formula and
  is not a row in the file.

## Entry and exit

Direction | How | Route or control
In | A `.md` file whose table is opened full width, from the tree or a tab | `C083`, `C078`
In | Open as a sheet, on an embedded table in a document | `C161`
In | A `.csv` or `.tsv` file from the tree | `C083`. Opens in the same grid (`68-SHEETS-SPEC.md` section 2.2)
In | A search result that lands in a table cell | the cell is focused, never rewritten
Out | The MD side of the Grid and MD switch | the table's source, in place. Same file, a different projection
Out | Any tree row or tab | `C083`, `C078`
Out | The document the table lives in, for a table opened from one | back to that document, scrolled to the table
Out | Share | `C076`, then S17. A published sheet follows `68-SHEETS-SPEC.md` section 6.3

## Anatomy

The desktop frame is `s39-sheet`, drawn at `docs/mvp0/screens/gen.mjs:2259`. The embedded frame is
`s39-sheet-in-doc`, at `docs/mvp0/screens/gen.mjs:2287`.

Region | Component | What it holds | Notes
Workspace header | `C076` | The usual header | `top()` at `docs/mvp0/screens/gen.mjs:891`
Tab strip | `C078` | The sheet's tab beside the document tabs | A sheet is a document, so it takes an ordinary tab
Left rail | `C083` | The tree, with the sheet's file selected | `tree()` at `docs/mvp0/screens/gen.mjs:918`
Sheet toolbar | `C155` | The table's name and size, the sort chip, the filter chip, one line saying the view never writes, and the Grid and MD switch | `SHBAR` at `docs/mvp0/screens/gen.mjs:2255`. Replaces `C077` while a table is open full width
Sort chip | `C156` | The active sort, named in words: the column and the direction. A close control clears it | `.chip.on`. **A sort chip is a view, never a write.** Sorting the file is a separate action, `F285`
Filter chip | `C156` | Opens the filter builder | Held per viewer (`68-SHEETS-SPEC.md` section 6.1)
Grid and MD switch | `C157` | Two positions: the grid, and the table's markdown source | `.seg.tight`, as the MD and Doc switch of `C077` is drawn
Formula bar | `C158` | The focused cell named by its column and its row's first cell, a function mark, and the column's formula | `FXBAR` at `docs/mvp0/screens/gen.mjs:2258`. Reads `Total, Design hours` and `= Qty * Price`. **It never shows an A1 address**
Grid | `C045` | Header row, row numbers, cells, the add-column control, the add-row row, the summary row | `sheetGrid()` at `docs/mvp0/screens/gen.mjs:2227`. `C045` EditableTable today re-serialises the whole table; see Data contract
Column header | `C045` | A type mark, the name, and a sort arrow on the sorted column | Types drawn: text, number, formula, checkbox, date
Computed column | `C045` | The column a `col.` formula fills, tinted and marked with the function icon | `td.calc`. The values in it are written into the file
Row numbers | `C045` | A muted number down the left | View only, never written. `SCREEN-REFERENCES.md` section 2 marks the convention `UNVERIFIED:`
Summary row | `C159` | `foot.` results under the grid, one per column that has one | `tfoot`. **Drawn, never written**
Add column, add row | `C045` | A plus at the end of the header, and a row at the foot | `.addcol`, `.addrow`
Formulas section | `C035` | The `fm-sheet@1` block as text, in the rail below the outline | `rail({ extra })` at `docs/mvp0/screens/gen.mjs:988`, with `FM_SHEET` at `docs/mvp0/screens/gen.mjs:2242`
Embedded sheet | `C160` | In a document: the same toolbar, compact, with Open as a sheet, the grid, and a line naming the formula block under it | `DOC_EMBED` at `docs/mvp0/screens/gen.mjs:2279`
Phone frame | `C086` | The header with the mode segment, a chip row, a note that more columns are a swipe away, and the grid with the first column pinned | `phone()` at `docs/mvp0/screens/gen.mjs:1022`
Phone cell editor | `C068` | A bottom sheet: the column name, the row, the value in a large field with its type, the values that depend on it, Previous, Next and Save | `psheet()` at `docs/mvp0/screens/gen.mjs:1037`, `.cellsheet` at `docs/mvp0/screens/gen.mjs:2271`

**What the drawing holds, so nobody re-derives it.** Six rows from `launch-costs.md` in the
Zephyrus booking project, sorted by Total, high to low, in the view only. Each Total is Qty times
Price, and the sum under the grid is 38720: 900 + 300 + 320 + 19200 + 15000 + 3000. The row
`WhatsApp template messages` is 400 times 0.8, which is exactly 320 in decimal, the case
`68-SHEETS-SPEC.md` section 4.5 exists for. On the phone, Price for Design hours is being changed
from 1200 to 1100, so its Total becomes 16 times 1100, which is 17600.

## Data contract

What | Direction | Port or store | Shape | When
Table bytes | read | `ProjectFiles.read(path)` | The document, from which the table and the block under it are parsed | On open
Parsed sheet | derived | `parseSheet` in the domain layer | `{ headers, rows, formulas: { col, foot }, types }` | On every read. Stateless, so the grid is a projection
Computed values | derived | `evaluateSheet` in the domain layer | One value or one error per computed cell, and one per summary | On every read. Deterministic, `68-SHEETS-SPEC.md` section 4.7
A cell edit | write | `ProjectFiles.splice(path, cellRange, bytes)` | One cell's bytes, escaped per `68-SHEETS-SPEC.md` section 5.3 | On Enter, Tab, blur or Save
A computed cell after an edit | write | the same splice, one per dependent cell | One cell's bytes each | With the edit that caused it
A structure edit | write | the same splice | One line for a new row; every row for a new column (`68-SHEETS-SPEC.md` section 5.4) | On the control
View state | read and write | per viewer, local only | `{ sort, filter, widths, frozen }` | Never sent, never written to the file
Entitlement | read | `limitsFor(account)` | Whether the document may be written | On open

**None of this is built as drawn.** At `cb7c16f` the shipped `C045` writes through `setTableCell`,
which re-serialises the whole table. `68-SHEETS-SPEC.md` section 5.1 names the three defects that
break the one-cell rule, `TD-024`, `TD-025` and `TD-026`, and says the grid cannot ship until they are
fixed. There is no evaluator and no `fm-sheet@1` parser in `src/`.

## Actions

Action | Trigger | What happens | Failure behaviour | Event id
Edit a cell | Type into a focused cell, then Enter or Tab | That cell's bytes are spliced. Dependent computed cells are recomputed and spliced one each | Row width differs from the header's, so `E500` refuses and nothing is written | `sheet.cell.edited`
Type a line break into a cell | Shift Enter in a pipe-table cell | Refused. GFM cells are single-line | `E513` | `sheet.edit.refused`
Sort the view | The sort chip, or a column header's menu | The rows are drawn in the new order. The file is untouched | none | `sheet.view.changed`
Filter the view | The filter chip | Rows that do not match are hidden from this viewer | none | `sheet.view.changed`
Sort the file | The column menu's Sort the file, kept apart from the chip | One queue item that reorders the rows between the first and the last that moved | Any row cannot be located, so `E026` refuses and the file is unchanged | `sheet.file.sorted`
Add a row | The add-row row, or Enter on the last row | One new line under the focused row | none | `sheet.structure.changed`
Add a column | The plus at the end of the header, then a name and a type | One cell appended to every row and to the delimiter row | A name that another header already has, so the column is not added and the reason is shown | `sheet.structure.changed`
Set a row formula | The formula bar, on a column | A `col.` line is added or changed in the block under the table, then the column is computed | A formula that refers to itself, so `E514`; an unknown function, so `E517` | `sheet.formula.set`
Show the source | The MD side of the switch | The table and its `fm-sheet@1` block shown as markdown, in place | none | `sheet.source.toggled`
Open as a sheet | On an embedded table | The same table full width, in its own view of the same file | none | `sheet.opened`
Toggle a checkbox cell | Click or Space on a `[ ]` or `[x]` cell | That cell's three bytes change | none | `sheet.cell.edited`

**A formula that meets text shows an error in that cell and writes nothing.** A cell such as `1,200`
is text by `68-SHEETS-SPEC.md` section 4.4, so the row's Total shows `E515`
and its bytes in the file are left as they were.

## States

State | When | What is shown | What the person can do
first-run | The first time a table is opened as a sheet on this account | One line under the toolbar: the view never writes, and a cell you type into does | Read it, then work.
empty | A table with a header and no rows | The header, the add-row row, and an empty summary | Add a row.
loading | The document is being read | The toolbar and the header render. Rows fill in | Wait.
partial | Some computed cells cannot be evaluated | Those cells show their error. Everything else computes | Fix the input cells.
offline | No connection, web only | The grid is editable. Edits queue as the workspace does on S24 | Keep working.
unauthorised | The viewer's role may not write | The grid is readable. Cells do not take focus for editing. Sort and filter still work | Read, sort, filter.
over-cap | The account is over its document cap, S33 | Readable and exportable. Edits that would create a document are blocked; edits to this one are not | Carry on, or free space.
AI-unavailable | n/a. Nothing on this screen calls a model | n/a | n/a.
conflict | A computed cell in the file disagrees with its formula, because it was edited elsewhere | Both values shown, and a recompute offered as a queue item. **Opening never writes** (`68-SHEETS-SPEC.md` section 7) | Accept the recompute on S20, or leave it.
error | The `fm-sheet@1` block cannot compute | The block renders its source and the reason, `E501`. The table stays editable as a plain table | Fix the block in MD.
degraded | More rows than fit the keystroke budget | `UNVERIFIED:` the threshold is unmeasured, `A820`. needs: the `A820` benchmark on the built grid. No number is drawn until one is measured | n/a until measured.

## Copy

Every string lives in `16-COPY-DECK.md`, section 14r.

id | what it carries
`K.s39.rows` | The table's size in the toolbar, rows and columns.
`K.s39.sort` | The sort chip, naming the column and the direction in words.
`K.s39.filter` | The filter chip.
`K.s39.viewnote` | That sort and filter are the viewer's own and the file keeps its order.
`K.s39.grid` | The Grid side of the switch.
`K.s39.md` | The MD side of the switch.
`K.s39.fxref` | The formula bar's cell name: the column, then the row's first cell.
`K.s39.fxhint` | That a row formula writes each result into its own cell.
`K.s39.summary` | The summary row's label.
`K.s39.addrow` | The add-row control.
`K.s39.addcol` | The add-column control's accessible name.
`K.s39.viewonly` | That row numbers, widths and the frozen header are view settings.
`K.s39.formulas` | The rail section's heading.
`K.s39.formulas.note` | That the formulas sit under the table and the sum is never written.
`K.s39.open.full` | Open as a sheet, on an embedded table.
`K.s39.embed.foot` | The embedded table's foot line naming the formula block.
`K.s39.swipe` | The phone's note that more columns are a swipe away.
`K.s39.cell.save` | The cell editor's line saying only this cell and its dependants change.
`K.s39.firstrun` | The first-run line.

**Reused unchanged.** `K.s08.table.shape` for the `E500` refusal, `K.common.saved`,
`K.common.outline`, `K.common.done`.

## Keyboard and focus

key | does
Arrow keys | Move the focused cell
Enter | Edit the focused cell, then commit and move down
Tab, Shift Tab | Commit and move right or left
Escape | Cancel the edit in progress; a second Escape leaves the grid for the toolbar
Space | Toggles a focused checkbox cell. Elsewhere it is the AI launcher's key, see below
Cmd or Ctrl Enter | Commit without moving
Cmd or Ctrl Shift M | The Grid and MD switch. `[O]` checked 19 Sep: no row in `15-INTERACTION-AND-KEYBOARD.md` and no binding under `src/`. `@codemirror/lint` 6.9.6 binds `Mod-Shift-m` to its lint panel (`node_modules/@codemirror/lint/dist/index.js:294`), a keymap `src/` does not load today. Needs a row in `15`, and a new chord if that keymap is ever loaded

**Focus on arrival is the first cell of the first row**, not the toolbar, so typing starts at once.

**Space conflicts, and the grid wins while it has focus.** The workspace draws Space as the Ask AI
key (`rail()` at `docs/mvp0/screens/gen.mjs:988`). Inside a focused grid, Space toggles a checkbox
or types a space. The AI launcher stays reachable from its button. Recorded for
`15-INTERACTION-AND-KEYBOARD.md`'s owner.

**No bare letter changes the file.** Typing starts an edit of the focused cell, which is the
spreadsheet convention, and it is committed only by Enter, Tab or blur.

## Responsive

- **The first column is pinned** and the rest scroll sideways. A one-line note says which columns are
  a swipe away.
- **A cell is edited in a bottom sheet**, `C068`, never in the cell itself: a large field, its type,
  the values that depend on it, Previous, Next and Save. `SCREEN-REFERENCES.md` section 2 records
  this as `INFERENCE:` from Airtable's expanded record.
- **The formula bar is folded into the sheet's header of the editor**: the focused column's formula
  appears in the cell editor rather than above the grid.
- **Rows are 40 px tall** so a thumb can hit one.
- **Not on the phone:** adding a column and setting a formula. Both need the desktop or a tablet.

## Desktop differences

- **A `.csv` on disk opens in place**, because the desktop build reads the file system.
- Everything else is identical.

## Acceptance

These rows are in `19-ACCEPTANCE-CRITERIA.md` section 12d, with its test ids.

id | criterion | test
`A815` | A cell edit in a padded table changes one line and only that cell's bytes | `T815`
`A817` | Opening a sheet whose computed cells disagree with their formula writes no byte | `T817`
`A821` | Sorting, filtering, resizing a column and toggling Grid and MD leave the file bytes equal | `T821`
`A822` | No control, label or formula on this screen shows a cell address such as `B2` | `T822`
`A823` | A `foot.` summary appears under the grid and nowhere in the file | `T823`
`A818` | The same bytes give the same computed values on every machine | `T818`
`A824` | 400 times 0.8 is written as `320` | `T824`

## What this screen must never do

- **Never show an A1 address**, in the formula bar or anywhere else.
- **Never write when a person only looks**: sort, filter, widths, row numbers and freezing are views.
- **Never re-pad or re-align a table** to make it tidy. A one-cell edit is a one-line diff.
- **Never write a summary into the file.**
- **Never write on open**, even when a computed cell is wrong. Offer the recompute as a change.
- **Never accept `=` typed into a cell as a formula.** It is text, and a formula lives in the block.
- **Never hide a column from a reader of a published sheet.** A filter is not a wall.
- **Never merge cells, colour cells or add a second tab** to a sheet (`68-SHEETS-SPEC.md` section 10).

## Open questions

id | question | why it is open
`D16` | Where Sort the file lives: the column menu only, or also a control beside the sort chip | Putting them side by side risks a person writing when they meant to view; hiding it risks nobody finding it
`D17` | Whether a phone may set a formula at all | The cell editor is drawn for values. A formula field on a 390 px sheet is untested
`D18` | Whether the grid shows `1200` or `1,200` | The file must hold `1200` to stay a number (`68-SHEETS-SPEC.md` section 4.4). Showing `1,200` is kinder to read but makes the grid disagree with the MD view. Drawn as `1200`

**Resolutions**, each `resolved (proposed 19 Sep, founder review)`. None is a founder matter: no
money, law, brand or promise to users turns on them. `56-OPEN-DECISIONS.md` section 5a is another
owner's, and still lists them as open.

id | Resolution | Rejected
`D16` | **The column menu only**, as drawn. Sort the file is a write, so it sits one step away from the view-only sort chip | A control beside the chip, which puts a write next to a view
`D17` | **The phone sets values only in the grid.** A formula is edited in the MD view, where the `fm-sheet@1` block is plain text, so nothing is withheld from a phone | A formula field in a 390 px cell editor, which is untested
`D18` | **`1200`**, as drawn. The grid shows the file's bytes, per the projection law | `1,200`, which makes the grid disagree with the MD view

## Register rows needed

**Allocated on 19 September**, see `tools/new-ids-allocation.md`. Every id below now has its row in
its register. The rows that widen or change an existing row, or ask another file's owner for
something, are still open for that owner.

Register | Row | For
`14-COMPONENT-INVENTORY.md` | `C155`, `C156`, `C157`, `C158`, `C159`, `C160`, `C161` | Anatomy
`14-COMPONENT-INVENTORY.md` | `C045` screens widened to S39, and its contract to one-cell writes | Anatomy
`14-COMPONENT-INVENTORY.md` | `C068` screens: S39's cell editor | Responsive
`16-COPY-DECK.md` | The nineteen `K.s39.*` strings above, in section 14r | Copy
`17-ERROR-AND-REFUSAL-CATALOGUE.md` | `E513`, `E514`, `E515`, `E517`, as `68-SHEETS-SPEC.md` section 11 already asks | Actions
`19-ACCEPTANCE-CRITERIA.md` | `A821`, `A822`, `A823`, `A824`, plus the `68` rows cited above | Acceptance
`55-MEASUREMENT-AND-EVENTS.md` | `sheet.cell.edited`, `sheet.edit.refused`, `sheet.view.changed`, `sheet.file.sorted`, `sheet.structure.changed`, `sheet.formula.set`, `sheet.source.toggled`, `sheet.opened` | Actions
`15-INTERACTION-AND-KEYBOARD.md` | The grid's keys, the Grid and MD chord, and the Space conflict | Keyboard
`56-OPEN-DECISIONS.md` | `D16`, `D17` and `D18`, in section 5a | Open questions
`11-SCREEN-INDEX.md` and `13-SCREEN-STATE-MATRIX.md` | An S39 row | This file
