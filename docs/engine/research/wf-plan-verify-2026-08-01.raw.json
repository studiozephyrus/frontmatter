{
  "summary": "End-to-end verification of PLAN.md v2.1.0 — cross-section consistency, staleness, and re-derivation",
  "agentCount": 7,
  "logs": [
    "Verifying PLAN.md v2.1.0 across 7 claim-classes."
  ],
  "result": {
    "classes_done": 7,
    "classes_total": 7,
    "verdicts": [
      {
        "class": "Staleness — what commit 4f97129 invalidated",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "cross-section-contradictions",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "Number audit — re-derive every load-bearing figure",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "External facts — spot-check the outside world",
        "verdict": "MATERIAL_TWEAKS"
      },
      {
        "class": "What the plan does not say, and should",
        "verdict": "MATERIAL_TWEAKS"
      }
    ],
    "severity_counts": {
      "BLOCKING": 27,
      "MATERIAL": 68,
      "MINOR": 25,
      "COSMETIC": 6
    },
    "blocking": [
      "scripts/fm-roundtrip-audit.mjs cannot run — it imports src/modules/share/domain/splice-frontmatter.mjs, which does not exist and was never committed. The 3.64% / 31.31% / 100% figures currently have no runnable source, and §3.1.10 plus §7.5 F3 both condition the step-2 gate on this oracle. Fix the import before any gate is trusted.",
      "Do NOT sync the plan to the commit message's 'yaml Document 284/907 (31.31%)'. Measured this session: 170 of those 284 are silent no-op publishes; only 114 (12.57%) publish and round-trip. This is the same error §7 L165 killed and §8 L425 forbids by name. The plan's existing 119/13.12% is the correct figure and must be defended, not corrected upward.",
      "§3.1.4 Violation 1 must be rewritten before step 2 is dispatched. As written it sends a builder to remove a matter.stringify call that no longer exists and to falsify a header comment that is now accurate.",
      "The export contract is DECIDED in §1.5 (D4: \"a ZIP of clean .md files ... forbids shipping a single-file container\") and OPEN in §12.1.1 (W-A) and §11.14 (K10). A builder reading §1 will treat the format as closed. Settle W-A — the deciding question takes ten minutes and has never been asked — or change §1.5's cell before anyone writes export code.",
      "§11 never schedules the offset model or the freezing of normalize/1 and slug/1, and §10.10.2 says both must land BEFORE the splice writer's second caller and BEFORE the first anchor is persisted. §11.16 puts the splice work on Tuesday of week one. Insert steps 0a and 0b into §11.1 and the week-one table first — retrofitting branded types after four call sites is a refactor, and changing normalize() after an anchor is stored is a silent detachment of every comment.",
      "The splice writer's module path disagrees across §3.1.6, §11.4.2 and §11.11.3, and the shipped code at 4f97129 uses a fourth (`src/modules/share/domain/splice-frontmatter.ts`). §11.11.3's disjoint-artifact map is the parallelism contract, and as written it no longer protects the file from a step-4 agent. Fix the map before the first parallel dispatch.",
      "§11.2.1's step-0 acceptance gate (`npx vitest list --filesOnly | wc -l` returns 83) fails on a correct tree today: the exclude landed in 4f97129 and the count is 84. It is an equality-pinned assertion, which §9.5 and §9.11 (LR#66) explicitly forbid. Rewrite it as a floor before it trains anyone to ignore the harness.",
      "§2.1 contains a `[primary]` verbatim blockquote of HANDOFF §0.7 that does not match the file (3,484 vs the real 3,604; the parenthetical is not in the source at all), and §14 D6's correction text repeats the fabrication. Fix both, then grep the tree for other quotations touched by the same find-replace, before any number in the chronology is quoted downstream.",
      "scripts/fm-roundtrip-audit.mjs cannot execute — it imports `../src/modules/share/domain/splice-frontmatter.mjs` and only the `.ts` exists, so all three impls die at module resolution. §7's F3 falsifier and §11 Step 2's gate both name this file as the independent oracle (LR#60). Until line 21 is fixed and the script is run for real, there is no independent byte comparator and no captured output to gate on. I reproduced all three impls only by copying the script into $TMPDIR against a type-stripped build.",
      "The corpus gate can pass without seeing the corpus. test/share/frontmatter-splice.test.ts skips a missing root rather than failing, and `it.skipIf(corpus.length === 0)` only fires when ALL roots are absent. On any machine without ~/Desktop/GitHub/md and ~/Desktop/GitHub/knowledge the gate runs on the 23 in-repo files, prints `23/23 identical (100.00%)`, and goes green. Add `expect(s.total).toBe(907)` before the build starts, or every subsequent `907/907` claim is unfalsifiable off this laptop.",
      "The RED baseline the build is told to expect is wrong. §11.4.3 instructs `Committed RED first. Against the current writer the test will fail at 17/736`; the actual gray-matter result under the shipped gate's own inverse is 33/907 (703 changed + 171 threw). Anyone writing the assertion from the plan will write a number the harness cannot produce. Settle 17/736 → 33/907 before Step 2 is executed.",
      "§11.4 is stale end to end — step 2 shipped in 4f97129, to share/domain rather than the specified mdmax/domain. Fix the section and the §11.11.3 disjoint-artifact map before anyone dispatches step 4, which owns 'share (all layers)' and would now overwrite the splicer.",
      "The step-2 corpus gate reports PASS over 23 of 907 files (2.54%) on any machine lacking ~/Desktop/GitHub/md and ~/Desktop/GitHub/knowledge — i.e. on the second developer's machine, a contractor's machine, or CI. It must refuse rather than shrink before it is treated as a definition of done.",
      "scripts/fm-roundtrip-audit.mjs does not run (`ERR_MODULE_NOT_FOUND`, `.mjs` path for a `.ts` file). The 907/907 headline has no executable derivation script, which fails the plan's own rule P2. One-character fix.",
      "§11.16 Tuesday 15:00-16:00 proves itself with `gate.mjs --verify-all`, and gate.mjs is not ported until Thursday 16:00-18:00. Re-sequence before Monday morning.",
      "Task 1d is scoped to 3 files / 2-3 d against a measured blast radius of the container singleton (25 importers) plus two process-global caches. Re-scope before the week is committed, or K1 fires for a reason the plan cannot learn from.",
      "There is no integration-test harness and no second GitHub identity, while §11.12.6 forbids mocks for the capability class. Task 1a-0 (2-3 d) must exist before task 1a's 0.5 d is believed.",
      "§11.13 condition 1 is unusable as written: one command returns a false negative today, has no exit code, and measures a developer committing source rather than a second user writing a document. Rewrite it before 2026-08-31 arrives and the gate is settled by argument.",
      "There is no CI in this repository. Decide before Monday whether to build it (0.5 d) or to delete every 'in CI' claim from §11.4.4, §11.12.4, §12.2 N12 and §12.6 item 1.",
      "`npm run lint` returns 930 errors and lints nothing — 100% are the `No tsconfigRootDir was set` parse failure from the two .claude/worktrees tsconfigs, 316 of them in the main tree. Every boundaries/dependencies and no-restricted-imports rule the plan cites as `[primary, eslint.config.mjs:NN]` is currently unenforced, and the planned eslint ban on importing matter.stringify (§10.6.5 rule 1) cannot work. One-line fix: add \\\".claude/**\\\" to eslint.config.mjs:16-27's `ignores`. Verify: 930 -> 25, 0 in src/.",
      "`npm run typecheck` returns 5 errors (missing @types/jsdom devDependency; all 5 in test/preview/html-policy-xss.test.ts, 0 in src/), so `npm run verify` dies at step 1 and no §11 gate worded 'typecheck green' is reachable on day one. Fix: npm i --save-dev @types/jsdom.",
      "§11.4.2 instructs the builder to locate the splice range with eemeli/yaml's `node.range`, which requires parsing with the library that fails on the 170 files the step exists to make writable. It directly contradicts §10.6.5 rule 1 ('WRITES NEVER PARSE... it is what makes the 170 unparseable files writable at all') and caps the result at 737/907, failing §11.4.3's own gate. Resolve before Tuesday, or the shipped 907/907 writer gets replaced by a worse one.",
      "The independent oracle §11.4.3 requires does not exist. scripts/fm-roundtrip-audit.mjs, committed in 4f97129 as if it were the oracle, fails at import (ERR_MODULE_NOT_FOUND — line 20 imports splice-frontmatter.mjs; only .ts exists) and violates all three independence conditions (imports gray-matter/yaml/spliceFrontmatterValue; compares with === not Buffer.compare). The 907/907 figure currently rests on a test that imports the writer under test.",
      "test/share/frontmatter-splice.test.ts silently degrades to 23 of 907 files on any machine without ~/Desktop/GitHub/md and ~/Desktop/GitHub/knowledge (CI, fresh clone, second developer) and reports the identical `11 passed`. Verified by simulation. Add `expect(corpus.length).toBe(907)` and print `corpus: N/907` before this gate is trusted anywhere but this laptop.",
      "§11.16's Monday 09:30 and Tuesday 09:00 rows both instruct work that is already done (the vitest exclude; the splice writer) with acceptance numbers that no longer hold (83, and the mdmax module path). Reconcile the schedule against HEAD 4f97129 before the week starts, or day one and day two are spent re-doing and diverging.",
      "The `inkeep/OpenKnowledge` → `inkeep/open-knowledge` path fix must land before any code or script in the F7 competitive-monitoring procedure is written — the capitalised form returns HTTP 404 from api.github.com and will silently produce an empty result rather than an error in a `curl | jq` pipeline.",
      "The Craft/Bear grep recipe must be corrected to specify script-stripped visible text before §7's F7 monitor is automated — run as written it reports 32 'comment' matches on Craft today and will fire a false competitive alarm on its first execution.",
      "The cmark-gfm Tier-D oracle build recipe in §10 must record the exact triple (tag `0.29.0.gfm.13` + `-DCMAKE_POLICY_VERSION_MINIMUM=3.5` + the CMake version it was verified against) before the certificate bench is built. Verified here against CMake 4.3.2; master's unreleased 3.5 bump means a future `master` build will silently not need the flag, which is exactly the version-fragility §3:679 warns about.",
      "§10 is entirely unscheduled in §11 and §11.17 does not exclude it — decide whether the OffsetMap, the normalize/slug freeze, the shape gate + worker pool, the hostile corpus and the lenient pre-pass are built in this cycle, and write the answer into §11.1's sequence table. A builder starting Monday against §11 as written builds none of them, and §10.10.2 row 1 says the offset model must land BEFORE the splice writer's second caller — which §11.16 schedules for Tuesday.",
      "The step-2 acceptance gate is unrunnable by anyone but Sagnik and green-lights at 2.5% of its stated population elsewhere. Measured: 907 files with both private vaults present, 23 without, and the test passes in both cases. Fix the gate to assert its denominator and fail on an incomplete corpus, and decide what public corpus a second person and CI will run it against — before step 2 is called done.",
      "There is no CI. `.github/` does not exist, yet §10.8 has three 'blocking, every PR — build fails' rows, §10.2/§10.3 have two 'CI assertion' obligations, and §8 makes a continuously-running byte-fidelity CI the only asset a competitor cannot copy. `main` auto-deploys to Vercel. Create the workflow and branch protection, or strike the word 'blocking' everywhere it appears.",
      "Step 1 task 1d cannot be implemented as written: the app requests OAuth scope `read:user` (auth-options.ts:29), which grants no repository access, and the plan never states the scope escalation or decides between `repo` and a GitHub App. §7.5 N2 and §11.3.5 give opposite answers on which is the design and which is the interim, and a GitHub App carries external lead time that appears in no estimate. Settle this on Monday, before 1b deletes the allowlist."
    ],
    "tweaks": [
      {
        "class": "Staleness — what commit 4f97129 invalidated",
        "tweaks": [
          "Fix scripts/fm-roundtrip-audit.mjs line 21 to import splice-frontmatter.ts, not .mjs, and actually run it — the oracle §3.1.10 and §7.5 F3 depend on has never executed as committed.",
          "Rewrite §3.1.4 Violation 1 as FIXED-in-4f97129 historical rationale; keep the gray-matter re-parse explanation, drop the present tense.",
          "Add the refusal split to test/share/frontmatter-splice.test.ts L184-185 so the suite stops emitting a bare 31.31% — the statistic §7 L165 and §8 L425 already killed.",
          "Apply §14's own D3 correction to 03-capabilities.md L156-157, then fix §14's APPLIED list which falsely claims it was already applied.",
          "Change 'two shipped destroyers/violations/callers' to one at all seven sites (01 L83/L430/L442, 03 L128/L159/L161, 10 L168), each with '(the second was fixed in 4f97129)'.",
          "Update §1.5's measured-state block: HEAD 4f97129, 0/20 ahead, engine code 164 lines, 84 files / 1,257 tests.",
          "Mark §11 step 0 DONE and change its gate from '83, not 247' to '84'; same at §11 L106 and L1208-1209.",
          "Rescope §11 step 2 to the PropertiesPanel path only, and rewrite the §11 L1154-1156 day plan — its morning is already committed and its '--broke' mutation no longer reproduces the defect.",
          "Reconcile one path for the splice writer across §3.1.7, §11 L787 and §11 L1154 — all three differ from where it landed (src/modules/share/domain/splice-frontmatter.ts).",
          "Add a §3.1 status line distinguishing what shipped (spliceFrontmatterValue) from what did not (splice(), U16Offset, SpliceReceipt, E-RANGE/E-SURROGATE).",
          "Mark §10.6.3's two share-writer rows FIXED and promote preview/frontmatter.ts:47 to the live defect.",
          "Add 'Until 4f97129' framing to the three surviving verbatim quotes of the removed line (§7 L258, §8 L394, §13 L1118).",
          "Fix §3.1.4's '170 of 907 blocks throw under gray-matter' to 171, and resolve the §3.1.5-vs-§14/§7 contradiction about whether the two sets are disjoint (they are not).",
          "Add the publish-actually-happened assertion to the corpus gate, per §7.5 F3's own wording.",
          "Update §11.2.0's live-git block (4f97129, 0/20) and §2 L480-491 / L856's now-false 'no exclude' and '0 lines of engine code' claims."
        ]
      },
      {
        "class": "cross-section-contradictions",
        "tweaks": [
          "Fix the fabricated verbatim quote of HANDOFF §0.7 in §2.1 and in §14 D6's own correction text — restore \"65 test files, 3,604 tests passing at last full run\" and re-cite it to §0.7 (line 22), not §10.",
          "Rewrite §11.2.1 / §11.1 step 0's acceptance gate from an equality (`wc -l` returns 83) to a floor-plus-zero (`grep -c worktrees` returns 0, file count in 80–100), and record in §11.2.0 that the vitest exclude landed in 4f97129 and the live count is 84.",
          "Insert steps 0a (offset model, 0.5 d) and 0b (freeze normalize/1 and slug/1, 0.5 d) into §11.1 and §11.16, and delete §11.12.2's P7 cell reading \"it appears in no effort estimate anywhere\".",
          "Reconcile the splice writer's path across §3.1.6, §11.4.2 and §11.11.3 with the shipped location `src/modules/share/domain/splice-frontmatter.ts`, and add it to step 4's must-not-touch column so the parallelism contract still holds.",
          "Change §1.5's D4 consequence cell from a settled ZIP answer to a pointer at W-A (OPEN), and add the same flag to §13.7's D4 restatement.",
          "Change §1.3 and §1.5's D5 row to admit the one AIOS mechanism §9.3 DECIDES and §11.12.3 budgets, instead of \"a working practice and nothing else\".",
          "Add §9.3.3's exit-6 REPRO_NOOP refusal to §11.12.3's port contract and unify the gate store path on `specs/harness/gates.jsonl`.",
          "Enforce §3.4.2's ruling downstream: strip 85.71% / 27.65% from §4.7 A5, §6.10 and §13.7's glossary; downgrade §13.4 C1's \"reproduced YES\"; add 13,026 to §13.6 row 6's arbitration.",
          "Reframe §4.11.6 / §4.13's TEI recommendation as the answer to §3.6.4's per-span OPEN decision, not as a competing vocabulary against OKF.",
          "Add §10.2.1's REFUTATION of the 93.8% divergence figure to §3.8.1 and §13.4 B8, and replace it with the non-BMP 3.51% / grapheme 3.87% figures.",
          "Qualify the \"sidecar-resolved block metadata\" exclusion row in §8.6 item 2 and §12.1.2 so it does not read as excluding the D2 comment system.",
          "Strike the JSON-LD justification from §1.5's D9 row per §6.4, and note §6.0's recommended amendment in §13.7.",
          "Separate F-ERA-1's 3.1× model-facing-stack threshold from §1.4b(5)'s 1.4388 generic-parser excess so the falsifier does not read as already fired; change §13.4 E8 from reproduced NO to PARTIAL.",
          "Set §3.0.1 row 7's container status to GATED on §11.9's P7a/P7b, and close §11.17 item 4's conditional.",
          "Rename §11.9's pack/unpack preconditions from P7a/P7b to PK-a/PK-b — P7 already names the normalize()-versioning process rule in §11.12.2, §10 and §12.",
          "Align the arXiv 1512.06195 tier across §2.5, §12.2 N4 and §13.4 A10 on \"[primary — abstract]\".",
          "Correct the kill-condition counts in §3.3.10 (four→five) and §3.7.9 (five→six), and cross-reference §11.14's K5/K5b/K5c split.",
          "Flag the 901 TypeScript-file count as contested (kill audit: 898) in §1.3, §9.4(a) and §9.6, and correct 296→298.",
          "Apply the 4f97129 staleness sweep: one D7 violation remains, not two; §11.1 step 2 is DONE; §11.16 Tuesday is reallocated to the properties panel."
        ]
      },
      {
        "class": "Number audit — re-derive every load-bearing figure",
        "tweaks": [
          "Fix scripts/fm-roundtrip-audit.mjs line 21 to import the .ts source (run via `npx tsx`), then re-run all three impls and paste the captured output beside §7 F3. Nothing downstream of that oracle is currently evidence.",
          "Global substitution: every `17/736 = 2.31%` and `17/907 = 1.87%` becomes `33/907 = 3.64%` (9 occurrences: §7 L7270, L7375, L7981; §8 L8392, L9007; §11 L11867, L11901, L12318, L12726). The 17 came from an always-delete inverse that destroys the 18 files already carrying public_slug.",
          "Add `expect(s.total).toBe(907)` to test/share/frontmatter-splice.test.ts:190-200 and change both `it.skipIf(corpus.length === 0)` to `it.skipIf(corpus.length < 907)`, so a partial corpus skips loudly instead of passing on 23 files.",
          "Fix the unreachable `refused` branch in the oracle (lines 98-101) and make `failed = changed + threw + refused`, so a refusing writer cannot score as byte-identical. Today yamldoc's 284 silently contains 170 refusals.",
          "Rewrite §11 Step 2: the Publish half is DONE (4f97129, oracle 907/907, 0 refusals, 0 throws). The remaining destroyer is preview/presentation/frontmatter.ts:47,59 via PropertiesPanel.tsx:79 → onEdit → saveDraft, whose gate is `a no-op properties edit dirties 0 of 737` (today 623, 84.53%, 46,732 bytes, 226 files move line numbers).",
          "Change §11 Step 0 and §7.2.6's vitest gate from `returns 83` to `returns ≥ 84 and `grep -c '.claude/'` returns 0` — live is 84 files / 1,257 tests, and equality gates punish adding tests (LR#66).",
          "Scope §14's 171→170 correction so it cannot be applied globally: 170 is the PARSE count, 171 is the ROUND-TRIP count (md/pj.md parses then throws in matter.stringify), and §7's correction table row 4 already says this correctly.",
          "Fix the two \"published and byte-identical\" pairings that mix scopes: §7 L7383 and §8 L8411 should quote 114/907 = 12.57% (file scope) against gray-matter's 3.64%, not 119/907 = 13.12% (block scope). Cite §13 B7's `5 separator-only` as the reconciliation; leave §3.1, §8 L8405, §10 L11143, §11 L11896 alone — they correctly say `blocks`.",
          "Correct the mirroring caveat in §7.1 and §8.1: 28 hashes shared across roots covering 57 entries / 32 redundant copies (2.95%), not `29 files (2.68%)`; the 29th group is 4 zero-byte files inside md alone. Deduplicated corpus = 1,052 distinct documents.",
          "Replace §7's \"100% of headlines refuted\" with the countable figure: 193 killed / 179 survived / 188 new defects across 18 areas = 51.9% of adjudicated claims, claims surviving in every area. The capability JSON has no headline-verdict field to support the original.",
          "Unify the repomix figures in §3.9 (327,543 and 327,053 both appear for 2026-07) to the live 339,504, and update the derived ratios to 873:1 and 2.84×. The 2025-07 baseline of 119,678 reproduces exactly.",
          "Add as-of timestamps to every live-registry number (GitHub topic counts, npm downloads, Obsidian plugin downloads). agent-skills 12,804→12,915, claude-skill 4,043→4,082, leader 165,486→165,613 stars in under a day.",
          "Fix the §11.7 example backlog item: `repro:` should be `npx tsx scripts/fm-roundtrip-audit.mjs splice`, not the non-existent `scripts/derive/roundtrip.mjs`, and its `claim:` should be 907/907 against a 33/907 baseline.",
          "Add a sixth caveat to §3.2.7 listing the five circulating block-version denominators (28,170 / 29,217 / 31,149 / 31,180 / 32,919 / 41,642) and requiring any re-derivation to name its population in the same sentence as its rate."
        ]
      },
      {
        "class": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "tweaks": [
          "Rewrite §11.4 as a post-mortem, not a task: step 2 shipped in 4f97129 to src/modules/share/domain/splice-frontmatter.ts (NOT the mdmax module §11.4.2 specifies). Fix §11.11.3's disjoint-artifact map for steps 2 and 4 accordingly, and delete §11.16's Tuesday 09:00-15:00 rows.",
          "Make the step-2 corpus gate refuse instead of shrinking: test/share/frontmatter-splice.test.ts skips only at corpus.length===0, and ROOTS.frontmatter=process.cwd() is always present, so on any machine without ~/Desktop/GitHub/md and /knowledge it asserts over 23 of 907 files (2.54%) and prints PASS. Throw on a missing root; assert corpus.length===907.",
          "Fix scripts/fm-roundtrip-audit.mjs line 20 (`.mjs` -> `.ts`): the committed oracle currently dies with ERR_MODULE_NOT_FOUND, so the 907/907 headline has no working derivation script under rule P2. Also fix the unreachable `refused` branch at line 97 so a silent no-op publish cannot score as byte-identical.",
          "Move the gate.mjs port from §11.16 Thursday 16:00-18:00 to Monday: Tuesday 15:00-16:00's acceptance proof is `gate.mjs --verify-all` green, and specs/harness/ contains no gate.mjs.",
          "Re-scope step 1 task 1d from 3 files / 2-3 d to ~30 files / 4-7 d: `export const container` (dependency-container.ts:44) is a module-level singleton imported by 25 files, plus snapshot-cache.ts (MAX_ENTRIES=3, global clear on commit) and search-index.ts:147 (single `_cache` slot) are process-global and break at the second vault.",
          "Insert task 1a-0 (2-3 d): stand up the integration harness. Today devDeps are vitest/jsdom/testing-library/fake-indexeddb only — no msw, nock, supertest, playwright or Firestore emulator — while §11.12.6 forbids mocks for the capability class. Provisioning a second GitHub identity is a calendar prerequisite scheduled nowhere. Revise §11.3's headline to 12-18 founder-days.",
          "Rewrite §11.13's condition 1: `git log --since=2026-08-01 -- src/` returns EMPTY today despite 4f97129 touching src/ that day (git approxidate fills the current time of day); the line has no exit code; and it observes a developer committing source, not a second user writing a document. Add `T00:00:00` to every --since in the section.",
          "Add §11.4.5 for Violation 2 (preview/presentation/frontmatter.ts:47,59 -> PropertiesPanel.tsx:79 -> EditorPane handleEdit -> saveDraft). It appears in §3, §5, §7, §10, §13, §14 and ZERO times in §11. Note that the shipped splicer cannot serve it unchanged (emitScalar rejects arrays; one key per call; no rename) and is not in the share barrel.",
          "Add §11.2.6: there is no .github/ directory. Four places in §11/§12 make CI load-bearing ('green in CI', 'eslint-plugin-redos in CI', §11.11.2's merge rule). Cost it at 0.5 d.",
          "Correct §11.2.1 and §11.1 row 0 from 83 to 84 and mark them done: measured `npx vitest list --filesOnly | wc -l` = 84, worktree matches 0, `npx vitest run` = 84 files / 1,257 tests / 4.06 s. As written the gate false-REDs on the correct fix.",
          "Define the 30-person probe in §11.12.7 or K5 and K8 can never be evaluated: §11.12.7 schedules five calls; K5 needs '5 of 30' and K8 needs '15 of 30'. Send 30 recruiting messages — the five calls fall out of the same funnel and the 30 replies are the denominator.",
          "Add the six pre-existing assertions that go red under step 1 to §11.3.3: test/auth/auth-options.test.ts:19,35,49 and test/config/env.test.ts:28,49,52. §11.11.2 blocks the merge on `npm run verify`, which runs all of them.",
          "Replace §11.12.6's tenancy GREEN cell with three mock-proof structural greps (no `sagnikmitra` in src — 3 hits today; no `GITHUB_REPO_TOKEN` outside config/env.ts — 3 hits today; no `export const container`). The five assertions alone are satisfiable by mocking, and test/auth/auth-options.test.ts already demonstrates the technique.",
          "Correct §11.12.4: 3.17 s is `verify-ladder.sh` (verbatim in wf-final-gate-2026-08-01.result.json), not `npm run verify`, which ends in `next build`. The test rung alone measures 4.06 s post-exclude.",
          "Give K3 and K4 real thresholds (K3: below 97.0%, or baseline within 3.0 points, on the >2-deleted-lines stratum; K4: 0.5% at n>=400 stratified by block token length). Both are currently unfalsifiable — the same defect §11.7 claims to have fixed in K5.",
          "Fix the §11.2.1 -> §11.12.5 dangling reference: §11.12.5 is the agent-dispatch prompt block and contains no RULE 2 confirmation procedure.",
          "Refresh §12's four 'verified live in this session' blocks: 24 commits (not 21), 15 docs-prefixed (not 13), 20 ahead of origin/main (not 17), src at 213 files / 21,578 LOC (not 212 / 21,415). `grep -ril mdmax src test scripts` is still 0, so N1's load-bearing claim is unaffected.",
          "Move §11.11.4's worktree preflight earlier and widen its slot: §11.11.4 costs it at ~3 h and says it lands before the first agent dispatch; §11.16 gives it 1 h on Friday. Both worktrees are still present and now 20 commits behind."
        ]
      },
      {
        "class": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "tweaks": [
          "§11.2 Step 0 — add a new §11.2.0b: `npm run typecheck` returns 5 errors (missing `@types/jsdom`, all in test/preview/html-policy-xss.test.ts, 0 in src/) and `npm run lint` returns 930 errors (100% the `No tsconfigRootDir was set` parse failure caused by the two .claude/worktrees tsconfigs; 316 of them in the main tree). `npm run verify` is red at steps 1 and 2 today. Fix both before any gate is worded 'typecheck green'. Acceptance: tsc silent; eslint 930 -> 25 with 0 in src/.",
          "§11.2.1 — mark the vitest exclude LANDED in 4f97129 and change the acceptance number from 83 to 84 here and at lines 491, 7160, 7389, 7713, 12637, 12638. Add that the same worktrees also break eslint entirely, and that the durable fix is adding \".claude/**\" to eslint.config.mjs:16-27's `ignores`, not just removing the worktrees.",
          "§11.4.2 — delete the `yaml` `node.range` prescription. It contradicts §10.6.5 rule 1 ('WRITES NEVER PARSE') and caps the writer at 737/907, failing §11.4.3's own gate. Replace with the line-wise byte scan that actually shipped, and record the measurement: the key is written in 907/907 files including all 170 unparseable ones, 0 refusals, 0 throws, round trip byte-identical 907/907.",
          "§11.4.3 — record that the independent oracle does not exist: scripts/fm-roundtrip-audit.mjs crashes at import (line 20 imports a .mjs that was never created) and imports all three banned modules (gray-matter line 18, yaml line 19, spliceFrontmatterValue line 20) while comparing with `===` instead of Buffer.compare. Step 2 is not done until a real oracle reports 907/907.",
          "§11.4.3 — require the corpus gate to assert its own size. `it.skipIf(corpus.length === 0)` does not fire when the private roots are absent; the suite silently runs over 23 files instead of 907 and prints an identical `11 passed`. Verified by simulation: degraded run scores splice 23/23 green.",
          "§3.1.5 — replace the 'Do not merge them / different parsers, different failures' paragraph. Both parsers fail on the SAME 170 files (symmetric difference zero, measured); 171 = 170 + md/pj.md, which parses under both and dies inside matter.stringify. §7.2.5 already states this correctly. Propagate 171 -> 170 at §11.4.3 and §11.16 Tuesday.",
          "§10.4 pass table row 4 — strike the claim that graph-data.ts:132 'silently drops an unresolved reference today'. §2 already refuted it; the real branch is line 176 and the module ships a typed UnresolvedLink[] with a discriminating `reason`. Also fix §2's own off-by-one: the `— drop` comment is at line 189, not 188.",
          "§7.2.5 — change '13.12% published-and-byte-identical (119/907)' to '31.31% (284/907)'. 119/907 is the no-edit round trip; 284/907 is the publish→unpublish cycle the sentence describes and the figure 4f97129's commit message reports. Add a one-line reconciliation to §3.1.5's table caption.",
          "§10.6.3 — change '737 run' to '736 run' and delete the invented arithmetic anomaly. 41 unchanged + 695 changed = 736 exactly; 41 = 33 fully byte-identical + 8 trailing-newline-only. §7.2.5 already uses 736.",
          "Add a dated 'FIXED in 4f97129' banner to §3.1.4 and one-line pointers at the eight other sites that still describe share-writer.ts as calling matter.stringify / matter() bare (2003, 2006, 7359-7362, 8380, 11140, 11141, 11471, 11852, 14935). Re-scope §10.7's #2 priority row to frontmatter.ts:47/:59 only — Violation 2 is still live, Violation 1 is not.",
          "§11.10 / §3.1.6 / §11.9 / §11.16 Tuesday — the splice writer shipped at src/modules/share/domain/splice-frontmatter.ts, not src/modules/mdmax/{splice.ts | domain/splice-frontmatter.ts}. There is no mdmax module. Point every reference at the real path and add 'do not create a second implementation'.",
          "§11.4.3 — the RED baseline is 33/907 under the committed test's restore-prior inverse, not 17/736 (which is the always-delete inverse). Both measured; state which operation each belongs to.",
          "§11.2.2 — retire the hedge. The corpus_id recipe reproduces sha256:3a010b16… exactly on first attempt and four adversarial variants fail, so it is uniquely determined; 1,084/1,084 files re-read and sha256-matched. The `[measured]` tags are not discounted. Only the bookkeeping (commit scripts/derive/corpus-id.mjs, add a `derivation` field) remains.",
          "§14 line 15317 — change '113 test cases' to '128 test cases', noting that 113 is `grep -c`'s matching-LINE count. The 13-file count and the '327 lines, 14 it( + 1 test(' figure at line 15141 are both exact.",
          "Standardise the WIKILINK_RE exponent on the measured k = 1.99 (1.993 / 1.995 / 1.997) across lines 3450, 11926, 12675, 13531, 14506, replacing the 1.98/2.00 split. Both quadratics reproduced this session; keep the k <= 1.05 regression gate as written.",
          "§2 and §10.7 EditableTable rows — name the collateral: test/preview/table-edit.test.ts asserts on setTableCell 14 times and must be trimmed in the same commit."
        ]
      },
      {
        "class": "External facts — spot-check the outside world",
        "tweaks": [
          "Fix the commits-per-year table: §1 (tree/01-orientation.md:152-153) 2023→12 and 2024→12 with the parenthetical deleted; §13 E1 (tree/13-appendix.md:661) 48/15/14/15/13/11/7/6 → 48/15/14/15/12/12/7/6. §2:843 is already correct and is the reference. Re-derived live against api.github.com/repos/commonmark/commonmark-spec/commits, total 129 since 2019-01-01.",
          "Rewrite the provenance sentence at tree/01-orientation.md:161-162 — it cites `api.github.com/repos/github/cmark-gfm/releases` for CommonMark dates that in fact come from spec.txt's `date:` key. Name both clocks (spec.txt 2019-04-06 / 2021-06-19 vs releases API 2019-04-08 / 2021-06-20); this also resolves the §1-§13 vs §2 date inconsistency.",
          "Downgrade \"Obsidian states plainly it has none\" in §8:92, §7:334 and §11:593 to \"the help file documents no cursors, no fine-grained permissions and a 20-user cap, and never mentions comments\" — the word 'comment' occurs zero times in `en/Obsidian Sync/Collaborate on a shared vault.md`. Also restore the file's stated permissions exception (only the owner can invite).",
          "Add \"visible text only, `<script>`/`<style>` stripped\" to the Craft/Bear grep in §8:93 and to the F7 quarterly monitor in §7:881. A literal grep over Craft's fetched HTML returns 32 'comment' / 175 'collaborat' / 86 'markdown'; the plan's 0/0/0 is correct but only after stripping. Bear's '7 markdown' reproduces exactly on visible text.",
          "Global replace `inkeep/OpenKnowledge` → `inkeep/open-knowledge` (§2:618, §3:459, §7:319, §11:596, §13:680). The capitalised form 404s on api.github.com; §7:881 already uses the correct form.",
          "Soften \"declined the teammate model in writing\" (§8:92, §7:319/334, §11:597, §13:680) to \"shipped comments as a local, uncommitted, human→agent queue\". The full changeset describes a batching review loop; the quoted sentence is a storage-scoping note, and §7:340/881 already says only sync and identity are missing.",
          "Fix §6:1010 \"only conformance tool\" — `openknowledge-sh/openknowledge` (33★, Apache-2.0, pushed 2026-07-31) ships `okn validate`. Restate as two structural validators, neither of which checks value drift; update 287 → 289 repositories.",
          "Re-tier the Hypothes.is figures to `[primary]` in §2:894 and §13 A10 and delete \"NOT read directly; verify before citing\" — I read arxiv.org/abs/1512.06195 live and all of 20,953 / 22% / 12% / 88% / 53% reproduce. §12:481-485 is already correct and is the reference wording.",
          "Note in §3:675 that cmark-gfm master fixed the CMake floor to 3.5 on 2026-07-13 (PR #383) but cut no release, so the pinned tag still needs `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`; and update §1:147, which names only the 2025-12-19 commit, to include the 2026-07-13 merge (F-ERA-2 should monitor commits, not just releases).",
          "Date-stamp the thirteen moved external figures and update the four that moved >1%: djot 52,610× → ~50,000×; AGENTS.md 175,424 → 174,880 (and upgrade it from `[secondary]` to `[primary]`, now re-derived); the 846:1 ratio → 914:1 next morning, so quote it as an order of magnitude; @inkeep/open-knowledge 14,790 → 17,305 with the window named as §8:106 already does.",
          "Add the six comments on repomix issue #226 alongside its zero reactions in §13 D3 and §12:583.",
          "Pin §10:1208's spec suite to the `0.31.2` tag explicitly — master carries 655 examples while still declaring version 0.31.2, so a gate that vendors master asserts 652/652 against the wrong denominator."
        ]
      },
      {
        "class": "What the plan does not say, and should",
        "tweaks": [
          "Add §11.4a 'The engine prerequisites' — a step-2a row in §11.1 plus the four-item table scheduling the OffsetMap, the normalize/slug version freeze, the lenient pre-pass, and the shape gate + worker pool; then amend §11.17 to say §10 is scheduled there and nowhere else.",
          "Add to §11.4.3 and §3.1.10: the corpus gate MUST assert its own denominator (expect 907), resolve roots from env vars with no homedir() default, and FAIL rather than skip when a root is missing — measured 2026-08-01, the shipped gate sees 23 files and passes on any machine without the two private vaults.",
          "Add step 0b to §11.1 and §11.2.6: there is no CI in this repository (.github/ does not exist), so create .github/workflows/verify.yml plus branch protection before any 'blocking, every PR' gate in §10.8 means anything.",
          "Split §11.3.3 task 1d into 1d-i (decide OAuth `repo` scope vs a GitHub App, record as D11, Monday) and 1d-ii (implement) — the shipped scope is `read:user` (auth-options.ts:29), which cannot write repository contents, and §7.5 N2 says the App 'is not two days'.",
          "Rename §11.4 to 'both callers', add §11.4.2b for frontmatter.ts:47 / PropertiesPanel.tsx:79, add gate 2 (no-op property edit byte-identical 907/907, RED at 119/907), and put it in §11.16 Tuesday 16:00–18:00.",
          "Add the 2026-08-01 correction to §11.4.2: the splicer shipped at src/modules/share/domain/, must move to src/modules/mdmax/domain/ before step 4 starts, or steps 2 and 4 share a file and rule P3 breaks; delete the conflicting path in §3.1.7.",
          "Add to §11.4.3: the oracle is a deliverable with its own gate — `npm run audit:fm`, appended to `verify`, env-var corpus roots, and a working import (as committed, `scripts/fm-roundtrip-audit.mjs` fails at module load on all three arms).",
          "Add the §12.5 row 10.8 decision ('who owns step 1') to §11.16 Monday 09:00–10:00, and add to §11.13 that K2 is not self-executing unless one of 1a/1f/1g is assigned to Amit on 2026-08-03.",
          "Renominate K5 and K8 to the five conversations that exist, and add §11.12.7a specifying the 30-user probe (frame, instrument, owner, effort) with an explicit 'UNEVALUABLE until it runs' note.",
          "Add the lenient pre-pass and WIKILINK_IN_FLOW_SEQ to §11.4.2 as item 3 with its own gate (907 parse, 96 refuse), add them to §11.16 Tuesday, and raise step 2 from 0.5 d to 2 d in §11.1.",
          "Rule the normalize() conflict in §10.3.2: the four steps stand, D2 makes §7.5 N4's HTML-comment strip moot, and amend N4 to say the gate was declined — settle it before any anchor is persisted, because rule P7 makes it irreversible.",
          "Add the notification delivery mechanism (and its email-provider dependency, ~3 founder-days) to §11.8's 'Ships as', or add it explicitly to §11.17's not-covered list.",
          "Add §11.11.0 'Running it, from a cold clone' — the five required env values from src/config/env.ts:4,44,45,46,85, the seven NEXT_PUBLIC_FIREBASE_* values at :210-216, the OAuth callback URL, ALLOWED_GH_LOGIN, and 'get a second GitHub account before Monday'.",
          "Change §11.2.1's and §11.1's step-0 gate from '=83' to a floor-and-ceiling band (worktrees==0, count >=83 and <100); measured 84 today, and an equality gate goes red for adding a test.",
          "Reconcile §11.1's step-2 effort (0.5 d) with §11.16 Tuesday's seven-hour allocation, and add the footnote that §11.16 governs on estimates.",
          "Renumber §12.2's pre-mortem risks N1–N13 to R1–R13, update the five §11 citations, add a glossary line, and disambiguate 'FEATURE-GAP-REPORT.md K5' from kill gate K5.",
          "Fix the §1.3 → §12.5 cross-reference: 'open decision 10.4 in §12.5', with a note that §12.5's rows are not in the D1–D10 namespace.",
          "Restate §11.12.3's UTF-8 gate `--broke` in this repository's own terms (a mis-boundaried Buffer.subarray in the splice writer), and gloss every numbered Learned Rule cited in the plan."
        ]
      }
    ],
    "findings": [
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "Cross-cutting — scripts/fm-roundtrip-audit.mjs, the oracle §3.1.10 and §7.5 F3 both depend on",
        "severity": "BLOCKING",
        "issue": "The independent oracle committed in 4f97129 cannot run. It imports a .mjs module that does not exist and was never committed, so the three headline figures credited to it (3.64% / 31.31% / 100%) are not reproducible from the artifact the repo now offers as their source.",
        "evidence": "Line 21 of scripts/fm-roundtrip-audit.mjs imports from '../src/modules/share/domain/splice-frontmatter.mjs'. Only splice-frontmatter.ts exists. `git log --all --diff-filter=A -- src/modules/share/domain/splice-frontmatter.mjs` returns nothing. Running `node scripts/fm-roundtrip-audit.mjs splice` gives ERR_MODULE_NOT_FOUND. §3.1.10 conditions the gate on 'verified by an oracle sharing no code with the writer'; §7.5 F3 repeats it citing LR#60.",
        "fix": "Change the import to '../src/modules/share/domain/splice-frontmatter.ts' (Node 24.6.0 strips types natively — verified working this session) and add `node scripts/fm-roundtrip-audit.mjs splice` to the step-2 gate so the oracle is executed, not merely committed. Until it runs, downgrade every 'independent oracle' credit to '[unverified — the oracle does not currently execute]'."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1.4 Violation 1 (03-capabilities.md L129-147)",
        "severity": "BLOCKING",
        "issue": "Entirely stale. share-writer.ts no longer calls matter.stringify, no longer imports gray-matter at all, and the header comment the plan calls 'false' is now true. A builder is sent to fix already-fixed code.",
        "evidence": "src/modules/share/infrastructure/share-writer.ts line 25 now reads `const next = spliceFrontmatterValue(file.content, \"public_slug\", slug);`. `grep -rn 'gray-matter|matter.stringify' src/` returns zero hits in the share module. Commit 4f97129 (HEAD) landed this.",
        "fix": "Replace the Violation-1 block with: 'Violation 1 — FIXED in 4f97129. src/modules/share/infrastructure/share-writer.ts now calls spliceFrontmatterValue and imports no YAML parser; its header comment is now accurate. Retained here as the historical justification for D7, not as an open defect.' Keep the gray-matter/index.js:161 re-parse explanation as rationale, marked historical."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "Cross-cutting — the yaml-path figure now emitted by test/share/frontmatter-splice.test.ts L184-185 and by 4f97129's commit message",
        "severity": "BLOCKING",
        "issue": "The new 'yaml Document 284/907 (31.31%)' figure is the exact statistic the plan already adjudicated and killed, recomputed. It scores the 170 files where yaml REFUSES (publishes nothing) as successes. If the plan is synced to this number it re-commits a defect §7 and §8 explicitly forbid.",
        "evidence": "Measured this session, corpus_id sha256:3a010b16, 907 files: yaml round-trip byte-identical 284 (31.31%), of which 170 (18.74%) are silent no-op publishes; only 114 (12.57%) both round-trip and actually publish. The refusal branch in the test (L122 `if (doc.errors.length > 0 || !isMap(doc.contents)) return src`) makes published===src, so score() counts it as identical. §7 L165 killed exactly this: 'A ~9-point gap was presented as ~28, by scoring eemeli's 170 refusals as successes.' §8 L425: 'Never quote 3.64% vs 31.86%.'",
        "fix": "Amend the test's console line to print the split: 'yaml Document: 284/907 round-trip-identical (31.31%) — but 170 of those are silent no-op publishes; only 114 (12.57%) publish AND round-trip.' Never quote 31.31% bare. The comparable pair against gray-matter's 3.64% is 12.57%, consistent with §3.1.5's 13.12%."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§14 preamble (14-verification.md L18-24) vs §3.1.4 (03-capabilities.md L156-157)",
        "severity": "MATERIAL",
        "issue": "§14 states defect D3 was APPLIED to the body. It was not. §3.1.4 still carries the pre-correction sentence verbatim. A builder reading §14 will believe §3 is clean when it is not — and no earlier fact-checker caught it because each saw only 3-4 sections.",
        "evidence": "§14 L21-23: 'APPLIED to the body above: D1 ..., D3–D4 (171 and 170 are the same 170 files...)'. §14 L46 quotes the claim to be replaced: 'Measured against every front-matter block in the pinned corpus: 170 of 907 blocks throw under gray-matter, and only 33 survive byte-identical (3.64%).' 03-capabilities.md L156-157 still reads that sentence word for word.",
        "fix": "Apply §14's own D3 correction text at 03-capabilities.md L156-157, replacing the sentence with the 119/170/618 wording §14 L48 specifies, and keeping 171/33/3.64% under Violation 1 only. Then either mark D3 applied or remove it from the APPLIED list."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1.4 (L156) vs §3.1.5 (L180-182) — same section, two subsections apart",
        "severity": "MATERIAL",
        "issue": "§3.1.4 says gray-matter throws on 170; §3.1.5 says 171 is gray-matter and 170 is yaml and 'do not merge them'. The section contradicts its own warning. Measurement says 171.",
        "evidence": "Measured this session on corpus_id sha256:3a010b16: gray-matter threw on 171 of 907, changed 703, left 33 identical (33+171+703=907). yaml refused 170. 4f97129's commit message also records 'gray-matter 33/907 ... 703 changed, 171 threw'.",
        "fix": "At 03-capabilities.md L156 change '170 of 907 blocks throw' to '171 of 907 blocks throw', or delete the sentence entirely as part of the D3 fix above (preferred — the sentence is misattributed to Violation 2 anyway)."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1.5 (L180-182) vs §14 L22-23 and §7 L167",
        "severity": "MATERIAL",
        "issue": "Unreconciled cross-section contradiction about whether 171 and 170 describe the same files. §3.1.5 says they are different sets; §14 and §7 say they are the same 170 files plus one.",
        "evidence": "§3.1.5: 'Different parsers, different failures. Do not merge them.' §14 L22: '171 and 170 are the same 170 files, not two sets — kill-audit D re-measured both parsers on the pinned corpus and found an identical set.' §7 L167: 'Exactly 170 files fail at matter() parse; exactly one more, md/pj.md, parses cleanly and throws at matter.stringify.' My counts (171 gray-matter, 170 yaml) are consistent with §14/§7, not with §3.1.5.",
        "fix": "Replace §3.1.5's 'Different parsers, different failures. Do not merge them.' with: 'The two numbers overlap almost exactly: the same 170 files defeat both parsers; gray-matter fails on one more (md/pj.md, which parses cleanly and throws inside matter.stringify), giving 171. Quote 170 for yaml and 171 for gray-matter, but do not present them as disjoint populations.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1 as a whole (03-capabilities.md L75, L188-238) — what actually landed vs what §3.1.6 specifies",
        "severity": "MATERIAL",
        "issue": "Capability 1 is only partly implemented, and nothing in the plan will tell the builder which part. The caller shipped; the primitive did not. Treating §3.1 as 'done' would silently drop the receipt and the boundary assertions.",
        "evidence": "src/modules/share/domain/splice-frontmatter.ts (164 lines) exports only spliceFrontmatterValue and emitScalar. It contains no SpliceReceipt, no U16Offset branded type, no exported splice(src, range, replacement), and no E-RANGE / E-SURROGATE assertions — all four are specified in §3.1.6 and §3.1.9.",
        "fix": "Add to §3.1 a status line: 'PARTIALLY SHIPPED in 4f97129. spliceFrontmatterValue (the §3.1.6 caller) exists at src/modules/share/domain/splice-frontmatter.ts and passes 907/907. The splice() primitive, the U16Offset branded type, SpliceReceipt, and the E-RANGE/E-SURROGATE assertions of §3.1.6/§3.1.9 are NOT implemented.' Update the §3.2 table row 1 effort from '0.5–1 day' to reflect only the remaining primitive."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1.7 (L239) and §11 (11-execution.md L787, L1154)",
        "severity": "MATERIAL",
        "issue": "Three different prescribed file paths for the splice writer, none of which matches where it actually landed. A builder following any of them creates a duplicate writer — the exact failure §10 L165-167 says the design exists to prevent.",
        "evidence": "§3.1.7: 'It ships as src/modules/mdmax/splice.ts'. §11 L787: 'new mdmax/domain'. §11 L1154: 'src/modules/mdmax/domain/splice-frontmatter.ts'. Actual: src/modules/share/domain/splice-frontmatter.ts (from 4f97129).",
        "fix": "Replace all three with src/modules/share/domain/splice-frontmatter.ts, or state explicitly that the file is to be MOVED to src/modules/mdmax/domain/ as part of step 2 and update the import in share-writer.ts, the test, and the oracle. Pick one; do not leave three."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "test/share/frontmatter-splice.test.ts L190-200 vs §7.5 F3 (07-product.md L877)",
        "severity": "MATERIAL",
        "issue": "The shipped gate omits the second conjunct the plan's own corrected gate requires. As written the corpus test passes for an implementation that returns src unconditionally, because a refusal round-trips byte-identically by definition.",
        "evidence": "The test asserts only s.changed===0, s.threw===0, s.identical===s.total. §7.5 F3 says: 'Restate the gate correctly: publish→unpublish byte-identical on 907/907, AND publish succeeds (no 502) on all 171.' I measured the splice at 0 refusals / 907 real publishes, so the gate is NOT vacuous today — but it is one refusal away from being so, and nothing detects that.",
        "fix": "Add to the corpus gate: assert that impl(src, KEY, VAL) !== src for every file (publish actually happened), and assert the published output contains the key. Measured baseline to lock in: 907/907 publish AND round-trip, 0 refusals."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§11.1 step 0 (11-execution.md L44) and §11.2",
        "severity": "MATERIAL",
        "issue": "Step 0's stated work is already done and its gate number is now wrong. A builder either redoes finished work or watches a satisfied gate report failure.",
        "evidence": "vitest.config.ts now contains `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]` (added in 4f97129) and is 17 lines, not 16. `npx vitest list --run` collects 84 unique files and 1,257 tests — verified this session. 83 was 247-164; the extra file is the new test/share/frontmatter-splice.test.ts.",
        "fix": "Mark step 0 DONE (4f97129) and change the gate cell from 'returns 83, not 247' to 'returns 84 (was 247 before 4f97129 added the .claude/ exclude)'. Same correction at §11 L106 and §11 L1208-1209."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§11.1 step 2 (11-execution.md L46)",
        "severity": "MATERIAL",
        "issue": "'Fix the two shipped destroyers' is half done, and both halves of the gate are already met on the Publish path. The remaining work is the properties-panel path only, which the step title does not say.",
        "evidence": "share-writer.ts fixed in 4f97129. Its read path is deps.reader.getFile(path), which returns raw {content, sha} with no parsing (src/modules/vault/application/ports.ts:25) — so the 502-on-171 condition is gone from Publish too. Still live: src/modules/preview/presentation/frontmatter.ts:47 and :59 (doc.toString()), reached via PropertiesPanel.tsx:79 → EditorPane.tsx:647 handleEdit (L498) → setContent + saveDraft. It reaches persistence.",
        "fix": "Rewrite step 2 as: 'The frontmatter splice — one destroyer remains. Publish (share-writer.ts) fixed in 4f97129: 907/907 byte-identical, no 502 on the 171. Remaining: route PropertiesPanel.tsx:79 through spliceFrontmatterValue instead of stringifyFrontmatterDoc, so preview/frontmatter.ts:47/59 stop reaching saveDraft.' Effort ~0.25 d."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§11 L1154-1156 (the day plan)",
        "severity": "MATERIAL",
        "issue": "The prescribed morning's work is already committed, at a different path, and the RED-first gate progression cited is no longer reachable.",
        "evidence": "L1154 asks for 'src/modules/mdmax/domain/splice-frontmatter.ts + barrel; rewrite share-writer.ts to use it; try/catch around the read so the 171 unparseable files stop 502-ing' with the gate 'moves from 17/736 toward 907/907'. All of that landed in 4f97129 at src/modules/share/domain/, and the gate is already at 907/907. The try/catch is moot — share-writer no longer parses.",
        "fix": "Replace the 09:00–13:00 row with the PropertiesPanel remediation (see step-2 finding) and the §3.1.6 splice() primitive. Replace the 15:00–16:00 row's '--broke' proof: reintroducing matter.stringify no longer reintroduces the defect at that call site, so the --broke mutation must now target spliceFrontmatterValue directly (e.g. make it return matter.stringify output) and be proven to go red."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§1.5 the measured-state block (01-orientation.md L465-476)",
        "severity": "MATERIAL",
        "issue": "Four of the block's lines are now false. This block is the plan's stated ground truth about the repo.",
        "evidence": "Measured this session: HEAD is 4f97129, not 1bd4dad. `git rev-list --left-right --count origin/main...HEAD` = 0/20, not 17. Engine code is no longer NONE — src/modules/share/domain/splice-frontmatter.ts is 164 lines. Tests: 84 files / 1,257 tests collected, not '83 test files' + '3,484 tests collected (66.4% abandoned worktrees)'.",
        "fix": "Set HEAD to '4f97129 fix(share): splice the public_slug byte range instead of regenerating frontmatter'; change 'engine code NONE' to 'engine code: src/modules/share/domain/splice-frontmatter.ts, 164 lines (4f97129). No core/ directory.'; change the tests lines to '84 test files / 1,257 tests collected [measured, vitest list --run, this session]' and delete the 3,484 / 66.4% line and its 'not re-run this session' caveat."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§1.2 the three-column table (01-orientation.md L72)",
        "severity": "MATERIAL",
        "issue": "MDMAX's Status cell reads 'Zero lines written'. No longer true.",
        "evidence": "src/modules/share/domain/splice-frontmatter.ts (164 lines) plus test/share/frontmatter-splice.test.ts (201 lines) landed in 4f97129.",
        "fix": "Change the MDMAX Status cell to 'First capability partially shipped: spliceFrontmatterValue, 164 lines, 907/907 on the pinned corpus (4f97129)'."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§2 (02-chronology.md L856)",
        "severity": "MATERIAL",
        "issue": "The justification for cutting all further research fan-out rests on a now-false measurement.",
        "evidence": "L856: 'Justification: 0 lines of engine code, 19 days after the first commit, with 12 of 20 commits being docs'. 4f97129 is a src/ commit; engine code exists.",
        "fix": "Change to: '0 lines of engine code at the time this was decided (2026-08-01, pre-4f97129); the first engine commit landed the same day. The cut stands on its original evidence — do not re-cite it as current state.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§2 (02-chronology.md L480-491)",
        "severity": "MATERIAL",
        "issue": "The test-count passage and its 'Contradiction, named and resolved' callout describe a configuration that no longer exists.",
        "evidence": "L482-483: 'vitest.config.ts sets no exclude and the default does not cover .claude/worktrees/**'. It now does (4f97129). L488-491 forbids repeating '65' and '3,484' but leaves 3,484 as the live figure; the live figure is 1,257.",
        "fix": "Append after L491: 'Superseded 2026-08-01 by 4f97129, which added `exclude: [\"**/node_modules/**\",\"**/dist/**\",\"**/.claude/**\"]` to vitest.config.ts. The honest current figure is 84 files / 1,257 tests. 247, 3,484, 2,314, 65 and 3,604 are all historical — none should be quoted as current.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§7.2.6 (07-product.md L283-287)",
        "severity": "MATERIAL",
        "issue": "Every load-bearing claim in this subsection is now false, including the verbatim file description.",
        "evidence": "'vitest.config.ts declares no exclude key at all (verified: the file is 16 lines and sets only environment, setupFiles and a @ alias)' — it is now 17 lines and sets exclude. '247 files, of which 164 (66.40%)' and 'the gate is vitest list --filesOnly moving 247 → 83' — measured now: 84 files, 1,257 tests, zero under .claude/.",
        "fix": "Retitle to '§7.2.6 The test suite proved nothing until 4f97129' and rewrite the body: 'Before 4f97129, npx vitest list --filesOnly returned 247 files of which 164 (66.40%) were abandoned worktrees. 4f97129 added the .claude/ exclude. Measured now: 84 files / 1,257 tests. Any N-tests-pass claim predating 4f97129 remains meaningless.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§10.6.3 (10-engine-spec.md L1004-1006)",
        "severity": "MATERIAL",
        "issue": "Two of the four defect rows describe code that no longer exists, and they are the two carrying the strongest consequence language ('18.7% of a real vault is unpublishable', 'live D7 violation on every Publish').",
        "evidence": "Row 'share-writer.ts:19 — calls matter(file.content) bare, no try/catch': share-writer.ts no longer calls matter at all; its read is deps.reader.getFile, which does not parse. Row 'share-writer.ts:26 — const next = matter.stringify(parsed.content, data)': line 25 is now spliceFrontmatterValue.",
        "fix": "Mark both rows 'FIXED 4f97129' and move them to a historical sub-table. Keep the gray-matter/index.js:161 row as rationale. The only row still describing live code is preview/presentation/frontmatter.ts:47 — promote it to the top and re-label the subsection 'One shipped D7 violation remains'."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§8 (08-market.md L393-394, L417) and §7 (07-product.md L255-258) and §13 (13-appendix.md L1118)",
        "severity": "MATERIAL",
        "issue": "Three further verbatim quotations of the removed line, each presented as current evidence of a live defect. §8 uses it as the public-facing proof point.",
        "evidence": "§8 L394 and §7 L258 both quote 'const next = matter.stringify(parsed.content, data);' as line 26 of share-writer.ts. §13 L1118: 'calls matter.stringify while its own header says \"splicing\" — silent data loss on every Publish'. The line is gone as of 4f97129.",
        "fix": "At each of the three sites, prefix the quote with 'Until 4f97129, line 26 read:' and append 'Fixed 2026-08-01; the header comment is now accurate.' Do not delete the passages — the historical defect is the argument for D7 — but they must not read as present tense."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "The 'two destroyers' framing — 01-orientation.md L83, L430, L442; 03-capabilities.md L128, L159, L161; 10-engine-spec.md L168",
        "severity": "MATERIAL",
        "issue": "Seven sites across four sections assert that two shipped paths violate D7. One remains. The count is load-bearing: §1 L442 uses it to close the argument that the splice writer must be built.",
        "evidence": "Verbatim: L83 'two shipped write paths in frontmatter destroy user data today'; L430 'Two shipped code paths violate this today'; L442 'a four-line function with two callers already broken in production'; L128 heading 'Two shipped code paths violate D7 today, on every Publish'; L159 'the only two write paths that ship'; L161 'two callers already broken in production'; L168 'two shipped code paths bypass'. `grep -rn 'gray-matter|matter.stringify' src/` now returns no write-path hit; only preview/presentation/frontmatter.ts:47/59 remains.",
        "fix": "Change every instance from two to one, in present tense, and add the parenthetical '(the second, share-writer.ts, was fixed in 4f97129)'. E.g. L83 → 'because one shipped write path in frontmatter still destroys user data today; a second was fixed on 2026-08-01.' L442 → 'it is a function that now exists, with one caller still broken in production.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§11 L374 (the 502 table row)",
        "severity": "MATERIAL",
        "issue": "Asserts a live 502 on the Publish path that can no longer occur.",
        "evidence": "L374: 'files that 502 because gray-matter throws and share-writer has no try/catch | 170 of 907'. share-writer.ts imports no YAML parser and reads raw bytes via reader.getFile. Separately §7 L281 already flags the 502 claim as never having been executed against a live vault.",
        "fix": "Change the row to 'files that 502 on Publish | 0 (was 170 of 907 before 4f97129; the path no longer parses YAML). The original claim was a code-path trace, never executed — see §7.2.5.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§11.2.0 (11-execution.md L76-88)",
        "severity": "MINOR",
        "issue": "The live-git block that exists specifically to correct a stale record is itself now stale.",
        "evidence": "Measured this session: refs/heads/engine/plan-and-diagnostics and refs/remotes/origin/engine/plan-and-diagnostics are both at 4f97129, not 1bd4dad; `git rev-list --left-right --count origin/main...HEAD` returns 0/20, not 0/17. origin/main is still 8eb4de2 (unchanged, correct).",
        "fix": "Update the two ref lines to 4f97129 and the count to '0 20'. Add a dated note: '[measured, re-run after 4f97129]' so the next reader can see which commit the block was taken at."
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§14 L23 (the APPLIED list, D6)",
        "severity": "MINOR",
        "issue": "The D6 test-count correction records numbers that have since moved.",
        "evidence": "L23: '65/3,604 replaced with 83 real files; vitest collects 247 files / 3,484 tests of which 66.4% are two abandoned worktrees'. Measured now: 84 files / 1,257 tests, zero worktree files.",
        "fix": "Append to the D6 entry: 'Superseded by 4f97129 (vitest exclude added): 84 files / 1,257 tests. 83 was the pre-fix real-suite count; the extra file is test/share/frontmatter-splice.test.ts.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.1.5 table (03-capabilities.md L170-175) — relationship to the new measurement",
        "severity": "MINOR",
        "issue": "The table's 119/13.12% will look wrong next to the test suite's 284/31.31% and invites a builder to 'correct' it upward. It is the more honest number and should be defended in place.",
        "evidence": "Measured this session under the publish-then-restore round trip: yaml leaves 284 byte-identical, but 170 of those are refusals; excluding them gives 114 (12.57%) — within half a point of the table's 119 (13.12%), which was measured as a no-edit round trip. The two agree; 31.31% is the outlier because it counts no-ops as wins.",
        "fix": "Add a footnote under the table: 'A publish-then-unpublish round trip over the same corpus scores yaml at 284/907 (31.31%) only because it counts the 170 refusals — files where nothing is published — as successes. Excluding them gives 114/907 (12.57%), consistent with the 119/13.12% above. Quote 13.12%, never 31.31%.'"
      },
      {
        "cls": "Staleness — what commit 4f97129 invalidated",
        "section": "§3.2 capability table (03-capabilities.md L75) and §3.1.11 kill condition",
        "severity": "MINOR",
        "issue": "Row 1 still prices the splice writer at 0.5–1 day with 'nothing' blocking, and §3.1.11 still speaks of the gate as unattempted.",
        "evidence": "§3.1.11: 'If the gate cannot reach 907/907 the defect is in our implementation.' The gate exists at test/share/frontmatter-splice.test.ts and reaches 907/907 — I reproduced it independently this session (907 identical, 0 changed, 0 threw, 0 refusals).",
        "fix": "Change the row-1 status to 'SHIPPED (frontmatter caller) / OPEN (splice primitive)' and effort to '0.25 d remaining'. In §3.1.11 add: 'Resolved 2026-08-01: the gate reaches 907/907 with 0 refusals [measured, corpus_id sha256:3a010b16, reproduced independently].'"
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§2 Chronology §2.1 (Round 3 blockquote) and §14 D6's correction text",
        "severity": "BLOCKING",
        "issue": "A `[primary]` verbatim blockquote of HANDOFF §0.7 does not match the source. The plan quotes the handoff as saying \"65 test files, 3,484 tests collected (66.4% of them abandoned worktrees) at last full run.\" The handoff actually says \"65 test files, 3,604 tests passing at last full run.\" A global find-replace of the test numbers was applied blindly and rewrote the inside of a quotation. §14 D6's own correction text repeats the fabricated quote a second time.",
        "evidence": "`grep -n \"Test suite is green\" HANDOFF-mdmax-markdown-engine-2026-08-01.md` → line 22: \"7. Test suite is green: **65 test files**, 3,604 tests passing at last full run.\" Line 342 of the same file: \"tests   65 test files · 3,604 passing at last full run\". §14 line 23 proves the authors knew the real figure (\"65/3,604 replaced with **83 real files**\"). §01 §1.6 additionally cites this to \"HANDOFF §10\"; the line is §0.7 (line 22), and §10's line 342 also says 3,604/65. The string \"3,604\" appears nowhere in the 14 tree files except that one §14 line.",
        "fix": "§02 §2.1: restore the blockquote to `> \"Test suite is green: **65 test files**, 3,604 tests passing at last full run.\"` and change the following sentence to \"The measured figure at the time was **247 files / 3,484 tests**, of which 164 files / 2,314 tests were stale duplicates.\" §14 D6: replace the phrase \"HANDOFF…§0.7's '83 test files under `test/`, 3,484 tests collected (66.4% of them abandoned worktrees)'\" with \"HANDOFF…§0.7's '65 test files, 3,604 tests passing'\". Then grep all 14 files for any other `[primary]` quotation containing \"3,484\" or \"83 test files\" and restore those too."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§11 Execution §11.2.1 acceptance gate, §11.1 step 0, §11.16 Monday 09:30 — against §09 §9.5/§9.11 (LR#66)",
        "severity": "BLOCKING",
        "issue": "The step-0 acceptance gate is an equality on a growing count, which §09 explicitly imports a rule against — and it is already tripped. The fix has landed (`vitest.config.ts` now excludes `**/.claude/**` at commit 4f97129) and the live count is **84**, not 83. Run as written, the gate goes RED on a healthy tree: exactly the false-RED failure mode the plan cites six times.",
        "evidence": "Live: `find test -name '*.test.ts' -o -name '*.test.tsx' | wc -l` → **84**. `vitest.config.ts` now reads `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]` with a comment citing PLAN.md S0. §11.2.1: \"`npx vitest list --filesOnly | wc -l` returns **83**\". §09 §9.5 Gate 2: \"**Assert floors on safety, never equalities on quality.**\" §09 §9.11 row #66: \"equality-pinned pass counts ... punish coverage growth and reward standing still.\"",
        "fix": "§11.1 step 0 gate and §11.2.1: replace with \"`npx vitest list --filesOnly | grep -c '\\\\.claude/worktrees'` returns **0**, and `npx vitest list --filesOnly | wc -l` is **≥ 80 and < 100**.\" Add to §11.2.0 (\"What is already done\"): \"§11.2.1's vitest exclude landed in `4f97129`; `vitest.config.ts` excludes `**/.claude/**` and the suite is 84 files. Step 0 now consists only of the worktree removal, the derivation script, the §1.1a reconciliation and the litter.\" Update the three surviving \"83 test files\" figures in §1.6, §7.1 and §9.6's rung table to **84**."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§1 Orientation §1.5 D4 row vs §12 Risks §12.1.1 (W-A) and §13 Appendix §13.7",
        "severity": "BLOCKING",
        "issue": "The export contract is DECIDED in §1 and OPEN in §12, and §13's restatement of D4 silently drops the clause that creates the conflict. §1.5's D4 consequence cell states the answer as settled and forbids the alternative; §12.1.1 states the same question is unresolved, offers the forbidden alternative as candidate A2, and says the deciding question \"has never been asked\"; §11.14 K10 also treats it as live. §13.7's \"ten settled decisions, restated\" omits the export clause entirely, so a reader who trusts the appendix sees no conflict at all.",
        "evidence": "§1.5 D4 consequence, verbatim: \"The export is a **ZIP of clean `.md` files**, because that is what a departing human actually wants. Forbids shipping a single-file container as the export format\". §12.1.1: \"**Status: OPEN. This is the one that most needs a founder in a room.** ... **These cannot both be the export.**\" and \"**Cost: ten minutes. It has never been asked.**\" §12.5 lists W-A as **OPEN**. §11.14 K10 makes it conditional on P7a. §13.7's D4 row reads only \"The durable artifact is plain markdown; if frontmatter dies we ship a full export and migration. Max tier gets offline access. **Not** 'your git repo is the backend'.\" — no export-format clause.",
        "fix": "§1.5 D4 consequence cell: replace with \"Obliges a full export and a migration. **The FORMAT of that export is OPEN — see W-A: a zip of clean `.md` files (recommended) versus one mother-markdown with breakpoints. Do not treat the zip as settled.**\" §13.7 D4 row: append \"**The export format is OPEN (W-A).**\" Leave §12.1.1 as the governing text — it is the only one that quotes the founder verbatim from the transcript and names the unasked question."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§1 Orientation §1.3 and §1.5 D5 row vs §9 AIOS §9.3–§9.5 and §11 §11.12.3 / §11.16",
        "severity": "BLOCKING",
        "issue": "§1 states that AIOS contributes a working practice and no toolchain, and that AIOS work consumes none of this plan's sequence or budget. §9 DECIDES a full Node reimplementation of `sgnk-regression-gate.sh` with a new refusal code, an environment-fingerprint schema, a `gates.jsonl` store, three named gates and a rung in `npm run verify`; §11 budgets it at ~1 founder-day and schedules it for Thursday of week one. Either §1 is wrong about the scope or §9/§11 are building something §1 says is out of scope.",
        "evidence": "§1.3: \"So the AIOS contribution to this plan is a working practice, not a toolchain. It appears in the working-practices section as the rule *make the gate go red first*, **and nowhere else**.\" §1.5 D5 consequence: \"AIOS work does not consume this plan's sequence, budget or attention. It contributes one working practice (make the gate go red first) **and nothing else**.\" §9.3: \"**DECIDED.** We import exactly one AIOS mechanism into the frontmatter repository: `~/.sgnk/bin/sgnk-regression-gate.sh` (164 lines, bash), reimplemented in Node as `specs/harness/gate.mjs`.\" §11.12.3: \"**Effort: ~1 founder-day**, most of it the selftest\". §11.16 Thursday 16:00–18:00: \"**§11.12.3** — port `gate.mjs`, selftest first\".",
        "fix": "§1.3, final paragraph: replace \"a working practice, not a toolchain ... and nowhere else\" with \"one working practice (*make the gate go red first*) and **one mechanism**: `sgnk-regression-gate.sh`, reimplemented in Node as `specs/harness/gate.mjs` (§9.3, ~1 founder-day, scheduled in §11.16). Nothing else transfers — see §9.7 for the seven components explicitly refused.\" §1.5 D5 consequence: replace \"and nothing else\" with \"plus one ported mechanism (§9.3), budgeted at ~1 founder-day in §11.12.3. Everything else in AIOS is refused in §9.7.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§11 Execution (whole sequence, §11.1–§11.16) vs §10 Engine spec §10.10.2 rows 1 and 4, and §3 §3.0.1 dependency column",
        "severity": "BLOCKING",
        "issue": "§10 names two prerequisites with hard ordering constraints and §11 never schedules either. The offset model appears **zero times** in §11. `normalize()` appears exactly once — in §11.12.2's rules table, whose own right-hand cell reads \"it appears in no effort estimate anywhere\" — and §11 then does not give it one. §11.16 schedules the splice writer for Tuesday of week one and the review loop (which persists anchors) as step 6, so both ordering constraints are violated by the plan's own calendar.",
        "evidence": "`grep -n \"offset model|OffsetMap|normalize/1|slug/1\" 11-execution.md` → only line 850, the P7 rules row. §10.10.2 row 1: \"**The offset model** (§10.2) | the first time a byte offset meets a UTF-16 offset ... **Half a day.** Must land **before** the splice writer's second caller\". §10.10.2 row 4: \"**`normalize/1` and `slug/1` unversioned** ... the first time either function changes **after** an anchor is persisted ... **One afternoon.**\" §3.0.1 lists capability 2's dependencies as \"§3.8 offset model, §3.8 `normalize/1`\" and capability 4's as \"§3.8 `slug/1` pinned\". §11.16 Tuesday 09:00–13:00 is the splice writer; §11.1 step 6 is the review loop.",
        "fix": "Add two rows to §11.1 between steps 0 and 2, and two slots to §11.16: \"**0a** | **The offset model** — `src/modules/mdmax/offsets.ts`, branded `U16Offset`/`ByteOffset`/`GraphemeIndex`, the eslint `no-restricted-syntax` rule | Sagnik | 0.5 d | nothing | **2** | `fromByte(toByte(u)) === u` at 100 sampled offsets per file over all 1,084 corpus files, and `assertBoundary` throws on a synthetic mid-surrogate offset\" and \"**0b** | **Freeze `normalize/1` and `slug/1`** — two `VERSION` constants, a 200-case golden file, one CI assertion (rule P7) | Sagnik | 0.5 d | nothing | **6** | the golden file's output changes ⇒ the build fails unless `NORMALIZE_VERSION` changed\". Schedule 0a on Monday 11:00–12:00 (before Tuesday's splice work) and 0b on Friday. Then delete the \"it appears in no effort estimate anywhere\" cell in §11.12.2's P7 row and replace with \"§11.1 step 0b, 0.5 d\"."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§3 §3.1.6 vs §11 §11.4.2 and §11.11.3 — against the shipped code at 4f97129",
        "severity": "BLOCKING",
        "issue": "The splice writer's module path is specified three different ways across the plan, and the code that has already landed uses a fourth. §11.11.3's parallelism contract depends on the path: it assigns step 2 the artifact `mdmax/domain` and forbids step 4 (share links) from touching it. With the writer actually living in `share/domain`, step 4 owns \"`share` (all layers)\" and the exclusion no longer protects the writer — two parallel agents can now be dispatched onto the same file.",
        "evidence": "§3.1.6: \"`// src/modules/mdmax/splice.ts` — the ONLY function permitted to write document bytes\". §11.4.2 (DECIDED): \"New module `src/modules/mdmax/domain/splice-frontmatter.ts`, exported from a new barrel `src/modules/mdmax/index.ts`.\" §11.11.3 disjoint map: step 2 owns \"new `mdmax/domain`\", step 4 owns \"`share` (all layers)\" and \"must not touch: `mdmax/domain`\". Live: `src/modules/share/domain/splice-frontmatter.ts` (7,236 bytes), imported by `share-writer.ts` as `@/modules/share/domain/splice-frontmatter` — a deep path, which §11.4.2's own barrel rule also forbids.",
        "fix": "Pick one and make all three agree. Recommended: keep the shipped location and correct the plan, because a domain-pure splicer inside `share/domain` still satisfies the `eslint-plugin-boundaries` constraint §11.4.2 cites. §3.1.6: change the header comment to `// src/modules/share/domain/splice-frontmatter.ts`. §11.4.2: replace the \"Where it lives\" paragraph with \"Shipped in `4f97129` at `src/modules/share/domain/splice-frontmatter.ts`. **Open**: whether it moves to a `mdmax` module when a second caller appears (the properties panel, §5.9.2). Until it does, it must be imported through `@/modules/share`, not the deep path.\" §11.11.3: change step 2's owned artifact to \"`share/domain/splice-frontmatter.ts`, `share/infrastructure/share-writer.ts`, `vault/infrastructure/markdown-parser.ts`\" and add \"`share/domain/splice-frontmatter.ts`\" to step 4's must-not-touch column."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§3 §3.4.2 (governing ruling) vs §4 §4.7 A5, §6 §6.10, §13 §13.4 C1, §13.6 row 6, §13.7 glossary",
        "severity": "MATERIAL",
        "issue": "§3 issues an explicit governing rule about the two-tier link numbers and four other sections ignore it. §3.4.2 rules that the `markdown-base` verifier governs, that the wikilink population is 13,026, and that \"the **gap**\" must be quoted \"rather than either percentage\". §4, §6 and §13 all quote 85.71% and 27.65% as settled. Worse, §13.4 C1 — the index a reader is told to consult before quoting anything — marks 27.65% \"reproduced **YES**\", and §13.6 row 6, which arbitrates this exact disagreement, never lists 13,026 at all.",
        "evidence": "§3.4.2: \"**Which governs: the verifier.** Quote 13,026 as the wikilink population, state that the resolution rate depends on whether front-matter links are counted, and quote the **gap** rather than either percentage.\" §4.7 A5: \"`[text](path.md)` resolves at **85.71%**, `[[Name]]` at **27.65%**\". §6.10: same pair. §13.4 C1: \"`[[Name]]` resolves **2,754/9,962 = 27.65%** ... reproduced **YES** — *'I replicated it a third time'*\". §13.6 row 6 lists \"13,028 wikilinks (`hold-more`) vs 9,962 `[[Name]]` (`docs/mdmax/PLAN.md`)\" and omits 13,026. Primary source (wf-final-gate result JSON, `markdown-base` verification): \"The pinned corpus contains 13,026 wikilinks, not 9,077. The 9,077 is a body-only count with YAML frontmatter stripped ... The delta is exactly 3,850.\"",
        "fix": "§4.7 A5 and §6.10: replace \"resolves at **85.71%**; `[[Name]]` at **27.65%** — a 58-point gap\" with \"the two syntaxes resolve **58 points apart** on identical files; do not quote either rate without its denominator — see §3.4.2.\" §13.4 C1: change the \"reproduced\" cell from **YES** to **REFUTED IN PART** and append \"the 9,962 denominator strips YAML frontmatter, removing 3,850 links; the pinned-corpus population is 13,026 (`markdown-base` verifier). The 58-point gap survives; the percentages do not.\" §13.6 row 6: add 13,026 to the list of values and change the ruling to \"State the parser configuration with every link number. For the wikilink population specifically, **13,026 governs** (frontmatter kept, code masked) per §3.4.2; 13,028 is `hold-more`'s regex over the same corpus with a different masking rule; 9,962 and 9,077 are frontmatter-stripped and must not be used as denominators for a corpus-wide rate.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§3 §3.6.2 (DECIDED) vs §4 §4.11.6 and §4.13 (RECOMMENDED), with §11 §11.15 and §12 §12.5 (10.5)",
        "severity": "MATERIAL",
        "issue": "Two different external provenance vocabularies are each adopted with the identical justification \"rather than invent our own\", in two sections, and nothing reconciles them. §3 DECIDES Open Knowledge Format; §4 RECOMMENDS TEI's attribute names. A third, ITS 2.0, supplies §3.6.4's per-span rule. §11 and §12 both restate OKF-only, so §4's TEI recommendation is orphaned — but §4 is the section that correctly identifies the gap OKF leaves (per-span source/method/confidence), which is exactly §3.6.4's unresolved OPEN decision.",
        "evidence": "§3.6.2: \"> **DECIDED: emit Open Knowledge Format (OKF) v0.2 §5 fields ... Do not invent a provenance vocabulary.**\" §4.11.6: \"**Adopt TEI's field names (`@cert`, `@resp`, `@source`) rather than invent our own**, so the vocabulary has a citable definition on day one.\" §4.13 RECOMMENDED list: \"Adopt **TEI's `@cert` / `@resp` / `@source`** names for provenance rather than inventing a vocabulary.\" §3.6.4 OPEN: \"Emitting OKF verbatim **silently descopes per-span provenance** ... **(a)** emit OKF for document-level provenance **and** keep a separate per-span `{method, confidence}` sidecar record, or **(b)** state plainly that per-span provenance is cut.\" §11.15: \"emit **OKF v0.2 §5.1** fields and move on.\" §12.5 10.5: \"a target, not a host ... emit its §5 provenance fields rather than inventing a vocabulary.\"",
        "fix": "They are not competing — they answer §3.6.4's two levels. Make that explicit. §4.11.6 and §4.13: replace \"Adopt TEI's field names ... rather than invent our own\" with \"**Document-level provenance is DECIDED as OKF v0.2 §5 (§3.6.2).** TEI's `@cert`/`@resp`/`@source` are the candidate vocabulary for the **per-span** slot OKF explicitly leaves empty — that is §3.6.4's OPEN decision (a), not a competing proposal. If (b) is chosen, TEI is dropped with it.\" §3.6.4: add to the OPEN box \"If (a): use TEI's `@cert` / `@resp` / `@source` names (a global attribute class since 2009, §4.11.6) rather than inventing field names; ITS 2.0's `annotatorsRef` supplies the method-required-with-confidence rule.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§10 §10.2.1 (explicit REFUTATION) vs §3 §3.8.1 and §13 §13.4 row B8",
        "severity": "MATERIAL",
        "issue": "§10 explicitly refutes the \"93.4% / 93.8% already diverge\" statistic as a risk figure — it is a tautology, being the definition of \"this file contains a non-ASCII character\" — and instructs that it not be used. §3.8.1 opens with it as the justification for the whole offset model, and §13.4 B8 records it in the measurement index with no refutation flag. §13 is the index a reader is told to consult before quoting a number.",
        "evidence": "§10.2.1: \"*'93.4% already diverge, so the boundary risk is 21x what the engine PLAN says.'* The verifier independently counted files containing at least one non-ASCII character and got **exactly 1,013** — identical to `bytes != UTF-16`. So that statistic is not a discovered hazard, it is the definition of 'this file is not pure ASCII' `[measured]`. **REFUTED. Do not use the 93.4% as a risk figure.**\" §3.8.1: \"**Only 67 of 1,080 files have `bytes == UTF-16 code units == code points`. 93.8% already diverge.**\" §13.4 B8: \"only **67 of 1,080** files have bytes == UTF-16 units == code points; **93.8% already diverge** ... reproduced **NO**\". Corroborated in the final-gate JSON kill audit: \"Files containing >=1 non-ASCII character = 1013 of 1084 (93.45%), identical to b_ne_u16 — and it is a tautology.\"",
        "fix": "§3.8.1: replace the opening two sentences with \"**3.51% of files (38 of 1,084) contain a non-BMP character, where a UTF-16 offset can split a surrogate pair, and 3.87% (42) have code-point boundaries that are not grapheme boundaries.** `[measured, corpus_id sha256:3a010b16…]` (Do **not** cite the 93.4% bytes-vs-UTF-16 divergence as a risk figure — §10.2.1 refutes it as a restatement of 'this file is not pure ASCII'.)\" §13.4 B8: append to the value cell \"— **but see §10.2.1: the 93.8% is a tautology (it equals the count of files containing any non-ASCII character) and is REFUTED as a risk figure. The live figures are non-BMP 3.51% and grapheme-divergent 3.87%.**\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§8 §8.6 item 2 and §12 §12.1.2 exclusion table, against §2 D2, §5 §5.10.3, §7 §7.5 N4 and §11 §11.8",
        "severity": "MATERIAL",
        "issue": "Both §8 and §12 list \"sidecar-resolved block metadata\" in the table of capabilities structurally excluded by D6. Read literally, that excludes the product: comments are block-scoped metadata stored in a sidecar, which D2 mandates and four other sections specify in detail. The rows are missing the qualifier that makes them true — the exclusion bites only when the *file* carries a reference the reader must resolve elsewhere, and under D2 the file carries nothing at all.",
        "evidence": "§8.6 item 2 table row: \"**Sidecar-resolved block metadata** (a block that carries meaning stored outside the file) | by definition the meaning is not in the file.\" §12.1.2 table row: \"**block-scoped metadata resolved from a sidecar** | the file carries a key; a plain reader gets the key, not the meaning\". Against: §1.5 D2 — \"Nothing about comments is ever written into the `.md`\"; §5.10.3 rail table — \"**Comment** | the annotation overlay — **sidecar, D2** ... outside the file entirely\"; §7.5 N4 — the full comment schema, sidecar, anchored by TextQuoteSelector; §11.8 — \"A `comments` collection ... **D2 gate, non-negotiable:** delete the entire sidecar → `git status` is clean\".",
        "fix": "§8.6 item 2 and §12.1.2, same row in both: replace the row with \"**Block metadata whose REFERENCE is written into the file** (a key, id or pointer the reader must resolve elsewhere) | the file carries a key; a plain reader gets the key, not the meaning. **This does NOT exclude the comment sidecar (D2), which writes zero bytes into the file — a plain reader sees an unannotated document, which is the intended degradation.**\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§1 Orientation §1.5 D9 row and §13 §13.7 restatement vs §6 §6.4 Corrections 1 and 2",
        "severity": "MATERIAL",
        "issue": "§1's stated evidence for D9 is refuted in §6 and neither §1 nor §13 records it. §1.5 cites JSON-LD `@context` as a format that \"names a vocabulary and never carries the definition\". §6.4 reads the W3C spec and shows an embedded `@context` is a carried definition and the first form the spec teaches, and §6.4 Correction 2 shows `@import` lets an embedded context dereference a remote IRI at processing time. §6.0 therefore RECOMMENDS amending D9. §13.7's \"ten settled decisions, restated\" carries D9 unamended and unqualified.",
        "evidence": "§1.5 D9 row: \"`HANDOFF…§7.1` — every self-declaring document format that succeeded (JSON-LD `@context`, the shebang line, Racket's `#lang`) names a vocabulary and never carries the definition\". §6.4 Correction 1: \"D9 as written forbids something the standard it cites explicitly permits ... *'The value of @context MUST be null, an IRI reference, a context definition, or an array composed of any of these.'* ... **An embedded `@context` is a carried definition, and it is the first form the spec teaches.**\" §6.4 Correction 2: \"**So an embedded context can dereference a remote IRI at processing time.** 'Closed' is false. 'No dereference' is false ... **do not cite JSON-LD as evidence that embedded denotations are safe.**\" §6.0: \"**RECOMMENDED** Amend D9 from *'a document NAMES a capability, never CARRIES one'* to *'a document may CARRY a denotation; it must only ever NAME a procedure; and it may lower its own capability, never raise it.'*\"",
        "fix": "§1.5 D9 \"Recorded in\" cell: replace the JSON-LD clause with \"`HANDOFF…§7.1` — the shebang line and Racket's `#lang` name a vocabulary and never carry the definition. (**JSON-LD `@context` was cited here as a third example and §6.4 refutes it: an embedded context is a carried definition and can dereference via `@import`. Do not use JSON-LD as evidence for D9.**)\" Add to the same row's consequence cell: \"§6.0 RECOMMENDS amending D9 to *'may CARRY a denotation, may only NAME a procedure, may lower its own capability and never raise it'* — evidence-driven, operationally inert this cycle.\" §13.7 D9 row: append \"**Amendment RECOMMENDED in §6.0; not yet accepted.**\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§1 Orientation §1.4d F-ERA-1 vs §1.4b(5) and §13 §13.4 row E8",
        "severity": "MATERIAL",
        "issue": "A pre-registered falsifier and the measurement it is supposed to test are two different ratios presented as one quantity, and the plan's own headline number is already below the threshold. F-ERA-1 fires when \"the excess growth ratio drops below 1.5×\" and states \"It is 3.1× today\" — but the 3.1× is one package (react-markdown) over the control median, while the CONFIRMED measurement two subsections earlier is the eight-parser median excess at **1.4388**. A reader applying the pre-registered threshold to the plan's own confirmed number concludes the era thesis is already dead. Separately, §1.4b(5) says the verifier reproduced every figure \"to the digit\" while §13.4 E8 marks the same claim reproduced **NO**.",
        "evidence": "§1.4d F-ERA-1: \"Excess growth ratio drops below **1.5×**. It is **3.1×** today (react-markdown ×8.75 ÷ control median ×2.81)\". §1.4b(5): \"Generic markdown parsers grew only **1.44×** faster than the npm registry baseline ... eight-parser median **4.0495**, fourteen-package non-markdown control median **2.8145**, excess **1.4388**.\" §13.4 E8: \"generic markdown parsers grew only **1.44×** faster than the whole npm registry ... reproduced **NO** — **JavaScript only**\". Primary source (final-gate `the-era` verification, Claim 3, verdict CONFIRMED): \"Recomputed: 8-parser median = 4.0495, 14-package control median = 2.8145, excess = 1.4388 ... Every one of the 22 raw download figures reproduces to the digit.\"",
        "fix": "§1.4d F-ERA-1: rewrite the threshold cell as \"**The model-facing stack** (react-markdown, remark-gfm, rehype-raw, streamdown, remend) falls back to within **1.5×** of the 14-package npm control. It is **3.1×** today (react-markdown ×8.75 ÷ control median ×2.81). **Note this is a different quantity from §1.4b(5)'s 1.4388, which is the eight-GENERIC-parser median excess — the generic parsers are already near the baseline and that is the point of claim (5). Do not apply this threshold to 1.4388.**\" §13.4 E8: change reproduced from **NO** to **PARTIAL** and set the cell to \"the 22 raw figures and the 4.0495 / 2.8145 / 1.4388 arithmetic were re-derived to the digit by the area's verifier (verdict CONFIRMED); what was **not** reproduced is any non-JavaScript registry — PyPI trends were unmeasurable.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§9 §9.3.3 and §9.4(b) vs §11 §11.12.3",
        "severity": "MATERIAL",
        "issue": "§11's port specification omits the one deliberate improvement §9 DECIDED, and writes the gate store to a different path. §9.3.3 identifies a hole in the shell original (`_add` never checks the `REPRO_NOOP` sentinel, so `--broke 'exit 97'` registers as proven) and DECIDES a fifth refusal code, exit 6, at registration time. §11.12.3's \"contract, kept verbatim\" enumerates exits 2/3/4/5 only and treats REPRO_NOOP as a `--verify-all` concern — i.e. it ports the bug §9 exists to close.",
        "evidence": "§9.3.3: \"**DECIDED — the port adds a fifth refusal code:** | `6` | `--broke` exited with the reserved `REPRO_NOOP` status. It reintroduced nothing; its red proves nothing; registration is refused.\" §11.12.3: \"Refuse with distinct exit codes: **2** already-red assertion · **3** vacuous gate · **4** missing repro · **5** write-did-not-land. `--verify-all` ... treats a reserved `REPRO_NOOP` as **INCONCLUSIVE, never as proof**.\" Store path: §9.4(b) \"Every row written to `specs/harness/gates.jsonl`\"; §11.12.3 \"storing rows to `specs/gates.jsonl`\".",
        "fix": "§11.12.3: add a fourth bullet to the contract — \"`--add` **also** refuses with exit **6** when `--broke` returns the reserved `REPRO_NOOP` status (97). The shell original checks this only in `_verify_all`, so `--broke 'exit 97'` registers as proven; that is the hole §9.3.3 requires the port to close.\" And change \"`specs/gates.jsonl`\" to \"`specs/harness/gates.jsonl`\" so it matches §9.4(b)."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§3 §3.0.1 status table vs §11 §11.9 / §11.15 and §12 §12.2 N6, arbitrated inconsistently by §13 §13.6 row 10",
        "severity": "MATERIAL",
        "issue": "The container's status is stated four ways and the appendix's arbitration picks a fifth. §3 gives it an affirmative RECOMMENDED status with a sequence position; §11 makes it a precondition that \"may never open\" and its governing lens lists it under NEVER BUILD; §11.15 and §12 both put it in do-not-build tables; §13.6 says \"the plan governs — a legitimate later feature\". §11.17 item 4 flags the §3-vs-§11 conflict only conditionally (\"If Section 3 ... sequences pack/unpack unconditionally\") — it does, so the conflict is live and unresolved.",
        "evidence": "§3.0.1 row 7: \"**The container — `pack` / `unpack`** | **RECOMMENDED, SEQUENCED after 1 and 2** | high\". §3.7.2: \"**RECOMMENDED, and sequenced AFTER capabilities 1 and 2. Not in the MVP.**\" §11.9: \"**step 7 is not a task. It is a precondition with a task behind it** ... it may never open\" and \"The `premortem-integrator` lens lists it under **'NEVER BUILD'**, and **that lens governs**.\" §11.15 row 1: \"**The container / mother-markdown / `mdmax pack` as a write surface**\" under \"what we do not build\". §12.2 N6: \"**Countermeasure.** Do not build the container.\" §13.6 row 10: \"**The plan governs** ... a bounded read-only machine-facing pack is a legitimate later feature.\"",
        "fix": "§3.0.1 row 7 status cell: replace \"**RECOMMENDED, SEQUENCED after 1 and 2**\" with \"**GATED — zero effort until BOTH §11.9 preconditions fire (P7a: the founder answers K-D; P7b: a live-model token measurement). §11.15 and §12.2 N6 list it under do-not-build. If both fire it ships as a read-only transport, sequenced after capabilities 1 and 2.**\" §3.7.2: replace the status box's first sentence with the same. §11.17 item 4: change \"If Section 3 of this plan sequences pack/unpack unconditionally\" to \"Section 3's §3.0.1 status cell has been corrected to GATED; if any future draft restores an unconditional sequence position, §11.9 governs.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§12 §12.2 N4 vs §2 §2.5 item 6 and §13 §13.4 row A10",
        "severity": "MATERIAL",
        "issue": "The same external paper carries three different evidence tiers in three sections, and the strongest claim is in the section §14 did not audit. §14 D2 fixes the §2 leg and names the §2-vs-§13 clash, but §12.2 N4 is the one that asserts `[primary]`, \"read directly\", and it is untouched — so applying D2 alone leaves §12 claiming a direct read that §13 says was an abstract only.",
        "evidence": "§12.2 N4: \"arXiv 1512.06195, **read directly**: **20,953** highlighted-text annotations; **~22%** can no longer be attached ... only ~12% are recoverable ... and **53%** of those still attached are in danger. `[primary]`\" §2.5 item 6: \"about 22% of 20,953 annotations can no longer attach `[secondary — arXiv 1512.06195, **NOT read directly**; verify before citing]`\". §13.4 A10: \"Hypothes.is: 20,953 annotations, ~22% no longer attachable | `against`, **arXiv abstract only** | `[secondary]` | reproduced **NO**\".",
        "fix": "Apply §14 D2 to §2 as written, and additionally amend §12.2 N4 and §13.4 A10 to one shared tier. §12.2 N4: replace \"read directly\" with \"**abstract read directly** by the final gate's `against` area; the full text was not retrieved\" and change the tier to `[primary — abstract]`. §13.4 A10: change the source cell to \"`against`, arXiv **abstract** read directly\" and the tier to `[primary — abstract]`, reproduced **NO**. All three sections then say the same thing: the four figures come from the abstract, not the paper."
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§3 §3.3.10 and §3.7.9 (kill-condition headers) vs §11 §11.14",
        "severity": "MINOR",
        "issue": "Two capability blocks announce a kill-condition count and then list a different number, so §11.14's kill-gate table cannot be reconciled against §3 by count. §3.3.10 says \"four, any one of which ends it\" and lists five; §3.7.9 says \"five, any one of which fires\" and lists six. §3.3.10's fifth item is K5, which §11.14 splits into K5, K5b and K5c — so the cert kill conditions total six across the plan and are labelled four in §3.",
        "evidence": "§3.3.10 header: \"#### 3.3.10 Kill conditions — four, any one of which ends it\", followed by numbered items 1–5 (item 5 being \"**Product gate K5** — fewer than 5 of 30 probed users say they would install it\"). §3.7.9 header: \"#### 3.7.9 Kill conditions — five, any one of which fires\", followed by numbered items 1–6 (item 6 being \"**K-D**, and it costs one conversation\"). §11.14 lists K5, K5b, K5c for the certificate and K10 for pack/unpack.",
        "fix": "§3.3.10: change the header to \"Kill conditions — five, any one of which ends it\" and append to item 5 \"(§11.14 splits this into **K5** — the 30-user probe — plus **K5b**, semantic BROKEN below 1% of blocks on an external corpus, and **K5c**, the 60-day decay test).\" §3.7.9: change the header to \"Kill conditions — six, any one of which fires\" and append to item 6 \"(this is §11.14's **K10** / §11.9's P7a).\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§9 §9.4(a) and §9.6 vs §1 §1.3, against the kill audit in the final-gate JSON",
        "severity": "MINOR",
        "issue": "The 901-file TypeScript count is contested inside the primary source and neither section that quotes it says so. The final gate's own kill auditor recounted and got 898 (299 git-tracked). §1.3 quotes 901 with a scope caveat but not the dispute; §9.4(a) and §9.6 quote it as settled and add a second figure (296) that is also off.",
        "evidence": "§9.4(a): \"901 first-party `.ts`/`.tsx` files including worktrees and **296** under `src/`, `test/` and `specs/` alone `[measured]`\". §9.6: \"**zero** of the repository's **901** first-party `.ts`/`.tsx` files\". §1.3: \"the 901 figure counts the whole working tree including `test/` and two abandoned git worktrees; `src/` alone holds **212**\". Final-gate JSON, kill audit: \"Minor: I count 898 first-party .ts/.tsx (299 git-tracked), not the verifier's 901.\" Live 2026-08-01: 903 including worktrees, **298** under `src`/`test`/`specs`, 301 git-tracked, 213 under `src/`.",
        "fix": "§9.4(a): replace \"901 first-party `.ts`/`.tsx` files including worktrees and 296 under `src/`, `test/` and `specs/` alone\" with \"roughly **900** first-party `.ts`/`.tsx` files including the two stale worktrees (the verifier says 901, the kill auditor 898, a live recount 903 — the spread is worktree churn) and **298** under `src/`, `test/` and `specs/` alone `[measured, 2026-08-01]`\". §9.6: change \"901\" to \"~900\". §1.3: append to the existing caveat \"— and the 901 is itself contested: the final gate's kill auditor recounted 898.\""
      },
      {
        "cls": "cross-section-contradictions",
        "section": "§1 §1.5 D7, §3 §3.1.4, §5 §5.11, §7 §7.2.5, §8 §8.5 item 6, §10 §10.6.3 / §10.10.2 row 2, §11 §11.1 / §11.4 / §11.16, §12 §12.1",
        "severity": "MATERIAL",
        "issue": "STALE since commit 4f97129. Every section that describes the D7 violations says two shipped write paths are live; one is fixed. `share-writer.ts` now calls `spliceFrontmatterValue` and never touches `matter.stringify`. The consequence is not cosmetic: §11.16's Tuesday 09:00–13:00 block is four hours allocated to work that has landed, §11.1 step 2 is marked as unstarted, and §10.10.2 hazard row 2 says \"replace **both** with `splice()` in the same commit\" when only one remains.",
        "evidence": "Live `src/modules/share/infrastructure/share-writer.ts`: `const next = spliceFrontmatterValue(file.content, \"public_slug\", slug);` — `matter` is no longer imported. Commit 4f97129 message records the gate: \"byte-range splice 907/907 100.00% 0 changed, 0 threw\", with the gate at `test/share/frontmatter-splice.test.ts`. Still live: `src/modules/preview/presentation/frontmatter.ts:47` and `:59` both call `doc.toString()`; `PropertiesPanel.tsx:79` calls `stringifyFrontmatterDoc(parsed.doc, parsed.body)` and passes it to `onEdit`. Against: §1.5 D7 \"Two shipped code paths violate this today\"; §3.1.4 heading \"Two shipped code paths violate D7 today, on every Publish\"; §10.10.2 row 2 \"`share-writer.ts:26` **and** `frontmatter.ts:47` ... every Publish and every properties-panel edit, **today**\".",
        "fix": "Global: change every \"two shipped code paths violate D7\" to \"**one** shipped code path violates D7\" and delete the `share-writer.ts:26` half from §3.1.4, §5.11, §7.2.5, §8.5 item 6, §10.6.3's `share-writer.ts:26` row and §10.10.2 row 2, replacing each with a one-line note: \"**FIXED in `4f97129`** — `share-writer.ts` now calls `spliceFrontmatterValue`; publish→unpublish is 907/907 byte-identical, gate at `test/share/frontmatter-splice.test.ts`.\" §11.1 step 2: change the status to DONE, keep the gate row, and re-point the remaining work at Violation 2 only. §11.16 Tuesday 09:00–13:00: replace with \"**Fix the properties panel** — replace `stringifyFrontmatterDoc` (`frontmatter.ts:47`, `:59`) with a range splice over `node.range`; gate: the no-op round trip dirties **0 of 737** (§5.9.2 F2).\" §1.5 D7 consequence: \"Forbids `matter.stringify`, `doc.toString()` and any `parse → stringify` round trip. One shipped path was fixed in `4f97129`; the properties panel still violates it and reaches disk through `EditorPane` `handleEdit` → `setContent` + `saveDraft`.\""
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§7 F3 (L7981), §7 correction table (L7270), §7.4 table (L7375), §8 (L8392, L9007), §11.6 (L11867), §11.4.3 (L11901), §11 backlog (L12318), §11 falsifiers (L12726) — 9 occurrences across 3 sections",
        "severity": "MATERIAL",
        "issue": "The figure `17/736 = 2.31%` / `17/907 = 1.87%` — the plan's stated publish-then-unpublish baseline and the RED value its gate is supposed to fail at — is an artifact of an incorrect inverse operation. It is reproducible only if 'unpublish' always DELETES `public_slug`, including on the 18 corpus files that already carried the key. Deleting a line and then asserting the line is still present is a bug in the test, not damage by the writer. The correct inverse (restore the prior value when the key pre-existed) — which is what the shipped gate and the shipped oracle both do — gives 33/907.",
        "evidence": "Re-derived over corpus_id sha256:3a010b16… with gray-matter 4.0.3 / js-yaml 3.14.2. Instrumented run (scratch copy of scripts/fm-roundtrip-audit.mjs): `{parsed:737, hadKey:18, alwaysDelete:17, restoreVariant:33}`. The always-delete inverse gives exactly 17; the restore inverse gives exactly 33. Full oracle run, graymatter impl: `BYTE-IDENTICAL 33 3.64% | CHANGED 703 77.51% | THREW 171 18.85%` (33+703+171=907). The committed gate `test/share/frontmatter-splice.test.ts:148-156` carries the same correction in a comment: \"If the file ALREADY carries the key, the inverse of 'publish' is 'restore the old value', not 'delete'.\" The denominator 736 is 737 parseable minus md/pj.md, i.e. numerator and denominator come from two different runs.",
        "fix": "Replace every occurrence of `17/736 parsed = 2.31%` / `17/907 = 1.87%` with `33/907 = 3.64%`. Specifically: §7 L7270 row 3 becomes \"3.64% (33/907) is a no-op write, and it is ALSO the publish-then-unpublish figure: gray-matter's damage comes from regenerating the block, so it is independent of which key is set. An earlier 17/736 = 2.31% used an always-delete inverse, which destroys the 18 files that already carry public_slug and is a defect in the harness, not the writer. Quote 33/907 = 3.64%.\" §7 F3: \"Baseline to beat: **3.64%** (33/907).\" §11.4.3 L11901: \"Against the current writer the test fails at **33/907** (703 changed + 171 threw).\" §8 L8392 and L9007, §11 L11867, §11 L12318, §11 L12726: same substitution."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "scripts/fm-roundtrip-audit.mjs:21 — the artifact §7 F3 and §11 Step 2 both depend on",
        "severity": "BLOCKING",
        "issue": "The \"independent oracle\" cannot execute. It imports `../src/modules/share/domain/splice-frontmatter.mjs`; only `splice-frontmatter.ts` exists. All three impls (`graymatter`, `yamldoc`, `splice`) die at module resolution before any measurement runs. Every number attributed to this oracle at HEAD was produced by something else.",
        "evidence": "`node scripts/fm-roundtrip-audit.mjs graymatter` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/Users/sagnikmitra/Desktop/GitHub/frontmatter/src/modules/share/domain/splice-frontmatter.mjs'`. `ls src/modules/share/domain/` → `slug.ts  splice-frontmatter.ts`. Reproduced the intended results only after copying the script to $TMPDIR and pointing it at a type-stripped .mjs build of the .ts source.",
        "fix": "Change line 21 to `import { spliceFrontmatterValue } from '../src/modules/share/domain/splice-frontmatter.ts'` and run it under `npx tsx scripts/fm-roundtrip-audit.mjs <impl>`, OR run it through vitest like the gate does. Then re-run all three impls and paste the captured output next to §7 F3. Until it runs, F3's falsifier (\"Replay the full pinned corpus … under an independent oracle\") is inert and LR#60 is unsatisfied."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "test/share/frontmatter-splice.test.ts:131-146 and :190-200 — the gate §11 Step 2 closes on",
        "severity": "MATERIAL",
        "issue": "The 907/907 corpus gate silently degrades to a 23-file gate on any machine that is not Sagnik's, and still reports PASS. `loadCorpus()` does `if (!base || !fs.existsSync(base)) continue` for each root, and `it.skipIf(corpus.length === 0)` only skips when ALL roots are missing. On CI or a second machine the `frontmatter` root resolves via `process.cwd()` and yields 23 frontmatter-bearing files, so the suite runs, prints \"23/23 identical (100.00%)\", and goes green. Nothing asserts the corpus size.",
        "evidence": "Re-derived per-root frontmatter counts over the manifest: `md` 662, `knowledge` 222, `frontmatter` 23 (total 907). `ROOTS.md`/`ROOTS.knowledge` are `path.join(os.homedir(), 'Desktop/GitHub/md')` and `.../knowledge` — absolute paths to two repos that are not this repo and are not vendored. The assertions at :197-199 are `expect(s.changed).toBe(0); expect(s.threw).toBe(0); expect(s.identical).toBe(s.total)` — all three are trivially satisfied by a 23-file corpus, or by a 0-file one if the skipIf ever stops firing.",
        "fix": "Add a size assertion inside the second `it`, before the others: `expect(s.total).toBe(907)` — and change `it.skipIf(corpus.length === 0)` to `it.skipIf(corpus.length < 907)` so a partial corpus SKIPS loudly instead of passing quietly. Also add to §11.4.3's gate block the line: `AND the gate must assert it saw 907 files, not merely that every file it saw was clean.`"
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "scripts/fm-roundtrip-audit.mjs:98-101 (the `refused` branch)",
        "severity": "MATERIAL",
        "issue": "The oracle's refusal counter is unreachable dead code, so every refusal is scored as BYTE-IDENTICAL. `if (back === src) { identical++ } else if (published === src && back === src) { refused++ }` — the second branch requires `back === src`, which the first branch already consumed. `refused` is structurally always 0. This directly contradicts the plan's own rule in §8 (L8406): \"in the publish path a refusal is **not safe** — it is a 502 and an unpublishable note.\"",
        "evidence": "Ran the oracle with the branch corrected to `if (published === src) { refused++; if (back === src) identical++ } else if (back === src) { identical++ }`. Result for `yamldoc`: `BYTE-IDENTICAL 284 31.31% | CHANGED 623 68.69% | THREW 0 | refused 170 18.74%` — i.e. 170 of the 284 \"passes\" are files the implementation declined to touch (114 real + 170 refused = 284). The shipped script reports `refused 0` and folds all 170 into the headline. For `splice` the corrected run gives `refused 0`, so today's 907/907 is genuine — but the gate cannot detect a regression into refusal.",
        "fix": "Replace lines 98-101 with: `if (published === src) { refused++; if (back === src) identical++; } else if (back === src) { identical++; } else { changed++; … }` and change the VERDICT line at 137 to `const failed = changed + threw + refused`. Add to §7 F3: \"The oracle must count a refusal separately from a byte-identical round trip and must FAIL on it; a writer that declines to publish is not a writer that preserves bytes.\""
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§11.1 execution table, Step 0 (L11539) and §7.2.6 (L7391)",
        "severity": "MATERIAL",
        "issue": "The repo-hygiene gate value is stale by one. Both places state the gate as `vitest list --filesOnly` returning **83**. Commit 4f97129 added `test/share/frontmatter-splice.test.ts`, so the live value is 84. A gate asserting equality to 83 now fails on a healthy tree — the false-RED class of LR#66.",
        "evidence": "`npx vitest list --filesOnly | grep -c .` → **84**. `npx vitest list | grep -c .` → **1257**. `vitest.config.ts` now carries `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]`, so the 247→N fix has already landed; the plan describes it as pending. The 164/247 = 66.3968% arithmetic in §11 L11601 reproduces exactly and is not disputed.",
        "fix": "§11 Step 0's gate cell: replace \"`npx vitest list --filesOnly \\| wc -l` returns **83**, not 247\" with \"`npx vitest list --filesOnly \\| wc -l` returns **≥ 84** (84 at 4f97129) and `grep -c '\\.claude/' ` returns **0**. Assert a floor plus a hard zero, never an equality — adding a test file must not turn the gate red.\" §7.2.6 L7391: replace \"the gate is `vitest list --filesOnly` moving 247 → 83\" with \"…moving 247 → 84 (landed in 4f97129; `vitest.config.ts` now excludes `**/.claude/**`, and the live suite is 84 files / 1,257 tests).\""
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§7.2.5 (L7383) and §8.5 item 6 (L8411) — the \"published and byte-identical\" pairings",
        "severity": "MATERIAL",
        "issue": "Two places pair a BLOCK-scope numerator (119 / 13.12%) against a FILE-scope one (33 / 3.64%) and label the pair \"published and byte-identical\". They are not the same measurement. 119 is how many YAML BLOCKS survive `parseDocument(block).toString()` unchanged; 33 is how many FILES survive a gray-matter write. The file-scope yaml number is 114.",
        "evidence": "Re-derived on corpus_id sha256:3a010b16…, yaml 2.9.0. Block scope — `doc.toString().replace(/\\n$/,'') === original block`: **119** of 907 (13.12%), drift 618. File scope — the exact code `stringifyFrontmatterDoc` ships (`---\\n${y}\\n---\\n\\n${body.replace(/^\\n+/,'')}`): **114** identical, **623** changed, 170 refused (114+623+170=907). The 5-file gap is already named in the plan's own §13 row B7: \"421 respaced / 197 reflowed / **5 separator-only**\" — 421+197=618 block-changed, +5 separator-only = 623 file-changed. The 5 are `md/Marketing & QA/Marketing & QA.md`, `md/Product Note KYC Delay.md`, `md/Zephyrus/ecosystem.md`, `md/otp-vs-biom.md`, `md/pj.md` — all files with no blank line after the closing fence, which `stringifyFrontmatterDoc` inserts.",
        "fix": "§7.2.5 L7383: replace \"it reaches only **13.12% published-and-byte-identical** (119/907)\" with \"it leaves the BLOCK byte-identical on 119 of 907 (13.12%) but the FILE byte-identical on only **114 of 907 (12.57%)** — `stringifyFrontmatterDoc` forces a blank line after the closing fence, costing 5 more files (§13 B7's 'separator-only' class)\". §8.5 L8411: replace \"or **3.64% vs 13.12%** (published and byte-identical)\" with \"or **3.64% vs 12.57%** (whole FILE published and byte-identical; the 13.12% / 31.86% pair is block-scope and must not be compared against gray-matter's file-scope 3.64%)\". Keep §3.1's table, §8 L8405, §10 L11143 and §11 L11896 unchanged — they all say \"blocks\" and are correct."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§14 defect list, entries at L15406 and L15036 vs §7 correction table row 4 (L7271)",
        "severity": "MATERIAL",
        "issue": "Section 14 rules that 171 is wrong and 170 is right (\"170 files are unreadable by both YAML parsers, not 171\"; \"kill-audit D re-measured both parsers on the pinned corpus and found gray-matter throws on 170\"). Applying that as a global 171→170 substitution would break three correct statements. §7's own correction table already resolves this properly and §14 contradicts it.",
        "evidence": "Re-derived: gray-matter `matter()` throws on **170**; eemeli/yaml `doc.errors.length > 0` on **170**; the two sets are IDENTICAL (intersection 170, gm-only [], yaml-only []). Separately `matter.stringify` throws on **1 more** file that parsed cleanly — `md/pj.md` — giving **171** throws across a full publish round trip. Oracle output for graymatter: `THREW 171`. §7 L7271 states this exactly: \"Exactly **170** files fail at `matter()` parse; exactly **one more**, `md/pj.md`, parses cleanly and throws at `matter.stringify`.\" §10 L11141 also has it right, naming md/pj.md and explaining \"33 + 8 + 695 = 736, not 737\".",
        "fix": "Scope §14's correction so it cannot be applied globally. Replace the L15406 correction text with: \"**170 is the PARSE count, 171 is the ROUND-TRIP count, and both are correct.** 170 files fail at `matter()`; `md/pj.md` parses and then throws inside `matter.stringify`. Use 170 wherever the subject is 'files no YAML parser can read', and 171 wherever the subject is 'files the Publish path cannot process'. Do not substitute one for the other. See §7's correction table row 4 and §10.6.3.\" Consequence for the build: §11 Step 2's prescribed `try/catch` around the READ catches only 170 — the 171st throws on write."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§7.1 (L7152) and §8.1 (L8029) — the corpus mirroring caveat",
        "severity": "MATERIAL",
        "issue": "\"29 files (2.68% of 1,084) are byte-identical across roots by sha256\" mislabels a count of duplicate GROUPS as a count of FILES, and one of the 29 groups is not cross-root at all. The real redundancy is roughly double what is stated.",
        "evidence": "Re-derived from corpus-manifest.json: **29** sha256 values have more than one member; they cover **61** file entries, i.e. **32** redundant copies. **28** of the 29 groups are cross-root, covering **57** entries; the 29th is the zero-byte hash `e3b0c442…` with 4 members, all inside the `md` root, so it is not cross-root. The cross-root set is `frontmatter/docs/*`, `frontmatter/docs/superpowers/*`, `frontmatter/docs/adr/*` and `frontmatter/src/**/README.md` mirrored into `md/`, plus `sgnk-react-workflow.md` which appears 3× (twice inside `md`). Deduplicated the corpus holds 1,084 − 32 = **1,052** distinct contents.",
        "fix": "Replace the caveat sentence in both places with: \"**28 content hashes are shared across roots, covering 57 of the 1,084 file entries — 32 redundant copies, 2.95% of the corpus** — because `frontmatter/docs/*`, `frontmatter/docs/superpowers/*`, `frontmatter/docs/adr/*` and `frontmatter/src/**/README.md` are mirrored into the `md` vault. A 29th duplicate group is 4 zero-byte files inside `md` alone. Deduplicated the corpus is **1,052 distinct documents**; any per-file rate derived from it is inflated by up to 3% on the mirrored subtree.\""
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§7 preamble (L7264)",
        "severity": "MATERIAL",
        "issue": "\"the 18-area capability run had **100% of headlines refuted by their own verifier**, always in the flattering direction\" is unsourced and contradicted by the only per-claim evidence in the file. `wf-mdmax-capability-2026-08-01.result.json` has no headline-verdict field at all — unlike the final gate, which does — so there is no artifact that could support a headline kill rate.",
        "evidence": "`jq keys` on the capability JSON → `[\"areas_completed\",\"areas_total\",\"per_area\",\"synthesis\"]`; no `headline_verdicts`. Each area's `verification` object has keys `intent, area, verdicts, survived, killed, new_defects` and no headline entry. Counting across all 18 areas: **survived=179, killed=193, new_defects=188** → 193/372 = **51.9%** of adjudicated claims killed. Every area had claims survive (minimum 7, `governance-and-moat`; maximum 14, `integrity`). By contrast the final-gate figure quoted in the same sentence — \"5 CONFIRMED, 10 OVERSTATED, 1 REFUTED\" — reproduces exactly from `headline_verdicts` (CONFIRMED: custom-pointers, hold-more, rendering-frontier, product-gap, competitive-live; REFUTED: founder-thesis; the other 10 OVERSTATED).",
        "fix": "Replace with: \"the 18-area capability run was adjudicated claim-by-claim rather than by headline — **193 of 372 adjudicated claims killed (51.9%), 179 survived, 188 new defects raised**, with claims surviving in all 18 areas `[re-derived from wf-mdmax-capability-2026-08-01.result.json, survived/killed counts]`; the 16-area final gate scored **5 CONFIRMED, 10 OVERSTATED, 1 REFUTED** on headlines `[headline_verdicts]`.\""
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§11.1 execution table, Step 2 (L11541); §11.4.3 (L11899-11906); §11.10 day plan (L12649); §3.1 \"Violation 1\"",
        "severity": "MATERIAL",
        "issue": "Step 2 is already done and its gate is already green. The plan schedules 0.5 d for \"fix the two shipped destroyers\" and describes share-writer.ts as calling matter.stringify. Commit 4f97129 replaced it with `spliceFrontmatterValue`. Only ONE destroyer remains, and it is the preview path.",
        "evidence": "`src/modules/share/infrastructure/share-writer.ts:25` now reads `const next = spliceFrontmatterValue(file.content, \"public_slug\", slug)`; grep for `matter(` across `src/modules/share/` and `src/app/api/share/` returns only two comment lines. Re-derived splice result: `BYTE-IDENTICAL 907 100.00% | CHANGED 0 | THREW 0 | refused 0` — the 907/907 is genuine, not refusal-inflated. Still LIVE: `src/modules/preview/presentation/frontmatter.ts:47` `const yaml = doc.toString()`, consumed by `PropertiesPanel.tsx:79` `onEdit(stringifyFrontmatterDoc(parsed.doc, parsed.body))` → EditorPane handleEdit → setContent + saveDraft. Its measured file-level damage is 623 of 737 (84.53%), 46,732 bytes, 226 files change line count.",
        "fix": "Rewrite Step 2's row as: \"**2 | The frontmatter splice — one destroyer left** | 0.25 d | GATE ALREADY GREEN for Publish: `share-writer.ts` uses `spliceFrontmatterValue` as of 4f97129, oracle reports 907/907, 0 refusals, 0 throws. REMAINING: `preview/presentation/frontmatter.ts:47,59` `doc.toString()` reached through `PropertiesPanel.tsx:79` → `onEdit` → `saveDraft`. Gate: a no-op properties-panel edit dirties **0 of 737** files (today it dirties **623**, 84.53%, |Δbytes| 46,732, moving line numbers in 226).\" Also drop \"no 502 on the 171\" from the Publish half — share-writer no longer parses, so there is no throw to 502 on; keep it only for any path that still calls gray-matter."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§3.9 (L3233-3235) — repomix / llm-code-format download figures",
        "severity": "MINOR",
        "issue": "Two adjacent bullets quote two different values for the same month (repomix, 2026-07): 327,543 in item 3 and 327,053 in item 4. Both are below the live figure, and the derived ratios (846:1, 2.73×) inherit the error.",
        "evidence": "Live npm registry API, fetched this session: `GET api.npmjs.org/downloads/point/2026-07-01:2026-07-31/repomix` → **339,504**. `.../2025-07-01:2025-07-31/repomix` → **119,678** (the plan's 2025-07 baseline REPRODUCES exactly). `.../2026-07-01:2026-07-31/llm-code-format` → **389**, plan says 387. Recomputed: 339,504 / 389 = **873:1** (plan 846:1); 339,504 / 119,678 = **2.84×** (plan 2.73×). The gap is consistent with npm backfilling July counts after 2026-08-01.",
        "fix": "Unify to one value with an as-of date. Item 3: \"**389 downloads/month against repomix's 339,504. 873:1.** `[measured, npm registry API, calendar month 2026-07, fetched 2026-08-02]`\". Item 4: \"repomix npm downloads went **119,678 (2025-07) → 339,504 (2026-07), 2.84× year on year** `[measured, same fetch]`\". The argument (incumbent strengthening, the one library that ships both halves gets no users) is unchanged and slightly strengthened."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§8.10.4 item 3 (L9030-9031) — GitHub topic counts",
        "severity": "MINOR",
        "issue": "Three live-registry counts are tagged `[measured]` with no as-of date. All three have already drifted upward within a day, so any future reader cannot tell whether a mismatch is drift or an error.",
        "evidence": "Live GitHub search API, fetched this session with per_page=1: `topic:agent-skills` → total_count **12,915**, top repo `anthropics/skills` at **165,613** stars (plan: 12,804 and 165,486). `topic:claude-skill` → **4,082** (plan: 4,043). All three moved up ~0.9-1.0%, i.e. the direction of the plan's argument reproduces.",
        "fix": "Append a fetch timestamp and state the direction rather than the point value: \"`topic:agent-skills` = **12,915** repositories, leader `anthropics/skills` at 165,613 stars; `topic:claude-skill` = **4,082**. `[measured, GitHub search API, 2026-08-02; growing ~1%/day — re-fetch before quoting externally]`\" Apply the same as-of rule to the plugin-download figures in item 2 (Relay 176,247 / 175,904, Self-hosted LiveSync 829,285, Readwise 238,596, digitalgarden 84,873)."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§11.7 backlog-item schema (L12320)",
        "severity": "MINOR",
        "issue": "The example backlog item's `repro:` field points at `node scripts/derive/roundtrip.mjs --corpus corpus-manifest.json`, which does not exist. The real oracle shipped at a different path in 4f97129, and the example's `claim:` carries the discredited 17/736 figure.",
        "evidence": "`ls scripts/` → `categorize-skills.mjs  fm-roundtrip-audit.mjs  rewrite-skills-wikilinks.mjs  sgnk-md-sync.sh  sgnk-md-sync.sh.bak-…  vercel-ignore-build.sh`. `ls scripts/derive` → does not exist. The plan references `scripts/derive/` in 9 places, all as future work, which is fine — but this one is presented as a worked example of a satisfiable backlog item.",
        "fix": "Replace the example block with the one that actually runs: `- id: S2-splice` / `claim: \"publish-then-unpublish is byte-identical on 907/907 files; the gray-matter path it replaced was 33/907\"` / `corpus_id: sha256:3a010b1649899795d79274fc528dbece97fdabf4ff0f81cc02ab619c048c51a4` / `repro: \"npx tsx scripts/fm-roundtrip-audit.mjs splice  # and 'graymatter' for the baseline\"` / `verifier_verdict: CONFIRMED`."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§3.1.5 table (L2044-2049), §5.1 (L5708-5719), §10.6.3 (L11141), §13 B7 (L14434) — the frontmatter corruption numbers",
        "severity": "COSMETIC",
        "issue": "PLAN_SOUND — recorded so this set is not re-litigated. Every number in these four places reproduces exactly against the pinned corpus. No change needed.",
        "evidence": "Re-derived, corpus_id sha256:3a010b16…, yaml 2.9.0 / gray-matter 4.0.3 / js-yaml 3.14.2, all 1,084 manifest files read and sha256-verified (1084/1084 match, 0 drift, 0 missing). §3.1.5 block-scope table: identical **119**, drift **618**, no-parse **170**, total **907** — 4/4 exact, so \"PERFECT REPRODUCTION, 4/4 numbers\" holds. §5.1: editable map **737**, no-op dirties **623** (84.53%), YAML-block line count moves **197/737**, whole-file line count moves **226/737** (30.7%), |Δbytes| **46,732** — 5/5 exact. §10.6.3: 737 run, **695** blocks changed (94.30%), **33** byte-identical, **8** trailing-newline-only, 0 bodies changed, and md/pj.md correctly identified as the stringify-thrower explaining why 33+8+695=736≠737 — 6/6 exact. §13 B7's decomposition 421+197+5=623 is exact and is the reconciliation of the 119-vs-114 scope gap.",
        "fix": "No edit. Add a one-line provenance note under §3.1.5: \"`[re-derived independently 2026-08-02 against corpus_id sha256:3a010b16… — 119/618/170/907 reproduce exactly; note these are BLOCK counts, see §13 B7 for the 5-file block-vs-file gap]`\"."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§1.4 corpus table (L594), §3.2.6 token stratification (L2280-2288), §4 C-5/C-6 (L4503-4510), §6.4 (L6737-6740), §9.6 (L9989), §10.5 (L11126-11133), §12 (L12857)",
        "severity": "COSMETIC",
        "issue": "PLAN_SOUND — recorded so these are not re-litigated. Every one of these reproduces or is already correctly reconciled inside the plan. No change needed.",
        "evidence": "Corpus totals: 756+272+56 = **1,084** files; 21,068,853+3,570,923+908,989 = **25,548,765** bytes — both exact against corpus-manifest.json, and all 1,084 sha256 hashes verify live. 907/1,084 = **83.6716%** → 83.67% exact. Per-root invalid-YAML: md **91/662 = 13.75%**, knowledge **79/222 = 35.59%**, frontmatter **0/23** — all three re-derived exactly; 91+79+0 = 170. Both parsers accept the same **737** — re-derived, 0 asymmetric acceptances. 170/899 = **18.91%** and 899/1,061 = **84.73%** are exact arithmetic on the stated populations. §3.2.6's buckets (5,329/2,360; 3,899/337; 3,298/10; 13,962/0; 2,697 of 2,707 = 99.63%) are verbatim from wf-final-gate's `hold-more` CONFIRMED entry. §10.5's 266/1,084 = 24.54% and 266/907 = 29.33% are exact. The 110.1×/2.79% (packed container 25,776,700 B vs described index 234,119 B) versus 242×/2.82% (raw 25,548,765 B vs one-line index 105,538 B) apparent conflict is explicitly reconciled at §12 L12857-12859 — both are in the final-gate JSON, they use different index definitions, and §12 already names which governs. 4,247 / 2.66% / ~113 / 28,170 in §11 L11691 and §12 L13157 is quoted verbatim from the final-gate `against`-area verifier. §9's 119 SKILL.md files and 3,295,299 bytes re-derived exactly via `find ~/.claude/skills-src -name SKILL.md`.",
        "fix": "No edit. If anything, add `[re-derived 2026-08-02]` to the §1.4 corpus table and to §6.4's 1,084 → 1,061 → 899 → 170 ladder, since those are the two places a future reader is most likely to distrust."
      },
      {
        "cls": "Number audit — re-derive every load-bearing figure",
        "section": "§1.3 known-weak table (L770), §2.5 (L1777-1787), §3.2.7 (L2293-2296), §4 (L3643)",
        "severity": "COSMETIC",
        "issue": "PLAN_SOUND on the 99.627% / 0.050% / 0.323% anchoring triple. The plan's own handling is correct and complete: it is flagged as re-derived by nobody in at least four places, §3.2.7 attaches five travelling caveats, and K3 is defined against it. Confirmed unverifiable from anything in this repo.",
        "evidence": "62 occurrences of 99.627 in PLAN.md. The figure appears in wf-final-gate (45×) and wf-mdmax-capability (27×) but always as a QUOTE of docs/mdmax/PLAN.md §4.1, never as a measurement: the final-gate JSON states verbatim \"The 99.627% / 0.050% / 0.323% figures — I did NOT re-derive them. I only read them from docs/mdmax/PLAN.md §4.1 and compared them against a competitor's design.\" §12 L13150 records that a grep for `99.627`, `41,642` and `41642` returns hits only in prose, no code. The supporting denominators (41,642 block-versions → 32,919 surviving) reproduce as internally consistent arithmetic but the underlying corpus was not rebuilt by anyone.",
        "fix": "No edit to the caveats. One addition to §3.2.7's list, since the denominator drift is the concrete risk: \"6. **Five different block-version denominators circulate** — 28,170 (anchorable, §3.2.4), 29,217 and 31,149 (LCS runs, §3.2.5), 31,180 (the kill's arithmetic), 32,919 (surviving, §1.1a) and 41,642 (full population). Any re-derivation must state which population it used in the same sentence as the rate, or the result will not be comparable to this one.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.4 (all of it), §11.1 row 2, §11.11.3 disjoint-artifact map, §11.16 Tuesday 09:00–15:00",
        "severity": "BLOCKING",
        "issue": "Step 2 has already shipped, and it shipped to a DIFFERENT module than §11 specifies. §11.4.1 presents `matter.stringify` as live code; §11.4.2 places the new function in `src/modules/mdmax/domain/`; §11.11.3 gives step 2 ownership of `new mdmax/domain`. All three are stale, and the module discrepancy silently breaks §11.1's parallelism argument and collides with step 4.",
        "evidence": "`git show --stat 4f97129` (Sat Aug 1 22:43:47 2026): `src/modules/share/domain/splice-frontmatter.ts | 164 ++++`, `src/modules/share/infrastructure/share-writer.ts | 17 +-`, `test/share/frontmatter-splice.test.ts | 201 +++`. Live `share-writer.ts` line 7 now reads `import { spliceFrontmatterValue } from \"@/modules/share/domain/splice-frontmatter\";` and contains no `matter` import at all. `ls src/modules/` has no `mdmax` directory; `grep -ril mdmax src test scripts` returns 0 files. §11.1's DECIDED note justifies parallelism with \"step 2 touches `src/modules/share/infrastructure` and a new `src/modules/mdmax/domain`. No shared file.\" — but §11.11.3 gives step 4 ownership of \"`share` (all layers)\", which now includes the splicer.",
        "fix": "Replace §11.4.1's opening with: \"**FIXED 2026-08-01 in `4f97129`.** `share-writer.ts` now calls `spliceFrontmatterValue`. The measurements below are the record of what it cost, not a description of live code.\" Replace §11.4.2's location paragraph with: \"**Shipped at `src/modules/share/domain/splice-frontmatter.ts`, not in a new `mdmax` module.** `share/domain` satisfies the same boundary constraint (`domain` may import only `domain`/`shared-domain`, eslint.config.mjs:105-108). It is NOT exported from `src/modules/share/index.ts` — any cross-module caller must add it to the barrel first (AGENTS.md §2).\" In §11.11.3, change step 2's row to `modules it owns: share/domain/splice-frontmatter.ts, share/infrastructure/share-writer.ts, vault/infrastructure/markdown-parser.ts` and add to step 4's row `must not touch: share/domain/splice-frontmatter.ts`. In §11.1, replace the DECIDED note's last sentence with: \"They were disjoint and step 2 landed first; step 4 now inherits `share/` and must treat `share/domain/splice-frontmatter.ts` as frozen.\" Delete §11.16's Tuesday 09:00–15:00 rows and reallocate to step 1 task 1c."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.4.3 (\"The gate\"), test/share/frontmatter-splice.test.ts:169-207",
        "severity": "BLOCKING",
        "issue": "The step-2 corpus gate silently degrades to 2.54% coverage on any machine that is not Sagnik's laptop, and reports PASS. It does not refuse — `skipIf` only fires at exactly zero files, and `ROOTS.frontmatter = process.cwd()` is always present, so the corpus is never empty.",
        "evidence": "test/share/frontmatter-splice.test.ts:96-99 hardcodes `md: path.join(os.homedir(), \"Desktop/GitHub/md\")` and `knowledge: path.join(os.homedir(), \"Desktop/GitHub/knowledge\")` — two sibling repos outside this one. `loadCorpus()` line 136: `if (!base || !fs.existsSync(base)) continue;` — a missing root is skipped, not fatal. Gate at line 183: `it.skipIf(corpus.length === 0)`. I measured the per-root split against the pinned manifest: md 662 files-with-frontmatter, knowledge 222, frontmatter 23 (total 907, reproducing the headline). On a clone without the two sibling repos, `corpus.length === 23`, skipIf does not fire, and the gate asserts `s.identical === s.total` over 23 of 907 files (2.54%) while printing \"23/23 identical (100.00%)\". This is precisely the machine state of the second developer that §11.13/K2 exists to produce.",
        "fix": "Add to §11.4.3, immediately after the fenced gate block: \"**The gate must REFUSE, not shrink.** `loadCorpus()` must assert full coverage or fail: after loading, `expect(corpus.length).toBe(907)` — and when a root is missing, throw with the missing path rather than `continue`. A partial corpus is INCONCLUSIVE (gate.mjs exit 4, missing repro), never PASS. Until the corpus is vendored or fetched by a script, this gate is green only on the machine that holds `~/Desktop/GitHub/md` and `~/Desktop/GitHub/knowledge`, and §11.11.2's 'merge only when the registered gate is green' does not hold for anyone else.\" Then in test/share/frontmatter-splice.test.ts replace `if (!base || !fs.existsSync(base)) continue;` with `if (!base || !fs.existsSync(base)) throw new Error(\\`corpus root missing: ${root} → ${base}\\`);` and replace both `it.skipIf(corpus.length === 0)` with `it(`."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.4.3 (\"Checked by an oracle that shares NO code with the writer\"), scripts/fm-roundtrip-audit.mjs:20",
        "severity": "BLOCKING",
        "issue": "The committed independent oracle does not execute. It imports a `.mjs` path for a file that is `.ts`. Under the plan's own rule P2 (§11.12.2: \"No headline without a committed derivation script and its captured output\") the 907/907 headline currently has no working derivation script.",
        "evidence": "`node scripts/fm-roundtrip-audit.mjs splice` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/Users/sagnikmitra/Desktop/GitHub/frontmatter/src/modules/share/domain/splice-frontmatter.mjs'`. Line 20 is `import { spliceFrontmatterValue } from '../src/modules/share/domain/splice-frontmatter.mjs'`; `ls src/modules/share/domain/` shows `slug.ts` and `splice-frontmatter.ts` — no `.mjs`. All three arms (graymatter, yamldoc, splice) fail at module load. I reproduced 907/907 only after copying the script to $TMPDIR and rewriting three paths. Separately, the docstring says \"Shares no code with the writer under test\" while line 20 imports the writer, line 18 imports `gray-matter` and line 19 imports `yaml` — the three imports §11.4.3 says it \"must not import\". Separately again, the `refused` counter at line 97 is dead code: `if (back === src) { identical++ } else if (published === src && back === src) { refused++ }` — the second condition is unreachable, so any future refusal is counted as BYTE-IDENTICAL. (Measured today the refusal rate is 0/907, so this is a latent gate defect, not a live wrong number.)",
        "fix": "In §11.4.3 replace \"Checked by an oracle that shares NO code with the writer.\" with: \"Checked by `scripts/fm-roundtrip-audit.mjs`, which is a three-arm COMPARATOR, not an independent oracle: it imports `spliceFrontmatterValue`, `gray-matter` and `yaml` in order to score all three writers on the same corpus. Its independence is limited to the comparison being raw byte equality. That is sufficient for the gate and the plan should say so rather than claiming more.\" Then fix line 20 to `'../src/modules/share/domain/splice-frontmatter.ts'` (Node 24.6 strips types natively; verified working), and fix the dead branch to `if (published === src) { refused++ } else if (back === src) { identical++ }` so a silent no-op publish can never score as a pass. Commit the captured stdout beside the script per P2."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.16 Tuesday 15:00–16:00 vs Thursday 16:00–18:00; §11.12.3",
        "severity": "BLOCKING",
        "issue": "Hard ordering error inside the first-week schedule. Tuesday's acceptance proof is a tool that is not built until Thursday evening.",
        "evidence": "§11.16 Tuesday row: \"15:00–16:00 | Register the **splice-conformance gate**; write its `--broke` (reintroduce `matter.stringify`) and prove it goes red | **`gate.mjs --verify-all` green**\". §11.16 Thursday row: \"16:00–18:00 | **§11.12.3** — port `gate.mjs`, selftest first | selftest 10/10\". §11.12.3 costs the port at \"~1 founder-day, most of it the selftest\" and states \"the tool is not trusted until it passes\". `ls specs/harness/` today shows `analyze.mjs`, `clean-architecture-report.mjs`, `import-boundary-report.mjs`, `route-inventory.mjs`, `server-folder-blocklist.mjs` — no `gate.mjs`, no `gates.jsonl`.",
        "fix": "Move the gate.mjs port to Monday. Replace §11.16's Monday 13:00–14:00 row with: \"13:00–17:00 | **§11.12.3** — port `gate.mjs` to `specs/harness/`, selftest FIRST | selftest 10/10; four distinct REFUSED exit codes (2/3/4/5) reproduce\", move \"§11.2.3 §1.1a contradiction + §11.2.4 litter\" to Monday 17:00–17:30, and delete the Thursday 16:00–18:00 row (reallocate to task 1d, which needs it — see the container finding). Tuesday's gate-registration row then has its tool."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.3.3 task 1d (\"2–3 d\", three files); §11.3.5 (\"1d is the risky task\")",
        "severity": "BLOCKING",
        "issue": "Task 1d's file list is materially incomplete and its estimate rests on that list. The composition root is a module-level singleton wired from process-level env, so per-user credentials are not a three-file change — they are a de-singletoning of the container plus every consumer plus two process-global caches. §11.3.5 correctly calls 1d the risky task but never names the mechanism, so an engineer scoping from §11.3.3 will underestimate by an order of magnitude.",
        "evidence": "`src/container/dependency-container.ts:44` is `export const container = {` — evaluated once at import time, wiring `githubVaultReader`, `githubWriter`, `githubFetch`, `snapshotCache` and `AUTHOR`. `grep -rln \"@/container/dependency-container\" src/` returns **25 files** (24 route/page files plus the container itself). `src/shared/infrastructure/github/client.ts:30` reads `repoEnv.GITHUB_REPO_TOKEN` inside `githubFetch`, which has 14 call sites. `src/modules/vault/infrastructure/snapshot-cache.ts:13` is `const MAX_ENTRIES = 3;` over a module-level `Map` keyed by sha only, and `dependency-container.ts:111` calls `snapshotCache.clear()` globally after every successful commit — with N users, one user's commit evicts every other user's cached vault. `src/modules/vault/infrastructure/search-index.ts:147` is `let _cache: IndexCache | null = null;` — a SINGLE slot; a second concurrent vault re-downloads and re-indexes the whole zipball on every search. §11.3.3 task 1d lists exactly three files and 2–3 days.",
        "fix": "Replace task 1d's Files cell with: \"`src/config/env.ts` (drop the `GITHUB_REPO_TOKEN`/`GITHUB_REPO` process-level path), `src/shared/infrastructure/github/client.ts` (thread a token parameter through `githubFetch` — 14 call sites), `src/modules/repository/infrastructure/github-writer.ts`, **`src/container/dependency-container.ts` (turn `export const container` into `makeContainer({token, author, repo})`), the 24 files that import it (`grep -rln \\\"@/container/dependency-container\\\" src/`), `src/modules/vault/infrastructure/snapshot-cache.ts` (key by `(vaultId, sha)`, raise MAX_ENTRIES, scope `clear()` to one vault), `src/modules/vault/infrastructure/search-index.ts` (`_cache` becomes a keyed map)**\" and change its Effort cell to \"4–7 d\". Add to §11.3.5's second bullet, after \"changes the rate limit model, the error surface and the offline story at once\": \"and it de-singletons the composition root: `container` is a module-level `const` consumed by 24 route files, and two module-level caches (snapshot LRU capped at 3 entries, search index at one slot) become correctness and capacity bugs the moment there are two vaults.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.3.2 (task 1a, 0.5 d), §11.12.6 tenancy/capability rows",
        "severity": "BLOCKING",
        "issue": "There is no integration-test infrastructure in this repo, and §11.12.6 forbids the only kind of test that can be written without it. Task 1a is costed at 0.5 d for \"commit the RED test\" — that covers the assertion, not the harness, not the second GitHub identity, not a Firestore target. This is the true head of the critical path and it is scheduled nowhere.",
        "evidence": "`package.json` devDependencies are `@eslint/js, @tailwindcss/postcss, @tauri-apps/cli, @testing-library/jest-dom, @testing-library/react, @types/*, eslint, eslint-plugin-boundaries, fake-indexeddb, jsdom, postcss, prettier, tailwindcss, typescript, typescript-eslint, vitest`. `grep -rln \"msw|nock|playwright|@firebase/rules-unit-testing|emulator|supertest\" test/ package.json` returns nothing. `test/setup.ts` is 8 lines (`cleanup()` after each test). No `e2e/` directory. Meanwhile §11.3.2 assertion 3 requires \"The persisted commit's author is **their** name and email — asserted by **reading the commit back**\", and §11.12.6's capability row says GREEN means \"it passes end-to-end **against a real second identity, not a mock**\". Reading a commit back requires a real second GitHub account, a real test repo and network; `memberUids` requires Firestore (`src/shared/infrastructure/firebase/client.ts:38 firestore()` — verified 0 callers anywhere in `src`).",
        "fix": "Insert a new task 1a-0 at the top of §11.3.3's table: \"| 1a-0 | **Stand up the integration harness**: a second GitHub account + OAuth app, a throwaway test repo, `@firebase/rules-unit-testing` + the Firestore emulator, and a `vitest.integration.config.ts` that is opt-in via `RUN_INTEGRATION=1` so the unit suite stays 4 s | `package.json`, `vitest.integration.config.ts`, `test/tenancy/harness.ts`, `.env.test.example` | **2–3 d** |\" and add to §11.3.5: \"**The unnamed head of the critical path is the harness, not the assertion.** Today the repo has vitest + jsdom + testing-library and nothing else; §11.12.6's 'not a mock' rule therefore cannot be satisfied by anything that exists. Provisioning a second GitHub identity is a prerequisite with a calendar cost, not an engineering one.\" Revise §11.3's headline from 8–12 to **12–18 founder-days** and say the revision is because 1a-0 and 1d were under-scoped."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.13 condition 1, §11.14 K1/K2, §12.6 items 1–2",
        "severity": "BLOCKING",
        "issue": "The 30-day gate — the plan's own \"the ones that matter\" — has three separate falsifiability defects: one of its two commands returns a false negative TODAY, the same command has no exit code, and it measures something other than the condition it is labelled with. §11.13 claims these are \"evaluated by command, not by feeling\".",
        "evidence": "(a) `git log --format='%an <%ae>' --since=2026-08-01 -- src/ | sort -u` returns EMPTY right now, although `4f97129` touched `src/modules/share/domain/splice-frontmatter.ts` on 2026-08-01 at 22:43:47 +0530. Cause: git approxidate fills a bare date with the CURRENT time of day (now 23:19), so the window opened after the commit. `--since=2026-07-31` returns `4f97129 Sagnik Mitra`. (b) `sort -u` always exits 0, so the line has no machine verdict — unlike condition 2, which ends in `grep -qi amit`. (c) The condition is labelled \"has a second GitHub login **written to a document**?\" but `git log ... -- src/` observes a developer committing product source to the `frontmatter` repo. A second user writing a document commits to their own vault repo, not to `src/`. The command cannot detect the condition under any circumstances.",
        "fix": "Replace §11.13's condition-1 block verbatim with:\n```bash\n# Condition 1a — does the tenancy assertion pass?\nRUN_INTEGRATION=1 npx vitest run -c vitest.integration.config.ts \\\n  test/tenancy/second-user-writes.test.ts     # must exit 0\n\n# Condition 1b — has a second GitHub login written a DOCUMENT?\n# (the vault repo, not this repo; substitute the test vault)\ngit -C \"$TEST_VAULT\" log --format='%an <%ae>' --since=2026-08-01T00:00:00 \\\n  | sort -u | grep -qv 'sagnikmitra123@gmail.com'   # must exit 0\n```\nand replace condition 2's `--since=2026-08-01` with `--since=2026-08-01T00:00:00`. Add one line under the block: \"Every `--since` carries an explicit `T00:00:00`: a bare date is filled with the current time of day by git's approxidate parser, which silently drops commits made earlier the same day — verified 2026-08-01, when the un-suffixed form returned empty against a commit made 36 minutes earlier.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11 as a whole (Violation 2 is absent from it), vs §5.9.2, §3.1.4, §10, §13, §14",
        "severity": "BLOCKING",
        "issue": "The second live frontmatter destroyer is described in five other sections — §5.9.2 titles it \"THIS IS THE HIGHEST-PRIORITY ITEM IN THE SECTION\" and gives it a gate that \"should stop the team\" — and appears ZERO times in §11. It has no step, no owner, no effort, no files, no gate, no place in the disjoint-artifact map, and no row in §11.16. An engineer executing §11 tomorrow will ship step 2 and believe the frontmatter problem is closed.",
        "evidence": "`grep -n \"PropertiesPanel|preview/presentation/frontmatter|stringifyFrontmatterDoc|Violation 2\" docs/mdmax/tree/*.md` returns hits in 03 (1), 05 (8), 07 (1), 10 (3), 13 (2), 14 (6) and **0 in 11-execution.md**. The path is live and reaches disk: `src/modules/preview/presentation/frontmatter.ts:47` `const yaml = doc.toString()` and `:59` same; `PropertiesPanel.tsx:79` `onEdit(stringifyFrontmatterDoc(parsed.doc, parsed.body))`, reached from `setValue`/`renameKey`/`removeKey`/`addProperty`; `EditorPane.tsx:647` `<PropertiesPanel content={previewSource} onEdit={handleEdit} />` with no feature flag; `EditorPane.tsx:498-502` `handleEdit` → `setContent(path, newContent)` + `setDirty(path, true)` + `saveDraft(...)`. §11.4.2's only trace of it is the imperative \"Never call `toString()` on a YAML Document\", which binds the new function and nothing else.",
        "fix": "Add a new §11.4.5 titled \"The second destroyer — the properties panel — and why it is NOT closed by the splice writer\", carrying §5.9.2's measurement (623 of 737 files dirtied by a no-op round trip, 84.5%; whole-file line count moves in 226/737 = 30.7%) and this scope note: \"`spliceFrontmatterValue` as shipped cannot serve this caller unchanged: its signature is `(src, key, value: string|number|boolean|null)` and `emitScalar` rejects arrays, while `PropertiesPanel.textToValue` returns `string[]` for list-valued fields; it also handles one key per call, and the panel does rename (delete+insert) and multi-key edits. Extending it is 1–2 founder-days, plus adding it to `src/modules/share/index.ts` because cross-module imports go through the barrel (AGENTS.md §2).\" Add its gate: \"a no-op `parseFrontmatter → stringify` round trip dirties **0 of 737** files, down from 623; if a splice cannot reach 0, the panel goes READ-ONLY before comments ship.\" Add a row to §11.11.3's map: \"| 2b properties panel | `preview/presentation/frontmatter.ts`, `PropertiesPanel.tsx`, `share/index.ts` barrel | `auth`, `config`, `container` |\", and a §11.16 slot."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.4.4, §11.12.4, §12.2 N12 countermeasure, §12.6 item 1",
        "severity": "MATERIAL",
        "issue": "There is no CI in this repository, and four separate places in §11/§12 make CI load-bearing. Building it is unscheduled and uncosted.",
        "evidence": "`ls -R .github` → \"NO .github DIRECTORY\"; `ls -a | grep -iE 'github|circle|travis|gitlab|husky'` → nothing. `scripts/` contains only `categorize-skills.mjs`, `fm-roundtrip-audit.mjs`, `rewrite-skills-wikilinks.mjs`, `sgnk-md-sync.sh`, `sgnk-md-sync.sh.bak-*`, `vercel-ignore-build.sh`. Against that: §11.4.4 \"**Ships as:** three commits plus an `eslint-plugin-redos` run **in CI**\"; §12.2 N12 countermeasure \"and `eslint-plugin-redos` **in CI**\"; §12.6 item 1 \"and the test that proves it is **green in CI**\"; §11.11.2 \"Merge to `main` only when `npm run verify` is green **and** the step's registered gate is green\" — enforced by nothing today.",
        "fix": "Add to §11.2 a new step 0 item **§11.2.6 — there is no CI**: \"`.github/` does not exist. Add `.github/workflows/verify.yml` running `npm ci && npm run verify` on push and pull_request, plus a second job gated on `RUN_INTEGRATION` for the tenancy harness. **Effort: 0.5 d.** Until it exists, every sentence in this plan and in §12 that says 'in CI' means 'on Sagnik's laptop', and §11.11.2's merge rule is a convention rather than a gate.\" Then either add the workflow or change §12.6 item 1 to read \"...and the test that proves it is green on a clean checkout that is not the author's working tree\"."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.2.1 acceptance gate; §11.1 row 0; §11.16 Monday 09:30–10:00",
        "severity": "MATERIAL",
        "issue": "The step-0 acceptance gate is already satisfied AND the number it asserts is wrong by one, so running it as written produces a false RED on a correct fix — the exact Learned-Rule-#65 failure the same subsection invokes.",
        "evidence": "The exclude landed in `4f97129`; `vitest.config.ts` now reads `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]`. Measured live: `npx vitest list --filesOnly | wc -l` → **84** (not 83); `npx vitest list --filesOnly | grep -c worktrees` → **0**; `npx vitest run` → \"Test Files 84 passed (84) / Tests 1257 passed (1257) / Duration 4.06s\"; `find test -name '*.test.ts*' | wc -l` → 84. The 83 in §11.2.1 and in §11.1 row 0 is 247−164, and one of those two inputs was off by one. Also note the shipped exclude omits `**/src-tauri/**` and `**/.next/**` from the plan's proposed config — harmless here (`find src-tauri -name '*.test.*'` returns nothing) but the configs differ.",
        "fix": "Replace §11.2.1's \"**Acceptance gate.**\" paragraph with: \"**Acceptance gate — landed 2026-08-01 in `4f97129`.** `npx vitest list --filesOnly | grep -c '\\\\.claude/'` returns **0** and `npx vitest list --filesOnly | wc -l` returns **84** (not 83 — 247−164 was off by one; 84 is the measured value and matches `find test -name '*.test.ts*' | wc -l`). Baseline recorded: **84 files, 1,257 tests, all passing, 4.06 s**, down from 247 files / 3,484 tests.\" Change §11.1 row 0's gate cell to \"already green — `npx vitest list --filesOnly | wc -l` returns **84**\" and its Effort cell to \"0.25 d (worktrees, corpus-id script and litter only)\". Delete §11.16's Monday 09:30–10:00 row."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.14 K5 and K8; §11.12.7",
        "severity": "MATERIAL",
        "issue": "Two kill gates are evaluated against a 30-person probe that the plan schedules nowhere, costs nowhere and describes nowhere. As written neither K5 nor K8 can be evaluated — the same disease §11.7 diagnoses in the previous draft of K5 (\"both previously had no threshold, which meant they could never fire\").",
        "evidence": "§11.14 K5: \"Fewer than **5 of 30** probed users say they would install `cert` | §11.12.7, **extended probe**\". K8: \"**15 of 30** probed users name the certificate as their reason to pay | §11.12.7\". §11.12.7 in full is titled \"The five conversations — one week of calendar, zero engineering\" and specifies \"**Five calls, one question each**, scored against the 14 pain themes.\" The words \"extended probe\", \"30\", and any instrument larger than five appear nowhere in §11.12.7. §11.16 schedules exactly five (Wed 12:00–13:00 two, Thu 13:00–14:00 two, Fri 11:00–12:00 one).",
        "fix": "Add a paragraph to §11.12.7 after the recruiting frames: \"**The five calls are the qualitative instrument. K5 and K8 need a quantitative one and it is a different artifact: a 30-respondent probe** — a single question ('would you install a tool that tells you which of your markdown files break on GitHub / Obsidian / Notion?') posted to the same three frames, with a yes/no/maybe tally. Send **30 recruiting messages, not 5**: the five calls come out of the same funnel at a realistic reply rate, and the 30 responses are exactly K5's and K8's denominator. **Effort: 0.5 d of writing, one week of calendar, zero engineering.**\" Change §11.16's Monday 17:00–17:30 row to \"Send **30** recruiting messages for §11.12.7 (5 calls + the K5/K8 denominator) | 30 sent; first replies expected Wednesday\"."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.3.3 tasks 1b and 1d; §11.11.2 merge rule",
        "severity": "MATERIAL",
        "issue": "Step 1's file list names one test file for deletion and misses six live assertions in two other test files that go red under 1b and 1d. §11.11.2 blocks the merge on `npm run verify`, which runs all of them, so those six are on the critical path and appear in no estimate.",
        "evidence": "§11.3.3 task 1b Files cell names `test/auth/allowlist.test.ts (delete)` only; task 1d names `src/config/env.ts` with no test file. Live: `test/auth/auth-options.test.ts` (62 lines) sets `process.env.ALLOWED_GH_LOGIN = \"sagnikmitra\"` at line 15 and asserts three signIn cases — \"returns true for the allowlisted login\" (:19), \"returns false for a non-allowlisted login\" (:35), \"returns false when profile login is undefined\" (:49); all three encode the behaviour 1b deletes. `test/config/env.test.ts` asserts \"defaults ALLOWED_GH_LOGIN to sagnikmitra\" (:28), \"throws when GITHUB_REPO_TOKEN is missing\" (:49), \"throws when GITHUB_REPO_TOKEN is empty string\" (:52) — the last two encode the required-server-token behaviour 1d removes.",
        "fix": "Change task 1b's Files cell to: \"`src/modules/auth/domain/allowlist.ts` (delete), `…/infrastructure/auth-options.ts`, `test/auth/allowlist.test.ts` (delete), **`test/auth/auth-options.test.ts` — rewrite all three signIn cases (:19, :35, :49) to assert account creation instead of allowlisting**, **`test/config/env.test.ts:28` — delete the `defaults ALLOWED_GH_LOGIN` case**\". Change task 1d's Files cell to add: \"**`test/config/env.test.ts:49,52` — the two `throws when GITHUB_REPO_TOKEN is missing/empty` cases become obsolete when the server token stops being required; delete or re-point them at the per-user credential**\". Add one line to §11.3.3: \"Six pre-existing assertions across two files encode single-tenancy and must be rewritten in the same branch; `npm run verify` runs all 1,257 tests and §11.11.2 blocks the merge on it.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.12.6 tenancy row; §11.3.2's five assertions",
        "severity": "MATERIAL",
        "issue": "The one-integration-test framing does force the three hardcodes, but as written all five assertions are satisfiable by mocking, and the repo already contains the pattern that does it. §11.12.6 says 'not a mock' but mechanises nothing, so the gate cannot distinguish 'a second user works' from 'a second user works against a fake'.",
        "evidence": "`test/auth/auth-options.test.ts:22-30` already calls the signIn callback directly with a fabricated profile object — `const signIn = authOptions.callbacks?.signIn; await signIn({ user: {...}, profile: { login: \"sagnikmitra\", ... } })`. The same technique satisfies §11.3.2 assertion 1 in twenty minutes with nothing changed but the allowlist. Assertions 3 and 4 (\"asserted by reading the commit back\", \"authorised by their credential\") are satisfiable against a stubbed `githubFetch`. §11.12.6's tenancy row's only mechanical green condition is `grep -rn \"sagnikmitra\" src/` returning nothing outside a comment — which I measured returns exactly 3 hits today (env.ts:47, env.ts:86, dependency-container.ts:42), none of which requires the container to be de-singletoned or the token to be per-user.",
        "fix": "Replace §11.12.6's tenancy row GREEN cell with: \"it passes, **and** all three of these structural invariants hold, none of which a mock can satisfy: (a) `grep -rn \\\"sagnikmitra\\\" src/` returns nothing outside a comment (today: 3 hits — `env.ts:47`, `env.ts:86`, `dependency-container.ts:42`); (b) `grep -rn \\\"GITHUB_REPO_TOKEN\\\" src/` returns nothing outside `src/config/env.ts` (today: 3 hits — `env.ts:85`, `client.ts:30`, `github-writer.ts:33`); (c) `grep -n '^export const container' src/container/dependency-container.ts` returns nothing — the module-level singleton has become a per-request factory.\" Add to §11.3.2, after the five assertions: \"**Why the assertions alone are not enough.** `test/auth/auth-options.test.ts` already invokes the signIn callback with a fabricated profile; the same technique satisfies assertion 1 against a deleted allowlist and nothing else. The three greps above are the part a mock cannot fake, and they are what actually forces 1d.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.12.4 (\"the verify ladder\")",
        "severity": "MATERIAL",
        "issue": "The 3.17 s figure is attached to the wrong artifact. It is the AIOS `verify-ladder.sh` measurement, not `npm run verify`, which includes `next build` and cannot complete in 3.17 s. The plan's own hedge (\"that predates the vitest exclude fix\") points the reader at the wrong correction.",
        "evidence": "The source, read from `docs/engine/research/wf-final-gate-2026-08-01.result.json`, says verbatim: \"the two AIOS tools that genuinely transfer already run on this repo unchanged (**verify-ladder: pass in 3.17 s on frontmatter**, fail on a deliberately broken tree)\". `package.json` defines `\"verify\": \"npm run typecheck && npm run lint && npm run test && npm run build && npm run arch\"`. Measured today, post-exclude, the **test rung alone** is 4.06 s (`npx vitest run` → Duration 4.06s, wall clock 4.449 s) — already longer than the whole quoted ladder, before `tsc --noEmit`, `eslint .` and `next build`.",
        "fix": "Replace §11.12.4's sentence \"It was measured passing in **3.17 s** on this repo by an external harness `[measured, aios-transfer]` — note that predates the vitest exclude fix and therefore ran the 247-file suite; re-measure after §11.2.1.\" with: \"The 3.17 s figure recorded in `aios-transfer` is **`verify-ladder.sh`, not `npm run verify`** — `npm run verify` ends in `next build` and cannot finish in seconds. Post-exclude, the `test` rung alone measures **4.06 s** (84 files, 1,257 tests, all passing, 2026-08-01). Measure and record the full-ladder wall clock once, in the commit message, and stop quoting 3.17 s for it.\""
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.11.4 vs §11.16 Friday 15:00–16:00",
        "severity": "MINOR",
        "issue": "§11.11.4 states the worktree preflight \"lands *before* the first parallel agent run, not after\" and costs it at ~3 hours; §11.16 schedules it in a 1-hour slot on Friday, four days after the first agent-eligible step. The two stale worktrees are also still present, contradicting §11.16's Monday row.",
        "evidence": "§11.11.4: \"Effort for the worktree preflight and HEAD reconciliation: **~3 hours, and it lands *before* the first parallel agent run, not after.**\" §11.16 Friday: \"15:00–16:00 | **§11.11.4** — worktree preflight + before/after HEAD reconciliation, wired before any parallel agent run\" — a 1-hour slot for a 3-hour task. §11.1 lists step 2's owner as \"Sagnik or **one dispatched agent**\" and step 5's as \"one dispatched agent\". Live: `git worktree list` still shows `.claude/worktrees/competent-bassi-5da9a1` and `.claude/worktrees/upbeat-euclid-60dbf4`, both at `8eb4de2` — now **20** commits behind HEAD (`git rev-list --left-right --count origin/main...HEAD` → `0 20`), not 17.",
        "fix": "Move §11.16's worktree preflight to Monday 11:00–12:00 as a 1-hour first pass and add a Tuesday 09:00–11:00 slot for the remaining two hours, or widen the Friday slot to 15:00–18:00. In §11.11.4 add: \"Both worktrees are still present as of 2026-08-01 22:43 and are now **20** commits behind, not 17 — remove them before the first dispatch, not after.\" In §11.2.1 update \"Both are 17 commits behind HEAD\" to \"20 commits behind HEAD as of `4f97129`\"."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.2.1 final paragraph",
        "severity": "MINOR",
        "issue": "Dangling cross-reference: the worktree deletion is routed through \"the RULE 2 confirmation in §11.12.5\", and §11.12.5 contains no confirmation procedure of any kind.",
        "evidence": "§11.2.1: \"This is a local, reversible operation on ignored directories; it is still a deletion, so **it goes through the RULE 2 confirmation in §11.12.5** like any other.\" §11.12.5 is titled \"Dispatching an agent — what it may and may not do\" and contains: the verbatim SCOPE/FORBIDDEN/DELIVERABLE/GATE prompt block, three dispatcher duties (HEAD reconciliation, 20-tool cap, gate-closes-ticket), and the do-not-import-from-AIOS list. No confirmation procedure, no destructive-op protocol.",
        "fix": "Replace the clause with: \"...it is still a deletion, so it follows the standing RULE 2 protocol — name the operation, state the blast radius (two worktrees at `8eb4de2`, 20 commits behind, containing no unpushed work: check `git -C <worktree> status --porcelain` and `git log <branch> --not --remotes` first), and get an explicit yes before running `git worktree remove`.\" Do not point at §11.12.5, which governs agent dispatch and not destructive ops."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.14 K3 and K4",
        "severity": "MINOR",
        "issue": "Two kill gates carry undefined thresholds, which is the defect §11.7 explicitly claims to have fixed elsewhere (\"both previously had no threshold, which meant they could never fire\"). K5b and K6 were given numbers; K3 and K4 were not.",
        "evidence": "K3: \"The re-derived anchor number is **materially below** 99.627%, **or** the 30-line LCS diff baseline **matches it**\" — neither \"materially below\" nor \"matches\" is defined, and the two candidate values quoted in the same cell are 86.658%/85.036% against 99.627%, a 13–14 point gap that would presumably never count as a match. K4: \"exceeds **~0.5%**\" — a tilde on a threshold, measured by \"hand audit, **n stated**\", with n neither stated nor floored, so the audit's power is undefined.",
        "fix": "Replace K3's condition cell with: \"The re-derived anchor number on the >2-deleted-lines stratum falls **below 97.0%** correct, **or** the diff3-assisted LCS baseline lands **within 3.0 percentage points** of it on that same stratum (baseline today 86.658%, re-derived 85.036%, vs hash-alone 77.297%)\". Replace K4's condition cell with: \"The **range** resolver's hand-audited false-match rate exceeds **0.5%** on a sample of **n ≥ 400** range anchors, stratified by block token length (3–5, 6–9, 10–19, 20+), with n reported per stratum\"."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§12.2 N1, N7, N8, N11 and §12.3 (the 'verified live in this session' blocks)",
        "severity": "MINOR",
        "issue": "Four of §12's warning-sign blocks are labelled `[measured, verified live in this session]` and are now stale by one commit day. §12 is the section that most insists numbers be re-derived rather than carried, so these are the ones a reader will check first.",
        "evidence": "Live 2026-08-01 23:19: `git log --oneline | wc -l` → **24** (plan: 21); `git log --format='%s' | grep -c '^docs'` → **15** (plan: 13); `git rev-list --left-right --count origin/main...HEAD` → `0 20` (plan: `0 17`, quoted in N8, §11.2.0 and §12.5 item 10.7); `find src -name '*.ts*' | wc -l` → **213** and total LOC **21,578** (plan: 212 / 21,415, quoted in §11.3.3 and N1). `grep -ril mdmax src test scripts` → still **0 files**, so N1's load-bearing claim survives intact. §11.13's \"all 20 commits in `frontmatter`\" is now 24.",
        "fix": "Add one line to §12.2's preamble and to §11.2.0: \"**Live counts as of `4f97129`, 2026-08-01 22:43 IST:** 24 commits (15 docs-prefixed), 20 ahead of `origin/main`, `src/` at 213 files / 21,578 LOC, `grep -ril mdmax src test scripts` still **0**. Every block below tagged 'verified live in this session' was measured before that commit; the direction of every claim is unchanged and the MDMAX-code-is-zero finding is unaffected.\" Update the four numeric blocks in N1, N7, N8 and N11 in place, and change §11.13's \"all 20 commits\" to \"all 24 commits\"."
      },
      {
        "cls": "execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path",
        "section": "§11.16 Monday 17:00–17:30 vs Wednesday 12:00–13:00",
        "severity": "COSMETIC",
        "issue": "The recruiting funnel assumes a ~40% same-week conversion on cold outreach to strangers: five messages sent Monday evening, two calls booked for Wednesday noon.",
        "evidence": "§11.16 Monday: \"17:00–17:30 | Send five recruiting messages for **§11.12.7** | five sent, replies expected by Wednesday\". §11.16 Wednesday: \"12:00–13:00 | **Conversation 1 and 2.**\" The frames named in §11.12.7 are forum posters on a thread with 246,568–249,616 views, commenters on `obsidian-git` issues 340/396/558, and authors of repos carrying an `llms.txt` — all cold, all unsolicited, none with a prior relationship.",
        "fix": "Change the Monday cell to \"Send **30** recruiting messages (see the §11.12.7 revision) | 30 sent\" and change Wednesday's to \"12:00–13:00 | **Conversations 1 and 2 if booked** — if fewer than two replied, this hour goes to task 1c and the calls slide to next week. Do not treat the conversation count as a schedule commitment; it is a funnel and 30 outbound is the honest top of it.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "WHOLE-PLAN BASELINE — read this first before acting on any finding below",
        "severity": "MINOR",
        "issue": "The plan's code claims are overwhelmingly CORRECT. I opened every named file at every named line. Reporting this explicitly so the tweaks below are not read as a general indictment.",
        "evidence": "CONFIRMED EXACT (file:line opened, content matches): allowlist.ts:9 (the case-insensitive comparison); dependency-container.ts:42 `const AUTHOR = { name: \"Sagnik Mitra\", email: \"sagnikmitra123@gmail.com\" } as const;` and its four consumers at 66/70/75/80 (`author: AUTHOR`); env.ts:47, :85-87, :252; auth-options.ts:32-66 (Credentials block), :76-77 (the comment), :82 (`return isAllowed(ghProfile?.login, authEnv.ALLOWED_GH_LOGIN);` — and grep proves it is isAllowed's ONLY non-test caller); github/client.ts:30 and github-writer.ts:33 (both `Bearer ${repoEnv.GITHUB_REPO_TOKEN}`); `grep -rn 'firestore()' src/ test/` returns EXACTLY ONE hit, the definition at firebase/client.ts:38; NO signup/register route exists (src/app/(auth) contains only login/page.tsx); firestore.rules canEditVault at 176 (not isVaultEditor), roles enforcement at 284-289, plan=='free' at 214, unchanged(['plan','email','createdAt']) at 219; frontmatter.ts:47 `const yaml = doc.toString().replace(/\\n$/, \"\");` and again at :59; PropertiesPanel.tsx:55 and :79 `onEdit(stringifyFrontmatterDoc(parsed.doc, parsed.body))`; EditorPane.tsx:498-502 handleEdit and :647 `<PropertiesPanel content={previewSource} onEdit={handleEdit} />` and :595 `<LivePreview`; the full §10.5.3(c) five-line browser-main-thread chain (LivePreview.tsx:1 \"use client\", :35 useMemo, block-split.ts:34-38 unified chain, :47 processor.parse) — all five exact; markdown-parser.ts:12 WIKILINK_RE and :111-120 try/catch; parseMarkdown imported in EXACTLY two places, dependency-container.ts:11 and search-index.ts:15, and zero \"use client\" files reach either; graph-data.ts unresolved handling; dto.ts:35-43; get-snapshot.ts:196-209 and :199 as the only `.frontmatter` consumer; SnapshotProvider.tsx:78; the four localeCompare sites (KnowledgePanels.tsx:27, SearchPanel.tsx:116, tree-order.ts:34, list-conflicts.ts:29) — exactly 4, exactly those; normalize( = 0 hits, NFC|NFD|NFKC = 0 hits, Segmenter = 0 hits in src/; html-policy.ts DISALLOWED=12 and SAFE=61 with the exact member lists, URL_ATTRS=10, `style` present ONLY as an element never as an attribute, :145-164 sanitizeProperties, :183-186 the text-node branch; components.tsx:36 `_onEdit`, :132-137, :133 `/language-(\\w+)/`; toolbar-transforms.ts:112-124; pdf-doc.ts:23/24/134; export-doc.ts:66; editor-store.ts:6/21/24/40/41/90/144/237; CommitBar.tsx:74/98; commit/route.ts:47; ImportModal.tsx:81; ExportMenu.tsx:93/95/156/158/160; AIMenu.tsx:25/192/248-251; CodeMirrorEditor.tsx:40/41/193/206/231; VaultWorkspace.tsx:170/199; FileTreeActions.tsx:68/69/83/153; share/route.ts:21 and its two 502 handlers; eslint.config.mjs:80-96/109-111/123-125/148-152; gray-matter/index.js:161; .gitignore:12; EditableTable imported by nothing. ALSO REPRODUCED INDEPENDENTLY, this session, on node v24.6.0: corpus 1,084/1,084 present, 1,084/1,084 sha256 match, corpus_id reproduces; 907 frontmatter files; yaml no-edit round trip 119 identical / 618 drift / 170 no-parse (§3.1.5 table, exact); gray-matter 33/703/171 (§7.2.5, exact); 17/736=2.31% and 17/907=1.87% (§7.2.5, exact); 624/736=84.78% date-to-ISO (§7.2.5, exact); PropertiesPanel no-op 623/737=84.5% dirty, 226/737=30.7% line-count change (§5.9.2, exact); two-parser divergence 625/737=84.80% over 973 occurrences with exactly ONE class, js-yaml Date vs eemeli string (§10.6.4, exact). WIKILINK_RE quadratic REPRODUCES: 588.6/2343.5/9339.4/37273.9 ms at 20k/40k/80k/160k (plan: 589.2/2315.9/9071.0/36865.0), k=1.993/1.995/1.997, and 12,193 ms on the 748,889-byte half-typed vault (plan: 11,012.9 ms). mdast REPRODUCES: 12,434 ms at 80k list items (plan: 12,429), micromark 1,332 ms (plan: 1,207), mdast list/para 20.9x (plan: 19.4x).",
        "fix": "No change. Do not let the tweaks below trigger a re-audit of the confirmed set — it has been checked line by line."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11 execution gates (all of them) — measured live, not quoted",
        "severity": "BLOCKING",
        "issue": "`npm run lint` emits 930 errors and lints NOTHING. 100% of them are the same typescript-eslint parse failure, caused by the two abandoned `.claude/worktrees/` each carrying a tsconfig.json. Every architecture rule the plan cites as `[primary, eslint.config.mjs:NN]` — boundaries/dependencies, no-restricted-imports, and the planned eslint ban on `matter.stringify` — is currently not running at all. §11.2.1 documents the worktrees at length but frames them exclusively as a vitest problem and prescribes a vitest-only fix.",
        "evidence": "`npx eslint . --max-warnings=0` -> `✖ 930 problems (930 errors, 0 warnings)`. Machine-readable breakdown: 930/930 are `Parsing error: No tsconfigRootDir was set, and multiple candidate TSConfigRootDirs are present: /…/frontmatter, /…/frontmatter/.claude/worktrees/competent-bassi-5da9a1, /…/frontmatter/.claude/worktrees/upbeat-euclid-60dbf4`. Distribution: 614 in the worktrees, 316 in the main tree — i.e. every source file including all of src/. Confirmed both worktree tsconfigs exist (`ls .claude/worktrees/*/tsconfig.json`). Controlled experiment: `npx eslint . --ignore-pattern '.claude/**'` -> 25 errors total, ZERO in src/ (22 no-undef on console/process/document in .mjs build+scratch scripts, 2 unused vars in docs/mdmax/build/build-site.mjs, 1 no-dupe-else-if in scripts/fm-roundtrip-audit.mjs:99). Confirmed eslint.config.mjs:16-27 `ignores: [...]` contains node_modules, .next, out, public, **/*.md, md/**, src-tauri — and does NOT contain .claude. Note this is invisible to a targeted invocation: `npx eslint <single file>` sets one root and works fine, which is why it has not been noticed.",
        "fix": "Add to §11.2.1, immediately after the vitest fix block, a second required edit: \"**The same two worktrees also break `npm run lint` completely, and this half is separate from the vitest fix.** Each worktree carries a `tsconfig.json`, so typescript-eslint's project service sees three candidate roots and refuses to parse ANY file: `npx eslint . --max-warnings=0` returns **930 errors, all of them the same `No tsconfigRootDir was set` parse failure** — 614 in the worktrees, **316 in the main tree, covering all of `src/`**. Until this is fixed, `boundaries/dependencies`, `no-restricted-imports`, and every rule this plan cites as `[primary, eslint.config.mjs:NN]` **are not being enforced at all**, and the planned eslint ban on importing `matter.stringify` (§10.6.5 rule 1) cannot work. **Fix:** add `\\\".claude/**\\\"` to the `ignores` array at `eslint.config.mjs:16-27`. **Acceptance gate:** `npx eslint . --max-warnings=0` drops from 930 to **25**, of which **0 are in `src/`** [measured 2026-08-01]. Removing the worktrees fixes it too, but the ignore entry is the durable fix — a future agent worktree will otherwise reintroduce it.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.16 Monday/Tuesday schedule; every gate phrased \"typecheck green\"; §11.12 `npm run verify` chain",
        "severity": "BLOCKING",
        "issue": "`npm run typecheck` is RED on this branch right now, for a reason unrelated to any planned work. `npm run verify` (typecheck && lint && test && build && arch) therefore fails at step 1 and step 2 on day one, and no §11 acceptance gate worded \"typecheck green\" can be met until this is cleared. Step 0 (§11.2) does not mention it.",
        "evidence": "`npx tsc --noEmit` -> 5 errors, ALL in `test/preview/html-policy-xss.test.ts`: `(33,23): error TS7016: Could not find a declaration file for module 'jsdom' … Try \\`npm i --save-dev @types/jsdom\\``, then `(81,17)`, `(86,35)`, `(87,20)`, `(94,19)` `error TS18046: 'el'/'attr' is of type 'unknown'` — all four downstream of the first. `grep -n jsdom package.json` -> line 89 `\"jsdom\": \"^29.1.1\"`; `node_modules/jsdom` present, `node_modules/@types/jsdom` ABSENT. Zero tsc errors originate in `src/`.",
        "fix": "Add a new item to §11.2 Step 0, before §11.2.1: \"#### 11.2.0b `npm run typecheck` is red, and it is not our code. `npx tsc --noEmit` returns **5 errors, all in `test/preview/html-policy-xss.test.ts`**, all downstream of a missing `@types/jsdom`: `jsdom@^29.1.1` is a devDependency (package.json:89) but ships no types, so `TS7016` on the import cascades into four `TS18046: 'el'/'attr' is of type 'unknown'`. **`src/` is clean — 0 errors.** Because `npm run verify` is `typecheck && lint && test && build && arch`, the chain dies at step 1 and every gate below worded 'typecheck green' is unreachable until this lands. **Fix:** `npm i --save-dev @types/jsdom`, or add `declare module 'jsdom';` to a `.d.ts`. **Acceptance gate:** `npx tsc --noEmit` exits silent [measured 2026-08-01].\" Also add to the Monday 09:30–10:00 row: \"…and clear the 5 `@types/jsdom` typecheck errors and the 930 eslint parse errors in the same commit, so `npm run verify` is green before any work is gated on it.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.4.2 \"What ships\" (line 11884-11891) vs §10.6.5 rule 1 (line 11178 region)",
        "severity": "BLOCKING",
        "issue": "The two sections prescribe incompatible implementations for the same function, and §11.4.2's version cannot meet §11.4.3's own gate. §10.6.5 rule 1 says the writer must NEVER invoke a YAML parser — which is precisely what makes the 170 unparseable files writable. §11.4.2 then instructs the builder to use eemeli/yaml's `node.range` to locate the byte range, which requires parsing with the very library that fails on those 170 files. A builder who follows §11.4.2 caps out at 737/907 and fails the §11.4.3 gate.",
        "evidence": "§10.6.5 rule 1, verbatim: \"**WRITES NEVER PARSE.** The splice writer locates `public_slug:` (or any key) by **byte scan inside the delimiter block** and splices, or appends. It never invokes a YAML parser. This is the real fix, and it is what makes the 170 unparseable files **writable at all**.\" §11.4.2, verbatim: \"`yaml@2.9.0` is **already a dependency** `[primary, package.json]` and exposes `node.range` on every AST node — use it to find the byte range, then throw the Document away without ever serialising it.\" Measured: eemeli/yaml `parseDocument` returns `errors.length > 0` on exactly 170 of the 907 frontmatter files (set verified byte-for-byte identical to gray-matter's 170 — see the next finding), so a node.range-based writer has no range to read for 18.74% of the corpus. The already-shipped `src/modules/share/domain/splice-frontmatter.ts` follows §10.6.5 (its own header: \"It does not parse YAML. It scans the frontmatter block line-wise\") and I measured it: it WRITES the key in 907/907 files (not merely refuses — I asserted `^public_slug: audit-test-slug$` is present in the output for all 907, including all 170), 0 refusals, 0 throws, and round-trips byte-identical 907/907.",
        "fix": "Delete the `node.range` sentence from §11.4.2 entirely and replace it with: \"**Do not use `yaml`'s `node.range` to find the byte span.** Reading `node.range` requires `parseDocument` to succeed, and it fails on the same 170 files that make this step necessary — a `node.range` writer tops out at 737/907 and cannot pass §11.4.3. The writer scans the delimiter block line-wise for a top-level `key:` at column 0, refuses on any shape it does not fully understand (duplicate key, block scalar, anchor/alias/tag, unterminated block, non-map document), and never constructs an AST. `yaml` is not imported by this file. **Measured on the shipped implementation: the key is actually written in 907/907 files, including all 170 that neither parser reads, with 0 refusals and 0 throws, and the publish→unpublish round trip is byte-identical 907/907** [measured 2026-08-01, corpus_id sha256:3a010b16…].\" Apply the same deletion to §5.9.2's \"THE FIX, verified feasible\" paragraph, or scope it explicitly to the PropertiesPanel (which only ever operates on files `parseFrontmatter` already accepted, so `node.range` is defensible there — say so)."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.4.3 \"The gate\" (line ~11900) — the independence requirement",
        "severity": "BLOCKING",
        "issue": "The independent oracle that §11.4.3 requires does not exist. The file committed as it, `scripts/fm-roundtrip-audit.mjs`, (a) crashes on import so it has never run, and (b) violates all three of §11.4.3's stated independence conditions. The 907/907 figure in commit 4f97129's message therefore has no oracle behind it — it was produced by the vitest gate, which shares the writer's code.",
        "evidence": "`node scripts/fm-roundtrip-audit.mjs splice` -> `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/…/src/modules/share/domain/splice-frontmatter.mjs'`. Line 20 of the script is `import { spliceFrontmatterValue } from '../src/modules/share/domain/splice-frontmatter.mjs'`; `ls src/modules/share/domain/` shows only `slug.ts` and `splice-frontmatter.ts` — there is no `.mjs`. Against §11.4.3's three conditions, verbatim (\"it must not import `spliceFrontmatterValue`, `yaml`, or `gray-matter`\" and \"re-reads the file from disk and compares raw bytes with `Buffer.compare`\"): line 18 `import matter from 'gray-matter'`, line 19 `import { parseDocument, isMap } from 'yaml'`, line 20 imports `spliceFrontmatterValue` — all three banned imports present; and the comparison is `if (back === src)` (line 97), a JS string `===`, not `Buffer.compare` on a re-read. Separately, `npx eslint scripts/fm-roundtrip-audit.mjs` -> `99:18 error no-dupe-else-if This branch can never execute` — the `refused` counter at line 100 is structurally dead, so that column would report 0 even if the script ran.",
        "fix": "Add to §11.4.3, immediately after the \"'Shares no code with the writer' is load-bearing\" paragraph: \"**STATUS 2026-08-01: this oracle does not exist yet.** `scripts/fm-roundtrip-audit.mjs`, committed in `4f97129` as if it were the oracle, fails at import (`ERR_MODULE_NOT_FOUND` — line 20 imports `splice-frontmatter.mjs`; only `splice-frontmatter.ts` exists) and has therefore never executed. It also violates all three conditions above: it imports `gray-matter` (line 18), `yaml` (line 19) and `spliceFrontmatterValue` (line 20), and compares with `===` on strings rather than `Buffer.compare` on re-read bytes. Its `no-dupe-else-if` at line 99 makes the `refused` column dead code. **The 907/907 figure in `4f97129`'s commit message came from `test/share/frontmatter-splice.test.ts`, which imports the writer under test — that is a self-check, not an oracle.** Step 2 is not done until an oracle that imports none of the three, writes the spliced bytes to a temp file, re-reads them, and `Buffer.compare`s against the original, reports 907/907. Delete `scripts/fm-roundtrip-audit.mjs` or rewrite it to that spec; do not cite it as evidence in its current state.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.4.3 \"The gate\" — the corpus-completeness assertion that is missing",
        "severity": "BLOCKING",
        "issue": "The committed corpus gate reports GREEN over 23 files instead of 907 on any machine that does not have the two private corpora checked out — which is every CI runner, every fresh clone, and any second developer. It prints the identical `11 passed` summary either way. The plan's gate text says 907/907, but nothing in the test asserts the corpus size.",
        "evidence": "`test/share/frontmatter-splice.test.ts:132-146` — `loadCorpus()` does `if (!base || !fs.existsSync(base)) continue;` per root, where `ROOTS = { md: ~/Desktop/GitHub/md, knowledge: ~/Desktop/GitHub/knowledge, frontmatter: process.cwd() }`. The guard is `it.skipIf(corpus.length === 0)` (lines 173, 190) and the assertions are `expect(s.changed).toBe(0); expect(s.threw).toBe(0); expect(s.identical).toBe(s.total)` — all relative to `s.total`, never to 907. Measured per-root frontmatter counts: md=662, knowledge=222, frontmatter=23. With the two private roots absent, `process.cwd()` always exists, so corpus.length = 23, `skipIf` does NOT fire, and I ran the degraded case: gray-matter scores identical=12 changed=11 threw=0 (so the LR#68 RED-proof `expect(gm.changed + gm.threw).toBeGreaterThan(0)` still passes) and splice scores 23/23 (so the gate passes). Full-corpus run on this machine for comparison: `gray-matter (shipped): 33/907 identical (3.64%), 703 changed, 171 threw / yaml Document: 284/907 (31.31%), 623 changed / splice: 907/907 (100.00%)` — and the vitest summary line is `Test Files 1 passed (1) / Tests 11 passed (11)` in BOTH cases.",
        "fix": "Add to §11.4.3's gate block a fourth line: \"The gate MUST assert its own corpus size. `test/share/frontmatter-splice.test.ts` currently guards with `it.skipIf(corpus.length === 0)`, which does not fire when the private roots are missing — `process.cwd()` always resolves, so the suite silently runs over **23** files instead of **907** and prints the identical `11 passed`. Verified by simulation: degraded run scores gray-matter 12/23 and splice 23/23, both green. **Required change:** replace the guard with `const FULL = corpus.length === 907;` plus `it.skipIf(!FULL)(…)` on both corpus tests and an explicit `expect(corpus.length).toBe(907)` inside a third test that is itself `skipIf(corpus.length === 0 || corpus.length === 23)`; and have the test print `corpus: N/907` on every run so a degraded run is visible in the log. A gate whose denominator is unasserted is not a gate.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§3.1.5 \"WARNING — do not take the yaml-library shortcut\", the paragraph beginning \"**Note the two nearby numbers are not the same number.**\" (line ~2049)",
        "severity": "MATERIAL",
        "issue": "Both halves of this normative instruction are false, and it directly contradicts §7.2.5, which has it right. The plan tells the builder that 170 and 171 come from two different parsers failing on two different file sets, and orders them not to be merged. Measured: the two parsers fail on the SAME 170 files — the sets are byte-for-byte identical, symmetric difference zero — and 171 is not a parse count at all.",
        "evidence": "§3.1.5 verbatim: \"**Note the two nearby numbers are not the same number.** 171 is the count of blocks `gray-matter` throws on; 170 is the count `yaml` cannot parse. Different parsers, different failures. Do not merge them.\" Measured over corpus_id sha256:3a010b16…: gray-matter `matter(src)` throws on **170** of 907; eemeli/yaml `parseDocument(...).errors.length > 0` on **170** of 907; set comparison — only-gray-matter = `[]`, only-yaml = `[]`, SETS IDENTICAL = true. The 171st file is `md/pj.md`, which parses cleanly under BOTH parsers and throws only inside `matter.stringify`, from gray-matter's re-parse of the body (gray-matter/index.js:161, verified verbatim). Contradicting section, §7.2.5, which is correct: \"gray-matter throws (unpublishable → HTTP 502) | **171 = 170 parse failures + 1 stringify failure (`md/pj.md`)**\" and \"it fails to parse **the same 170 files**\".",
        "fix": "Replace the whole paragraph with: \"**Note the two nearby numbers, and note that they are NOT two parsers disagreeing.** Both parsers fail on **exactly the same 170 files** — measured, symmetric difference zero. The 171st is `md/pj.md`, which parses cleanly under both and throws only inside `matter.stringify`, from gray-matter's re-parse of the body (`gray-matter/index.js:161`). So: **170 = files no YAML parser reads. 171 = 170 + `md/pj.md`, the extra one being a *serialiser* failure, not a parse failure.** Quote 170 whenever the subject is parseability and 171 only when the subject is 'what the shipped gray-matter write path throws on'. §7.2.5 states this correctly; this paragraph previously did not. [re-measured 2026-08-01, corpus_id sha256:3a010b16…]\" And apply the same correction at §11.4.3, where \"including on the **171** that no YAML parser reads\" must become \"including on the **170** that no YAML parser reads, plus `md/pj.md`, which parses but kills `matter.stringify`\"; and at §11.16 Tuesday 09:00–13:00, where \"the **171** unparseable files stop 502-ing\" must become \"the **170** unparseable files stop 502-ing\"."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§10.4, the pass-interface table, row 4 `resolve` (line 10294)",
        "severity": "MATERIAL",
        "issue": "The plan's own binding normative table encodes a claim that the plan itself already REFUTED, in detail, eight thousand lines earlier. §10.4's preamble says \"'May not' is binding: a pass that does a thing in its 'may not' column is a bug even if the output looks right\" — so this row will be read as a specification of current behaviour and copied into the engine's tests.",
        "evidence": "§10.4 row 4 `resolve`, MAY NOT column, verbatim: \"silently drop an unresolved reference (`graph-data.ts:132` does this today)\". §2 (line ~3497), verbatim: \"The founder-thesis area justified a 'program-wide blind spot' by quoting `src/modules/graph/presentation/graph-data.ts:132` as `if (targetPath === undefined) continue; // unresolved — drop`. **That line does not exist.** … **The paraphrased-as-verbatim code quote is itself a defect, and the artifact cited does the opposite of what the claim asserted.**\" I opened the file: line 132 is the comment text `* problems for the author.`; the real branch is `graph-data.ts:176` `if (targetPath === undefined) {`, preceded at 174-175 by `// Unresolved links are not edges — there is no node to point at — but // they are a diagnostic, so they are reported rather than dropped.`; the module exports a typed `UnresolvedLink[]` (declared line 109), a `reportUnresolved()` closure (line 160), and a `reason` field discriminating `\"target-excluded\"` from `\"no-such-note\"` (lines 178-183). Note also that §2's own correction is off by one: it says the `— drop` comment \"exists at line 188\"; line 188 is `const key = ...` and the comment is at line **189**, `if (seenLinks.has(key)) continue; // duplicate — drop`.",
        "fix": "Replace the MAY NOT cell of §10.4 row 4 with: \"silently drop an unresolved reference. **Note: the shipped `graph-data.ts` already does this correctly** — see §2. It reports through a typed `UnresolvedLink[]` with a `reason` of `\\\"target-excluded\\\"` or `\\\"no-such-note\\\"` (`graph-data.ts:160-184`). Model pass 4 on it; do not treat it as the counter-example.\" And in §2, change \"The `— drop` comment quoted exists at line 188\" to \"…exists at line **189**\"."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§7.2.5, the blockquote beginning \"**What this is not.**\" (line 7383)",
        "severity": "MATERIAL",
        "issue": "The sentence quotes a no-edit-round-trip figure to characterise a publish operation, understating the yaml path by a factor of 2.4. The word \"published\" makes the operation explicit and the number attached to it is for a different operation. This matters because the reader is being asked to reject the yaml swap, and the honest rejection number is weaker than the one printed.",
        "evidence": "Verbatim: \"ported to the same corpus it reaches only **13.12% published-and-byte-identical** (119/907)\". Measured two ways: (a) no-edit round trip, `parseDocument(block).toString()` compared to the original block, no key set or deleted — **119/907 identical, 618 drift, 170 no-parse**, exactly reproducing the §3.1.5 table; (b) the operation the product actually performs, publish (`doc.set('public_slug', …)`) then unpublish (`doc.delete`), whole file compared — **284/907 identical (31.31%), 623 changed, 0 threw**. 4f97129's own commit message reports the second (\"yaml Document API 284/907 31.31%\"), so the plan and the repo's commit log currently state two different numbers for the same library with no reconciliation anywhere.",
        "fix": "Replace the clause with: \"ported to the same corpus it reaches only **31.31% published-and-byte-identical** (284/907) — a *no-edit* round trip with no key set at all is byte-identical on just **119/907 (13.12%)**, which is the figure §3.1.5 tabulates — and it fails to parse **the same 170 files**.\" Then add one sentence to §3.1.5's table caption: \"These are **no-edit** round-trip figures. Under the operation Publish actually performs (set the key, then delete it) the yaml path scores **284/907 = 31.31% byte-identical**; the conclusion is unchanged — 31.31% is not a fix either — but quote the right number for the right operation. [both measured 2026-08-01]\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§10.6.3, the `share-writer.ts:26` row and the `gray-matter/index.js:161` row",
        "severity": "MATERIAL",
        "issue": "The denominator is off by one and, as a direct result, the section invents and then mis-explains an arithmetic anomaly that does not exist. It attributes a phantom off-by-one to gray-matter's body re-parse, which is a real mechanism but not the cause of this. A builder reading it will look for a missing file that is not missing.",
        "evidence": "Verbatim: \"Over the corpus: **737 run**, **695 frontmatter blocks changed (94.30%)**, 33 byte-identical, 8 trailing-newline-only, **0 bodies changed**\" and \"This is why the reported triple does not sum: 33 + 8 + 695 = 736, not 737\". Measured: the gray-matter write path throws on **171** of 907 (170 parse + `md/pj.md` in stringify), so **736 run, not 737**. Decomposing those 736 by comparing the frontmatter block only: **41 unchanged / 695 changed**, and 41 + 695 = 736 exactly. The 41 splits into the 33 that are fully byte-identical and the 8 that differ only in a trailing newline — 33 + 8 = 41. Everything sums at 736 with no residue. §7.2.5 already uses 736 correctly (\"17/736 parsed\", \"624 of 736 parsed\"), so §10.6.3 is the only place carrying 737.",
        "fix": "Rewrite the `share-writer.ts:26` row's measurement as: \"Over the corpus: **736 run** (171 throw), **695 frontmatter blocks changed (94.43% of 736)**, 41 blocks unchanged — of which **33 are fully byte-identical** and **8 differ only in a trailing newline** — and **0 bodies changed**. 695 + 41 = 736; it sums.\" And delete the sentence \"This is why the reported triple does not sum: 33 + 8 + 695 = 736, not 737 `[primary + measured]`\" from the `gray-matter/index.js:161` row, replacing it with: \"It is also why the run count is **736 and not 737**: `md/pj.md` parses under both YAML libraries but dies inside `matter.stringify`.\" Keep the rest of that row — the body-truncation mechanism is real and verified verbatim at gray-matter/index.js:161."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.2.1 acceptance gate (line ~11655) and §11.16 Monday 09:30–10:00 and 10:00–10:30 (lines 12637-12638); also §1 line 491, §7.2.5 line 7389, §7.5 line 7713, §7.2 line 7160",
        "severity": "MATERIAL",
        "issue": "The task is already done and the acceptance number is now wrong by one, so the gate as written FAILS on a clean tree. Six places in the plan hard-code 83; the answer is 84, because the same commit that landed the `exclude` also added the corpus gate test. Executing §11.2.1 as written costs a builder time hunting a one-file discrepancy.",
        "evidence": "`vitest.config.ts` at HEAD already reads `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]` with the comment \"Two abandoned agent worktrees under .claude/ were being collected, inflating the suite by 66.4%…\" — landed in 4f97129. Measured now: `npx vitest list --filesOnly | wc -l` -> **84**; `npx vitest list --filesOnly | grep -c worktrees` -> **0**; `find test -name '*.test.ts*' | wc -l` -> **84**. The 84th file is `test/share/frontmatter-splice.test.ts`, added by the same commit. Note also that the landed exclude differs from §11.2.1's prescribed fix: it drops `**/.git/**` (which `configDefaults.exclude` supplies) and omits `**/src-tauri/**` and `**/.next/**`. `git worktree list` still shows both worktrees at 8eb4de2, so the removal half of §11.2.1 is genuinely outstanding.",
        "fix": "Replace §11.2.1's \"**The fix.**\" code block and gate with: \"**STATUS: LANDED in `4f97129`.** `vitest.config.ts` now sets `exclude: [\\\"**/node_modules/**\\\", \\\"**/dist/**\\\", \\\"**/.claude/**\\\"]`. Verify rather than re-do: `npx vitest list --filesOnly | wc -l` returns **84** (not 83 — the same commit added `test/share/frontmatter-splice.test.ts`) and `| grep -c worktrees` returns **0** [measured 2026-08-01]. Two gaps remain: the landed exclude drops `**/.git/**` and omits `**/src-tauri/**` and `**/.next/**`; and **the worktrees themselves are still present** (`git worktree list` shows both at `8eb4de2`) — which still breaks `npm run lint` entirely, see §11.2.0b.\" Change 83 -> 84 at lines 491, 7160, 7389, 7713, 12637 and 12638; at 7389 and 7713 the sentence \"the gate is `vitest list --filesOnly` moving 247 → 83\" becomes \"moved 247 → 84 (landed `4f97129`)\"."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§3.1.4 \"Violation 1\" (lines 2003-2018); §7.2.5 (lines 7359-7362); §8 (line 8380); §10.6.3 rows for share-writer.ts:19 and :26 (lines 11140-11141); §10.7 row 2 (line 11471); §11.10 (line 11852); §14 defect table (line 14935)",
        "severity": "MATERIAL",
        "issue": "Nine locations still describe a defect that was fixed in the commit at HEAD. The plan describes `share-writer.ts` as calling `matter.stringify` and `matter()` bare; it now calls `spliceFrontmatterValue` and parses nothing. Any of these read as current-state will send a builder to re-fix a fixed file, and the §10.7 priority table ranks the fix as the #2 outstanding item.",
        "evidence": "HEAD 4f97129 `src/modules/share/infrastructure/share-writer.ts` line 7: `import { spliceFrontmatterValue } from \"@/modules/share/domain/splice-frontmatter\";`, line 25: `const next = spliceFrontmatterValue(file.content, \"public_slug\", slug);`. `grep -n 'matter' src/modules/share/infrastructure/share-writer.ts` -> no gray-matter import, no `matter(` call, no `matter.stringify`. The pre-fix file (`git show 96b5948:…`) did have `matter(file.content)` at 19 and `matter.stringify(parsed.content, data)` at 26, so the plan's line numbers were correct when written. The 502 rationale is also void for this path: I measured the splice writer over all 907 files — 0 throws, so no YAML failure can reach `src/app/api/share/route.ts`'s 502 handler any more.",
        "fix": "Do not delete these — they are the historical record and the justification. Add a single dated banner to §3.1.4 immediately under the heading, and a one-line pointer at each of the other eight sites: \"**FIXED 2026-08-01 in `4f97129`, after this plan was written.** `share-writer.ts` no longer imports `gray-matter`. Line 25 is now `const next = spliceFrontmatterValue(file.content, \\\"public_slug\\\", slug);` and the writer parses nothing, so the 502 path is closed too (measured: 0 throws over all 907). **Violation 1 is history; Violation 2 (`frontmatter.ts:47`/`:59` → `PropertiesPanel.tsx:79` → `EditorPane.tsx:498-502` → `setContent`+`saveDraft`) is still LIVE and still reaches disk via the commit path** — verified line by line this session. Everything below describes the pre-fix state and is retained as the justification for the splice writer, not as a description of the tree.\" Then in §10.7's priority table, row 2 must be re-scoped: strike `share-writer.ts:26` and leave only `frontmatter.ts:47`, so the #2 item reads \"**`frontmatter.ts:47` and `:59`** (§5.9.2) | every properties-panel edit, today | replace `stringifyFrontmatterDoc` with a range splice\"."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§3.1.6 (lines 2062, 2113), §11.9 (line 11554), §11.10 \"Where it lives\" (line 11884), §11.16 Tuesday (line 12649)",
        "severity": "MATERIAL",
        "issue": "The plan names three different homes for the splice writer, none of which is where it actually shipped. Executing Tuesday's row as written creates a SECOND, divergent splice implementation. The §11.9 parallelism argument also silently breaks, because steps 1 and 2 are no longer file-disjoint in the way it claims.",
        "evidence": "§3.1.6 line 2113: \"It ships as `src/modules/mdmax/splice.ts` (~40 lines\" — and line 2062's code header agrees. §11.10 line 11884: \"New module `src/modules/mdmax/domain/splice-frontmatter.ts`, exported from a new barrel `src/modules/mdmax/index.ts`\" — a different path in the same document. §11.9 line 11554: \"step 2 touches `src/modules/share/infrastructure` and a new `src/modules/mdmax/domain`. No shared file.\" Actual: `ls src/modules/` -> ai, ai-tools, app-shell, auth, drafts, editor, export, graph, preview, repository, share, vault. **There is no `mdmax` module.** The function shipped at `src/modules/share/domain/splice-frontmatter.ts` (7,236 bytes, 164 lines), and `share-writer.ts:7` deep-imports it. That intra-module deep import is legal — AGENTS.md §2 restricts only *cross*-module imports to the barrel — and `eslint.config.mjs`'s `boundaries/elements` pattern `src/modules/*/domain` covers `share/domain` exactly as it would `mdmax/domain`, so nothing is broken; only the plan is out of date.",
        "fix": "Replace §11.10's \"**Where it lives.**\" paragraph with: \"**Where it lives — LANDED 2026-08-01 in `4f97129`, at a different path than planned.** The function shipped as `src/modules/share/domain/splice-frontmatter.ts` (164 lines), consumed by `share-writer.ts:7` via an intra-module deep import. **Do not create `src/modules/mdmax/domain/splice-frontmatter.ts` — that would be a second implementation.** The architecture argument is unchanged: `src/modules/share/domain` matches `boundaries/elements`' `src/modules/*/domain` pattern, so under `eslint.config.mjs:109-111` it may import only `domain`/`shared-domain` and is impossible to contaminate with I/O (the shipped file imports nothing at all); `share-writer.ts` is `infrastructure`, allowed to import `domain` per `eslint.config.mjs:123-125`; and the deep path is legal because AGENTS.md §2 constrains only *cross*-module imports. If a second consumer outside `share/` ever appears, promote the file to its own module then — not before.\" Change §3.1.6's `src/modules/mdmax/splice.ts` to the same path at both lines 2062 and 2113. Rewrite §11.9's disjointness sentence as: \"step 2 touches `src/modules/share/{domain,infrastructure}`; step 1 touches `src/modules/auth`, `src/config`, `src/container`, `src/app/(auth)`. Still no shared file.\" Rewrite the Tuesday 09:00–13:00 row as: \"**Step 2 is LANDED — verify, do not rebuild.** Run `npx vitest run test/share/frontmatter-splice.test.ts` and confirm `splice: 907/907 identical (100.00%)`. Remaining step-2 work: the independent oracle (§11.4.3) and the corpus-size assertion.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.4.3 \"The gate\", the line \"**Committed RED first.** Against the current writer the test will fail at **17/736**.\"; also §11.16 Tuesday's proof column",
        "severity": "MATERIAL",
        "issue": "The RED baseline the plan tells the builder to expect cannot be produced by the test that now exists, because the two use different inverse operations. A builder who sees 33/907 where 17/736 was promised will believe the test is wrong.",
        "evidence": "The committed `test/share/frontmatter-splice.test.ts` `roundTrip()` (lines 148-156) inverts a publish by RESTORING the prior value when the key pre-existed — verbatim comment: \"publish, then invert. If the key pre-existed, the inverse is restore — not delete.\" 18 of the 907 files already carry `public_slug`, so the two methodologies must differ. I measured both against gray-matter: publish-then-always-delete gives **17 identical, 171 threw, 736 parsed → 17/736 = 2.31%** (exactly the plan's figure, so §7.2.5's number is right for its stated operation); publish-then-restore-prior gives **33 identical, 703 changed, 171 threw → 33/907 = 3.64%**. The live test prints `gray-matter (shipped): 33/907 identical (3.64%), 703 changed, 171 threw`.",
        "fix": "Replace the sentence with: \"**Committed RED first.** Two baselines exist for the shipped gray-matter writer and they differ only in how a publish is inverted. `test/share/frontmatter-splice.test.ts` restores the prior value when the key pre-existed (18 of 907 files already carry `public_slug`, and asserting byte-identity after *deleting* a line the original contained would be a bug in the test, not the writer), and it therefore scores gray-matter at **33/907 = 3.64%**. Inverting by unconditional delete scores **17/736 parsed = 2.31%**, which is the figure §7.2.5 quotes. **Expect 33/907 from the committed gate.** Both are correct for their stated operation; do not treat the difference as a defect. [both measured 2026-08-01]\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§11.2.2 \"The corpus derivation script\", the closing hedge",
        "severity": "MINOR",
        "issue": "The hedge is now retirable, and leaving it costs the plan credibility it has actually earned. It tells the reader that every `[measured]` tag in the document is \"one step weaker than it looks\" — but the recipe the plan itself recovered reproduces the hash exactly on first attempt. The remaining task (commit the script) is real; the epistemic discount is not.",
        "evidence": "Verbatim: \"Until it does, **`corpus_id` is a bare hash over a file list, not a pin**, and every `[measured]` tag in this document that cites it is one step weaker than it looks.\" I implemented the stated recipe — sha256 of newline-joined sorted `root/path:sha256`, no trailing newline — over `docs/engine/research/corpus-manifest.json` and got `sha256:3a010b1649899795d79274fc528dbece97fdabf4ff0f81cc02ab619c048c51a4`, matching `corpus_id` exactly on the first attempt. Four adversarial variants all fail to match (trailing newline: no; manifest order instead of sorted: no; paths only: no; hashes only: no), so the recipe is uniquely determined. I also re-read all 1,084 files from `~/Desktop/GitHub/{md,knowledge}` and this repo and re-hashed them: **1,084 present, 0 missing, 1,084/1,084 sha256 match**. `ls scripts/derive` -> No such file or directory; the manifest has no `derivation` key.",
        "fix": "Replace the hedge with: \"**The recipe is now independently verified.** sha256 of newline-joined sorted `root/path:sha256` with no trailing newline reproduces `sha256:3a010b16…` exactly on the first attempt, and four adversarial variants (trailing newline, manifest order, paths only, hashes only) all fail — so the recipe is uniquely determined, not merely consistent. Independently, all 1,084 files were re-read and re-hashed: **1,084 present, 0 missing, 1,084/1,084 sha256 match** [measured 2026-08-01]. **The `[measured]` tags in this document are NOT discounted.** What remains is bookkeeping: commit `scripts/derive/corpus-id.mjs` (`ls scripts/derive` -> absent) and add a `derivation` field to the manifest (absent) so the next reader does not have to re-derive it.\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§14 defect S13/G-series correction at line 15317 (the proposed replacement text for the \"19 preview tests\" claim)",
        "severity": "MINOR",
        "issue": "§14's own correction ships a number that will read as a test-case count but is a matching-LINE count, understating by 15. Since §14's corrections are the text that gets merged into the body, this propagates.",
        "evidence": "Proposed replacement, verbatim: \"**13** preview test files / **113** test cases … [measured 2026-08-01, `ls test/preview/` and `grep -c -E '\\\\b(it|test)\\\\('`]\". `grep -c` counts matching LINES, not matches. I ran the exact command: `grep -c -E '\\b(it|test)\\(' test/preview/*` sums to **113** — so §14 reported its command honestly. Counting occurrences instead gives **128**. The 13-file count is confirmed exactly. Separately confirmed §14's other figure at line 15141: `test/preview/html-policy-xss.test.ts` is **327 lines with 14 `it(` + 1 `test(`** — exact.",
        "fix": "Amend the §14 correction's replacement text to: \"**13** preview test files / **128** test cases, including `wikilink.test.ts`, `embeds.test.ts`, `callout.test.ts`, `html-policy-xss.test.ts` [measured 2026-08-01; note `grep -c -E '\\\\b(it|test)\\\\('` sums to **113** because it counts matching *lines*, not matches — 128 is the occurrence count].\""
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§2 line 3450, §11.4.4 line 11926, §11.16 line 12675, §12 line 13531, §14 G1 line 14506 — the WIKILINK_RE exponent",
        "severity": "COSMETIC",
        "issue": "The plan states the same measured exponent as both k=1.98 and k=2.00 in five places, with no note that they are the same measurement. Trivial, but the fix is one word and §11.4.4's row is what a regression gate will be written from.",
        "evidence": "§2 line 3450 and §14 G1 line 14506: \"k = 1.98\". §11.4.4 line 11926 and §11.16 line 12675: \"k=2.00\". §12 line 13531: \"k≈2.00\". I re-ran the scaling sweep on the shipped regex `/(!?)\\[\\[([^\\]]+)\\]\\]/g`: 588.6 / 2343.5 / 9339.4 / 37273.9 ms at 20,000 / 40,000 / 80,000 / 160,000 unclosed `[[` (node v24.6.0, darwin arm64), giving successive ratios 3.98 / 3.99 / 3.99 and **k = 1.993 / 1.995 / 1.997**. The plan's own run A (589.2 / 2315.9 / 9071.0 / 36865.0) is within 3% of mine at every point.",
        "fix": "Standardise on the measured value everywhere: \"**k = 1.99 (three successive doublings: 1.993 / 1.995 / 1.997) — strictly quadratic. 37,274 ms on 320 KB of `[[`**, independently re-measured 2026-08-01 on node v24.6.0/darwin-arm64 against the original run's 36,865 ms.\" Keep the regression gate at \"k <= 1.05 at 80,000 unclosed `[[`\" — it is correctly specified and the fix (bounded index scan) is correct."
      },
      {
        "cls": "Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.",
        "section": "§2 kill-list row \"`EditableTable` / `setTableCell`\" (line ~3492) and §10.7 row \"The `EditableTable` / `setTableCell` write path\" (line 11462)",
        "severity": "MINOR",
        "issue": "The delete instruction omits its collateral: a 14-case test file goes with it. A builder will either leave an orphaned failing test or delete coverage without noticing.",
        "evidence": "The dead-code claim itself is CONFIRMED — `grep -rn 'EditableTable' src/ | grep -v editable-table.tsx` returns empty, and `components.tsx:36` reads `_onEdit: ((newContent: string) => void) | undefined,` (Markdown.tsx:48 does pass `onEdit` into the factory, so the wiring dead-ends inside components.tsx exactly as described). But `setTableCell` has a live second consumer the plan does not mention: `test/preview/table-edit.test.ts` imports `{ findGfmTables, setTableCell }` and exercises `setTableCell` in 14 assertions (lines 78-142), including the `Alice|Bob` pipe case at line 110 that the plan cites as the corruption example. Deleting `src/modules/preview/presentation/table-edit.ts`'s `setTableCell` breaks that file at import time; `findGfmTables` in the same module has separate coverage that should survive.",
        "fix": "Append to both rows: \"**Collateral, name it in the commit:** `test/preview/table-edit.test.ts` imports `setTableCell` and asserts on it 14 times (lines 78-142, including the `Alice|Bob` pipe case this section quotes). Delete those 14 cases in the same commit and keep the `findGfmTables` cases — otherwise the file fails at import and the suite goes red for a reason unrelated to the change. Expect `npx vitest list --filesOnly | wc -l` to stay at 84 and the preview case count to drop from 128 by 14.\""
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§1.6 Orientation, tree/01-orientation.md:152-153 — AND §13 Appendix, tree/13-appendix.md:661",
        "severity": "MATERIAL",
        "issue": "The CommonMark spec commits-per-year table prints the WRONG pair for 2023/2024 as its primary figure, and §13 prints the wrong pair with no caveat at all. §2 has it right. The plan's own text calls this block \"the single most decisive fact in the area\", so the wrong number is in the most load-bearing table in the document.",
        "evidence": "§1 verbatim: \"  2023  13     (a second, independent count says 12 — see the contradiction note below)\" / \"  2024  11     (a second, independent count says 12 …)\". §13 E1 verbatim: \"Spec commits/yr **48/15/14/15/13/11/7/6** from 2019 to 2026\". §2 (tree/02-chronology.md:843) verbatim: \"2019=48, 2020=15, 2021=14, 2022=15, 2023=12, 2024=12, 2025=7, 2026=6\". I re-derived live, 2026-08-01, paginating api.github.com/repos/commonmark/commonmark-spec/commits?per_page=100&since=2019-01-01, counting commit.committer.date year on the default branch: {2019:48, 2020:15, 2021:14, 2022:15, 2023:12, 2024:12, 2025:7, 2026:6}, total 129. §2 reproduces exactly on all eight years. §1's 13/11 and §13's 13/11 are the outlier. (For completeness, restricting to path=spec.txt gives 39/10/9/10/9/6/5/1 — neither section's numbers, so that is not the source of the 13/11 either.)",
        "fix": "In §1 replace the two lines with: \"  2023  12\" and \"  2024  12\" (no parenthetical — the contradiction is resolved) and delete the \"see the contradiction note below\" pointer for these two rows. In §13 E1 replace \"Spec commits/yr **48/15/14/15/13/11/7/6** from 2019 to 2026\" with \"Spec commits/yr **48/15/14/15/12/12/7/6** from 2019 to 2026 (re-derived live 2026-08-01 against `api.github.com/repos/commonmark/commonmark-spec/commits`, default branch, all paths; total 129)\"."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§1.6 Orientation, tree/01-orientation.md:161-162 (the provenance sentence under the CommonMark evidence block)",
        "severity": "MATERIAL",
        "issue": "The provenance line cites the wrong repository's API endpoint for the CommonMark release dates, and the dates actually printed are not from any releases API — they are the `date:` key inside spec.txt. Two separate provenance errors in one sentence, attached to the block the plan calls its most decisive fact.",
        "evidence": "Verbatim: \"the example counts were produced by pulling `spec.txt` at tags 0.29 / 0.30 / 0.31.0 / 0.31.2 from `raw.githubusercontent.com` and counting 32-backtick example fences; the release dates came from `api.github.com/repos/github/cmark-gfm/releases`.\" That endpoint returns cmark-gfm's dates (0.29.0.gfm.13 → 2023-07-21T15:22:23Z, .12 → 2023-07-13, .11 → 2023-04-06) — it contains no CommonMark spec releases. Live, 2026-08-01: api.github.com/repos/commonmark/commonmark-spec/releases gives published_at 0.29 = 2019-04-08T17:09:27Z, 0.30 = 2021-06-20T15:59:45Z, 0.31.0 = 2024-01-28T07:57:28Z, 0.31.1 = 17:00:58Z, 0.31.2 = 19:26:15Z. The dates the block actually prints (2019-04-06, 2021-06-19) match neither; they match the spec.txt front-matter `date:` key, which I pulled at each tag: 0.29 → date: '2019-04-06', 0.30 → date: '2021-06-19', 0.31.2 → date: '2024-01-28'. The example counts 649/652/652 and master's 655 all reproduce exactly.",
        "fix": "Replace the clause after the semicolon with: \"; the version and date shown for each release are the `version:` and `date:` keys in that same `spec.txt` (0.29 → 2019-04-06, 0.30 → 2021-06-19, 0.31.2 → 2024-01-28), which differ by one to two days from the GitHub release `published_at` (2019-04-08, 2021-06-20, 2024-01-28) returned by `api.github.com/repos/commonmark/commonmark-spec/releases`. The `cmark-gfm` release dates in the block below came from `api.github.com/repos/github/cmark-gfm/releases`.\" This also removes the §1/§13 (2021-06-19) vs §2 (2021-06-20) inconsistency by naming which clock each date is on."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§8 competitor table, tree/08-market.md:92 (Obsidian row) — same claim in §7 tree/07-product.md:334 and §11 tree/11-execution.md:592-594",
        "severity": "MATERIAL",
        "issue": "Three sections assert that Obsidian *states* it has no comments. The cited primary source never mentions comments at all. Silence is being reported as a statement, and it is the single strongest-sounding sentence in the competitive argument.",
        "evidence": "§8 Obsidian row, review-loop column verbatim: \"✗ — states plainly it has none\". §7:334: \"Obsidian states it plainly.\" §11:593: \"Obsidian's own help file for shared vaults states plainly: no cursors, no fine-grained permissions, no comments.\" I fetched the cited file live: raw.githubusercontent.com/obsidianmd/obsidian-help/master/en/Obsidian%20Sync/Collaborate%20on%20a%20shared%20vault.md (2,544 bytes, full text read). `grep -oiE 'comment'` over it returns ZERO matches — the word does not occur. What the file does say, byte-exact and all confirmed: \"Fine-grained permissions are not supported yet. All collaborators receive the same permissions as the vault owner, with one exception: only the vault owner can invite collaborators.\" / \"You will not see the other user's cursor, and their edits will only appear once the changes are synced.\" / \"The maximum number of collaborators on a shared vault is 20 users.\" / \"All collaborators must have an active Sync subscription to access a shared vault.\" So §8's four quoted facts are correct and the verifier's \"four of four quotes byte-exact\" verdict stands — but none of those four is about comments, and the adjacent cell's stronger claim is not among them. (The plan also drops the file's stated exception on permissions.)",
        "fix": "§8 Obsidian review-loop cell → \"✗ — no comment feature exists, and the help file is silent on the subject (it documents no cursors, no fine-grained permissions, a 20-user cap; it never mentions comments)\". §7:334 → \"Obsidian's shared-vault help file documents no cursors and no fine-grained permissions and never mentions comments at all.\" §11:593 → \"Obsidian's own help file for shared vaults states plainly: no live co-editing, no other user's cursor, fine-grained permissions 'not supported yet', a 20-user cap — and says nothing about comments.\" Also add the omitted exception to §8's evidence cell: \"…all collaborators get the vault owner's permissions with one stated exception — only the owner can invite.\""
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§8 competitor table, tree/08-market.md:93 (Craft / Bear row) — and the §7 F7 monitoring recipe, tree/07-product.md:881",
        "severity": "MATERIAL",
        "issue": "The stated command does not reproduce the stated result. Run literally against the fetched HTML, Craft's homepage returns 32 matches for \"comment\", not 0. The plan's numbers are only reachable after stripping <script> — a step the plan never states. §7's F7 row makes this exact grep the quarterly competitive-monitoring procedure, so the first person to run it gets a false alarm.",
        "evidence": "§8 verbatim: \"Craft's homepage contains **0** occurrences of \\\"markdown\\\", \\\"comment\\\" or \\\"collaborat\\\". Bear's contains 7 \\\"markdown\\\", **0** \\\"comment\\\", **0** \\\"collaborat\\\".\" Fetched live 2026-08-01 with `curl -sL -A Mozilla/5.0`: https://www.craft.do/ (3,659,389 bytes) and https://craft.do/ (identical) → RAW counts markdown 86, comment 32, collaborat 175. After stripping <script>/<style> and tags, VISIBLE counts are markdown 0, comment 0, collaborat 0 — the plan's numbers. https://bear.app/ (216,996 bytes) → RAW markdown 20, comment 0, collaborat 0; VISIBLE markdown 7, comment 0, collaborat 0 — the plan's \"7\" reproduces exactly on visible text only. So the plan's methodology is sound and its conclusion is right; only the stated command is wrong. (moment.dev is unaffected: raw grep over its homepage, /pricing and /docs returns 0 for comment|suggest either way, and its pricing renders $0 / $30 / $6 as claimed.)",
        "fix": "§8 Craft/Bear evidence cell → \"Visible text only (`<script>`/`<style>` stripped before matching — a raw grep over Craft's HTML hits 32 'comment' inside the JS bundle): Craft's homepage contains **0** occurrences of 'markdown', 'comment' or 'collaborat'; Bear's contains 7 'markdown', **0** 'comment', **0** 'collaborat'. `[measured, re-verified 2026-08-01]`\". §7 F7 row → change \"fetch moment.dev/pricing and `grep -ioE 'comment|suggest'`\" to \"fetch moment.dev/pricing, strip `<script>`/`<style>`, then `grep -ioE 'comment|suggest'` over the visible text — a raw grep over a modern JS-bundled marketing site produces false positives\"."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§2 tree/02-chronology.md:618, §3 tree/03-capabilities.md:459, §7 tree/07-product.md:319, §11 tree/11-execution.md:596, §13 tree/13-appendix.md:680",
        "severity": "MATERIAL",
        "issue": "`inkeep/OpenKnowledge` is not a resolvable GitHub path — it 404s. The repository is `inkeep/open-knowledge`. Five sections use the wrong form; §7's own F7 monitoring row uses the correct hyphenated form, so the plan contradicts itself on the identifier it tells a future maintainer to curl quarterly.",
        "evidence": "Live 2026-08-01: `curl https://api.github.com/repos/inkeep/OpenKnowledge` → {\"message\":\"Not Found\"}. `curl https://api.github.com/repos/inkeep/open-knowledge` → 200, full_name inkeep/open-knowledge, 3,242 stars, GPL-3.0, created 2026-06-03T…, pushed 2026-08-01T…, description \"Beautiful, AI-native markdown IDE and LLM wiki\". §7:881 (F7) already writes it correctly: \"`api.github.com` stars and `pushed_at` for `inkeep/open-knowledge`\". Everything else about the repo checks out exactly: HN 381 points / 173 comments on 2026-06-25 (hn.algolia.com, Show HN title matches); the comments changeset merged 2026-07-30T23:39:04Z as \"Comments feature (#2944)\"; npm `@inkeep/open-knowledge` dist-tags latest 0.45.4 published 2026-07-31T22:56:04Z and beta 0.46.0-beta.32 published 2026-08-01T02:38:33.569Z (the plan's timestamp is exact to the second); packages/server/src/comments/anchor.ts is exactly 261 lines; packages/core/src/comments/passage-match.ts and packages/app/src/editor/clipboard/comment-scrub.ts both exist.",
        "fix": "Global find/replace `inkeep/OpenKnowledge` → `inkeep/open-knowledge` in §2:618, §3:459, §7:319, §11:596, §13:680. Where the product is named in prose rather than as a path, \"OpenKnowledge\" is fine — only the org/repo token must be hyphenated and lowercase."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§8 tree/08-market.md:92 (\"declined the teammate model in writing\"), §7 tree/07-product.md:319 and :334, §11 tree/11-execution.md:597-599, §13 tree/13-appendix.md:680",
        "severity": "MATERIAL",
        "issue": "\"Declined the teammate model in writing\" over-reads the cited changeset. Read in full, the same file describes a batching comment queue with cross-file spanning, pre-send review and Send-to-AI — i.e. a review loop, just human→agent rather than human→human. The quoted sentence is a scoping note about local storage, not a strategic refusal. The plan's own §7 risk row contradicts the framing.",
        "evidence": "I fetched raw.githubusercontent.com/inkeep/open-knowledge/main/.changeset/comments-v1.md in full. The plan's quote is byte-exact and correctly attributed: \"Comments are stored per-machine next to your content and are not committed — a comment here is a note to your own agent, not a message to a teammate.\" But that is the file's LAST line and it is a consequence of the storage decision stated in the same sentence. The rest of the file: \"Comments are the queue for your agent… nothing is sent until you decide the pass is done.\" / \"**The queue spans files.** Comment on three passages here, open another file, comment on two more, then send.\" / \"Uncheck anything you changed your mind about before it goes.\" / \"**Send now or send later.**\" Meanwhile §7:340 says of the same company: \"is one identity-and-sync layer away from the whole loop\" and §7:881 says \"only sync and identity are missing\". A deliberate refusal and a two-component gap are different claims about the same competitor.",
        "fix": "§8 cell → \"**Built the hard half; the shipped comment model is human→agent, not human→human**\". §7:334 → \"OpenKnowledge built the anchoring machinery and shipped comments as a local, uncommitted queue addressed to your own agent — a review loop, but not between two people.\" §13 F6 → replace \"**explicitly declined the teammate model**\" with \"**shipped comments as a per-machine, uncommitted agent queue — the teammate model is out of scope for now, not renounced**\". Keep the verbatim quote everywhere it appears, but stop attaching \"declined\" to it."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§6 tree/06-conventions.md:1010",
        "severity": "MATERIAL",
        "issue": "\"Whose only conformance tool\" is factually wrong — there are at least two, and §6 itself cites the second one ninety lines later. The differentiation argument in the next paragraph survives, but the superlative does not.",
        "evidence": "Verbatim: \"A GitHub search for *\\\"open knowledge format\\\"* returns 287 repositories, whose only conformance tool (`Sudhakaran88/okf-conformance`, 16 stars) checks structural conformance, not value drift.\" Live 2026-08-01: `openknowledge-sh/openknowledge` — 33 stars, Apache-2.0, created 2026-06-15, pushed 2026-07-31, description \"CLI tool for managing Open Knowledge Format (OKF) bundles\", homepage openknowledge.sh — ships a validator; its README says \"maintain, validate, and publish\", shows `okn validate Wiki`, and links Wiki/features/commands/validate.md. The plan already knows this repo: §6:1100-1104 discusses fetching `openknowledge-sh/openknowledge`'s README. Sudhakaran88/okf-conformance confirmed at 16 stars / 5 forks, but pushed only 2026-06-16. The repo-search count is now 289, not 287.",
        "fix": "Replace with: \"A GitHub search for *\\\"open knowledge format\\\"* returns 289 repositories (2026-08-01). Two conformance tools exist — `Sudhakaran88/okf-conformance` (16★, last pushed 2026-06-16) and the `okn validate` command in `openknowledge-sh/openknowledge` (33★, Apache-2.0, pushed 2026-07-31) — and **both check structural conformance against the spec, not value drift across a corpus**. Nothing infers the vocabulary from the corpus.\""
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§2 tree/02-chronology.md:893-894 and §13 tree/13-appendix.md:578 (A10), against §12 tree/12-risks.md:481-485",
        "severity": "MINOR",
        "issue": "Three sections disagree on the evidence tier of the Hypothes.is orphan figures, and the two that downgrade it are now stale. I read the source directly today; all four digits reproduce exactly. This matters because §12 uses it as the only external base rate for the design's headline failure mode.",
        "evidence": "§2:894 tags it \"`[secondary — arXiv 1512.06195, NOT read directly; verify before citing]`\". §13 A10 tags it \"`[secondary]`\" with \"verified: **NO**\". §12:481-485 tags it \"`[primary]`, read directly\". I fetched https://arxiv.org/abs/1512.06195 live (HTTP 200, 41,212 bytes) — title \"Quantifying Orphaned Annotations in Hypothes.is\", submitted 19 Dec 2015. Abstract verbatim: \"…annotation system (containing 20,953 highlighted text annotations). We found that about 22% of highlighted text annotations can no longer be attached to their live Web pages. Unfortunately, only about 12% of these annotations can be reattached using the holdings of current public web archives, leaving the remaining 88% of these annotations orphaned. For those annotations that are still attached, 53% are in danger of becoming orphans if the live Web page changes.\" That is 20,953 / 22% / 12% / 88% / 53% — §12's sentence is correct in every digit, including the 88% and the direction of the 12%.",
        "fix": "§2:894 → replace the tag with \"`[primary — arXiv 1512.06195, abstract read directly, re-verified live 2026-08-01: 20,953 / 22% / 12% recoverable / 88% orphaned / 53% at risk]`\" and delete \"verify before citing\". §13 A10 → change tier to `[primary]` and the verified column to \"**YES** — re-fetched from arxiv.org 2026-08-01, all figures reproduce\". Leave §12 as written."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§3.3.8 build hazard, tree/03-capabilities.md:675-679; and §1.6 tree/01-orientation.md:147",
        "severity": "MINOR",
        "issue": "The cmark-gfm build hazard is real and the remedy is right, but upstream has since fixed it on master (unreleased), and §1's \"recent commits\" note is 7 months out of date on a repo that F-ERA-2 exists to monitor.",
        "evidence": "§3 verbatim: \"`cmark-gfm 0.29.0.gfm.13`'s `CMakeLists.txt` declares `cmake_minimum_required` below 3.5 and modern CMake refuses… It needs `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`.\" Confirmed live: raw.githubusercontent.com/github/cmark-gfm/0.29.0.gfm.13/CMakeLists.txt line 1 = `cmake_minimum_required(VERSION 3.0)`; local `cmake --version` = 4.3.2, which removes <3.5 compatibility. But master line 1 = `cmake_minimum_required(VERSION 3.5)`. There have been exactly TWO commits to cmark-gfm since the 2023-07-21 release: \"Update cmake minimum requirement\" (2025-12-19) and \"Merge pull request #383 from kevinbackhouse/cmake-version\" (2026-07-13). §1:147 names only the 2025-12-19 one. Repo pushed_at 2026-07-13T14:36:08Z, archived false, open_issues_count 135 (§1's figure, confirmed), master still PROJECT_VERSION_MINOR 29 / PROJECT_VERSION_GFM 13 — so the CommonMark-0.29 pin is intact and the freeze thesis holds.",
        "fix": "§3 → append: \"Master has since fixed this (`cmake_minimum_required(VERSION 3.5)`, PR #383, merged 2026-07-13) but has cut no release, so the pinned tag still needs the flag. Pin the recipe as: tag `0.29.0.gfm.13` + `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`, verified against CMake 4.3.2.\" §1:147 → \"Recent commits are build-system housekeeping only — two since the 2023-07-21 release: 'Update cmake minimum requirement' (2025-12-19) and its merge, PR #383 (2026-07-13). Master still declares PROJECT_VERSION 0.29.0.gfm.13.\" §1 F-ERA-2 → add \"monitor commits as well as releases; the repo is maintained but not shipping.\""
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "Cross-cutting: every point-in-time external figure. §2:839/847, §3:1345/1359/1484, §7:319, §8:104-106/906, §11:596/653, §12:147/583/584, §13:643/661/680",
        "severity": "MINOR",
        "issue": "Thirteen live external figures have moved since the plan was written. Every one moved in the direction that STRENGTHENS the plan's argument, so no conclusion changes — but several are quoted bare, without the as-of date that §8 and §13 do give, and a reader who re-runs the curl will find none of them match.",
        "evidence": "All re-fetched 2026-08-01. npm last-month: llm-code-format 387 → **359**; repomix 327,543 → **328,087**; ratio 846:1 → **913.9:1**. npm last-week: @inkeep/open-knowledge 14,790 → **17,305** (I confirmed the plan's 14,790 is the exact sum of the 2026-07-24..07-30 daily buckets, which §8:106 states correctly; §2:618 and §11:596 quote it bare). markdownlint 2.89M → **2,908,292**/wk (npm `created` 2015-03-15 → 11y4m confirmed). Stars: OpenKnowledge 3,239 → **3,242**; repomix 27,551 → **27,561**; Aider 47,848 → **47,861**; Outline 39,932 → **39,945**; okf-gem 115 → **116**; okf-skills **206** and okf-conformance **16** unchanged. djot: remark-parse 183,264,086 / @djot/djot 3,630 = **50,486×**, not 52,610×. GitHub repo search \"Open Knowledge Format\" 287 → **289**. GitHub code search filename:AGENTS.md 175,424 → **174,880** (stable across three samples 5s apart, incomplete_results false — a real move, not sampling noise). Obsidian forum t/81878: 9,010 → **9,015** views (3 posts, 1 like, created 2024-05-15 all exact; 26 months confirmed); t/26329: 13,451 → **13,547** views (58 posts, 256 likes exact).",
        "fix": "Add \"(2026-08-01)\" to each bare quotation, and update the four whose drift exceeds 1%: §2:839 djot \"**52,610× behind remark-parse**\" → \"**~50,000× behind remark-parse** (183.3M vs 3.6K npm downloads/month, 2026-08-01)\"; §2:839 and §2:166 \"**175,424 files on GitHub**\" → \"**174,880 files on GitHub** `[primary — api.github.com/search/code?q=filename:AGENTS.md, authenticated, three stable samples 2026-08-01]`\" (this also upgrades it off `[secondary]`); the 846:1 ratio everywhere it appears → \"**846:1 as measured 2026-08-01-eve; 914:1 the following morning — quote it as roughly three orders of magnitude, not a fixed ratio**\"; §11:596 and §2:618 → add \"(week 2026-07-24..07-30; 17,305 for 07-25..07-31)\" after 14,790."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§13 tree/13-appendix.md:643 (D3) and §12 tree/12-risks.md:583 — the repomix #226 \"revealed demand\" evidence",
        "severity": "MINOR",
        "issue": "The issue's zero reactions are quoted, but its six comments are not. \"Nobody wanted it\" is being carried by a metric that suppresses the one signal pointing the other way.",
        "evidence": "§13 D3 verbatim: \"repomix issue #226 (unpack): **0 reactions after 19 months**\". Live api.github.com/repos/yamadashy/repomix/issues/226: title \"Support for unpack\", state **open**, created_at 2024-12-29T17:48:56Z, reactions.total_count **0** (all eight buckets zero) — the plan's claim is exactly right — but comments = **6**. 2024-12-29 → 2026-08-01 is 19 months, confirmed.",
        "fix": "§13 D3 → \"repomix issue #226 ('Support for unpack'): **0 reactions in 19 months**, though it drew 6 comments and is still open\". §12:583 → same addition. The 846:1 download ratio remains the load-bearing evidence; the issue is corroboration, not the argument."
      },
      {
        "cls": "External facts — spot-check the outside world",
        "section": "§1.2 tree/01-orientation.md:44-48, §8 tree/08-market.md:617-619, §13 tree/13-appendix.md:661-662, §10 tree/10-engine-spec.md:1208 — the freeze thesis and the spec suite",
        "severity": "COSMETIC",
        "issue": "No defect — recording the positive result so the build does not re-litigate it. The entire CommonMark/cmark-gfm freeze argument reproduces exactly against live sources, including the digits nobody had re-derived.",
        "evidence": "Live 2026-08-01. commonmark/commonmark-spec releases: 0.31.0 2024-01-28T07:57:28Z, 0.31.1 2024-01-28T17:00:58Z, 0.31.2 2024-01-28T19:26:15Z — three releases, one day, none since. §1's \"last three releases all landed on the same day, 2024-01-28\" confirmed. spec.txt 32-backtick example fence counts at each tag: 0.29 = **649**, 0.30 = **652**, 0.31.2 = **652**, master = **655** with version still '0.31.2' — §13 E1's \"+3 unreleased\" confirmed, and 2024-01-28 → 2026-08-01 is 2 years 6 months, so \"2.5 years\" is exact. cmark-gfm: last release 0.29.0.gfm.13 on 2023-07-21T15:22:23Z, master CMakeLists still PROJECT_VERSION_MAJOR 0 / MINOR 29 / PATCH 0 / GFM 13 — the CommonMark-0.29 pin holds; §13 E2 confirmed. commonmark-spec has no CONTRIBUTING.md, no GOVERNANCE.md and no CODE_OF_CONDUCT.md on master (all HTTP 404) — §2:843 confirmed; 128 open issues; 5,127 stars. §10's \"CommonMark 0.31.2 spec suite, 652 examples\" is the correct count for the released 0.31.2 — but the suite it would vendor from master carries 655.",
        "fix": "No change required to the freeze argument. One addition to §10:1208 so the gate is unambiguous about which artifact it pins: change \"CommonMark **0.31.2** spec suite, 652 examples\" to \"CommonMark **0.31.2** spec suite, **652** examples — pin the `0.31.2` tag, not `master`, whose `spec.txt` carries **655** while still declaring `version: '0.31.2'`; a gate that vendors master will assert the wrong denominator.\""
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11 Execution — the whole section, measured against §10 Engine spec. §11.17 'What this section does not cover'",
        "severity": "BLOCKING",
        "issue": "§10 specifies nine DECIDED engine components and §11 schedules none of them — and §11.17's explicit not-covered list does not mention them either. A builder who executes §11 literally builds zero of the engine specification, and the plan never says so.",
        "evidence": "I grepped the entire §11 (docs/mdmax/tree/11-execution.md, 1250 lines) for every §10 component name. Result, all zero hits: `OffsetMap` 0, `normalize/1` 0, `slug/1` 0, `shape gate` 0, `gate()` 0, `worker` 0, `hostile` 0, `lenient` 0, `WIKILINK_IN_FLOW_SEQ` 0, `LivePreview` 0, `Satinath` 0, `branded` 0, `surrogate` 0. §11's only offset-adjacent item is the imported AIOS 'UTF-8 integrity' gate at §11.12.3, which is about artifacts leaving a writer, not about §10.2's OffsetMap. Meanwhile §10.10.2 ranks these 'by when they bite': row 1 the offset model, 'Must land BEFORE the splice writer's second caller, because retrofitting branded types after four call sites is a refactor rather than a decision'; row 6 LivePreview, 'bites TODAY, on a real file already in the founders' vault: 19-31 seconds'; row 3 the YAML value slot, 'Must ship in the same PR as item 2'. §11.17 lists as not-covered only: pricing mechanics, marketing, legal, hiring, AIOS, mobile-native wrappers, importers, search at scale, product-plan Phases 2/4/5. None of §10 is on that list.",
        "fix": "Add a new subsection §11.4a immediately after step 2, titled 'The engine prerequisites — what §10 obliges and when', with this table, and add a row to §11.1's sequence table (`| 2a | Engine prerequisites | Sagnik | 2.5 d | nothing | 2, 6 | the four gates below |`):\n\n| # | §10 item | when it must land | effort §10 states | gate |\n|---|---|---|---|---|\n| 2a-1 | `offsets.ts` + branded `U16Offset`/`ByteOffset`/`GraphemeIndex` + the `no-restricted-syntax` lint rule (§10.2.2–10.2.4) | BEFORE the splice writer acquires a second caller | half a day | `fromByte(toByte(u))===u` at 100 sampled offsets on all 1,084 files; `assertBoundary` throws on a synthetic mid-surrogate offset |\n| 2a-2 | `normalize.ts` + `slug.ts` with `NORMALIZE_VERSION`/`SLUG_VERSION` and the 200-case golden file (§10.3.2, §10.4) | BEFORE the first anchor is persisted (rule P7) | one afternoon | CI fails if any golden output changes without a VERSION bump |\n| 2a-3 | the lenient frontmatter pre-pass + the `WIKILINK_IN_FLOW_SEQ` refusal (§10.6.5 rules 2 and 3) | SAME PR as step 2, per §10.10.2 row 3 | included in step 2 | the 170 unparseable files become readable; the 96 nested-array files raise a defect instead of indexing `[[\"A\"]]` |\n| 2a-4 | `gate()` + the `RefusalReason` enum + the `worker_threads` pool at `maxOldGenerationSizeMb: 32` (§10.5.5, §10.5.6) | BEFORE `LivePreview` is left on the main thread another week | not estimated in §10 — estimate it | `md/_Archive/Satinath Content JSON (raw source).md` opens in Live mode without freezing the tab |\n\nThen amend §11.17's first sentence to read: '**Not covered here.** The §10 engine prerequisites are scheduled in §11.4a and nowhere else in this section; everything else in §10 is deliberately deferred until step 6. Pricing mechanics and billing integration…'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.4.3 'The gate' and §3.1.10 'The gate, and it must go RED first'",
        "severity": "BLOCKING",
        "issue": "The flagship gate silently shrinks to 23 of 907 files on any machine that is not Sagnik's laptop, and still passes. The plan specifies the gate's denominator in prose but never obliges the test to assert it, so the gate cannot detect its own precondition failing.",
        "evidence": "§11.4.3 states the gate as 'Given every one of the 907 frontmatter-bearing files in corpus_id sha256:3a010b16… publish(file) → unpublish(file) must be BYTE-IDENTICAL to the original — 907/907.' The shipped implementation `test/share/frontmatter-splice.test.ts:100-106` resolves roots as `md: path.join(os.homedir(),\"Desktop/GitHub/md\"), knowledge: path.join(os.homedir(),\"Desktop/GitHub/knowledge\"), frontmatter: process.cwd()` and `loadCorpus()` line 137 does `if (!base || !fs.existsSync(base)) continue;` — a silent skip per missing root. The assertions are `it.skipIf(corpus.length === 0)` with `expect(s.identical).toBe(s.total)`. I measured the denominator both ways with a standalone node script replicating `loadCorpus`: **all roots present: 907; frontmatter-root only: 23.** `process.cwd()` always exists, so `corpus.length` is never 0 and `skipIf` never fires — the gate runs green over 23 files (2.5% of the stated denominator) with no warning printed. `md` and `knowledge` are private vaults (§13.1.3: 'md -> /Users/sagnikmitra/Desktop/GitHub/md (github.com/sagnikmitra, private vault)'), and §13.1.3 already concedes 'This mapping is not in the manifest, which is a defect'.",
        "fix": "Add to §11.4.3, immediately after the fenced gate block: '**The gate must assert its own denominator.** The corpus lives in two private vaults that no second machine has. The test therefore MUST (a) resolve root paths from an env var `MDMAX_CORPUS_ROOT_<NAME>` with no `homedir()` default, (b) `expect(corpus.length).toBe(907)` and FAIL — never `skipIf`, never `continue` — when a root is missing, with the message `corpus incomplete: root <name> not found at <path>; this gate is unrunnable, not passing`, and (c) print `corpus_id`, the resolved root paths and the per-root file counts on every run. Measured 2026-08-01: with both vaults present the gate sees 907 files; with only the `frontmatter` root it sees **23** and still passes. A gate that green-lights on 2.5% of its stated population is the false-green class Learned Rule #67 exists to kill.' Add the same paragraph to §3.1.10. Add a row to §11.16 Monday 11:00–12:00: 'Publish a redacted public corpus — the 56 `frontmatter`-root files plus a synthetic reproduction of each of the 170 unparseable shapes — so the gate is runnable by a second person and by CI.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§10.8 'Differential testing' (tiers A/B/C 'blocking, every PR'), §11.12.3–11.12.4, §8's moat argument",
        "severity": "BLOCKING",
        "issue": "The plan mandates PR-blocking CI in at least six places and names a continuously-running public CI as the only uncopiable asset — and this repository has no CI at all. No step in §11 creates one, and §11.17 does not list it as out of scope.",
        "evidence": "`ls -la .github/` → 'No such file or directory'; no `.gitlab-ci.yml`, no CI config anywhere outside `node_modules` (`find . -maxdepth 2 -name '*.y*ml'` returns only `polyg.yml`, an untracked scratch file). Against that: §10.8's table has three rows reading 'blocking, every PR … build fails'; §10.2.4 item 3 'A CI assertion'; §10.3.2 item 4 'A CI assertion makes (a) impossible to forget'; §11.4.4 'Ships as: three commits plus an `eslint-plugin-redos` run in CI'; §11.16 Friday 09:00–11:00 repeats it. And PLAN.md:8711 makes CI the moat: '| **Reputation: \"has never corrupted a byte of your file\"** | cannot be cloned; must be earned | A public, continuously-running byte-fidelity CI over a **public** corpus, green for a long time | … | **The only asset a weekend cannot copy** |'. §11.11.2 says merges to `main` are auto-deployed by Vercel, so today the only thing between a splice bug and production is a human running `npm run verify` locally.",
        "fix": "Add to §11.1's sequence table, as step 0b: '| **0b** | **CI — the thing every gate below assumes** | Sagnik | 0.5 d | nothing | 2, 4, 6 | a pull request with a deliberately reintroduced `matter.stringify` is REJECTED by GitHub before a human looks at it |'. Add §11.2.6: '**There is no CI in this repository.** `.github/` does not exist `[measured, 2026-08-01]`. Every \"blocking, every PR\" row in §10.8, every \"CI assertion\" in §10.2–§10.3, and the byte-fidelity-CI moat in §8 currently has no mechanism. Ships as: `.github/workflows/verify.yml` running `npm ci && npm run verify` on pull_request and on push to main, plus branch protection on `main` requiring that check. The corpus gate joins it the day the public corpus of §11.4.3 exists. Effort: half a day. Until it lands, \"blocking, every PR\" means \"Sagnik remembered\", and that must be stated wherever the phrase appears.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.3.3 task 1d, cross-checked against §7.5 N2 and `src/modules/auth/infrastructure/auth-options.ts:29`",
        "severity": "BLOCKING",
        "issue": "Task 1d — 'replace one server token with the signed-in user's OAuth token' — is impossible with the OAuth scope the app requests. The plan never states the scope change, never decides between OAuth-scope-escalation and a GitHub App, and §7 and §11 give opposite answers on which one is the design.",
        "evidence": "`src/modules/auth/infrastructure/auth-options.ts:29` reads `authorization: { params: { scope: \"read:user\" } }` `[measured, read live]`. `read:user` grants profile access only — it cannot read or write repository contents, which is why `src/shared/infrastructure/github/client.ts:30` and `github-writer.ts:33` both use the server-side `GITHUB_REPO_TOKEN`. §11.3.3 budgets 1d at '2–3 d' as `src/config/env.ts` + `client.ts` + `github-writer.ts` edits, and names no scope, no consent-screen change and no GitHub App registration. §11.3.5 calls a GitHub App 'an acceptable interim'. §7.5 N2 says the opposite and warns against exactly this estimate: 'Missing: a **GitHub App installation** per user … a GitHub App installation flow is a real piece of work with a review step on GitHub's side and an unavoidable second consent screen. It is not two days. Budget it as its own deliverable, not as a sub-task of N1.' The plan therefore contains a 2–3 day sub-task whose own product section says it is not a sub-task, resting on a credential the app has never requested.",
        "fix": "Replace §11.3.3's row 1d with two rows and add the decision to §11.16 Monday:\n\n| 1d-i | **DECIDE the credential model, and record it as D11.** Two options, both with a consent-screen cost: (a) escalate the OAuth scope from `read:user` to `repo` — one line in `auth-options.ts:29`, but `repo` is full read/write on every private repo the user owns and the demand corpus's pain 9 population will read it that way; (b) a GitHub App with `contents:write` on selected repositories — narrower grant, but requires an app registration, a callback, an installation flow and a second consent screen. **Lead time on (b) is external and is not measured anywhere in this plan.** | `docs/mdmax/PLAN.md` §1.5 (new row D11) | 0.5 d, Monday |\n| 1d-ii | Implement the chosen model: `auth-options.ts:29` scope or the App installation flow, then drop `GITHUB_REPO_TOKEN` from `client.ts:30` and `github-writer.ts:33` | as listed | 2–3 d if (a); **re-estimate if (b)** |\n\nAnd amend §11.3.5's second bullet: 'If it stalls, an acceptable interim is a GitHub App installation token per vault — but note that §7.5 N2 argues the App is the design and not the interim, and that the two sections have not been reconciled. Reconcile them in 1d-i before writing code.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.4 'Step 2 — THE FRONTMATTER SPLICE', §11.16 Tuesday, cross-checked against §10.10.2 row 2",
        "severity": "MATERIAL",
        "issue": "Step 2 is titled 'fix the two shipped destroyers' but only one destroyer is scheduled, and only one has a gate. The second — the properties panel — is the one still live after commit 4f97129, and it reaches disk.",
        "evidence": "§11.1 row 2: '**The frontmatter splice** — fix the two shipped destroyers'. §11.4.2 'What ships' names only `spliceFrontmatterValue` and `share-writer.ts`. §11.4.3's gate is publish-then-unpublish only — no assertion touches the properties-panel path. §11.16 Tuesday 09:00–13:00 reads 'src/modules/mdmax/domain/splice-frontmatter.ts + barrel; rewrite `share-writer.ts` to use it; `try/catch` around the read' — `frontmatter.ts` and `PropertiesPanel.tsx` appear nowhere in the first week. §10.10.2 row 2 says the opposite: '**`share-writer.ts:26` and `frontmatter.ts:47`** … replace both with `splice()` **in the same commit**'. Live state confirms the asymmetry: `share-writer.ts` now calls `spliceFrontmatterValue` (commit 4f97129), while `src/modules/preview/presentation/frontmatter.ts:47` still reads `const yaml = doc.toString()` and `PropertiesPanel.tsx:79` calls `commit() { onEdit(stringifyFrontmatterDoc(parsed.doc, parsed.body)) }` on every `setValue` / `renameKey` / `removeKey` `[measured, read live]`. §10.6.3 measures that path byte-identical on **119 of 907** blocks.",
        "fix": "Rename §11.4 to 'Step 2 — THE FRONTMATTER SPLICE, both callers' and add §11.4.2b: '**The second caller, which is the one still live.** `src/modules/preview/presentation/frontmatter.ts:47` (`doc.toString()`) feeds `PropertiesPanel.tsx:79`, which passes the regenerated document to `onEdit` → `EditorPane.handleEdit` → `setContent` + `saveDraft`. Every property edit therefore rewrites the whole block on disk; the `yaml` library's own no-edit round trip is byte-identical on **119 of 907** blocks. Ships as: `stringifyFrontmatterDoc` is deleted and `PropertiesPanel` calls `spliceFrontmatterValue` per changed key. A rename is a delete plus an insert, in that order, each a splice.' Add a second gate to §11.4.3: '**Gate 2 (properties panel).** For every one of the 907 frontmatter-bearing files, `setValue(k, currentValue(k))` — a no-op edit on each existing key in turn — is byte-identical, 907/907. Committed RED: against `stringifyFrontmatterDoc` it fails at 119/907. Its `--broke` reintroduces `doc.toString()`.' Add to §11.16 Tuesday: '| 16:00–18:00 | Step 2, second caller: delete `stringifyFrontmatterDoc`, rewrite `PropertiesPanel.commit()` as per-key splices | gate 2 moves from 119/907 toward 907/907 |' and move task 1b to Wednesday."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.11.3 'The disjoint-artifact map', §11.4.2, §3.1.7",
        "severity": "MATERIAL",
        "issue": "The plan gives two different paths for the splice writer, the code shipped it to a third, and under the shipped path the parallelism contract that makes steps 2 and 4 safe to run concurrently no longer holds.",
        "evidence": "§11.4.2: '**Where it lives.** New module `src/modules/mdmax/domain/splice-frontmatter.ts`, exported from a new barrel `src/modules/mdmax/index.ts`.' §3.1.7 of the same plan: 'It ships as `src/modules/mdmax/splice.ts` (~40 lines…)' — a different path for the same file, and neither section acknowledges the other. Live: `ls src/modules/mdmax/` → 'No such file or directory'; the function ships at `src/modules/share/domain/splice-frontmatter.ts` (7,236 bytes, 2026-08-01) and `share-writer.ts` imports `@/modules/share/domain/splice-frontmatter` `[measured]`. §11.11.3's map assigns step 2 'new `mdmax/domain`, `share/infrastructure/share-writer.ts`' and step 4 '`share` (all layers)' with 'must not touch `mdmax/domain`', under the standing rule 'One agent per module boundary, never two agents on the same file' and rule P3. With the splicer inside `share/domain`, step 4's ownership of 'share (all layers)' now includes the splice writer — the two steps are no longer disjoint, and §11.1's 'Run them in parallel … **DECIDED**' rests on a disjointness that no longer exists.",
        "fix": "Pick one path and propagate it. Recommended, because it preserves the map and §10's four other `src/modules/mdmax/*` files: add to §11.4.2 — '**Correction, 2026-08-01.** Commit `4f97129` shipped the splicer at `src/modules/share/domain/splice-frontmatter.ts`, not at the path below. Move it to `src/modules/mdmax/domain/splice-frontmatter.ts`, add the barrel `src/modules/mdmax/index.ts`, and change `share-writer.ts` to import `@/modules/mdmax`. This is a rename plus one import line and it must happen before step 4 starts, because §11.11.3 gives step 4 all of `share` and gives step 2 the splicer — leaving the splicer inside `share/domain` puts two steps on one file and breaks rule P3.' Delete the conflicting path in §3.1.7 and replace with 'It ships as `src/modules/mdmax/domain/splice-frontmatter.ts` (see §11.4.2 for the module rationale).' Add to §11.11.3's map, step 2 row: 'owns `mdmax/` **including the rename out of `share/domain`**'."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.4.3 'Shares no code with the writer is load-bearing', §11.12.4 'The verify ladder'",
        "severity": "MATERIAL",
        "issue": "The independent oracle the gate depends on does not run at all, has no npm script, and is not in the verify ladder — so nothing re-runs it and nothing notices it is broken.",
        "evidence": "§11.4.3: 'Checked by an oracle that shares NO code with the writer … Concretely: the oracle re-reads the file from disk and compares raw bytes with `Buffer.compare`; it must not import `spliceFrontmatterValue`, `yaml`, or `gray-matter`.' The shipped oracle is `scripts/fm-roundtrip-audit.mjs`. Running it: `node scripts/fm-roundtrip-audit.mjs splice` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '…/src/modules/share/domain/splice-frontmatter.mjs'` — line 21 imports a `.mjs` file that does not exist (the source is `.ts`). It fails at module load, so **all three** arms (`graymatter`, `yamldoc`, `splice`) are unrunnable `[measured, 2026-08-01]`. It also hardcodes `path.join(process.env.HOME, 'Desktop/GitHub/md')` at line 24, and it does import `gray-matter` and `yaml` at lines 18–19, which §11.4.3 forbids. `package.json` `scripts` (read in full) contains no entry for it, and `verify` is `typecheck && lint && test && build && arch`.",
        "fix": "Add to §11.4.3, after the 'shares no code' paragraph: '**The oracle is itself a deliverable with a gate.** It must (a) be invocable as `npm run audit:fm -- <impl>`, added to `package.json` `scripts` and appended to `verify` after `arch`; (b) load the writer through the same module resolution the app uses (`tsx scripts/fm-roundtrip-audit.mts`, or compile first) — as committed it imports `../src/modules/share/domain/splice-frontmatter.mjs`, a file that does not exist, so **all three arms fail at module load** `[measured, 2026-08-01]`; (c) take its corpus roots from env vars, not from `process.env.HOME`; and (d) hold to its own rule — it currently imports `gray-matter` and `yaml`, which the paragraph above forbids; those belong in the *comparison arms*, which must live in a separate file the splice arm does not import. Until `npm run audit:fm -- splice` prints a number, the 907/907 claim rests on the vitest suite alone, which is not an independent oracle.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.13 'The 30-day test', §11.14 K2, cross-checked against §11.16 and §12.5 row 10.8",
        "severity": "MATERIAL",
        "issue": "K2 makes 'Amit has not committed code by 2026-08-31' a company-deciding kill gate, and the plan schedules no work Amit can do in that window. The one decision that would fix it is dated 'this week' in §12 and appears nowhere in §11's calendar.",
        "evidence": "§11.14: '| **K2** | **Amit has not committed code by 2026-08-31** | §11.13, condition 2 | same |', and '**K1 and K2 are the ones that matter.** They fall due in thirty days.' §11.1's owner column: step 1 'Sagnik (or Amit, see §11.13)', steps 0/2/3/4 Sagnik or an agent, step 5 'one dispatched agent', step 6 'Sagnik + Amit' — and step 6 is 'Blocked by: 1, 2, 4' at 20–30 founder-days, so it cannot start inside August. §11.16, the plan's only calendar, assigns no row to Amit across all five days; Monday 09:00–09:30 is 'Read this section and §11.14 out loud, together' and nothing else is joint. §11.13's three choices — '(a) Amit owns step 1 end to end … (b) contracted out … (c) shelved' — are explicitly the *remedy after* the gate fires on 2026-08-31. §12.5 row 10.8 says the opposite about timing: '| **10.8** | Who owns item 1, and by what date | **OPEN, and this is the one that decides the company** | Amit owns it end to end with his own commits, **or** it is contracted out, **or** K7 fires now. **Pick this week** | nothing. This cannot be deferred without deferring the kill gates, which is N13 |'.",
        "fix": "Add to §11.16 Monday, as the first row, replacing the 09:00–09:30 entry: '| 09:00–10:00 | Read §11.14 out loud together, agree 2026-08-31, and **settle §12.5 row 10.8 in the same sitting: who owns step 1, Sagnik or Amit or a contractor.** Write the answer into §11.1's owner column and into `docs/mdmax/PLAN.md` under a heading dated 2026-08-03. | the owner column names a person, not a slash |'. Add to §11.13, after the two conditions: '**K2 is not self-executing.** As §11.1 is written, the only step naming Amit is step 6, which is blocked by steps 1, 2 and 4 and cannot begin before September. If K2 is to be a real test rather than a formality, one of step 1's sub-tasks — 1a (the RED test), 1f (the repo-picker route) or 1g (role enforcement) — must be assigned to Amit **on 2026-08-03**, with its own branch. §12.5 row 10.8 already says \"pick this week\"; this section is where that pick is recorded.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.14 kill gates K5, K5b and K8; §11.12.7 'The five conversations'",
        "severity": "MATERIAL",
        "issue": "Three kill gates are denominated in 30 probed users. The plan schedules five conversations and never designs, dates, resources or assigns the 30-user probe — so K5, K5b and K8 cannot be evaluated by anything the plan contains.",
        "evidence": "§11.14: 'K5 | Fewer than **5 of 30** probed users say they would install `cert` | §11.12.7, extended probe | cut `mdmax cert`' and 'K8 | `mdmax cert` promotes itself: **15 of 30** probed users name the certificate as their reason to pay | §11.12.7 | **the inverse kill**'. §11.12.7 is titled 'The five conversations — one week of calendar, zero engineering' and specifies exactly '**Five calls, one question each**'. The phrase 'extended probe' appears in §11.14 and nowhere else in §11 — no sample frame beyond the three recruiting sources listed for the five, no script, no date, no owner, no effort line. §11.7 makes step 5 'Blocked by: the five conversations (§11.12.7). They can tell you nobody wants it' — so a 2-day capped build is gated on a probe that resolves 5 of the 30 responses its own kill gate requires.",
        "fix": "Either renominate the gates to the sample that exists, or schedule the one that does not. Recommended — do both, since K5's 5/30 = 16.7% bar is not recoverable from n=5: replace the K5 and K8 rows with 'K5 | **Fewer than 1 of the 5 conversations (§11.12.7) names degradation or fidelity unprompted** | the five calls, scored against the 14 pain themes of §8.4 | do not start `mdmax cert`; re-gate on the 30-user probe below |' and 'K8 | **3 or more of the 5 name the certificate as a reason to pay** | same | the inverse kill — run the 30-user probe before promoting |'. Then add §11.12.7a: '**The 30-user probe, if K5 does not settle it.** Frame: the three enumerable sources in §11.12.7 (forum.obsidian.md/t/2049 posters, obsidian-git issues 340/396/558 commenters, authors of repos carrying an `llms.txt`). Instrument: one screen-recorded `npx mdmax cert` run on a repo the respondent chose, then one question — \"would you install this?\" — recorded verbatim. Owner: Sagnik. Effort: 5 calendar days, roughly 6 hours of calls. **Date: it does not start until step 1 is green, and K5/K5b/K8 are formally UNEVALUABLE until it does — say so rather than reporting them as untripped.**'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§10.6.5 rule 2 and rule 3; §10.10.2 row 3; §11.4 and §11.16 Tuesday",
        "severity": "MATERIAL",
        "issue": "The lenient frontmatter pre-pass and the WIKILINK_IN_FLOW_SEQ refusal — the only things that actually fix the 170 loud and 96 silent broken files — are never scheduled, and the one thing that is scheduled (a try/catch) does not fix either class.",
        "evidence": "§10.6.5 rule 2: '**READS USE A LENIENT PRE-PASS.** `src/modules/mdmax/frontmatter-lenient.ts` runs before the YAML parser and quotes two things: bare `[[…]]` runs, and plain scalars opening with a YAML reserved indicator (`@`, backtick).' Rule 3: 'a **`WIKILINK_IN_FLOW_SEQ`** defect … 96 files hit this today.' §10.10.2 row 3: 'Must ship in the **same PR** as item 2 or the 170-file case stays broken.' Grepping §11 for `lenient` → 0 hits; for `WIKILINK_IN_FLOW_SEQ` → 0 hits. What §11.16 Tuesday actually schedules is '`try/catch` around the read so the 171 unparseable files stop 502-ing' — which converts a 502 into a silent skip; those files remain unreadable, unindexed and unsearchable, and the 96 silently-corrupted files (§10.6.2: 'parses to a nested array, links destroyed … **no. Nothing to catch**') are not addressed at all. §10.6.2's own total: '266 of 1,084 files — 24.54% of the corpus'.",
        "fix": "Add to §11.4.2, as a third 'What ships' item: '**3. The read path, same PR (§10.10.2 row 3).** `src/modules/mdmax/domain/frontmatter-lenient.ts` — quote bare `[[…]]` runs and reserved-indicator scalars (`@`, backtick) on a *copy* fed to the parser, zero bytes changed in any user file — plus a `WIKILINK_IN_FLOW_SEQ` defect emitted whenever a value parses to an array of depth ≥ 2 whose leaves look like wikilink targets. Gate: of the 907 frontmatter-bearing files, **907 parse** after the pre-pass (up from 737) and **96** raise `WIKILINK_IN_FLOW_SEQ` instead of indexing `[[\"A\"]]`. A `try/catch` alone converts the 502 into a silent skip and fixes neither class.' Add to §11.16 Tuesday 09:00–13:00 the words 'plus `frontmatter-lenient.ts` and the `WIKILINK_IN_FLOW_SEQ` refusal', and raise §11.1's step-2 effort from 0.5 d to 1.5 d."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§10.3.2 'The specification' versus §7.5 N4 'One correctness gate that is easy to miss and is load-bearing'",
        "severity": "MATERIAL",
        "issue": "The plan gives two incompatible specifications of `normalize()` and never says which governs. Rule P7 makes the choice irreversible the moment the first anchor is persisted, so a builder implementing either one silently commits the project to a full 384-configuration re-sweep plus a migration if the other was right.",
        "evidence": "§10.3.2, marked **DECIDED**: '`src/modules/mdmax/normalize.ts` exports exactly this and nothing else' — `.normalize(\"NFC\").replace(/\\s+/gu, \" \").trim().toLowerCase()`, four steps, no HTML-comment handling — followed by rule 3: 'Changing `normalize()` in any way … requires **all three** of: (a) bump `NORMALIZE_VERSION`; (b) re-run the 384-configuration sweep and republish the correct / false / refusal triple …; (c) ship a migration that re-resolves and re-stamps every stored anchor.' §7.5 N4 of the same plan: '**One correctness gate that is easy to miss and is load-bearing.** `normalize()` must strip HTML comments before hashing a block … The plan's `normalize()` is currently \"NFC + collapse whitespace + lowercase\". … a 0.27% silent identity corruption is not acceptable in the anchoring layer.' §14.1 touches the 0.27% figure only to correct its corpus_id, and reaches no ruling on the function. Note the N4 rationale is 'the sentinel write would otherwise change the identity of the block it annotates' — and D2 forbids any sentinel write into the file, which makes the rationale moot but leaves user-authored HTML comments (911 of 337,410 blocks) inside the hash input either way.",
        "fix": "Add to §10.3.2, immediately after the code block: '**A conflict inside this plan, ruled here.** §7.5 N4 states that `normalize()` \"must strip HTML comments before hashing a block\". **It must not, and D2 is the reason:** N4's stated rationale is that a sentinel write would change the identity of the block it annotates, and under D2 no sentinel is ever written into the file. User-authored HTML comments are ordinary content and belong in the hash. **The four steps above are `mdmax/normalize@1` and are frozen.** If the ruling is ever reversed, rule 3 applies in full — new VERSION, a re-run 384-configuration sweep, and a migration over every stored anchor — which is why this is settled before the first anchor is persisted and not after.' And amend §7.5 N4 to read: 'One correctness gate that was proposed and **declined** — see §10.3.2: `normalize()` does not strip HTML comments, because D2 means no sentinel is ever written into the file.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11 (whole section), §7.5 N10 'Notifications', §11.17",
        "severity": "MATERIAL",
        "issue": "Notifications — which §7 calls a precondition for the review loop being a review loop — appear nowhere in the execution plan, and are not on §11.17's list of what the execution plan does not cover.",
        "evidence": "§7.5 N10: '**Status: MISSING, and unnamed anywhere in the record.** A review loop where the author never learns a comment arrived is not a review loop. Email on comment, on reply, on @mention, on suggestion. **OPEN** — it appears in no plan document, no research area, and no roadmap. Flagging it here is the first time.' §7.5 N12's dependency tree puts N10 directly under N4 (comments). Grepping §11 (docs/mdmax/tree/11-execution.md): `notification` → 0 hits, case-insensitive. §11.8's 'Ships as' for step 6 names the `comments` collection, the three-tier resolver, the orphan metric and the D2 gate — no delivery mechanism. §11.17's not-covered list is: pricing mechanics, marketing, legal, hiring, AIOS, mobile-native wrappers, importers, search at scale, product-plan Phases 2/4/5 — notifications is not on it, so the plan neither schedules nor excludes it.",
        "fix": "Add to §11.8, after the 'Ships as' paragraph: '**And the delivery half, which §7.5 N10 flags as unnamed anywhere in the record.** A comment nobody is told about is a note to self. Ship, in the same step: email on comment, on reply, on @mention and on suggestion, with a per-thread digest and one unsubscribe link. It needs a transactional email provider — a vendor choice this plan has not made and which is a hard dependency of step 6, not a polish item. **Add ~3 founder-days to step 6's 20–30 and name the provider before step 4 ships**, because the share-link email is the same integration.' If it is instead deliberately deferred, add 'Notification delivery for the review loop (§7.5 N10)' to §11.17's not-covered list so a builder does not discover it in a demo."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11 (whole section) — the claim 'It is written for someone who has never opened this repository'",
        "severity": "MATERIAL",
        "issue": "The plan never says how to run the application. A builder who has never opened this repository cannot make step 1's RED test go green, because five required environment values and a Firebase project are undocumented anywhere in the plan.",
        "evidence": "§11's opening: 'It is written for someone who has never opened this repository.' §11.11.1 documents accounts and *push* credentials only ('A push failing with \"Repository not found\" almost always means the token was not sourced'). Reading `src/config/env.ts`, the values with no default and no `.optional()` are `APP_URL` (line 4), `AUTH_GITHUB_ID` (44), `AUTH_GITHUB_SECRET` (45), `AUTH_SECRET` (46), `GITHUB_REPO_TOKEN` (85) — plus seven `NEXT_PUBLIC_FIREBASE_*` values read literally at lines 210–216 `[measured]`. None of these names appears anywhere in `docs/mdmax/PLAN.md`; the grep for `env.example` returns 0 hits and the six `npm install` hits are all about a subagent incident, not setup. §11.16 Monday's first engineering row assumes a working `npx vitest`; Wednesday's 1c row assumes a live Firestore ('`firestore()` finally has callers').",
        "fix": "Add §11.11.0, before 'The repo and the accounts': '**Running it, from a cold clone.** `nvm use && npm ci`, then `cp .env.example .env.local` and fill five values with no default — `APP_URL`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, `AUTH_SECRET`, `GITHUB_REPO_TOKEN` (`src/config/env.ts:4,44,45,46,85`) — plus the seven `NEXT_PUBLIC_FIREBASE_*` values read at `src/config/env.ts:210-216`. `AUTH_GITHUB_ID`/`SECRET` come from a GitHub OAuth app whose callback is `<APP_URL>/api/auth/callback/github`; the Firebase values come from the `frontmatter-md` web app config under `studiozephyrus@gmail.com` (§11.11.1). `ALLOWED_GH_LOGIN` defaults to `sagnikmitra` — set it to your own login or step 1a's RED test fails for the wrong reason. `npm run dev` serves on 3000; `npm run verify` is the gate. **Step 1 also needs a second GitHub account to sign in as; get one before Monday.** The pinned corpus is NOT in this repository — see §11.4.3 for what that means for the gate.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.2.1 'The vitest exclude' — the acceptance gate",
        "severity": "MATERIAL",
        "issue": "Step 0's gate is pinned to an equality that is already false, and the plan's own §11.12 imports the discipline that forbids exactly this shape.",
        "evidence": "§11.2.1: '**Acceptance gate.** `npx vitest list --filesOnly | wc -l` returns **83**, and `npx vitest list --filesOnly | grep -c worktrees` returns **0**.' §11.1's step-0 row repeats it: 'returns **83**, not 247'. Measured live: `npx vitest list --filesOnly | wc -l` → **84**; `| grep -c worktrees` → **0** `[measured, 2026-08-01]`. The exclude has already landed (`vitest.config.ts` now sets `exclude: [\"**/node_modules/**\", \"**/dist/**\", \"**/.claude/**\"]`), and 83 became 84 the moment `test/share/frontmatter-splice.test.ts` was added — i.e. the gate went red for adding a test. §11.11.4 item 3 and §11.12 explicitly import the opposite discipline ('a harness that reports false FAILures trains you to ignore it'), and the plan quotes the equality-versus-floor correction elsewhere.",
        "fix": "Replace the gate in §11.2.1 and in §11.1's step-0 row with: '**Acceptance gate.** `npx vitest list --filesOnly | grep -c worktrees` returns **0**, and `npx vitest list --filesOnly | wc -l` is **≥ 83 and < 100** — a floor plus a ceiling, never an equality, because an equality goes red the moment somebody adds a test and that trains you to ignore the harness. Measured 2026-08-01 after the exclude landed and the splice test was added: **84**.' Also note the shipped config differs from the prescription in §11.2.1's code block — it sets `**/dist/**` and omits `**/src-tauri/**` and `**/.next/**`; either update the code block to match what shipped or add the two missing globs, and say which."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.1 sequence table (step 2 effort) versus §11.16 Tuesday",
        "severity": "MINOR",
        "issue": "Step 2 is budgeted at half a founder-day in the sequence table and allocated seven hours — more than one founder-day — in the only calendar, before the two items this review adds to it.",
        "evidence": "§11.1: '| **2** | **The frontmatter splice** … | Sagnik or one dispatched agent | **0.5 d** |'. §11.16 Tuesday allocates 09:00–13:00 (step 2 proper), 13:00–15:00 (the oracle), 15:00–16:00 (register the gate and prove its `--broke` red) — seven hours against §11's own definition, '**Effort unit.** One **founder-day** = one person, six focused hours.' That is 1.17 founder-days before the properties-panel caller and the lenient pre-pass are added.",
        "fix": "Change §11.1's step-2 effort cell from `0.5 d` to `2 d` and add a footnote under the table: '**Step 2's estimate was raised from 0.5 d on 2026-08-01.** §11.16 Tuesday already allocates seven hours (1.17 founder-days at six hours/day) for the writer, the oracle and the gate alone; the properties-panel caller (§11.4.2b) and the lenient read pre-pass (§10.10.2 row 3, \"must ship in the same PR\") are additional. Where §11.1 and §11.16 disagree on an estimate, §11.16 governs, because it is the one with hours in it.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§7.5 N1–N12 versus §12.2 N1–N13; §11 references to both",
        "severity": "MINOR",
        "issue": "Two different N1–N13 namespaces run through the plan and §11 cites both without qualifying, so a reader following an N-reference lands in the wrong table.",
        "evidence": "§7.5 defines N1 'Identity and tenancy', N4 'The comment data model', N9 'Mobile layout', N10 'Notifications'. §12.2 defines N1 'The research never became code', N4 'Users rejected the refusal model', N9 'The certificate was perishable', N10 'Two decisions deleted the wedge'. §11 cites the §12.2 sense sometimes qualified and sometimes not: §11.8 '`[primary, premortem N4 countermeasure]`' (qualified), §11.7 '`[primary, premortem N9 countermeasure]`' (qualified), but §11.2.0 'Risk **N8 — the evidence base evaporated** is retired' and §11.5 'logs this as risk **N10 — two decisions deleted the wedge**' (unqualified), and §11.13 'ranks its top two risks — **N2** … and **N1**' (unqualified). A related smaller collision: §7.5 N2 cites '`FEATURE-GAP-REPORT.md` K5', which reads as kill gate K5 (`mdmax cert`, §11.14).",
        "fix": "Renumber one namespace. Cheapest: prefix the pre-mortem risks. In §12.2 change every `N<n>` heading to `R<n>` ('#### R1 — The research never became code'), and in §11 change 'risk N8' → 'risk R8', 'risk N10' → 'risk R10', '`premortem N4 countermeasure`' → '`premortem R4 countermeasure`', '`premortem N9 countermeasure`' → '`premortem R9 countermeasure`', and §11.13's 'N2 the editor floor never shipped and N1 the research never became code' → 'R2 … and R1 …'. Add a line to §13.7 Glossary: '**N-numbers vs R-numbers** — `N1…N12` are the product needs in §7.5. `R1…R13` are the pre-mortem risks in §12.2 (called N1–N13 in the research JSONs; renumbered here to stop the collision).' And change §7.5 N2's '`FEATURE-GAP-REPORT.md` K5' to '`FEATURE-GAP-REPORT.md` row K5 (not kill gate K5)'."
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§1.3 rule 2, pointing at §12.5",
        "severity": "MINOR",
        "issue": "A cross-reference names a decision id that does not exist and collides with one that does, sending a new reader to the wrong table.",
        "evidence": "§1.3: 'Decision **D10.4** in the **open-decisions section** settles this: \"Does MDMAX have a business model? No.\"' There is no section titled 'open decisions' in §13 (§13.6 is 'Contradictions inside the evidence base'); the row lives in **§12.5 'The open decisions'**, where it is numbered plainly `10.4`, alongside `W-A`, `W-B`, `W-C` and `10.1`–`10.8`. Meanwhile §1.5's table defines **D10** as 'The name is MDMAX', so a reader who follows a 'D10.4' reference into the D-namespace finds the naming decision. Confirmed by locating the row: `docs/mdmax/PLAN.md:13733`, enclosing heading `### 12.5 The open decisions` at line 13720.",
        "fix": "Change §1.3 rule 2 to read: '… no separate business model. **Open decision 10.4 in §12.5** settles this: *\"Does MDMAX have a business model? No.\"* (Note: §12.5's rows are numbered `W-A`–`W-C` and `10.1`–`10.8`; they are **not** in the `D1`–`D10` namespace of §1.5, where `D10` is the name decision.)' Add the same disambiguation as a lead-in sentence to §12.5: 'These rows are numbered `W-A`–`W-C` and `10.1`–`10.8`. They are open decisions, not the settled `D1`–`D10` of §1.5 — do not write them as `D10.4`.'"
      },
      {
        "cls": "What the plan does not say, and should",
        "section": "§11.12.3 'The gate registry', the third row of 'The first three gates to register'",
        "severity": "MINOR",
        "issue": "One of the three founding gates is specified with a `--broke` that names tools and a defect belonging to a different repository, so it cannot be implemented as written by someone working only in this one.",
        "evidence": "§11.12.3: '| **UTF-8 integrity** | no artifact leaves a writer undecodable | swapping `perl -CSD` for `head -c`, red on a Devanagari fixture (reproduction exists: **216 bytes in, 200 out, decode error at position 198**) |' followed by 'The UTF-8 gate is ~2 hours and can land immediately.' `perl -CSD` and `head -c` appear in no file under `src/`, `test/` or `scripts/` in this repository — the incident is the `sgnk-snapshot` truncation bug recorded as Learned Rule #68 in `~/.claude/CLAUDE.md`, outside this repo. §11's stated audience 'has never opened this repository', and therefore certainly has not read `~/.claude/CLAUDE.md`.",
        "fix": "Replace that row with a `--broke` expressed in this repository's own terms: '| **UTF-8 integrity** | no artifact leaves a writer undecodable | replacing the splice writer's `string.slice(start, end)` with a `Buffer.subarray(startByte, endByte).toString(\"utf8\")` whose `endByte` is one byte short of a boundary — red on a Devanagari fixture, 216 bytes in / 200 out, decode error at position 198 |' and add: '(The original formulation of this gate — `perl -CSD` versus `head -c` — comes from an incident in a different repository, recorded as Learned Rule #68 in the AIOS working notes at `~/.claude/CLAUDE.md`. It is restated above in this repository's terms because §11 is written for a reader who has not seen those notes. Every other numbered Learned Rule cited in this plan should carry the same one-line gloss or a pointer to §13.7.)'"
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Verify"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "verify:staleness",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "aa77e5dd522aab1ce",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379835,
      "queuedAt": 1785606379825,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785606998662,
      "tokens": 145773,
      "toolCalls": 34,
      "durationMs": 618827,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"Staleness — what commit 4f97129 invalidated\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"Cross-cutting — scripts/fm-roundtrip-audit.mjs, the oracle §3.1.10 and §7.5 F3 both depend on\",\"severity\":\"BLOCKING\",\"issue\":\"The independent oracle committed in 4f97129 cannot run. It imports a .mjs module that does not exist and was never committed, so the three headline fig…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "verify:cross-section",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "afce17a7c6a611ed8",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379836,
      "queuedAt": 1785606379825,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607179363,
      "tokens": 580979,
      "toolCalls": 51,
      "durationMs": 799526,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"cross-section-contradictions\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"§2 Chronology §2.1 (Round 3 blockquote) and §14 D6's correction text\",\"severity\":\"BLOCKING\",\"issue\":\"A `[primary]` verbatim blockquote of HANDOFF §0.7 does not match the source. The plan quotes the handoff as saying \\\"65 test files, 3,484 tests collected (66.4% of them abandoned worktrees) a…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "verify:numbers",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "ae18878063399eaf6",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379836,
      "queuedAt": 1785606379825,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607288159,
      "tokens": 203080,
      "toolCalls": 61,
      "durationMs": 908323,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"Number audit — re-derive every load-bearing figure\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"§7 F3 (L7981), §7 correction table (L7270), §7.4 table (L7375), §8 (L8392, L9007), §11.6 (L11867), §11.4.3 (L11901), §11 backlog (L12318), §11 falsifiers (L12726) — 9 occurrences across 3 sections\",\"severity\":\"MATERIAL\",\"issue\":\"The figure `17/736 = 2.31%` / `17/907 = 1…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "verify:execution",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "af065339abc42eae7",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379837,
      "queuedAt": 1785606379825,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607219506,
      "tokens": 227249,
      "toolCalls": 62,
      "durationMs": 839669,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"execution-plan-executability (§11 + §12): falsifiability of gates, defensibility of estimates, stated vs unstated blockers, ordering, and whether item 1 is really the critical path\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"§11.4 (all of it), §11.1 row 2, §11.11.3 disjoint-artifact map, §11.16 Tuesday 09:00–15:00\",\"severity\":\"BLOCKING\",\"issue\":\"Step 2 has alread…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:code-claims",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "aab100de32e94d3a2",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379837,
      "queuedAt": 1785606379825,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607625354,
      "tokens": 245750,
      "toolCalls": 92,
      "durationMs": 1245517,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"Code claims — every assertion in docs/mdmax/PLAN.md about src/, test/, scripts/, firestore.rules, eslint.config.mjs and package.json, verified by opening the named file at the named line at HEAD 4f97129; plus independent re-measurement of the two performance quadratics and the full 907-file frontmatter corpus argument.\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:external",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "aa47fa7c7bfcdc047",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379837,
      "queuedAt": 1785606379826,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607279145,
      "tokens": 163072,
      "toolCalls": 54,
      "durationMs": 899307,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"External facts — spot-check the outside world\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"§1.6 Orientation, tree/01-orientation.md:152-153 — AND §13 Appendix, tree/13-appendix.md:661\",\"severity\":\"MATERIAL\",\"issue\":\"The CommonMark spec commits-per-year table prints the WRONG pair for 2023/2024 as its primary figure, and §13 prints the wrong pair with no caveat at …"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:omissions",
      "phaseIndex": 1,
      "phaseTitle": "Verify",
      "agentId": "a35978e9c97bbda2d",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1785606379838,
      "queuedAt": 1785606379826,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DONE",
      "promptPreview": "YOU ARE VERIFYING A BUILD PLAN THAT IS ABOUT TO BE EXECUTED. Wrong numbers become wrong code.\n\nTHE PLAN: /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/PLAN.md — 15,504 lines, 14 sections, v2.1.0.\nAlso available as 14 separate files in /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdmax/tree/ (easier to read one at a time)\nplus /Users/sagnikmitra/Desktop/GitHub/frontmatter/docs/mdma…",
      "lastProgressAt": 1785607141633,
      "tokens": 280899,
      "toolCalls": 61,
      "durationMs": 761795,
      "resultPreview": "{\"intent\":\"DONE\",\"class\":\"What the plan does not say, and should\",\"verdict\":\"MATERIAL_TWEAKS\",\"findings\":[{\"section\":\"§11 Execution — the whole section, measured against §10 Engine spec. §11.17 'What this section does not cover'\",\"severity\":\"BLOCKING\",\"issue\":\"§10 specifies nine DECIDED engine components and §11 schedules none of them — and §11.17's explicit not-covered list does not mention them …"
    }
  ],
  "totalTokens": 1846802,
  "totalToolCalls": 415
}