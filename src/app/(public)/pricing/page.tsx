import { PendingLegalPage } from "../pending-legal-page";

export const metadata = { title: "Pricing · frontmatter" };

export default function PricingPage() {
  return (
    <PendingLegalPage
      title="Pricing"
      summary="Free and Pro, with every cap and every price stated including GST, readable without an account."
    />
  );
}
