---
id: S40
title: S40. Board
mode: reference
tier: canonical
status: specified
spec: boards/board
verified_against: cb7c16f
updated: 2026-09-19
owner: sagnik
---

# S40. Board

> **Where the ids come from.** Every `F`, `C`, `E`, `A`, `D` and `K` id below lives in its register:
> `10-FEATURE-REGISTER.md`, `14-COMPONENT-INVENTORY.md`, `17-ERROR-AND-REFUSAL-CATALOGUE.md`,
> `19-ACCEPTANCE-CRITERIA.md`, `56-OPEN-DECISIONS.md` and `16-COPY-DECK.md`. This file first carried
> placeholders; they were allocated on 19 September, and `tools/new-ids-allocation.md` maps each one.

> **What this screen draws.** The files, the writes and the refusals belong to
> `69-BOARDS-SPEC.md`. This file says only how a person meets them. Where the two disagree,
> `69` wins and this file is the one to fix.

## Purpose

See where every task stands, move a card when its work moves, and see what an agent wants to move
before it happens.

## The rules this screen carries

- **One card is one markdown file; the columns come from its `status` key.** The board file names
  the folder, the key, the column order and the limits (`69-BOARDS-SPEC.md` section 2).
- **A move is one line in one file**, entering the change queue (`69-BOARDS-SPEC.md` section 3.1).
- **A limit warns and never blocks.** `Doing 4 / 3` turns the count to the warning colour and nothing
  else (`69-BOARDS-SPEC.md` section 4.1).
- **An agent never moves a card.** Its move is drawn as a pending change: the card ghosted in the
  target column, while the real card stays where the file says it is (`69-BOARDS-SPEC.md` section 5).
- **An agent's move is never one click to accept.** As on S20, the diff comes first. The ghost card
  offers Show diff and Reject; Accept appears beside the diff (S20, "Agent item").
- **Filters are the viewer's own and never written** (`69-BOARDS-SPEC.md` section 4.2).

## Entry and exit

Direction | How | Route or control
In | A board file from the tree, `tasks/BOARD.md` in the drawing | `C083`
In | A notification that an agent proposed a move | the board opens with that card's panel open
In | S15's Make a board, once its proposal is accepted | `69-BOARDS-SPEC.md` section 6
Out | A card, then Open as a document | the card's file in the editor, S04
Out | A document link on a card face | that document, S04
Out | The MD side of the Board and MD switch | the board file's markdown, in place
Out | The pending-move pill | S20, filtered to Agents, for the whole queue
Out | Any tree row or tab | `C083`, `C078`

## Anatomy

The desktop frame is `s40-board`, drawn at `docs/mvp0/screens/gen.mjs:2352`. The card panel frame is
`s40-board-card`, at `docs/mvp0/screens/gen.mjs:2369`.

Region | Component | What it holds | Notes
Workspace header | `C076` | The usual header, with presence | `top()` at `docs/mvp0/screens/gen.mjs:891`
Tab strip | `C078` | The board file's tab | A board is a document, so it takes an ordinary tab
Left rail | `C083` | The tree, with the board file selected in its folder | `tree()` at `docs/mvp0/screens/gen.mjs:918`. Hidden in the card panel frame to give the columns room
Board toolbar | `C162` | The board's name, the card count and folder, the key it groups by, the pending-move pill, and the Board and MD switch | `BDBAR` at `docs/mvp0/screens/gen.mjs:2340`
Pending-move pill | `C163` | How many agent moves wait on this board | `.pill.ai`. Opens S20 filtered to Agents
Quick filter row | `C164` | Mine, Due this week, a label, text search, a line saying filters are never written, and the swimlanes toggle, drawn off | `QF` at `docs/mvp0/screens/gen.mjs:2341`. Swimlanes are not in v1 (`69-BOARDS-SPEC.md` section 8), so the toggle is drawn off and disabled
Board | `C101` | Five columns side by side | `boardCols()` at `docs/mvp0/screens/gen.mjs:2330`. `C101` is registered as a read-only kanban; see Register rows needed
Column | `C165` | The name, the count, the limit where one is set, a menu, the cards, and New card | `.bcol`. The column over its limit takes a faint warning border as well as the coloured count
Card | `C166` | Title, labels, due date, one document link, the assignee's avatar | `bcard()` at `docs/mvp0/screens/gen.mjs:2324`. **No body text on the face**, as `SCREEN-REFERENCES.md` section 3 records from Linear
Overdue due date | `C166` | The date in the warning colour with a crossed calendar | No other colour. `SCREEN-REFERENCES.md` section 3 declines Trello's yellow and pink
Pending move | `C167` | The card ghosted in its target column with a dashed accent border, who proposed it, Show diff and Reject | `.bcd.pend`. The real card stays in its own column
Card panel | `C168` | The card's path, its front matter as fields, the proposed change with its diff and three equal controls, the body with its acceptance list, and Open as a document | `.cardpanel` at `docs/mvp0/screens/gen.mjs:2375`
Proposed change | `C119`, `C120` | The agent's name, the move in words, the one-line diff, Accept, Reject and Reply | The same controls and weights as S20
Phone column switcher | `C169` | One pill per column with its count, the current one filled | `.colsw` at `docs/mvp0/screens/gen.mjs:2359`
Phone column | `C165` | One column as a list, its limit, a swipe hint, the cards, New card, and page dots | `.pcol`
Phone frame | `C086` | The header with the Board and MD switch | `phone()` at `docs/mvp0/screens/gen.mjs:1022`

**What the drawing holds, so nobody re-derives it.** Fifteen cards in `tasks/`: Backlog 3, To do 3,
Doing 4, Review 2, Done 3, which is 3 + 3 + 4 + 2 + 3 = 15. Doing has a limit of 3, so it reads
`4 / 3` in the warning colour. One agent, named by the person as Claude Code on my laptop, proposes
moving Deposit by UPI from Doing to Review. Its due date, 17 September, is before today's 19
September in the drawing, so it is marked overdue. Review's count stays 2 because the ghost is not
a card until accepted.

## Data contract

What | Direction | Port or store | Shape | When
Board file | read | `ProjectFiles.read(path)` | `{ board, cards, key, columns, limits }` from front matter | On open
Card files | read | `ProjectFiles.list(folder)` then `read` | Each card's front matter and first H1 | On open, and on every change in the folder
Board model | derived | `projectBoard` in the domain layer | Columns with ordered cards, an Other column, counts, limits | On every read. Stateless
Pending moves | read | `ChangeQueue.forFolder(folder)` | Items whose span is a card's `status` or `order` line | On open, and on every arrival
A move | write | `ProjectFiles.splice(path, keyLine, bytes)`, as a queue item | One `status` line in one card | On drop, or on Accept of a pending move
A reorder | write | the same splice | One `order` line in one card (`69-BOARDS-SPEC.md` section 3.2) | On drop within a column
A field edit | write | the same splice | One `assignee` or `due` line | On change in the panel
A new card | write | a `create` queue item | A new file with `status` set to that column | On New card
Filter state | read and write | per viewer, local only | `{ mine, dueThisWeek, label, text }` | Never sent, never written

**None of this is built.** At `cb7c16f` there is no board parser and no board view in `src/`. The
front matter writer exists, and `69-BOARDS-SPEC.md` section 3.1 names the defect in it that gates
every move: a `set` deletes the blank lines after the key. The queue has no `create` kind yet.

## Actions

Action | Trigger | What happens | Failure behaviour | Event id
Move a card | Drag to another column, or the card menu's Move to | One queue item setting `status`. A person's own move is applied on drop as any edit of theirs is | The `status` line cannot be addressed, so `E004` or `E009` refuses and the card stays | `board.card.moved`
Move past a limit | The same, into a full column | Written as any move. The count turns to the warning colour | none. A limit never blocks | `board.card.moved`
Reorder | Drag within a column | One `order` line in that card | No number fits between the neighbours, so each renumbered card is its own grouped item | `board.card.reordered`
Open a card | Click, or Enter on a focused card | The card panel opens beside the board | Card file missing, so `E105` and the card is marked stale | `board.card.opened`
Show diff | On a pending move | The panel opens on that card with the diff. Accept appears only there | Proposed bytes missing, so `E027` and the item closes as stale | `doc.change.diff.shown`
Accept a move | Accept in the panel | The `status` line is spliced, the card moves, the ghost goes | The line moved under it, so `E026` refuses and the item stays | `doc.change.accepted`
Reject a move | Reject on the ghost or in the panel | The item closes. The card file is untouched | none | `doc.change.rejected`
Reply | Reply in the panel | A comment against the card. The move stays waiting | none | `doc.change.replied`
Filter | A chip, or text in the search | Cards that do not match are hidden from this viewer | none | `board.filter.applied`
New card | New card at a column's foot | A new file with that column's `status`, as a `create` item | The folder is missing, so `E524` | `board.card.created`
Edit a field | A field in the panel | One line in the card | As for a move | `board.field.edited`
Show the source | The MD side of the switch | The board file as markdown | none | `board.source.toggled`

## States

State | When | What is shown | What the person can do
first-run | The first board on this account | One line: a card is a file, and a move changes one line in it | Read it, then work.
empty | A board whose folder holds no cards | The columns with New card in each | Make a card, or make a board from a blueprint on S15.
loading | The folder is being read | The toolbar and the column headers render. Cards fill in | Wait.
partial | Some card files cannot be parsed | Those files sit in the Other column with their reason | Open one and fix it.
offline | No connection, web only | The board is readable. Moves queue as S24 does | Keep working.
unauthorised | The viewer's role may not write | Cards cannot be dragged. Filters work. Accept is absent on pending moves, Reply stays | Read, filter, reply.
over-cap | The account is over its document cap, S33 | Moves and field edits work. New card is blocked, since it creates a document | Move cards, or free space.
AI-unavailable | n/a. Nothing on this screen calls a model. Make a board is on S15 | n/a | n/a.
conflict | Two moves of one card, or a move whose line changed under it | The pending move is marked stale with its original text. Serious cases go to S31 | Reject it, or resolve on S31.
error | The board file cannot be read as a board: a missing version, two columns with one name | The board file opens as a note with the reason, `E523` or `E522` | Fix it in MD.
degraded | `limits` and `columns` differ in length | The board shows no limits, and the problems panel names the key, `E521` | Fix the board file.

## Copy

Every string lives in `16-COPY-DECK.md`, section 14r.

id | what it carries
`K.s40.count` | The card count and folder in the toolbar.
`K.s40.groupedby` | The key the board groups by.
`K.s40.pending` | The pending-move pill, with its count.
`K.s40.limit` | A column's count against its limit, and its tooltip saying nothing is blocked.
`K.s40.newcard` | New card, at a column's foot.
`K.s40.newcard.in` | New card in a named column, on the phone.
`K.s40.mine` | The Mine chip.
`K.s40.duesoon` | The Due this week chip.
`K.s40.label` | The Label chip.
`K.s40.find` | The text search's placeholder.
`K.s40.filternote` | That filters are the viewer's own and the board file never changes.
`K.s40.swimlanes` | The swimlanes toggle's label.
`K.s40.proposedby` | The ghost card's line naming the agent.
`K.s40.showdiff` | Show diff on a ghost card. `K.s20.showdiff` may serve, if its owner agrees
`K.s40.moves` | The panel's line saying the move in words.
`K.s40.onlyaccept` | That nothing moves until a person accepts, and agent changes go one at a time.
`K.s40.overdue` | The overdue due date in the panel.
`K.s40.opendoc` | Open as a document.
`K.s40.swipe` | The phone's swipe hint naming the next column.
`K.s40.other` | The name of the Other column.
`K.s40.firstrun` | The first-run line.

**Reused unchanged.** `K.common.accept`, `K.common.reject`, `K.common.reply`, and `K.s20.who.agent`
for the agent's attribution.

## Keyboard and focus

key | does
Arrow keys | Move focus between cards and columns
Enter | Opens the focused card's panel
Escape | Closes the panel and returns focus to the card
Option or Alt Shift Up, Down | Moves the focused card to the top or bottom of its column, as `SCREEN-REFERENCES.md` section 3 records from Linear
Cmd or Ctrl Shift Left, Right | Moves the focused card one column. A modifier, because it writes
Tab, Shift Tab | Inside the panel, through the fields, then Show diff or Accept, Reject and Reply

**Focus on arrival is the first card of the first column.**

**Space does not peek.** Linear peeks at a card with Space (`SCREEN-REFERENCES.md` section 3), but
this workspace draws Space as the Ask AI key, so Enter opens the panel instead. Recorded for
`15-INTERACTION-AND-KEYBOARD.md`'s owner.

**Accept and Reject are never on a bare key**, as `15-INTERACTION-AND-KEYBOARD.md` requires and S20
follows. Every key above that writes carries a modifier.

## Responsive

- **One column at a time.** A row of pills switches columns, each with its count, and the current
  one filled. A swipe moves to the next column, and dots show where you are. `69-BOARDS-SPEC.md`
  section 4.4 records this as the proposal for the phone.
- **The limit stays in the column's heading**, in the warning colour, and in the pill.
- **A pending move shows inside the real card** with Show diff and Reject, since there is no second
  column on screen to ghost it into.
- **The card panel becomes a full screen** with the fields, the proposed change, three full-width
  equal controls, the acceptance list and Open as a document.
- **Moving a card** is the card menu's Move to, a bottom sheet of columns. Dragging between columns
  is not offered on the phone.
- **Not on the phone:** adding, renaming or reordering columns, and setting limits.

## Desktop differences

- **A watched folder makes the agent source real.** An agent that edits a card file on disk enters
  the queue as a pending move, the same as on S20.
- Everything else is identical.

## Acceptance

`19-ACCEPTANCE-CRITERIA.md` already has these for the queue, and they apply to a pending move.

id | criterion | test
`A112` | An agent-written change offers Show diff first and Reject, and no one-click Accept | `T177`
`A113` | A rejected item leaves the bytes equal to the pre-proposal bytes | `T178`

**In `19-ACCEPTANCE-CRITERIA.md` section 12d since 19 September**, with its test ids.

id | criterion | test
`A825` | A card move changes one key in one file, checked by byte diff | `T825`
`A827` | An agent's move leaves the card file unchanged until accepted | `T827`
`A828` | A move past the limit is written | `T828`
`A829` | A pending move does not change either column's count | `T829`
`A830` | Every filter leaves the board file and every card file byte-equal | `T830`
`A831` | A card face shows no body text | `T831`

## What this screen must never do

- **Never move a card for an agent.** A pending move changes nothing until a person accepts it.
- **Never accept an agent's move before its diff is shown.**
- **Never block a move because a column is over its limit.**
- **Never write a filter**, a column width or a collapsed column into any file.
- **Never rewrite a card file** to move it. One line changes.
- **Never refuse a status value the board does not know.** The card sits in Other.
- **Never show a sprint, a story point or a burndown** (`69-BOARDS-SPEC.md` section 9).
- **Never make Accept more prominent than Reject.**

## Open questions

id | question | why it is open
`D19` | The default columns for a new board | Drawn as Backlog, To do, Doing, Review, Done from the brief for this screen. `69-BOARDS-SPEC.md` section 2.1's example uses four, and its section 8 names `Todo, Doing, Done` as the bare bone. The founder picks
`D20` | Whether a person's own drag lands at once or waits in the queue like everything else | The plan sends every change by a person into the queue (plan v5, S20), but a board whose own drags wait for self-acceptance is a board nobody uses. Drawn as landing at once
`D21` | Whether the card panel edits the body, or only shows it and sends the person to the document | `69-BOARDS-SPEC.md` section 4.3 says both a document and a side panel. Drawn read-only for the body

**Resolutions.** None yet. All three are open in `56-OPEN-DECISIONS.md` section 5a.

## Register rows needed

**Allocated on 19 September**, see `tools/new-ids-allocation.md`. Every id below now has its row in
its register. The rows that widen or change an existing row, or ask another file's owner for
something, are still open for that owner.

Register | Row | For
`14-COMPONENT-INVENTORY.md` | `C162`, `C163`, `C164`, `C165`, `C166`, `C167`, `C168`, `C169` | Anatomy
`14-COMPONENT-INVENTORY.md` | `C101` split into the read-only view and the writable board, as `69-BOARDS-SPEC.md` section 10 asks | Anatomy
`14-COMPONENT-INVENTORY.md` | `C119` and `C120` screens widened to S40 | Anatomy
`16-COPY-DECK.md` | The twenty-one `K.s40.*` strings above, in section 14r | Copy
`17-ERROR-AND-REFUSAL-CATALOGUE.md` | `E521` to `E524` and `E566`, the board refusals `69-BOARDS-SPEC.md` section 10 asked for | States
`19-ACCEPTANCE-CRITERIA.md` | `A829`, `A830`, `A831`, plus the `69` rows cited above | Acceptance
`55-MEASUREMENT-AND-EVENTS.md` | `board.card.moved`, `board.card.reordered`, `board.card.opened`, `board.filter.applied`, `board.card.created`, `board.field.edited`, `board.source.toggled` | Actions
`15-INTERACTION-AND-KEYBOARD.md` | The board's keys, and Enter in place of Space to open a card | Keyboard
`56-OPEN-DECISIONS.md` | `D19`, `D20` and `D21`, in section 5a | Open questions
`11-SCREEN-INDEX.md` and `13-SCREEN-STATE-MATRIX.md` | An S40 row | This file
