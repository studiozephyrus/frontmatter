---
id: screens-S14-S26
title: Resolution log for screens S14 to S26
mode: reference
tier: canonical
status: living
verified_against: e0f6f89
updated: 2026-09-18
owner: sagnik
---

# Resolution log for screens S14 to S26

One row per open point in `docs/pack/12-screens/S14.md` to `S26.md`, written as each was closed.
The brief is `docs/pack/tools/RESOLVE-BRIEF.md`. Nothing here is `[Z]`: every resolution is a
proposal of 18 September for the founder's review.

file | id or line | the open point | what you did | basis | needs founder (yes/no)
---|---|---|---|---|---
S14 | Keyboard, UNVERIFIED | Single-letter keys may collide with the global map in file 15 | Checked and confirmed: no collision. Replaced the tag with the evidence and one condition (letter keys inactive in a text field) | `[O]` `grep -nE` over `15-INTERACTION-AND-KEYBOARD.md`: the only bare key is `/`, editor-only; section 2 is all modified chords | no
S14 | Keyboard, note | File 15 section 13 says no chord may be shown that it does not list, and it lists none of S14's keys | Not fixed here: file 15 is not mine. Its owner should add the screen-local keys of S14 to S26 | `docs/pack/15-INTERACTION-AND-KEYBOARD.md` section 13 | no
S14 | D30 | `F` ids for features collide with the plan's `F0NN` audit findings | Resolved (proposed): already settled by the feature register, features from `F101`, every lower number reserved. Flagged the stale `F001` example in file 65 section 3 for its owner | `[O]` `10-FEATURE-REGISTER.md` sections 0 and 4.1; `validate-pack.py` treats `F0NN` as findings | no
S14 | D31 | Rewrite cap counted per blueprint, per session, or per idea | Resolved (proposed): per blueprint run, as `rewritesUsed` on the idea record; only a new run, which spends a blueprint credit, resets it. Rejected per session. `21-DATA-MODEL.md` has no idea or blueprint collection: a data-model row is needed, for that file's owner | `[P]` file 53 section 3.1 "per blueprint"; file 28 section 4.1 | no
S14 | D32 | Where a background High research pass shows progress | Resolved (proposed): on the idea's own row in the Ideas section, with one toast on finish. Rejected a notification centre | `[Z]` Ideas as a workspace section, `SCREEN-CHANGES-2026-09-18.md` section 10.1 | no
S15 | Keyboard, UNVERIFIED | Collisions with file 15 not checked | Checked and confirmed: no collision; bare `C`, `L`, `H` are safe because the pane renders in Reading mode. Added the focus condition and the Escape order | `[O]` `grep -nE` over file 15; section 5 Escape stack | no
S15 | D33 | Is revocation per version or per kit | Resolved (proposed): per kit, every version stops, `E601` to old hashes. Rejected per-version revoke | `[P]` the meaning of revoke; A600 covers publish, not revoke | no
S15 | D34 | Is a published kit page indexed | Resolved (proposed): never, and `flag.publish.indexed` does not reach kits. Rejected letting the flag cover kits | `[O]` `28-CONFIGURATION-PANEL-SPEC.md` section 6 carries the flag for published pages only; S15 prints "not indexed" | no
S15 | D35 | May the desktop write the kit into a repository working tree | Resolved (proposed): only into a new or empty folder; any file at a kit path refuses, naming it. Rejected queue-routed overwrites | `[P]` file 15 section 9 rule 2, a drop that would overwrite refuses | no
S16 | Entry table, line 35 | A stray line holding only `x` broke the entry table | Fixed: removed the line | `[O]` `sed -n '33,40p' docs/pack/12-screens/S16.md` | no
S16 | Keyboard, UNVERIFIED | Collisions with file 15 not checked | Checked and confirmed: `V`, arrows and Escape are free on the graph. Named the live C1 conflict on `Cmd/Ctrl + G`, which this screen inherits | `[O]` `grep -nE` over file 15; file 15 section 4, C1 | no
S16 | D36 | The node budget before degrading to a subgraph | Resolved (proposed): defined as the count that keeps INP under 200 ms (`PB-06`), measured in the S16 batch; no count written because none was measured. Rejected the 4,600 figure | `[P]` `47-PERFORMANCE-BUDGET.md` `PB-06` | no
S16 | D37 | Replace the tag grouping with kit kinds, or coexist | Resolved (proposed): coexist; `kind` from the path for S16, tag groups on the whole-vault graph only. Finding: `groupForTags` hard-codes one vault's tags. Note for file 66's owner: its `open:` on the four kinds can take S16's `doc`, `spec`, `why`, `agent` | `[O]` `sed -n '41,70p' src/modules/graph/presentation/graph-data.ts`; `66-FORMAT-SPECIFICATIONS.md` graph.json | no
S17 | Keyboard, UNVERIFIED | Collisions with file 15 not checked | Checked; claim partly false. `Shift Cmd S` is Firefox's screenshot chord. Fixed: the web build ships no share chord until file 15 assigns one; the desktop may keep it. Other keys confirmed free. File 15 should record this as a conflict row | `[M]` `browser-sets.inc.xhtml` line 352 and `screenshots.ftl` in mozilla-firefox/firefox on GitHub, opened 18 Sep 2026 | no
S17 | D38 | The invite credit value, and its abuse | Needs founder for the number. Recommended: keep 5 as the panel default, add a monthly per-inviter ceiling. A panel row is needed in file 28 for that ceiling, for its owner | `[O]` `policy.invite.credits` at file 28 section 4.2; `[Z]` "number not final" | yes
S17 | D39 | Does Pro have a live-collaborator ceiling | Resolved (proposed): no ceiling by default, the stored row stays unlimited, the panel can set one. Rejected an invented number | `[O]` file 53 section 3.1; plan section 15, one Durable Object per live document; participant cost is `INFERENCE:` | no
S17 | D40 | May a link-shared document be indexed on request | Resolved (proposed): never; publishing is the way to be found. Rejected an index switch on links | `[P]` `flag.publish.indexed` covers published pages only, file 28 section 6 | no
S18 | Keyboard, UNVERIFIED | Collisions with file 15 not checked | Checked and confirmed: no editor and no window chord on a public route; Escape closes the topmost layer | `[O]` file 15 sections 2 and 5 | no
S18 | D44 row citation | The row cited `docs/mvp0/screens/gen.mjs:1338` for the footer, which has moved | Fixed to `:1595`, the footer carrying `frontmatter.in/p/zephyrus-booking-brief.md` | `[O]` `grep -n "twin" docs/mvp0/screens/gen.mjs` | no
S18 | Must-never line | "Never index a page the owner has not chosen to index, which is still an open decision" | Rewritten to "never against the setting in force when it was published", pointing at D42 | `[P]` file 28 section 6 | no
S18 | D41 | Which protocol scheme the desktop registers, and how the bar detects it | Resolved (proposed): `frontmatter://` via the Tauri `deep-link` plugin; detection by a local hint written by the app, never a probe. Rejected a timing probe | `[M]` https://v2.tauri.app/plugin/deep-linking/ opened 18 Sep 2026; `29-PLATFORM-AND-DESKTOP-SPEC.md` records the plugin absent | no
S18 | D42 | Are published pages indexed by default | Needs founder: a promise to users, and plan section 29 wants it answered before the pilot. Recommended: keep `flag.publish.indexed` true, say "findable" on S17's publish row. File 66 section 5.2 sends `noindex` on the twin and should follow the flag, for its owner | `[O]` file 28 section 6, default true | yes
S18 | D43 | What a single document's llms.txt contains | Resolved (proposed): already specified in file 66 section 5.4 | `[M]` https://llmstxt.org/index.md opened 18 Sep 2026 | no
S18 | D44 | Twin at `/<slug>.md` or a separate path | Resolved (proposed): `/<slug>.md`, per file 66 section 5.1. The generator still draws `/p/...` at `gen.mjs:1542` and `:1595`: a redraw is needed, for the generator's owner | `[O]` `grep` over `gen.mjs`; `src/proxy.ts:50` | no
S19 | Keyboard, UNVERIFIED | Collisions with file 15 not checked | Checked and confirmed: the screen adds no chord. Named the untested undo-in-session case, file 15 section 8 rule 5 and `D-KB-3` | `[O]` file 15 sections 5 and 8 | no
S19 | D45 | Does live editing ship at all | Resolved (proposed): built in its batch, shipped off behind `flag.collab.live`; turned on after the Zed Delta rebuttal and a red-proofed undo test. Rejected on-by-default | `[Z]` D02, everything built; `[O]` file 28 section 6, default false | no
S19 | D46 | How many collaborators Pro allows | Resolved (proposed): same as S17 `D39`, unlimited in the stored row, panel-settable | `[P]` S17 D39; file 53 section 3.1 | no
S19 | D47 | Which region a session object lives in | Resolved (proposed): the apac hint from the first participant, measured as `PB-11` before the flag turns on. Rejected an EU or US jurisdiction | `[O]` `47-PERFORMANCE-BUDGET.md` PB-11; plan section 15 | no
S20 | Anatomy, C082, UNVERIFIED | "First drawn on S10, so the id is S10's to assign" | Checked and confirmed resolved: `C082 FilterSegment` is registered as shared for S10, S11, S20. Also fixed the stale `gen.mjs:716` to `:841`. For file 14's owner: its variants column says two or three items, and S20 and S21 draw four; S21 is not in its screen list | `[O]` `grep -n C082 docs/pack/14-COMPONENT-INVENTORY.md`; `grep -n filterseg docs/mvp0/screens/gen.mjs` | no
S20 | Keyboard, UNVERIFIED | Bare `A`, `R`, `C`, `D`, `J`, `K` assumed free and safe | Checked; claim partly false. No collision, but file 15 section 1 rule 2 and section 13 forbid Accept and Reject on a bare key. Fixed: withdrew `A` and `R`; Accept and Reject by `Tab` then `Enter` or pointer | `[O]` file 15 sections 1 and 13 | no
S20 | Data contract, Actions, Copy | Counts `{ all, people, machine }`, "three-position control", `K.s20.filter.ai` as "AI and agents" | Fixed to four positions to match the anatomy and the decided filter. The copy deck still says "AI and agents" and has no Agents string: a copy-deck row is needed, for file 16's owner | `[O]` `16-COPY-DECK.md` `K.s20.filter.ai`; `REVIEW-COVERAGE-2026-09-18.md` section 6 | no
S20 | D48 | How an agent is named on its row | Resolved (proposed): a name typed by the person when the token is made; watched-folder writes name no product; mirror edits show provider and `externalAuthor`. `agentTokens` needs a name field: a data-model row is needed, for file 21's owner. Rejected showing the token id | `[O]` `21-DATA-MODEL.md` `agentTokens`; `67-SYNC-AND-CONFLICT.md` section 8.1 | no
S20 | D49 | Three filter positions or four | Resolved (proposed): four, as drawn; "other reviews" is Agents, which also holds mirror edits. Rejected a fifth position | `[O]` `REVIEW-COVERAGE-2026-09-18.md` section 6 rows 50 and 51; plan section 5 S20 | no
S20 | D50 | Queue items when the document is deleted or the project removed | Resolved (proposed): items travel with the document; frozen in trash, restored with it, purged with it including R2 bytes; the delete confirmation names the count. Rejected auto-reject on trash | `[M]` Firebase delete-data guide, opened 18 Sep 2026; `[O]` 30-day trash in `21-DATA-MODEL.md` | no
S21 | Anatomy | The People and AI filter the founders asked for on history was missing from the anatomy | Fixed: added the `C082` source filter row and its digit keys | `[Z]` plan section 5 S21; `[O]` `HIST_FILTER` at `docs/mvp0/screens/gen.mjs:1662` | no
S21 | Keyboard, UNVERIFIED | File 15 lists no history binding, so nothing collides | Checked and confirmed; added that Restore must never get a bare key | `[O]` file 15 sections 1, 5 and 8 | no
S21 | D51 | Commit-log history or the R2 version store in phase A | Resolved (proposed): R2 version store first; keep the shipped port and routes, change the store behind them. Rejected the commit log | `[Z]` D03, our copy canonical; `21-DATA-MODEL.md` versions | no
S21 | D52 | Prune to the head, or keep a daily version | Resolved (proposed): prune to the head, said on screen first, export offered. Rejected a daily keep, which makes the window unbounded | `[O]` `21-DATA-MODEL.md` version retention; plan section 18; the compounding cost quoted there from plan section 15 | no
S21 | D53 | Does a restore carry the old attribution | Resolved (proposed): attributed to the person who restored, with the restored version named; the old row keeps its mark. Rejected copying the old mark | `[P]` the version record fields in `21-DATA-MODEL.md`; `K.s21.restored` | no
S22 | Keyboard, UNVERIFIED | File 15 lists no import binding, so nothing collides | Checked and confirmed; noted file 15 section 9 agrees on folder drops and progress | `[O]` file 15 section 9 | no
S22 | D54 | Live folder in Chromium only, and what others are told | Resolved (proposed): as plan section 11, plus the desktop app; offered by feature detection; elsewhere the card says what it needs. Rejected hiding it | `[O]` plan section 11, "every browser imports, Chromium browsers can keep a live folder" | no
S22 | D55 | Notion through its API instead of the zip | Resolved (proposed): already in `50-ROADMAP.md` batch 11; arrives as an S23 connection feeding the queue. Its roadmap row has no feature id: a feature-register row is needed, for file 10's owner | `[O]` `50-ROADMAP.md` section 3.12 | no
S22 | D56 | A file that is neither text nor a known attachment | Resolved (proposed): stored as an upload, counted, held to the per-file cap, served only as a download. Rejected refusing unknown kinds | `[P]` the no-loss rule of this screen; `limits.uploads.file` in file 53 | no
S23 | Keyboard, UNVERIFIED | File 15 proposes a settings binding and nothing for this section | Checked and confirmed: `Cmd/Ctrl + ,` is `PROPOSED` for opening Settings and nothing is bound inside | `[O]` file 15 sections 2 and 5 | no
S23 | Data contract, Drive poll row | "Background, every five minutes" | Fixed to the watch channel plus a backstop poll at `DRIVE_POLL_INTERVAL`, pointing at D58 | `[O]` `67-SYNC-AND-CONFLICT.md` section 8.3 | no
S23 | D57 | Does the MCP server leave the Later column | Needs founder: it is D04 in file 56, still open, and it moves a phase. Recommended: move it to batch 4 as a read-and-propose server | `[O]` `56-OPEN-DECISIONS.md` D04; `50-ROADMAP.md`; the Z3 and Z4 research rows | answered 18 Sep `[Z]`, D04: yes, read and propose, scheduled in 50-ROADMAP.md.
S23 | D58 | Does the Drive poll stay at five minutes as users grow | Resolved (proposed): settled in file 67 section 8.3, watch channel plus a 60-minute backstop; card wording kept; `A126` tested with the channel live | `[O]` file 67 section 8.3 arithmetic, about 13,201 against about 102,564 people | no
S23 | D59 | Where a desktop watched folder appears | Resolved (proposed): a fourth card on this page, desktop only, with path, last send and a stop control. Rejected the Settings desktop section | `[O]` file 67 section 8.6, `source: "agent"` | no
S24 | Keyboard, UNVERIFIED | File 15 lists no binding for the offline state, so nothing collides | Checked; one condition added. File 15 section 5 gives Escape in the editor to three layers, so the card's Escape now acts only with focus on the card | `[O]` file 15 section 5 | no
S24 | D60 | What a Safari user is told near the seven-day eviction | Resolved (proposed): ask for persistence first; only if refused with unsynced work, one banner line, with no date. A copy-deck row is needed for that line, for file 16's owner. Rejected a computed deadline | `[M]` https://webkit.org/blog/14403/updates-to-storage-policy/ opened 18 Sep 2026, persistent mode excluded from eviction | no
S24 | D61 | Is sync on return page-driven everywhere | Resolved (proposed): already settled in file 67 section 4.2, page triggers plus Chromium Background Sync as an optimisation | `[O]` `67-SYNC-AND-CONFLICT.md` section 4.2 | no
S24 | D62 | Which local model, and how large | Resolved (proposed): premise partly false, plan section 14 names Ollama and four models from 2.0 GB to 5.2 GB. Default set by a measured eval before the desktop batch; offered on first use, size shown. Rejected bundling | `[O]` plan section 14 table; file 27 row 6 | no
S25 | Desktop differences, protocol row | "Registered, which is what S18's open-in bar detects" | Fixed to match S18 D41: `frontmatter://`, learned from a local hint, never a probe | `[P]` S18 D41 | no
S25 | D63 | Who signs Windows, and at what price | Needs founder: money, and D09 in file 56 is open. Recommended two written quotes in batch 1; Windows stays "coming" until signed. Price kept `UNVERIFIED:` with what would check it | `[O]` `56-OPEN-DECISIONS.md` D09; `50-ROADMAP.md` section 3.12 | yes
S25 | D64 | Desktop before or after sync | Resolved (proposed): after sync, as batch 8 depends on batch 5 and D09 recommends. Rejected desktop first | `[O]` `50-ROADMAP.md` section 3.9; file 56 D09 | no
S25 | D65 | Is a cloud document written to disk on the desktop | Resolved (proposed): yes, as a device copy in the app's data folder, never in a chosen folder; local folders are the real files. Rejected a folder mirror and cloud-only | `[Z]` D03; `[O]` file 67 section 8.6 watcher | no
S26 | Escape, entry, actions and keyboard rows | Escape "closes and keeps nothing", which discards typed text | Fixed: Escape keeps the text as an unsent draft for the next open. It contradicted file 15 section 13 and this screen's own never-lose rule | `[O]` `15-INTERACTION-AND-KEYBOARD.md` section 13, "Never make Escape discard typed text" | no
S26 | D66 | Is the global chord configurable, and what if it is taken | Resolved (proposed): configurable in S28's Shortcuts section; a taken default raises `E804` with a link there; never a silent substitute. Rejected a fixed chord | `[O]` file 15 section 1 rule 5 | no
S26 | D67 | Does iOS get a capture route | Resolved (proposed): in-app capture by long-press now; an Apple Shortcut with a scoped token after batch 10's agent tokens. Rejected a mail-to-inbox address | `[O]` this screen's entry table; `50-ROADMAP.md` section 3.11 | no
S26 | D68 | Where the inbox note lives with no notes project | Resolved (proposed): the first capture creates a Notes project and `inbox.md`, shown on the pill before saving; at the cap the capture is held locally. Rejected asking for a project | `[P]` this screen's never-ask-for-a-folder rule | no
S26 | Acceptance, `A652` row | The screen's summary read "Escape discards and saves nothing" | Fixed to the register's own wording, note bytes unchanged, plus the kept draft. The register row in file 19 already agrees and needs no change | `[O]` `grep -n A652 docs/pack/19-ACCEPTANCE-CRITERIA.md` | no
S15 to S26 | 51 prose citations "section N, at line NNNN" into `docs/mvp0/PRODUCT-PLAN.md` | The line halves predate the plan's 18 September edits and point at the wrong lines (for example section 29 now starts at line 1906, not 1815) | Fixed: kept the section, removed the line number, as file 65 section 4 and the validator ask. S14 and S16 had none | `[O]` `grep -n "^## 29" docs/mvp0/PRODUCT-PLAN.md`; counts per file: S15 1, S17 6, S18 2, S19 7, S20 4, S21 4, S22 5, S23 9, S24 7, S25 4, S26 2 | no
S14 to S26 | `docs/mvp0/screens/gen.mjs:NNN` citations | Many predate the v8 redraw and have drifted (`filterseg` was cited at 716 and is at 841) | Not fixed wholesale: only the ones touched here were corrected (S16 none, S18 `:1595`, S20 `:841`, S21 `:1662`). The rest still resolve to a line, so the validator passes, but may name the wrong one. A re-pin pass is needed | `[O]` `grep -n "function filterseg" docs/mvp0/screens/gen.mjs` | no
