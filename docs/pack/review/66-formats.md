---
id: 66-formats
title: Resolution log for 66, the format specifications
mode: reference
tier: canonical
status: living
verified_against: e0f6f89
updated: 2026-09-18
owner: sagnik
covers: [review-66-formats]
---

# Resolution log for 66, the format specifications

Every `open:` marker and `UNVERIFIED:` claim in `66-FORMAT-SPECIFICATIONS.md`, and how each was
treated on 18 September 2026 under `docs/pack/tools/RESOLVE-BRIEF.md`. One row per item, written as
each item was finished. Line numbers are the file's lines at `e0f6f89`, before this pass.

file | id or line | the open point | what you did | basis | needs founder (yes/no)
--- | --- | --- | --- | --- | ---
66 | line 23, the state legend. | `open:` defined as a state. | Replaced with two states, `resolved (proposed 18 Sep, founder review)` and `needs founder`, and a pointer to this log. | RESOLVE-BRIEF treatment 1. | no.
66 | section 0 row 1 (line 46). | `/<slug>` or `/p/<slug>` as the canonical URL. | Resolved (proposed): `/<slug>` is canonical, `/p/<slug>` stays a redirect for old links. Rejected: making `/p/<slug>` canonical, which would move every link S18 draws. | `docs/mvp0/SCREEN-CHANGES-2026-09-18.md` S18 "Fine as drawn" `[Z]`; `src/app/(public)/p/[slug]/page.tsx` already redirects. | no.
66 | section 0 row 5 (line 50). | Pandoc `:::` div for paragraph borders and shading against carrier invariant 6. | Resolved (proposed): the div is never written; borders and shading move to plan section 7's X class, and a callout is the offered substitute. Rejected: an exception to invariant 6. | `adr/ADR-0002-render-carrier.md` rejects `:::` as output; `specs/render/carrier.md` measured a `:::` container splitting silently. | no.
66 | section 0 row 9 (line 54). | JSON Canvas 1.0 (plan) against Excalidraw (S08) for the drawing. | Resolved (proposed): the stored file is Excalidraw's own `.excalidraw` JSON. The plan's JSON Canvas wording is a category error, since JSON Canvas nodes are text, file, link or group only. | `[M]` https://jsoncanvas.org/spec/1.0/ and https://raw.githubusercontent.com/excalidraw/excalidraw/master/dev-docs/docs/codebase/json-schema.mdx, opened 2026-09-18. | no.
66 | section 0 row 12 (line 57). | Which callout title form is written. | Resolved (proposed): the product writes `> [!KIND]` alone, then `> ### Title`; both forms are read. Rejected: the marker-line title S08 draws. | `[O]` GitHub's `POST /markdown` API, gfm mode, 2026-09-18 17:31 UTC: `> [!TIP] Deposits` came back a plain blockquote; the heading form came back a tip alert with an `h3`. | no.
66 | section 0 row 14 (line 59). | PDF by Paged.js and Pandoc, or headless Chromium. | Resolved (proposed): keep headless Chromium, the engine that ships; Paged.js may run inside it later for page setup. Rejected: Pandoc on the server, a second runtime and a GPL binary for one output. | `src/app/api/export/pdf/[...path]/route.ts` (`format: "A4"`); plan section 8 lists Pandoc as GPL. | no.
66 | section 2.2 (line 139). | Whether Doc mode writes subscript, given GFM's single-tilde strikethrough. | Resolved (proposed): Doc mode writes `<sub>` and `<sup>` raw HTML, never the Pandoc `~x~` or `^x^`. Rejected: disabling single-tilde strikethrough. | `[O]` GitHub `POST /markdown` gfm, 2026-09-18: `<sub>` renders, `H~2~O` becomes `<del>`; `src/modules/preview/presentation/markdown/html-policy.ts` lists `sub` and `sup`. | no.
66 | section 2.3 (line 150). | Whether the published page keeps `remark-breaks`. | Resolved (proposed): keep it, one pipeline for preview and page; agents read the twin's bytes. Rejected: CommonMark breaks on the published page only. | `src/app/(public)/[slug]/page.tsx` renders through `PublicNoteView`, which imports `Markdown` from `@/modules/preview`; the projection law in `CLAUDE.md`. | no.
66 | section 3.6 (line 309). | Ids for the two writer defects. | Resolved (proposed): no `E` id, since they are wrong writes, not refusals; both go to `44-TECH-DEBT-REGISTER.md` as `TD-` rows, with the fix stated. Rejected: allocating refusal ids. Both defects re-run at `e0f6f89` and still reproduce. | `[O]` `spliceFrontmatterValue` run under `node --experimental-strip-types`, 18 Sep: `a: 1` then a blank line lost the blank; `\n\nHello` became `\nHello` after the block. | no.
66 | section 3.9 table (line 346). | `page` and `margins` value shapes, in the reserved-keys table. | Resolved (proposed): both are scalar strings, specified under the table. See the next row. | the rule written for line 355. | no.
66 | section 3.9 (line 355). | The shapes of `page` and `margins`. | Resolved (proposed): `page` is a paper size plus optional `landscape`, default `A4`; `margins` is one to four CSS lengths, default `1.6cm`; a bad value falls back to the default without a rewrite. Rejected: a nested margins map. | `[O]` `node_modules/puppeteer-core/lib/types.d.ts` `PaperFormat`; `src/app/api/export/pdf/[...path]/route.ts` `format: "A4"`; `print-css.ts` `PRINT_PAGE_MARGIN = "1.6cm"`. | no.
66 | section 3.10 (line 360). | Whether to stamp `verified` at all (D06). | Needs founder. Recommendation written: stamp on accept, in phase D, as D06 recommends. Rejected: deciding it here, because D06 sits in the founder's list and was not among the four answered on 18 Sep. | `docs/pack/56-OPEN-DECISIONS.md` section 0 (D01, D02, D03, D10 answered) and section 3 D06. | answered 18 Sep `[Z]`, D06: stamp on accept.
66 | section 3.10 (line 364). | `UNVERIFIED:` Google's Open Knowledge Format page not opened. | Checked and confirmed, with one correction to our shape: OKF v0.2 does define `verified` as a list of `{by, at}`, but writes each entry as a one-line flow map, and `by` carries an actor prefix, `human:<id>`. The example and the append rule were changed to match. | `[M]` https://cloud.google.com/blog/products/data-analytics/okf-v0-2-adds-trust-signals, opened 2026-09-18 with `curl -sL --compressed`. | no.
66 | section 3.10 (line 383). | What `by` holds in the `verified` stamp. | Needs founder, recommendation written: `human:<handle>`. Rejected: an email (published with every page) and a uid (meaningless to a reader). A privacy promise to users, so the founder decides. | `[M]` OKF v0.2 page as above, the `human:<id>` tier rule; `12-screens/S30.md` portfolio `handle`. | yes.
66 | section 4.2 (line 463). | Which callout title form the product writes. | Resolved (proposed): marker alone, then `> ### Title`, always H3; both forms still read. Rejected: the marker-line title, which GitHub renders as a plain quote. S08 had no founder instruction on 18 Sep, so the carrier spec stands. | `specs/render/carrier.md` invariant 5; `[O]` GitHub `POST /markdown`, 2026-09-18; `docs/mvp0/SCREEN-CHANGES-2026-09-18.md` S08 "No instruction given". | no.
66 | section 4.2 (line 464). | `UNVERIFIED:` how GitHub renders marker-line text after `[!TIP]`. | Checked, and the claim resolved against the marker-line form: GitHub renders it as a plain blockquote. Also checked: lower-case `[!tip]` is an alert, and an unknown kind is a plain quote. Replaced with a four-row table of what GitHub returned. | `[O]` `curl -s -X POST https://api.github.com/markdown` with `mode: gfm`, 2026-09-18 17:31 UTC. | no.
66 | section 4.4 table (line 510). | The values of `kind`. | Resolved (proposed): `pie`, `bar`, `line`, per the next row. | the rule written for line 544. | no.
66 | section 4.4 (line 544). | Which chart kinds v1 draws (S08's D22). | Resolved (proposed): `pie`, `bar`, `line`, drawn by translating to Mermaid `pie` and `xychart-beta`, so no new dependency. Rejected: pie only, and a separate charting library. | `[O]` `ls node_modules/mermaid/dist/chunks/mermaid.core/` shows `pieDiagram-4H26LBE5.mjs` and `xychartDiagram-2RQKCTM6.mjs`; `grep -o` found `"bar"` and `"line"` in the latter. | no.
66 | section 4.5 (line 556). | `UNVERIFIED:` whether Excalidraw writes JSON Canvas. | Checked and false. Excalidraw saves its own `.excalidraw` JSON; JSON Canvas 1.0 has only text, file, link and group nodes and cannot hold strokes. The claim was replaced with both sources, and plan section 8's wording is flagged as wrong. | `[M]` https://jsoncanvas.org/spec/1.0/ and the Excalidraw json-schema page on raw.githubusercontent.com, both opened 2026-09-18. | no.
66 | section 4.5 (line 557). | The drawing block's key, storage and format (S08's D23). | Resolved (proposed): one key, `file`, a relative path ending `.excalidraw`; stored as an upload under `u/{vaultId}/{docId}/{uploadId}/{filename}`, a new upload per save; mirrored beside the note. Rejected: JSON Canvas and a separate drawing store. | `docs/pack/21-DATA-MODEL.md` section 21.8 upload key; `12-screens/S08.md` `AttachmentReader.get(id)`; the Excalidraw schema page, 2026-09-18. | no.
66 | section 4.6 (line 571). | Whether `fm-flow` exists at all. | Resolved (proposed): it does not; the plan's section 20 row is withdrawn and Flow view keeps headings as its carrier. Rejected: specifying it ahead of a screen. | `docs/mvp0/SCREEN-CHANGES-2026-09-18.md` S09 "Deferred" `[Z]`; section 4.6's own heading rule. | no.
66 | section 4.7 (line 589). | How the published page draws Mermaid without a third-party script. | Resolved (proposed): render the SVG at publish into the stored rendered page; keep the bundled dynamic import until then. Also corrected the premise: the published page already loads Mermaid first-party, only the PDF path uses the CDN. Rejected: the Mermaid runtime for every reader. | `[O]` `src/modules/preview/presentation/mermaid-block.tsx` line 25 `import("mermaid")`; `PublicNoteView.tsx` imports `Markdown`; `21-DATA-MODEL.md` `renderedKey`. | no.
66 | section 4.9 (line 615). | The AI mark's placement and how `-->` in an ask is escaped. | Resolved (proposed): after the span on the same line; `%`, `<`, `>` and line breaks percent-encoded; the full ask written, never truncated, since a length cap would be an invented number. Rejected: stripping `--`, and a block comment. | `[M]` https://html.spec.whatwg.org/multipage/syntax.html, opened 2026-09-18, the comment text restrictions. | no.
66 | section 4.10 (line 622). | Silent or shown normalisation of pasted `:::`. | Resolved (proposed): convert a paste as it lands, with a notice and an undo; never rewrite a directive already on disk; offer that conversion as an S10 fix through the change queue. Rejected: silent normalisation on save. | `specs/render/carrier.md` "Open" and invariant 6; `adr/ADR-0006-projection-law-and-splice-only.md`; the change queue rule in `CLAUDE.md`. | no.
66 | section 4.10 (line 625). | Doc mode drops paragraph borders and shading, or the carrier spec gains an exception. | Resolved (proposed): Doc mode drops the div; the feature moves to plan section 7's X class; a callout is the substitute. Rejected: an exception to invariant 6. | `adr/ADR-0002-render-carrier.md` alternatives table; `specs/render/carrier.md` behaviour table; plan section 7 lists "Cell background, borders, padding" as X. | no.
66 | section 5.1 (line 651). | Whether `/p/<slug>.md` is also served. | Resolved (proposed): not served, a plain 404. No link to it can exist, because the twin was never built, and a redirect would break the twin's never-redirected rule. Rejected: a legacy copy. | section 5.1's own route table (the twin is `specified, not built`); `docs/mvp0/SCREEN-CHANGES-2026-09-18.md` section 10.2. | no.
66 | section 5.2 (line 676). | A field on `shares/{slug}` naming the source version. | Resolved (proposed): `sourceHash`, 64-char lower-case hex SHA-256 of the published bytes, set at publish; the twin reads `v/{vaultId}/{docId}/{sourceHash}`. The owner of 21 copies the row in. Rejected: deriving it from the rendered page. | `docs/pack/21-DATA-MODEL.md` section 21.4 fields and section 21.8 version key. | no.
66 | section 5.2 (line 678). | Whether the twin serves an inline AI mark. | Resolved (proposed): yes, byte-exact, because the twin is the published bytes and its `ETag` is their hash; inline marking is off by default. Rejected: stripping on serve. | section 5.2's byte-exact rule; plan section 20 (strip on export only). | no.
66 | section 5.4 (line 714). | The public origin in `llms.txt`, which waits on the name (D05). | Resolved (proposed) for the format: `{origin}` is `APP_URL`, the validated config value, never a request header. Needs founder for the value only, since D05 (the name) is his; today's live apex is `frontmatter.in`. | `[O]` `src/config/env.ts` line 4 `APP_URL: z.string().url()`; `AGENTS.md` section 8 names `frontmatter.in`; `56-OPEN-DECISIONS.md` D05. | answered 18 Sep `[Z]`, D05: the name stays frontmatter, so the origin is its domain.
66 | section 6.1 (line 791). | Whether a kit is always fifteen files, or the template sets the count. | Resolved (proposed): fixed at fifteen; a template names only the two `specs/` files. Rejected: a template-set count, which would reverse a `[Z]`. | `docs/mvp0/PRODUCT-PLAN.md` section 9, "Fifteen files in a skill folder" tagged `[Z]`, and its one-call-per-file cost model; `19-ACCEPTANCE-CRITERIA.md` `A090`. | no.
66 | section 6.2 (line 801). | The `DECISIONS.md` shape and its status line. | Resolved (proposed): one H2 per question, first line `Status: decided` or `Status: open`, Not sure always open. Rejected: a callout per decision. | plan section 9 "Not sure is never written as the founder's decision"; `A087`; `adr/ADR-0002-render-carrier.md` consequence that a callout writer prefixes every line. | no.
66 | section 6.2 (line 840). | The map's four node kinds. | Checked and corrected: S16 does list them, `doc`, `spec`, `why`, `agent`. Resolved (proposed): `graph.json` uses those ids, assigned by path; the example's placeholder `document` became `doc`; `links` fixed as `{source, target}`. Rejected: a stored, hand-chosen kind. | `docs/pack/12-screens/S16.md` kind table; `docs/pack/16-COPY-DECK.md` `K.s16.node.kind`; `GraphLink` in `src/modules/graph/presentation/graph-data.ts`. | no.
66 | section 6.2 (line 844). | The body of `MAP.md`. | Resolved (proposed): a deterministic projection of `graph.json`, four H2 sections by kind, one link line per node, byte-stable across builds. Rejected: model-written prose. | plan section 5 and S16, "rebuilt from the files on every save". | no.
66 | section 6.3 (line 887). | `version` and `kitVersion` as two fields. | Resolved (proposed): keep both; `version` is the format, `kitVersion` the publish revision. Rejected: S15's single field, which conflates the two. | plan section 20's version rule; `F206` in `10-FEATURE-REGISTER.md`. | no.
66 | section 6.4 (line 912). | No `E` id for a failed publish-time checksum. | Resolved (proposed): reuse `E511`, the failed-export row, with its trigger widened by the owner of 17 to name a kit publish. Rejected: a new id for the same recovery. | `docs/pack/17-ERROR-AND-REFUSAL-CATALOGUE.md` section 1.4, `E511`. | no.
66 | section 6.5 (line 937). | An R2 key for kits. | Resolved (proposed): `k/{kitId}/{kitVersion}/{rootHash}.tar.gz`, content-addressed, an unused prefix; the owner of 21 copies it in. Rejected: the lifecycle-expired `x/` export key. | `docs/pack/21-DATA-MODEL.md` section 21.8 key table and its never-overwrite rule. | no.
66 | section 7.1 (line 991). | Whether exports inline KaTeX's stylesheet. | Resolved (proposed): HTML and Word exports inline the CSS and WOFF2 fonts, only when the document has maths; PDF loads them from our origin. Rejected: the CDN link. | `[O]` `wc -c node_modules/katex/dist/katex.min.css` = 23826; `cat node_modules/katex/dist/fonts/*.woff2. | wc -c` = 259792 over 20 files; `KATEX_VERSION = "0.17.0"` in `export-doc.ts` and `pdf-doc.ts`. | no.
66 | section 7.1 (line 993). | Paged.js and Pandoc, or headless Chromium, as the PDF target. | Resolved (proposed): headless Chromium, with Paged.js allowed inside it later; no Pandoc. Rejected: Pandoc, a second engine and GPL. Same decision as section 0 row 14. | `src/app/api/export/pdf/[...path]/route.ts`; plan section 8 table, "Paged.js for the browser, Pandoc on the server, MIT, GPL". | no.
66 | section 7.1 (line 999). | `UNVERIFIED:` whether Download .md keeps a BOM and CRLF. | Checked, and the claim split: the BOM is kept; CRLF is kept until the first edit, then CodeMirror's `doc.toString()` writes LF. Recorded as a defect for 44 with the fix (`lineSeparator` facet). A new, narrower `UNVERIFIED:` was added for the save path, with the test that would settle it. | `[O]` `node -e` on `Buffer.from(...).toString("utf-8")` and on `@codemirror/state` 6.6.0 `EditorState.create`, 18 Sep; `src/modules/editor/presentation/CodeMirrorEditor.tsx` lines 245 and 431. | no.
66 | section 8 (lines 1003 to 1030). | The open-items table and who decides. | Rewritten: every row now states its proposed resolution, who confirms it, and whether it is resolved or `needs founder`; added the edits handed to the owners of 17, 21, 44 and the plan. | the rows above. | no.
66 | section 9 limits (lines 1045 to 1051). | The four could-not-verify items. | All four checked on 18 Sep and removed; one narrower item added, the CRLF save path. | the rows for 3.10, 4.2, 4.5 and 7.1 above. | no.
