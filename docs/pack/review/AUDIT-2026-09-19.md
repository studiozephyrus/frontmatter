---
id: AUDIT-2026-09-19
title: Pre-development audit, 19 September 2026
mode: reference
tier: canonical
status: living
verified_against: 463adcd
updated: 2026-09-19
owner: sagnik
---

# Pre-development audit, 19 September 2026

Auditor: a read-only agent. Nothing in the repo was changed except this file. No commit, no push, no token file sourced, no `.env` file opened.

| Item | Value |
|---|---|
| Repo | `/Users/sagnikmitra/Desktop/GitHub/frontmatter`. |
| Branch | `audit-response/2026-09-17`. |
| HEAD | `463adcdd5f11a34c9b92b7ad9acf5ee9bf40a57b`. |
| Clock at start | `2026-09-18T19:38Z` (UTC). IST = UTC + 5:30 = 01:08 on 19 September. |
| Severity scale | `docs/pack/65-CONVENTIONS.md` section 7: CRITICAL (data loss, security hole, money wrong), HIGH (a person cannot complete a core job), MEDIUM (job done badly), LOW (cosmetic or internal). |

## 1. Every route, locally

The dev server was started with `npm run dev > $TMPDIR/dev.log 2>&1 &` (process id 45575). It answered `/login` within about 2 seconds (`Ready in 238ms`, Next.js 16.2.6, Turbopack, `Environments: .env.local`). It was stopped with `kill 45575`; afterwards `lsof -iTCP:3000 -sTCP:LISTEN` returned nothing.

Every request was signed out, with no cookie. POST and DELETE carried `content-type: application/json` and the body `{}`. The slug used for the public note routes is `audit-probe-slug`, which does not exist.

**The server started, but the local environment is missing three variables.** The dev log shows, verbatim:

- `MissingSecret: Please define a secret` from Auth.js, 33 times. Every `/api/auth/*` call returns 500, so nobody can sign in locally.
- `Invalid repo environment: GITHUB_REPO_TOKEN: ... received undefined; GITHUB_REPO: ... received undefined`, 10 times, on `/audit-probe-slug`, `/decisions` and `/prototype`.

The variable names come from the log, not from the `.env.local` file, which was not opened.

### 1.1 Pages

| Route | Expected | Actual | Result |
|---|---|---|---|
| `/login` | 200 | 200 | pass. |
| `/` | 200 (the layout shows the sign-in screen) | 200 | pass. |
| `/pricing` | 200 | 200 | pass. |
| `/privacy` | 200 | 200 | pass. |
| `/refunds` | 200 | 200 | pass. |
| `/terms` | 200 | 200 | pass. |
| `/p/audit-probe-slug` | 301 or 308 to `/audit-probe-slug` (`src/proxy.ts` comment: "Legacy URL ... 301 redirected by the handler") | 200, with `NEXT_REDIRECT;replace;/audit-probe-slug;308` inside the streamed HTML | fail (soft redirect, see F-04). |
| `/audit-probe-slug` (missing note) | 404 | 200, body renders "not found"; the log shows the `GITHUB_REPO` error | fail (soft 404, see F-04). |
| `/settings` (protected, no page) | 307 to `/login` | 307 to `/login` | pass. |
| `/does-not-exist/x` | 307 to `/login` | 307 to `/login` | pass. |

### 1.2 API routes (26 `route.ts` files)

| Route | Method | Expected | Actual | Result |
|---|---|---|---|---|
| `/api/ai/complete` | POST | 401 | 401 `{"error":"unauthorized"}` | pass. |
| `/api/ai/complete` | GET | 405 | 405 | pass. |
| `/api/ai/generate-doc` | POST | 401 | 401 | pass. |
| `/api/ai/link-doctor` | POST | 401 | 401 | pass. |
| `/api/ai/refine` | POST | 401 | 401 | pass. |
| `/api/ai/suggest-links` | POST | 401 | 401 | pass. |
| `/api/ai/summarize` | POST | 401 | 401 | pass. |
| `/api/auth/session` | GET | 200 with an empty session | 500 `There was a problem with the server configuration` | fail (F-05). |
| `/api/auth/providers` | GET | 200 | 500 | fail (F-05). |
| `/api/auth/csrf` | GET | 200 | 500 | fail (F-05). |
| `/api/commit` | POST | 401 | 401 | pass. |
| `/api/export/pdf/README.md` | GET | 401 | 401 | pass. |
| `/api/export/vault` | GET | 401 | 401 | pass. |
| `/api/share` | GET | 401 | 401 | pass. |
| `/api/share` | POST | 401 | 401 | pass. |
| `/api/share` | DELETE | 401 | 401 | pass. |
| `/api/share/conflicts` | GET | 401 | 401 | pass. |
| `/api/vault/create` | POST | 401 | 401 | pass. |
| `/api/vault/delete` | POST | 401 | 401 | pass. |
| `/api/vault/file` | GET | 401 | 401 | pass. |
| `/api/vault/file` | POST | 405 | 405 | pass. |
| `/api/vault/folder` | POST | 401 | 401 | pass. |
| `/api/vault/history` | GET | 401 | 401 | pass. |
| `/api/vault/merge` | POST | 401 | 401 | pass. |
| `/api/vault/raw/README.md` | GET | 401 | 401 | pass. |
| `/api/vault/rename` | POST | 401 | 401 | pass. |
| `/api/vault/restore` | POST | 401 | 401 | pass. |
| `/api/vault/search` | GET | 401 | 401 | pass. |
| `/api/vault/snapshot` | GET | 401 | 401 | pass. |
| `/api/vault/unlinked` | GET | 401 | 401 | pass. |
| `/api/vault/upload` | POST | 401 | 401 | pass. |
| `/api/vault/version` | GET | 401 | 401 | pass. |

No API route returned 200 with data while signed out. The three 500s all come from the auth route and all trace to `MissingSecret`.

### 1.3 Static assets and metadata routes

| Route | Expected | Actual | Result |
|---|---|---|---|
| `/robots.txt` | 200 | 200 | pass. |
| `/sitemap.xml` | 200 | 200 | pass. |
| `/favicon.ico` | 200 | 200 | pass. |
| `/favicon.png` | 200 | 200 | pass. |
| `/icon.png` | 200 | 200 | pass. |
| `/manifest.webmanifest` | 200 | 200 | pass. |
| `/opengraph-image` | 200 | 200 | pass. |
| `/llms.txt` | 200 | 200 | pass. |
| `/markdown-bitmap.svg` | 200 | 200 | pass. |
| `/markdown-bitmap.light.svg` | 200 | 200 | pass. |
| `/markdown-bitmap.brand.svg` | 200 | 200 | pass. |
| `/mdx-dark.png` | 200 | 200 | pass. |
| `/mdx-light.png` | 200 | 200 | pass. |
| `/sgnkai.png` | 200 | 200 | pass. |
| `/sgnkai-light.png` | 200 | 200 | pass. |
| `/sw.js` | 200 | 200 | pass. |
| `/theme-init.js` | 200 | 200 | pass. |
| `/decisions/` | 200, the static decisions index | 308 to `/decisions` | fail (F-07). |
| `/decisions` | 200, the static decisions index | 200, but it is the app's `[slug]` page rendering "not found" (log: `path: '/decisions'`, `GITHUB_REPO` error) | fail (F-07). |
| `/decisions/index.html` | 200 | 200, title `frontmatter · decisions` | pass. |
| `/decisions/app.js` | 200 | 200 | pass. |
| `/decisions/app.css` | 200 | 200 | pass. |
| `/decisions/manifest.webmanifest` | 200 | 200 | pass. |
| `/decisions/build-questions.py` | not served | 200, the Python source | fail (F-10). |
| `/prototype/` | 200 | 308 to `/prototype`, which renders the same "not found" page | fail (F-07). |

### 1.4 Section 1 totals

| Group | Checks | Pass | Fail |
|---|---|---|---|
| Pages | 10 | 8 | 2. |
| API | 32 | 29 | 3. |
| Static and metadata | 25 | 21 | 4. |
| **Total** | **67** | **58** | **9** |

Side effect recorded for honesty: the dev server rewrote `next-env.d.ts` (the import moved from `./.next/types/routes.d.ts` to `./.next/dev/types/routes.d.ts`). This is Next.js's own behaviour, not an edit by the auditor; `npm run build` in section 2 writes it back.

## 2. Every gate

The gates ran from a script in the session scratchpad, one after another, inside the OS sandbox. The build was then re-run outside the sandbox, because the sandbox blocked it (see the build row).

HEAD moved while the gates ran. It was `463adcd` at the start and `ad5d784` by the end. That commit is not the auditor's: it is `ad5d784 Sagnik Mitra 2026-09-19 01:09:23 +0530`, "D14 widened: voice typing with restructuring levels and a command mode, English only", and it touches one file, `docs/pack/56-OPEN-DECISIONS.md` (7 lines added, 1 removed). No code changed, so the code gates stand for both commits.

| Gate | Exit | Tail | Result |
|---|---|---|---|
| `npm run typecheck` | 0 | `tsc --noEmit` printed nothing. | pass. |
| `npm run lint` | 0 | `eslint . --max-warnings=0` printed nothing. | pass. |
| `npm run test` | 0 | `Test Files 100 passed (100)`, `Tests 1598 passed, 6 expected fail (1604)`, 5.02 s. | pass. |
| `npm run build` (sandboxed) | 1 | `Failed to fetch Google Sans from Google Fonts`, and `EPERM ... stat .env.local`. Both are the sandbox, not the code. | not counted. |
| `npm run build` (re-run with network) | 0 | 10 s, every route listed, `Proxy (Middleware)` present. It also wrote `next-env.d.ts` back, so that file is clean again. | pass. |
| `npm run arch` | 0 | `"total": 0`, `"filesScanned": 214`, no violations. | pass. |
| `npm run spec` | 0 | `SPECS OK, 4 scanned, 0 errors, 0 warnings`; `ungoverned 169 of 171 module files (INFO while bootstrapping)`; all 4 specs at `draft`. | pass. |
| `npm run corpus` | 0 | `CORPUS CLEAN, 8513/8513 byte-identical`. | pass. |
| `python3 docs/pack/tools/validate-pack.py` | 0 | `pack: 132 files, 267 covered ids`, `problems: 0`. The first run failed on this report alone, which had no front matter yet; fixed in this file. | pass. |
| `python3 decisions/tools/validate.py decisions/v2` | 0 | `275 cards, 0 errors, 5 warnings`: US spelling "color" in D21 and P3, and 29 to 30 word sentences in E1, G22 and N3. | pass, with warnings (F-12). |
| `node docs/pack/tools/gen-codemap.mjs --check` | 1 | `codemap is STALE`. | fail (F-08). |
| `python3 docs/mvp0/tools/check-pdf.py` on `frontmatter-Screens-v8.1-2026-09-18-1315.pdf` | 0 | 30 pages, trailer ok, 0 pages with ink past a margin, 0 stranded headings, 1 table (0 broken), 43 screen pairs and 86 images. It lists pages 1 and 2 as nearly empty (1.0% and 1.1% ink). | pass. |
| `~/.sgnk/bin/sgnk-docs-gate.sh` on the repo | 1 | `GATE: FAIL`: `STATUS: STALE`, `declared marker '4ad10b2' is not a commit in this repo`, 115 code commits since the oldest doc, and no file matching `AI-LAYER`. | fail (F-09). |

**The codemap is stale by content, not only by date.** `node docs/pack/tools/gen-codemap.mjs --stdout` diffed against the committed `docs/pack/59-CODEMAP.md` gives 294 changed lines. The committed file lists 1,196 files and 60.4 MB at commit `c2b51ea`; the regenerated one lists 1,240 files and 61.6 MB. The difference is 1,240 minus 1,196 = 44 tracked files the map does not describe.

**The prose gate over the pack.** `python3 ~/Desktop/GitHub/sgnkai/scoring/gate.py --file <f> --strict` was run on every `docs/pack/**/*.md`. That is 141 files, including this report. The other 140 all passed. This report failed only its own early draft, on long table rows, and was fixed; its final result is in section 6.

| Group | Checks | Pass | Fail |
|---|---|---|---|
| Gates (the sandboxed build not counted) | 12 | 10 | 2. |
| Prose gate, pack files other than this report | 140 | 140 | 0. |
| **Total** | **152** | **150** | **2** |

## 3. The live deployments, signed out

Every request is `curl -sI` (a HEAD request, no redirects followed), made at about `2026-09-18T19:40Z`. The app's CSP, below, is this string sent as `content-security-policy-report-only`: `default-src 'self'; ... script-src 'self' 'unsafe-inline' 'unsafe-eval'; ... frame-ancestors 'none'; ... object-src 'none'`. It is the same string as `src/proxy.ts` sets. The apex HSTS is `max-age=63072000`, with no `includeSubDomains` and no `preload`.

| URL | Expected | Status | Key headers | Result |
|---|---|---|---|---|
| `https://frontmatter.in` | 200 | 200 | CSP report-only only; HSTS `max-age=63072000`; `x-frame-options: DENY`; `x-matched-path: /`. | pass. |
| `https://www.frontmatter.in` | 301 to the apex (`AGENTS.md` section 8: "`www` 301s to it") | 307 to `https://frontmatter.in/` | HSTS `max-age=63072000`. | fail (F-11). |
| `https://frontmatter.sgnk.ai` | 200 | 200 | Same headers as the apex. | pass. |
| `https://frontmatter-decisions-sagnik.vercel.app` | 200, not indexed | 200 | `x-robots-tag: noindex`; HSTS `max-age=63072000; includeSubDomains; preload`; no CSP. | pass. |
| `https://frontmatter-decisions-founders.vercel.app` | 200, not indexed | 200 | HSTS with `includeSubDomains; preload`; **no `x-robots-tag`**; no CSP. | fail (F-03). |
| `https://frontmatter.in/privacy` | 200 | 307 to `/login` | None beyond HSTS and `x-frame-options`. | fail (F-01). |
| `https://frontmatter.in/terms` | 200 | 307 to `/login` | As above. | fail (F-01). |
| `https://frontmatter.in/pricing` | 200 | 307 to `/login` | As above. | fail (F-01). |
| `https://frontmatter.in/refunds` | 200 | 307 to `/login` | As above. | fail (F-01). |

Twelve further checks on the same hosts:

| URL | Expected | Status | Note | Result |
|---|---|---|---|---|
| `/login` | 200 | 200 | CSP report-only. | pass. |
| `/audit-probe-slug` (missing note) | 404 | 200 | `x-matched-path: /[slug]`. | fail (F-04). |
| `/p/audit-probe-slug` | 301 or 308 | 200 | `x-matched-path: /p/[slug]`; this route alone carries an enforced `content-security-policy`. | fail (F-04). |
| `/api/auth/providers` | not 500 | 400 | A HEAD request to Auth.js; the 400 is the method, and it is not the local 500. | pass. |
| `/settings` | 307 to `/login` | 307 to `/login` | None. | pass. |
| `/robots.txt` | 200 | 200 | `text/plain`. | pass. |
| `/sitemap.xml` | 200 | 200 | `application/xml`. | pass. |
| `/decisions/` | 200 | 308 to `/decisions` | None. | fail (F-07). |
| `/decisions` | the static index | 200 | `x-matched-path: /[slug]`, so it is the note page. | fail (F-07). |
| `/decisions/index.html` | 200 | 307 to `/login` | The live build predates the `/decisions/` allowlist. | fail (F-07). |
| `/decisions/build-questions.py` on the apex | not served | 307 to `/login` | Not exposed live today. | pass. |
| `/build-questions.py` on the sagnik decisions site | not served | 404 | None. | pass. |

| Group | Checks | Pass | Fail |
|---|---|---|---|
| The nine URLs asked for | 9 | 3 | 6. |
| Further checks | 12 | 7 | 5. |
| **Total** | **21** | **10** | **11** |

**Why the legal pages fail live but pass locally.** The allowlist for them is commit `b2f5897` (2026-09-17, "app: public legal pages, inline-SVG icons, notices, and a green lint gate"). `git branch -a --contains b2f5897` names only `audit-response/2026-09-17` and its remote copy. The local `origin/main` ref is at `8eb4de2` of 2026-07-25, 259 commits behind HEAD. That ref was not refreshed, because fetching needs the Zephyrus token and this audit sourced no token file, so "not on main" is **unverified** against the remote. What is verified is the live result: production redirects all four pages to `/login`.

## 4. TLS certificates

Each certificate was read with `openssl s_client -servername <h> -connect <h>:443 | openssl x509 -noout -issuer -subject -dates -ext subjectAltName`. Days left = (notAfter minus now) / 86400, computed with `date -j` at `2026-09-18` UTC and rounded down.

| Host | Issuer | notAfter (GMT) | Days left | Subject alternative names (SAN) | Covers the name | Result |
|---|---|---|---|---|---|---|
| `frontmatter.in` | Let's Encrypt YR1 | Dec 15 15:52:27 2026 | 87 | `DNS:frontmatter.in` | yes | pass. |
| `www.frontmatter.in` | Let's Encrypt YR2 | Dec 14 19:27:34 2026 | 86 | `DNS:www.frontmatter.in` | yes | pass. |
| `frontmatter.sgnk.ai` | Let's Encrypt YR1 | Dec 13 23:01:17 2026 | 86 | `DNS:frontmatter.sgnk.ai` | yes | pass. |
| `frontmatter-decisions-sagnik.vercel.app` | Google Trust Services WR1 | Nov 27 19:48:08 2026 | 70 | `DNS:*.vercel.app` | yes, by wildcard | pass. |
| `frontmatter-decisions-founders.vercel.app` | Google Trust Services WR1 | Nov 27 19:48:08 2026 | 70 | `DNS:*.vercel.app` | yes, by wildcard | pass. |

Nothing is under 30 days. Five checks, five passes.

## 5. Findings

Ranked by `docs/pack/65-CONVENTIONS.md` section 7. There is no CRITICAL finding: no API route leaked data signed out, no route returned a 500 in production, and no certificate is near expiry. Nothing below was fixed by the auditor.

| Id | Severity | Finding | Command that shows it | Proposed fix |
|---|---|---|---|---|
| F-01 | HIGH | Production sends `/privacy`, `/terms`, `/pricing` and `/refunds` to `/login`. A stranger cannot read the price, the terms or the refund policy before signing in, which is the core job of those four pages. The fix exists on `audit-response/2026-09-17` (commit `b2f5897`) and is not what production serves. | `curl -sI https://frontmatter.in/pricing` prints `307` and `location: /login`. | Founder merges the branch to `main`, or cherry-picks the `src/proxy.ts` hunk of `b2f5897`, then re-runs the same four `curl -sI` checks and expects 200. |
| F-02 | MEDIUM | On every app page except `/p/<slug>`, production sends the CSP only as `content-security-policy-report-only`, with `'unsafe-inline' 'unsafe-eval'` in `script-src`. The browser enforces nothing, so an injected script would run. No injection was found; this is a missing defence, not an open hole. | `curl -sI https://frontmatter.in/login` shows only `content-security-policy-report-only`. | Move the proxy header to an enforced `Content-Security-Policy`, first dropping `'unsafe-eval'` if the editor runs without it, and keep a report endpoint for the first week. |
| F-03 | MEDIUM | `frontmatter-decisions-founders.vercel.app` answers 200 to anyone, with no `x-robots-tag: noindex`, while the sibling site sends one. The founders' open decisions can be indexed by a search engine. | `curl -sI https://frontmatter-decisions-founders.vercel.app` has no `x-robots-tag` line. | Add the same `X-Robots-Tag: noindex` header to that project's `vercel.json`, and decide whether it should sit behind Vercel deployment protection. |
| F-04 | MEDIUM | A missing note returns HTTP 200 with a "not found" body, locally and in production; `/p/<slug>` also returns 200 and redirects inside the streamed HTML instead of by status. Crawlers see a soft 404, and a link checker cannot tell a dead share link from a live one. The likely cause is the root `src/app/loading.tsx`, which starts streaming before `notFound()` or `permanentRedirect()` runs; that cause is **unverified**. | `curl -sI https://frontmatter.in/audit-probe-slug` prints `200` with `x-matched-path: /[slug]`. | Remove the root `loading.tsx` from the `(public)` tree, or resolve the note before any Suspense boundary, then expect 404 and 308. Add a test that asserts the status code, not the body. |
| F-05 | MEDIUM | The local environment lacks `AUTH_SECRET` (Auth.js `MissingSecret`), `GITHUB_REPO` and `GITHUB_REPO_TOKEN`. Every `/api/auth/*` route returns 500 locally, so a developer cannot sign in. Production is not affected: the live auth route did not return 500. | `npm run dev`, then `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/auth/session` prints `500`, and the dev log prints `MissingSecret`. | The founder runs the `vercel env pull` step from `AGENTS.md` section 6 with the Zephyrus token, then re-runs this section. Add a start-up check that names each missing variable once, instead of a 500 per request. |
| F-06 | MEDIUM | Where `GITHUB_REPO` is missing, the note page throws `Invalid repo environment` and the visitor sees "not found" with a 200. A configuration fault is shown as an ordinary missing note, which is guessing rather than refusing (`AGENTS.md` section 0, rule 2). | The dev log after `curl http://localhost:3000/audit-probe-slug` shows `Invalid repo environment` next to `GET /audit-probe-slug 200`. | Let the configuration error reach `error.tsx` with a 500 and a plain message, and keep `notFound()` for a note that truly does not exist. |
| F-07 | LOW | `/decisions/` and `/prototype/` redirect to the form without a slash, which then matches the app's `[slug]` page, so the copy of the decisions site in `public/decisions/` is reachable only at `/decisions/index.html`. In production even that 307s to `/login`, because the live build predates the allowlist. | `curl -sI https://frontmatter.in/decisions` prints `x-matched-path: /[slug]`. | Add rewrites from `/decisions` and `/prototype` to their `index.html` in `next.config.ts`, then check both with and without the slash. |
| F-08 | LOW | `docs/pack/59-CODEMAP.md` is stale: it lists 1,196 files at `c2b51ea`, and the tree now has 1,240 tracked files. | `node docs/pack/tools/gen-codemap.mjs --check` exits 1. | Regenerate with `node docs/pack/tools/gen-codemap.mjs` in the next pack commit, and add the `--check` to the pack's own validation run. |
| F-09 | LOW | The docs gate fails: its declared marker `4ad10b2` is not a commit in this repo, and there is no `AI-LAYER` spec. The staleness verdict falls back to file times, so it cannot be read as a real measurement until the marker is fixed. | `~/.sgnk/bin/sgnk-docs-gate.sh /Users/sagnikmitra/Desktop/GitHub/frontmatter` prints `GATE: FAIL`. | Point the marker at a real commit, then either add the AI layer spec or record that the pack's own files cover it. |
| F-10 | LOW | `public/decisions/build-questions.py`, a build script, is served as a public file by the app (200 locally). Its opening comment names the location of a 1.5 MB agent transcript kept outside the repo. Live, it is not served today, but it would be once `b2f5897` ships. | `curl -s http://localhost:3000/decisions/build-questions.py` returns the source. | Stop copying `.py` files into `public/decisions/`, or exclude them in the copy step. |
| F-11 | LOW | `www.frontmatter.in` redirects with 307, a temporary redirect, where `AGENTS.md` section 8 says 301. The apex HSTS also lacks `includeSubDomains`. | `curl -sI https://www.frontmatter.in` prints `307`. | Set the `www` domain to a permanent redirect in the Vercel domain settings, and correct `AGENTS.md` if 307 is intended. |
| F-12 | LOW | The decisions validator passes with 5 warnings: US "color" in D21 and P3, and three 29 to 30 word option sentences in E1, G22 and N3. | `python3 decisions/tools/validate.py decisions/v2`. | Change "color" to "colour" in both cards and split the three sentences, then rebuild with `python3 decisions/tools/build-v2.py decisions/v2`. |
| F-13 | LOW | `robots.txt` carries `Disallow: /(vault)`, which is a route group name, not a URL, so it blocks nothing. | `curl -s http://localhost:3000/robots.txt` during section 1. | Remove that line from `src/app/robots.ts`, or replace it with the real signed-in paths. |

## 6. Totals

| Section | Checks | Pass | Fail |
|---|---|---|---|
| 1. Local routes | 67 | 58 | 9. |
| 2. Gates and the pack prose gate | 152 | 150 | 2. |
| 3. Live deployments | 21 | 10 | 11. |
| 4. TLS certificates | 5 | 5 | 0. |

Findings: 0 CRITICAL, 1 HIGH, 5 MEDIUM, 7 LOW.

This report itself passes `python3 ~/Desktop/GitHub/sgnkai/scoring/gate.py --file docs/pack/review/AUDIT-2026-09-19.md --strict` and `python3 docs/pack/tools/validate-pack.py`.
