---
id: files-01-39
title: Resolution log for files 01 to 39
mode: reference
tier: canonical
status: living
verified_against: e0f6f89
updated: 2026-09-18
owner: sagnik
covers: [review]
---

# Resolution log for files 01 to 39

Every `UNVERIFIED:` claim and open question in the numbered files `01-` to `39-`, excluding
`14-COMPONENT-INVENTORY.md`, `16-COPY-DECK.md` and `12-screens/`, and how each was treated on
18 September 2026 under `docs/pack/tools/RESOLVE-BRIEF.md`.

file | id or line | the open point | what you did | basis | needs founder (yes/no)
--- | --- | --- | --- | --- | ---
27 | 83, section 2.1 row 5. | SambaNova pool, region and rate limit not opened. | Checked. Rate-limits page returns 403 to curl, so the limit stays `UNVERIFIED:` with a needs. Found the Free plan now reads `Add a payment method and purchase credits to run your first requests.` Row marked held out of the chain; section 9 step 6 and the limits list updated to match. | [M] `https://cloud.sambanova.ai/plans`, `https://docs.sambanova.ai/docs/en/get-started/rate-limits` (403), both 2026-09-18. | no
27 | 106, training clause table. | SambaNova training sentence not opened. | Confirmed and quoted. The cloud licence limits use of Customer Content to providing the Service, `and for no other purposes`. | [M] `https://sambanova.ai/cloud-end-user-license-agreement`, 2026-09-18. | no
27 | 231, section 2.4 Cerebras. | Plan's $5 trial against L1's rate table; which is true. | Resolved: one page carries both, the table and the FAQ. Quoted `Is there a permanently free tier? No.` and replaced the `UNVERIFIED:` with the evidence. | [M] `https://inference-docs.cerebras.ai/support/rate-limits`, 2026-09-18. | no
27 | 276, section 2.5. | Incident history windows incomplete; OpenRouter and Mistral status pages 403. | Rechecked. Mistral still 403; OpenRouter returns a script shell and its incidents API 503. Kept `UNVERIFIED:` and added a needs: a browser read of both. | [O] curl of both on 2026-09-18. | no
27 | 488, section 4.6 Groq reset. | Groq daily reset instant not stated. | Page rechecked: still not stated, but its header example shows a daily count resetting in under three minutes. Changed the ledger rule to read `x-ratelimit-reset-requests` rather than assume midnight. | [M] `https://console.groq.com/docs/rate-limits`, 2026-09-18. | no
27 | 870, section 8 paid step. | Workers Paid fee quoted second-hand, our Worker usage not modelled. | Confirmed the $5 minimum on Cloudflare's page and the neuron price. Showed our Worker usage is nil (no wrangler file in the repo; Workers AI is called over REST). | [M] `https://developers.cloudflare.com/workers/platform/pricing/` and `.../workers-ai/platform/pricing/`, 2026-09-18; [O] `git ls-files \| grep -ci wrangler` = 0. | no
27 | 996, section 9.5 Turnstile. | Turnstile's free usage ceiling unknown, limits page 404. | Confirmed from the plans page: 20 widgets, unlimited challenges on Free. Replaced the tag with the quotation. | [M] `https://developers.cloudflare.com/turnstile/plans/`, 2026-09-18. | no
27 | 1036, section 9.6. | 119.44 million credit cards in April 2026, from a news report. | Found the primary release (RBI Bankwise ATM/POS/Card Statistics, April 2026, revised) but its XLSX gave an empty reply to curl. Kept `UNVERIFIED:` with a needs naming the file. | [O] `https://www.rbi.org.in/Scripts/ATMView.aspx` opened 2026-09-18; `rbidocs.rbi.org.in` XLSX empty reply. | no
34 | 239, GitHub App permissions. | Whether GitHub adds read-only Metadata to every App. | Partly checked. No docs page opened says it is automatic; the webhook events page says several events need Metadata read. Kept `UNVERIFIED:` with a needs and added: request Metadata read either way. | [M] GitHub docs webhook events and permissions pages, 2026-09-18. | no
34 | 292, Drive scope. | CASA assessment price. | Rechecked two primary pages; no price stated. Kept `UNVERIFIED:` with a needs (an assessor's quote). Moot while the full scope is forbidden. | [M] `https://appdefensealliance.dev/casa`, `https://support.google.com/cloud/answer/13465431`, 2026-09-18. | no
34 | 306, Drive write precondition. | Whether `files.update` honours `If-Match`. | Confirmed there is none: v3 files have no ETag. Tag replaced with the quotation. | [M] `https://developers.google.com/workspace/drive/api/guides/v2-to-v3-reference`, 2026-09-18. | no
34 | 517, limits. | Whether `firestore.rules` passes an emulator run. | Cannot run here: no Java, no firebase CLI, and installs are out of scope. Kept `UNVERIFIED:` with a needs. | [O] `java -version` fails, `which firebase` not found. | no
34 | 519, limits. | Google OAuth consent screen verification state. | Console-only fact. Kept `UNVERIFIED:` with a needs. | none reachable. | no
34 | 520, limits. | Drive byte fidelity and precondition. | Split. Precondition answered (no ETag in v3). Byte fidelity kept `UNVERIFIED:`, needs the benchmark's live round trip. | [M] as row above. | no
34 | 522, limits. | CASA price, repeated. | Pointed at the section 8 recheck; kept `UNVERIFIED:`. | as the 292 row. | no
39 | 155, section 5.4. | Deployment protection setting on the `frontmatter` project. | Checked from outside: an anonymous request to the branch alias returns 200 with the app's title and no login redirect. Tag replaced; the dashboard value itself is named as unread. | [O] `curl -D -` on `frontmatter-git-audit-response-2026-09-17-zsco.vercel.app`, 2026-09-18. | no
39 | 274, limits. | Deployment protection, repeated. | Rewritten to the same finding. | as above. | no
39 | 276, limits. | GitHub OAuth App callback list. | Needs the owner's GitHub settings page. Kept `UNVERIFIED:` with a needs. | none reachable. | no
39 | 277, limits. | Whether legal placeholders still say pending. | Confirmed on the preview (`due by 15 October 2026`). Found production `/privacy` answers 307 to `/login`; the likely cause, `origin/main` lacking both the page and the proxy entry, is marked `INFERENCE:`. | [O] curl of both hosts; `git show origin/main:src/proxy.ts \| grep -c '"/privacy"'` = 0. | no
38 | 157, section 5.1 clocks table. | Data protection breach deadline, rules not opened. | Opened the Rules (G.S.R. 846(E)). Rule 7(2)(b): the detail to the Board `within seventy-two hours`; each person `without delay`. In force eighteen months after publication, so May 2027. Row rewritten, and the section now says adopt it now. | [M] `https://www.meity.gov.in/static/uploads/2025/11/53450e6e5dc0bfa85ebd78686cadad39.pdf`, 2026-09-18. | no
38 | 332, limits. | Breach deadline, repeated. | Rewritten to the opened figure. | as above. | no
38 | 334, limits. | Which clock governs, IT Rules or consumer rules, or both. | A legal reading. Kept `UNVERIFIED:` with a needs (a lawyer's opinion); the stricter reading stays meanwhile. | none reachable. | yes
37 | 198, section 5. | Breach deadline and inactivity period. | Opened the Rules. Quoted rule 7(2)(b), rule 8 and the Third Schedule (three years, binds only very large platforms, not us), rule 8(2) notice, rule 8(3) one-year log floor. | [M] the Rules PDF, as above. | no
37 | 331, limits. | Same two facts, repeated. | Rewritten to point at section 5. | as above. | no
37 | 333, limits. | Retention rows not cross-checked against the legal section. | Cross-checked. All rows match plan section 18. Two conflict with rule 8(3): security log raised to 365 days, resolved (proposed 18 Sep, founder review); keeping processing logs a year after account deletion needs founder, since it changes a promise to users. | [O] `sed -n 1548,1567p docs/mvp0/PRODUCT-PLAN.md`; [M] the Rules PDF. | yes
36 | 244, limits. | Whether browser storage beyond the six `sgnk-md` entries is written. | Checked with a wider grep over `src` and `public`. Found eleven more keys, none holding document text, including the service worker cache `sgnk-md-v2`. Added a table to section 1. | [O] `grep -rnE 'localStorage\|sessionStorage\|idb-keyval\|caches\.open' src public`. | no
36 | 248, limits. | Safari seven-day eviction, quoted from the plan. | Re-opened WebKit's post and quoted the rule and the home-screen exception. | [M] `https://webkit.org/blog/10218/full-third-party-cookie-blocking-and-more/`, 2026-09-18. | no
32 | 388, limits. | Exact `vercel promote` syntax for the installed CLI. | Confirmed with the installed CLI's help; section 5.1's command is valid. Added the caveat that the repo does not pin `vercel`. | [O] `vercel --version` = 50.13.2; `vercel promote --help`. | no
32 | 391, limits. | Domain registration owner. | Read the registry RDAP record: GoDaddy, registered 2026-06-24, expires 2029-06-24, registrant redacted. Kept `UNVERIFIED:` with a needs (the GoDaddy account). | [O] `https://rdap.nixiregistry.in/rdap/domain/frontmatter.in`, 2026-09-18. | no
33 | 181, limits. | Exact Vercel error wording for an invalid `vercel.json`. | Checked the installed CLI bundle: the string is not there, so it comes from Vercel's server. Kept `UNVERIFIED:` with a needs (a scratch deploy of a bad config, which is a cloud action and was not taken). | [O] python search of `/opt/homebrew/lib/node_modules/vercel/dist/index.js`. | no
33 | 183, limits. | Whether the 66.4 percent suite inflation is current. | Measured: 164 test files under `.claude/worktrees` against 100 outside. The figure is historical; today the exclusion shields more than double. Tag replaced. | [O] `find` counts, 2026-09-18. | no
35 | 324, limits. | That verify, corpus and the restamp check all pass. | Ran all three. Corpus clean at 8,513 files; restamp 0 broken citations; typecheck, lint, 1,598 tests, arch and spec all pass. The build step failed only on a Google Fonts fetch the sandbox blocks, so the build alone stays `UNVERIFIED:` with a needs. Also removed the "not re-derived" line on the 8,513 figure, now re-derived. | [O] the commands, at `e0f6f89`, 2026-09-18. | no
35 | 328, limits. | Whether Tauri reads its version from `tauri.conf.json` or `package.json`. | Answered from Tauri's config reference: the config's own value wins, or a path to a `package.json`. Proposed pointing it at `../package.json`, resolved (proposed 18 Sep, founder review). | [M] `https://v2.tauri.app/reference/config/`, 2026-09-18. | no
30 | 297, rotation. | No rotation schedule, owner or runbook for the eleven secrets. | Confirmed absent. Found two exposed, unrotated tokens recorded in the 13 September handover. Proposed a policy (on leak, on departure, yearly; owner as the plan's security-log row), resolved (proposed 18 Sep, founder review). The two exposed tokens need the founder. | [O] `git grep -n -i rotat`; `HANDOFF-frontmatter-2026-09-13.md:109`. | yes
30 | 354, limits. | Auth.js v5 beta 31 resolution order for `AUTH_URL`, `NEXTAUTH_URL` and the request origin. | Read the installed library: `AUTH_URL ?? NEXTAUTH_URL`, then forwarded host or host. Tag replaced with file and line. | [O] `node_modules/next-auth/lib/env.js:6`, `node_modules/@auth/core/lib/utils/env.js:81`. | no
23 | 295, section on security headers. | Whether the enforced public-page policy reaches `/[slug]`. | Tested on the branch preview: it does not. `/p/<slug>` carries the enforced policy, `/<slug>` only the report-only one with `unsafe-eval`. Claim corrected. Proposed moving the enforced header into `src/proxy.ts` for the public note route, resolved (proposed 18 Sep, founder review). | [O] `curl -sI` on both paths, 2026-09-18; `next.config.ts:48`. | no
31 | 329, section 7. | Whether anybody has run the Firebase emulators. | No trace of a run in the repository: no debug log, no `.firebaserc`, no emulator variable or script. Tag replaced with the evidence plus an `INFERENCE:` that they were never run. | [O] `ls`, `git grep`, `git log -S emulators`. | no
21 | 579, section 21.8. | R2 per-object and per-bucket limits not re-opened. | Re-opened and quoted: unlimited per bucket, 1,024-byte keys, 5 TiB objects, 5 GiB single-part uploads; location hints no finer than Asia-Pacific. | [M] `https://developers.cloudflare.com/r2/platform/limits/` and `.../r2/reference/data-location/`, 2026-09-18. | no
15 | D-KB-1, section 14. | Does the graph keep `Cmd/Ctrl + G`. | Decided: no chord; palette and button only, plus the `defaultPrevented` guard. Rejected the two nearby chords because CodeMirror owns them. Resolved (proposed 18 Sep, founder review). | [O] `node_modules/@codemirror/search/dist/index.js:1038`, `:1045`, `:1048`. | no
15 | D-KB-2, section 14. | Does Vim mode survive. | Decided: yes, off by default behind the setting. Rejected removal on D02. Resolved (proposed 18 Sep, founder review). | [Z] D02 in `56-OPEN-DECISIONS.md` section 0; C5 in this file. | no
15 | D-KB-3, section 14. | Undo in a live session. | Decided: local-only undo through `Y.UndoManager` with `trackedOrigins`. Rejected a shared stack. Resolved (proposed 18 Sep, founder review). | [M] `https://docs.yjs.dev/api/undo-manager`, 2026-09-18. | no
15 | D-KB-4, section 14. | A separate Doc mode keyboard map. | Decided: yes, Google Docs' paragraph chords written as markdown splices. Rejected one shared map. Browser collisions not checked. Resolved (proposed 18 Sep, founder review). | [M] `https://support.google.com/docs/answer/179738`, 2026-09-18; [O] grep of CodeMirror bindings. | no
15 | 350, section 11. | Nobody has tested with an iPad keyboard. | Needs a live device. Kept `UNVERIFIED:` and added a needs. | none reachable. | no
18 | section 12, row 1. | A third sign-in route (magic link) and second 2. | Recommended: flag off at launch; when on, a third button, never a form. Needs founder, since it is his question 18. | [Z] ADR-0009; plan section 29 question 18. | yes
18 | section 12, row 2. | What an EU visitor sees at second 0. | Decided: detect by `x-vercel-ip-country`, show S37 without sign-in buttons and one plain line; store nothing. Resolved (proposed 18 Sep, founder review). | [M] `https://vercel.com/docs/headers/request-headers`, 2026-09-18; plan section 23. | no
18 | section 12, row 3. | Whether the age floor appears in the first sixty seconds. | Recommended one line under the buttons, no birth date. Needs founder, since it is legal risk and his question 12. | plan section 23. | yes
18 | section 12, row 4. | The greeting's timezone. | Decided: the device clock, with stated boundaries as a copy choice. Resolved (proposed 18 Sep, founder review). | `K.s02.greeting` in `16-COPY-DECK.md`. | no
25 | 508, limits. | NF-4 key equality (NFC against NFD) left open by the splice-writer spec. | Decided on the engine document's section 75.1: normalise the request only, refuse a non-NFC file key as `NOT_NFC` with a repair. Rejected normalising the file. Resolved (proposed 18 Sep, founder review). `specs/engine/splice-writer.md:85` is outside my files and still reads open. | [P] `docs/ENGINE.md` section 75.1 as quoted in 25.8; `sed -n 85p specs/engine/splice-writer.md`. | no
10 | 423, section 5 crosswalk. | The summary column paraphrases cited plan lines, not the findings. | Checked against the audit file. Found every one of the 119 plan-line citations stale after the 18 September plan edits; regenerated the column by grep (70 rows changed). Six sampled summaries match their findings; `AF076` follows a plan tag that points at the wrong finding (`F076` is icons; `F066` is the `llms.txt` one), marked `INFERENCE:` for the plan's owner. | [O] python check of each `plan:NNN` line for its id; `verify/2026-09-17/CLAUDE-AUDIT-FINDINGS.jsonl`. | no
08 | 47, section 1. | Whether a separate `frontmatter` project exists on the personal team. | Checked read-only with the CLI's login: it exists, id matches the local link, newest deployment 48 days old, so it is the pre-migration project. Deleting it is destructive, so the recommendation (re-link to `zsco`, then retire it) needs founder. Limits list updated. | [O] `vercel project inspect` and `vercel ls` on `team_CDEATPKml1m8SIZSJ0DKdEjG`, 2026-09-18. | yes
03 | 62, Splice journal. | Named in `CLAUDE.md`, not specified under `specs/`. | Checked: now specified in `67-SYNC-AND-CONFLICT.md` section 2 and ADR-0003; still absent from `specs/`. Tag replaced with the evidence and the source column pointed at file 67. | [O] `grep -rl -i 'splice journal' specs/ docs/pack/67-SYNC-AND-CONFLICT.md docs/pack/adr/`. | no
06 | 157, section 3. | OpenKnowledge's CRDT, from a news article with no repository link. | Found the repository through the Show HN post, and read its package files: Yjs and Hocuspocus. Tag replaced with the evidence. | [M] `https://hn.algolia.com/api/v1/items/48675435`, raw `package.json` files in `inkeep/open-knowledge`, 2026-09-18. | no
01 | 237, limits. | The causal link between the stale `main` tip and the four redirecting legal routes. | Checked from outside: all four routes 307 to `/login` on production, and the pages do not exist on `origin/main`. The `zsco` project settings stay unread (the CLI login cannot see that scope). Tag replaced with the evidence and a narrower `INFERENCE:`. | [O] curl of the four routes; `git cat-file -e` on `origin/main`; `vercel inspect frontmatter.in --scope zsco`, 2026-09-18. | no
05 | section 7, recordings row. | Where research recordings live. | Decided: a separate private R2 bucket on the company's account, founders only. Rejected the product bucket. Resolved (proposed 18 Sep, founder review). | [Z] D10 in `56-OPEN-DECISIONS.md` section 0. | no
05 | section 7, handle row. | A handle scheme for participants. | Decided: `U01` to `U20` by first session, key kept only beside the consent. Rejected initials. Resolved (proposed 18 Sep, founder review). | `grep` found no existing `U` id in the pack. | no


## Totals

- **55 rows.** 44 `UNVERIFIED:` claims and 11 open questions from decision tables. No `open:` marker
  exists in these files; `grep -n 'open:'` found none.
- **Decisions resolved as proposals: 14.** Nine of the eleven standalone questions (`D-KB-1` to
  `D-KB-4`, two in file 18, one in file 25, two in file 05), and five inside `UNVERIFIED:` rows
  (files 27, 37, 35, 30, 23). The other two in file 18 carry a recommendation and need the founder.
- **Needs founder: 6 rows**, marked `yes` above.
- **`UNVERIFIED:` claims: 44 checked.** 23 confirmed with evidence, 8 found wrong or stale and fixed,
  13 still unverified, each with a needs.
- **Found on the way, outside the brief's items:** SambaNova's free plan now asks for credits; the
  public note route is not under the enforced policy; production `/privacy` redirects to `/login`;
  every `plan:NNN` citation in file 10 had gone stale; the plan tags its `llms.txt` rule with the
  wrong finding id.
