---
id: S11
title: S11. Instruction files
mode: reference
tier: canonical
status: specified
spec: instructions/file-set
verified_against: 0af3c90
updated: 2026-09-18
owner: sagnik
---

# S11. Instruction files

> This screen was a health panel on one `AGENTS.md`. On 18 September the founders reversed it
> toward the research: it is now the whole instruction-file set. It is also the screen with the
> strictest rule about what it may claim.

> **Where the ids come from.** Every `C`, `E`, `A` and `K` id below lives in its register:
> `14-COMPONENT-INVENTORY.md`, `17-ERROR-AND-REFUSAL-CATALOGUE.md`, `19-ACCEPTANCE-CRITERIA.md` and
> `16-COPY-DECK.md`. The ids this screen first proposed were reconciled into those registers on 18
> September 2026. The old-to-new mapping is in `tools/error-map.json`, `tools/component-map.json`
> and `tools/acceptance-map.json`, and `tools/validate-pack.py` fails if any id here has no home.

## Purpose

Show every instruction file a project keeps for its agents, say which are linked and which are
copies, and say plainly which of them has drifted.

## The reversal, and the rule it carries

`docs/mvp0/SCREEN-CHANGES-2026-09-18.md:87` recorded the screen as fine as drawn. `:88` flagged it
against the research, and `:303` is the founders' answer:

> S11 follows the research. The one-file health panel becomes the whole instruction-file set.

**Two findings drove it**, both `[O]` from the research round of 18 September:

Finding | What it changed
The loudest artefact in the whole round is `anthropics/claude-code#6235`, 6,651 reactions on 18 September (6,644 when the round read it), on a request about **several** instruction files, and a three-tool team keeps four or more | the screen is a set, not a file
Two independent studies find these files do not raise task success, and one measured over 20 per cent added cost | what the screen is allowed to claim

### The claim rule, stated as a rule because it is one

**The screen may say a file is correct. It may not say a file makes an agent better.**

May say | May not say
this file is under the size cap this tool publishes | this file will make the agent follow your rules
the setup commands are present | a tidier file gets better results
this copy is three lines behind its source | tidying this raises task success
these lines restate what a linter already enforces, and cost tokens every session | this file is why the agent got it right

The honesty footer, `C130`, carries the reason on the screen. It exists because a person looking at
a green panel will otherwise infer the claim the studies do not support.

## Entry and exit

Direction | Route or control | Goes to | Notes
Arrive | opening any file in the set from the tree | this panel beside it | still S04, a rail panel
Arrive | the tree foot's readout of the set | the same | the readout is a link
Arrive, phone | More, then Instruction files | `C088`, the right drawer | a drawer, not a page
Leave, a file | click a row in `C135` | that file opens in the editor | the panel stays
Leave, Diff | `C135` | a diff of the copy against its source | read-only
Leave, Regenerate | `C135` | replaces the copy from the source | see the prohibitions
Leave, Add as an import | `C085` | creates the missing file as a one-line import | never as a copy, where the tool supports an import
Leave, Tidy this file | `C061` | one AI call on the open file | a credit, stated first

## Anatomy

Region | Component | What it holds | Notes
Panel | `C096` | The set, the drift card, the checks, the footer | the right rail on S04
Set filter | `C082` | All, Linked, Copies, each with a count | `.fseg`; the same component as `C082`
File row | `C135` | A state icon, the file path, its relationship, and which tools read it | `.file` with `ok`, `warn` or `off`
Add an import | `C085` | One row offering a missing file as an import | `.drophint`; the offer names the tool it is for
Drift card | `C135` | Which copy drifted, by how much, why it has to be a copy, then Diff and Regenerate | `.chk` with two buttons
Checks list | `C135` | Four rows, each a check with its evidence | `.chk`, mixed `ok` and `warn`
Honesty footer | `C130` | What these checks do and do not claim | `.foot`; **not optional**
Tidy this file | `C061` | One AI action on the open file, with its cost | `.railfoot`, with the credit meter
Set readout | `C133` | How many files, and how many drifted | the tree foot and the panel head carry the same numbers
Phone drawer | `C088` | The whole panel as the right drawer | `pdrawer(..., 'right')`

### The set, as drawn

Five rows, and each is a different relationship. The point of the screen is that these five are not
five copies of one thing.

File | Relationship | State | Which tools read it
`AGENTS.md` | the source | ok | Claude Code, Codex, Cursor, Jules, Amp, opencode, Zed and Warp
`CLAUDE.md` | a one-line import of the source | ok | Claude Code
`.github/copilot-instructions.md` | a copy, in step | ok | the Copilot coding agent
`.cursor/rules/style.mdc` | a copy, drifted | warn | Cursor
`GEMINI.md` | missing | off | the Gemini command-line tool

- **`AGENTS.md` is used by over 60,000 projects**, `[M]` "used by over 60k open-source projects" on
  https://agents.md/, opened 18 September 2026. That is why it is the source
  rather than one format among five.
- **An import beats a copy, and the tool teaches the import.** All six of our own dual-file
  repositories use the import, which is `[O]` from the plan.
- **A copy exists only where the tool cannot read an import.** The drift card says so on the row it
  applies to: Cursor does not read a plain markdown file in that folder, so that one has to be a
  copy.
- The filter counts the source as linked. Two linked, two copies, one missing, five in all.

### The four checks, and their evidence

Every check states the evidence beside it, so a person can disagree with it.

Check | State | Evidence shown
Under every published size limit | ok | the word count, the byte size, and the cap the tool publishes
Setup commands present | ok | which commands were found
Claims unverified since a date | warn | the line numbers, and how long since anyone ran them
Rules a linter already enforces | warn | the line range, which linter, and that they cost tokens every session

**The last check is the one that earns the screen.** It finds text that costs tokens in every
session and changes nothing, which is the measured cost the research names.

## Data contract

What | Direction | Port or store | Shape | When
The set | in | `InstructionSetReader.scan(projectId)` | one record per known instruction file, with its relationship | on mount, and on any write to a member
File bytes | in | `DocumentReader.open(path)` | bytes | per member, on scan
Published caps | in | a shipped table of tool limits | tool, cap, and where it was read | on mount
Drift | in | derived, by comparing a copy to its source | a line delta | on scan
Checks | in | `CheckRunner.run(bytes, instructionRules)` | findings | on scan
Add an import | out | `DocumentWriter.create()` | the one-line import file | on `C085`
Regenerate | out | `DocumentWriter.splice()` | the copy's full range, replaced from the source | on `C135`, after a confirmation
Tidy | out | `LlmClient.generate()` then the change queue | a proposal, never a write | on `C061`

- **`InstructionSetReader` is `specified, not built`.** Nothing under `src/modules/` scans a project
  for instruction files today.
- **The caps table is shipped data, not a guess.** Each row records where the number was read, so a
  stale cap is findable rather than invisible.
- **Tidy produces a proposal.** It goes through the change queue like every other AI edit, and S20
  accepts or rejects it span by span.

## Actions

Action | Trigger | What happens | Failure behaviour | Event id
Scan the set | opening the panel, or a write to a member | rebuilds the set and the checks | a member could not be read `E105`; the source is missing `E552` | `instructions.set.scanned`
Change the filter | `C082` | re-filters in place | none | `instructions.filter.changed`
Open a file | a row in `C135` | that file in the editor | `E105` | `doc.opened`
Diff a copy | `C135` | a read-only diff against the source | the copy drifted beyond a clean diff `E508` | `instructions.copy.diffed`
Regenerate a copy | `C135` | confirms, then replaces the copy from the source | the copy has hand edits that would be lost `E118`, and it refuses | `instructions.copy.regenerated`
Add as an import | `C085` | creates a one-line import for that tool | the tool does not support an import `E554`, and it offers a copy instead | `instructions.import.added`
Tidy this file | `C061` | one call, then a proposal in the change queue | over the cap `E071`; chain down `E080`; the call failed `E083` | `instructions.tidy.run`
Check a cap | on scan | compares size against the published cap | the cap for that tool is unknown `E553`, and the row says unknown rather than ok | `instructions.cap.checked`

## States

State | When | What is shown | What the person can do
first-run | the first project scanned | the set, and `C130` | read, fix a drift, add an import
empty | the project has no instruction file | one row offering to create `AGENTS.md` as the source | create it, or ignore
loading | the scan is running | the panel head with the counts reserved | keep writing
partial | the set is read, a cap is unknown | the row marked unknown, never marked ok | the rest of the panel works
offline | no network | **the whole panel works**, because the scan is local | fix drift, add imports; Tidy is unavailable
unauthorised | the role cannot edit | the panel is read-only; Diff works, Regenerate and Tidy are absent | read the state
over-cap | no credits | the panel works; Tidy is disabled with the reason | everything except Tidy is free
AI-unavailable | the chain is down | the same; only Tidy needs a model | the same
conflict | a member has an unresolved conflict | that row is marked and its actions are disabled | resolve on S31
error | the source is missing but copies exist | `E552`, and the copies are listed as orphans rather than as ok | promote one to the source, or create it
degraded | the caps table is older than the tools | affected rows say the cap is unverified, with its date | trust the rest

## Copy

`K.s11.head`, `K.s11.filter.all`, `K.s11.filter.linked`, `K.s11.filter.copies`,
`K.s11.rel.source`, `K.s11.rel.import`, `K.s11.rel.copy`, `K.s11.rel.copydrifted`,
`K.s11.rel.missing`, `K.s11.addimport`, `K.s11.drift.head`, `K.s11.drift.why`, `K.s11.diff`,
`K.s11.regenerate`, `K.s11.regenerate.confirm`, `K.s11.check.size`, `K.s11.check.setup`,
`K.s11.check.unverified`, `K.s11.check.lintduplicate`, `K.s11.honesty`, `K.s11.tidy`,
`K.s11.tidy.cost`, `K.s11.capunknown`, `K.s11.orphan`.

**`K.s11.honesty` is the one string on this screen that cannot be cut for space.** It says the
checks report that the file is correct, and that they do not claim it makes an agent better, and it
names the measurement: two studies found no gain in task success and one measured over 20 per cent
added cost.

## Keyboard and focus

- The panel opens with focus on the filter.
- Up and down move through the file rows; Enter opens the focused file.
- The drift card's two buttons are one Tab apart, with Diff first, because reading before replacing
  is the order that avoids losing work.
- Regenerate never fires on Enter alone. It opens a confirmation that names what will be replaced.
- Escape closes the panel.
- The full map is `15-INTERACTION-AND-KEYBOARD.md`.

## Responsive

- The panel becomes the right drawer, reached from More.
- The file rows keep the full path. A truncated path is useless on this screen, because
  `.cursor/rules/style.mdc` and `.github/copilot-instructions.md` are told apart by their folders.
- The tool list on each row shortens to the first tool and a count of the rest.
- **The honesty footer stays in full.** It is the last thing that would be cut, not the first.

## Desktop differences

- The desktop build can scan files on disk that are not in the cloud project, so the set can include
  an instruction file an agent wrote locally.
- Tidy can run on a local model, with no credit and nothing leaving the machine.

## Acceptance

Id | What it asserts | Test id
A074 | The panel lists every instruction file found, not one | T-S11-01
A577 | Each row states its relationship: source, import, copy or missing | T-S11-02
A074 | A drifted copy is named, with the size of the drift and why it must be a copy | T-S11-03
A075 | The honesty footer renders in every state, including the phone drawer | T-S11-04
A578 | No string on this screen claims a gain in agent task success | T-S11-05
A579 | A size cap is shown only with the tool it belongs to, and an unknown cap renders as unknown | T-S11-06
A580 | Add as an import creates an import, not a copy, where the tool supports one | T-S11-07
A581 | Regenerate refuses when the copy carries hand edits | T-S11-08
A502 | Tidy produces a change-queue proposal and writes nothing directly | T-S11-09
A582 | The whole panel except Tidy works with the network disabled | T-S11-10

**A578 is a copy test, and it is the most important one on this screen.** It fails against any
string containing a claim of improved agent performance, and it should be run over
`16-COPY-DECK.md` rather than over the rendered screen, so a claim cannot be added later without
tripping it.

## What this screen must never do

- **Never claim that tidying an instruction file makes an agent better at its job.** Two studies
  measured no gain in task success and one measured over 20 per cent added cost. The screen reports
  correctness, not effectiveness.
- **Never show one file as though it were the whole set.**
- **Never present a copy as equivalent to an import.** An import cannot drift; a copy can, and the
  screen exists partly to say which is which.
- **Never create a copy where the tool supports an import.**
- **Never regenerate over hand edits without refusing first.**
- **Never state a size cap without naming the tool it belongs to.** The caps are per tool, and a
  number with no owner is worse than no number.
- **Never mark a row ok when its cap is unknown.**
- **Never let Tidy write directly to a file.** It proposes, and the queue decides.
- **Never drop the honesty footer for space, on any surface.**
- **Never use an emoji for a row's state.**

## Open questions

Id | Question | Where it came from
D31a | Which tools are in the shipped caps table, and where was each cap read? | the drawn example names one cap, for one tool, and the set names eight tools on the source row alone
D32a | Can a person mark a copy as deliberately divergent, so it stops being reported as drift? | a real project may want a tool-specific rule, and nothing in the review or the plan covers it
D33a | Does the set include an instruction file this product did not create, found in an imported vault? | the import path brings in whatever the folder held

**These three carry suffix letters**, because `D31`, `D32` and `D33` are already taken by S14 and
S15. `65-CONVENTIONS.md` section 1 allows a suffix and forbids renumbering.

### Resolutions

- **D31a, resolved (proposed 18 Sep, founder review). One row per tool the set names, and a cap is
  written only once it is read from that tool's own source.** Every other row ships as unknown and
  renders through `E553`, never as ok.
- D31a, the first row, checked. Codex caps `AGENTS.md` at 32 KiB by default: `[M]`
  `DEFAULT_PROJECT_DOC_MAX_BYTES: usize = 32 * 1024` at `codex-rs/config/src/config_toml.rs:75` in
  `openai/codex`, opened 18 September 2026. It is a default the person can change.
- D31a, the table's shape: tool, cap, unit, source address and date read, as a data file in the
  code. Rejected: filling the other seven from memory, which is the guess the screen refuses.
- **D32a, resolved (proposed 18 Sep, founder review). Yes, a copy can be marked divergent on
  purpose.** The mark is stored on the project, not in the file, and it lapses when the source
  changes, so new drift is reported again.
- D32a, rejected: never allowing it, because a warning nobody can clear trains people to ignore the
  drift card. Also rejected: a marker in the copy, which changes a file another tool reads.
  Register rows needed: a relationship string and state for "divergent on purpose".
- **D33a, resolved (proposed 18 Sep, founder review). Yes, every instruction file found is in the
  set**, whoever made it. `A074` already asserts every file found. Import writes nothing to them.
- D33a, how. The relationship is detected, never assumed. An unmatched file shows as a drifted copy
  of the source, or as an orphan under `E552` when there is no source. Rejected: hiding files the
  product did not create, which would show part of the set as the whole.

## Limits of this file

- **Checked 18 September: the two studies exist and say what this file says.** `[M]` arXiv API,
  `export.arxiv.org/api/query`: 2602.11988 "does not generally improve task success rates, while
  increasing inference cost by over 20%", and 2607.27250 "does not measurably move correctness".
- **The literature is split, and the screen should know it.** The same query returns 2601.20404,
  which found an `AGENTS.md` associated with "lower median runtime" and fewer output tokens. The
  claim rule holds either way; any cost wording on the screen must not say the files always cost more.
- **Checked: the 32 KiB cap is Codex's default**, per D31a above, and not a hard limit.
- **Checked: the two counts.** `[M]` GitHub API `repos/anthropics/claude-code/issues/6235` gave 6,651
  reactions at 17:38 UTC on 18 September, and https://agents.md/ says over 60k projects. The body
  carries both with their dates.
- **What would falsify this file:** a study showing these files do raise task success, which would
  free the screen to say so, with the citation on the row.
