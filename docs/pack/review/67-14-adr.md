---
id: 67-14-adr
title: Resolution log for 67, 14 and the decision records
mode: reference
tier: canonical
status: living
verified_against: e0f6f89
updated: 2026-09-18
owner: sagnik
covers: [review-67-14-adr]
---

# Resolution log for 67, 14 and the decision records

Every `open:` marker, open question and `UNVERIFIED:` claim in `67-SYNC-AND-CONFLICT.md`,
`14-COMPONENT-INVENTORY.md` and `adr/`, and how each was treated on 18 September 2026 under
`docs/pack/tools/RESOLVE-BRIEF.md`. One row per item, written as each item was finished.

file | id or line | the open point | what you did | basis | needs founder (yes/no)
--- | --- | --- | --- | --- | ---
67 | section 5.5, `open:` | Value of `MERGE_CELL_BUDGET`, and whether the merge moves to a worker. | Resolved (proposed): 4,000,000 cells per diff, merge in a Web Worker. Measured the shipped `merge3` at four sizes and wrote the table in. Rejected no budget and a rewrite of the witness. | [O] `node --experimental-strip-types bench2.ts <n>` over a copy of `merge3.ts`, 18 Sep; `shape-gate.ts:24`. | no
67 | section 5.6, `open:` | Is placement permitted when another author's version is in the chain. | Resolved (proposed): the stricter rule, own uid only, becomes the rule. Rejected cross-author placement on disjoint regions, which section 16 names as the falsifier. | [P] invariant 12, `docs/mvp0/PRODUCT-PLAN.md` section 17. | no
67 | section 6, `open:` `D65` | Three or more diverged versions on one conflict. | Resolved (proposed): extra sides append to `sideVersionIds[]`; S31 shows the head against one side at a time, each resolved against the current head, and `conflictOpen` clears when none is left. Rejected three panes and pre-folding the sides. Handed to S31's owner for the drawing. | [P] section 6's writes, which already compare against the head at render; S31 `three-way` state. | no
67 | section 7.1, `open:` | May a repository we create drop the `docs/` prefix. | Resolved (proposed): no, `docs/` in every repository. Rejected a per-origin prefix. | [P] `F242`, `E056`, `A124` in `19-ACCEPTANCE-CRITERIA.md`. | no
67 | section 7.2, `open:` | Does an automatic mirror push on Free spend the 20-push quota. | Resolved (proposed): Free has no automatic GitHub push; each push is pressed by the person and counts. Drive on Free stays automatic. The cadence table is adopted. Rejected automatic Free pushes against the quota. | [P] `F243`; `docs/mvp0/PRODUCT-PLAN.md` section 11; `STORAGE-BENCHMARK.md` section 6.4. | yes, it bounds a promise to Free users
67 | section 15 row | The mirror cadence table. | Closed with the 7.2 item above: adopted as proposed by the benchmark. | [R] `STORAGE-BENCHMARK.md` section 6.4. | yes, a pricing choice per the benchmark
67 | section 7.3, `open:` | The host for the mirror worker and the inbound worker. | Resolved (proposed): one Cloudflare Durable Object per connection, woken by the save and by webhooks, clocked by its alarm; webhook routes stay on Vercel. Quoted Cloudflare's alarm guarantees. Rejected Vercel Cron. | [M] `https://developers.cloudflare.com/durable-objects/api/alarms/`, 18 Sep; [P] ADR-0007. | no
67 | section 8.1, `open:` | The queue's `source` enum has no value for a mirror edit. | Resolved (proposed): add `source: "mirror"` with `externalAuthor` as a display string; S20 treats it as an agent row. Rejected `person` and `agent`. Adoption sits with 21 and S20. | [P] `21-DATA-MODEL.md` section 21.4; section 11 of 67. | no
67 | section 8.3, `open:` | Lengthen `DRIVE_POLL_INTERVAL` now a watch channel exists. | Resolved (proposed): 60 minutes. Arithmetic written in: about 13,201 people at five minutes against about 102,564 at sixty before the daily threshold. Rejected five minutes and no poll. | [O] `python3` over `34-INTEGRATIONS.md` section 8 figures and `STORAGE-BENCHMARK.md` section 2.4. | no
67 | section 8.4, `open:` | The queue has no `kind` for create or delete. | Resolved (proposed): `kind: "splice" \| "create" \| "delete"`, default `splice`, plus `pairId` for a rename accepted whole or not at all; added `pairId` to section 11. Rejected a whole-file splice as a delete. | [P] `21-DATA-MODEL.md` section 21.4. | no
67 | section 10.1, `open:` | A moved upload lives only in Drive, against "our copy is canonical". | Resolved (proposed): the moved upload becomes a link we no longer guarantee, told before the move; checksum-verified copy, link rewrite, logged key delete, a tombstone record per upload; offered only with a Drive connection. Rejected keeping an R2 copy. | [P] D03 in `56-OPEN-DECISIONS.md` section 0; `21-DATA-MODEL.md` section 21.8. | yes, it withdraws a guarantee from stored content
67 | section 13, `open:` | Does a hard deletion in our copy ever delete the mirror file. | Resolved (proposed): never. At trash expiry our mirror record goes and the file stays with the person; the confirmation says so. Corrected the section 13 bullet that said the delete reached the mirror. Rejected deleting the mirror after 30 days. | [P] D03; `37-BACKUP-AND-RECOVERY.md` section 3. | yes, it sets what "delete" promises
67 | section 7.5, `UNVERIFIED:` | Does Drive keep a revision of a foreign edit our write overwrote. | Checked. Partly confirmed: non-head revisions are typically kept 30 days, purgeable earlier past 100 revisions; quoted in. One narrower point stays `UNVERIFIED:` (whether close uploads get separate revisions), needs falsification test 2. | [M] `https://developers.google.com/workspace/drive/api/guides/manage-revisions`, 18 Sep. | no
67 | section 10 table, `UNVERIFIED:` | Race recovery from Drive revisions. | Replaced with the 30-day finding of 7.5. | [M] same page, 18 Sep. | no
67 | section 16, `UNVERIFIED:` | Race recovery in 7.5, revisions guide not opened. | Fixed: the guide was opened; the limit now says documented, not tested. | [M] same page, 18 Sep. | no
67 | section 9.4, `UNVERIFIED:` | The host's request body limit behind `SAVE_INLINE_MAX_BYTES`, not opened. | Checked and confirmed: Vercel caps a function body at 4.5 MB with a 413. Proposed the constant at 3 MiB, since 4 MiB (the shape gate) as base64 exceeds the cap. Also wrote the two other proposed values into the constants table. | [M] `https://vercel.com/docs/functions/limitations`, 18 Sep; [O] `shape-gate.ts:23`, `python3` arithmetic. | no
14 | lines 238 to 320, 15 matches of `open:` | The 15 hits counted as `open:` markers. | Checked each: every one is the prop `open: boolean` in a component's props column, not an open point. Nothing to resolve, text unchanged. | [O] `grep -n "open:" docs/pack/14-COMPONENT-INVENTORY.md`. | no
14 | section 5.2 | `C024` `PasswordLoginForm` against the no-password promise, "recorded, not resolved". | Resolved (proposed): remove it before the first stranger signs in; keep it until then as the allowlisted login's fallback; the row stays, marked withdrawn. Rejected narrowing S01's promise. | [Z] ADR-0009; [O] `LoginScreen.tsx:141`; `24-SERVER-SPEC.md` section 24.7. | no
14 | section 6, "Not established" | The module for 28 components marked `unassigned`, called an architecture decision. | Resolved (proposed): assigned all 28 in the index table and wrote the mapping into section 6; `C144` and `C145` stay unassigned by design. Used module names other files already name (ideas, import, connections, billing, config) and one new one, `queue`. `C096` marked `INFERENCE:`. Rejected a catch-all module. | [O] `grep -c` before 30, after 2; module names from S14, S15, S22, S23, S29, S34 to S38; `AGENTS.md` section 2. | no
ADR-0001 | What would reverse it, `UNVERIFIED:` | A new dialect's share of `.md` volume, not rerun since 29 July. | Partly checked: reran the npm download half for djot, MDX, Markdoc and remark-parse and wrote the figures in; the share of `.md` volume stays `UNVERIFIED:` with a needs (an authenticated GitHub code search). | [O] `curl -s https://api.npmjs.org/downloads/point/last-week/<package>`, 18 Sep. | no
ADR-0003 | Evidence, `UNVERIFIED:` | OpenKnowledge as a CRDT bet, its own pages not opened. | Checked and confirmed from its repository: `yjs` and Hocuspocus dependencies in three packages, and CRDT convergence tests; its home page also says it syncs through git. | [M] `https://openknowledge.ai/`, `https://github.com/inkeep/open-knowledge` (raw package.json files and the git tree API), 18 Sep. | no
ADR-0005 | Two disagreements, `UNVERIFIED:` | Is the 2022 RBI circular still in force. | Checked. False as cited elsewhere: the 2026 E-mandate Framework repeals the 16 June 2022 circular and restates ₹15,000 in paragraph 8(a). Quoted in; named the three pack files that cite the repealed circular. | [M] `https://www.rbi.org.in/Scripts/NotificationUser.aspx?Id=13374&Mode=0`, 18 Sep. | no
ADR-0005 | Two disagreements, `UNVERIFIED:` | Does Razorpay also allow only one attempt. | Checked. False: Razorpay Subscriptions retry automatically from the next day until `halted`, and the card recurring API allows a manual retry every 36 hours. The RBI framework has no attempt rule, so the rule is Stripe's. Fixed the claim, flagged the decision bullet as corrected, narrowed the build rule to "no scheduler of our own", and updated the limits. | [M] `https://razorpay.com/docs/payments/subscriptions/payment-retries/`, `https://razorpay.com/docs/payments/recurring-payments/cards/faqs/`, RBI framework page, 18 Sep. | yes, `CLAUDE.md` lists one attempt as settled
ADR-0006 | Header, `UNVERIFIED:` | The date the projection law and splice-only writing were first adopted. | Checked in git: first written down on 29 July 2026 in commit `5ff90a4`. Replaced the tag, noting git dates the text, not the choice. | [O] `git log --reverse -i -S "splice-only"` and `-S "projection"`. | no
ADR-0009 | Header, `UNVERIFIED:` | The exact day the founders first chose sign-in-first. | Checked in git: first written into the plan on 17 September 2026, commit `7b49f84`. Replaced the tag, noting git dates the text, not the conversation. | [O] `git log --reverse -i -S "Sign in first" -- docs`, and `-S "no captcha"`. | no
ADR-0010 | The tagline test, `UNVERIFIED:` | No sample size for the three-line test. | Treated as a decision. Resolved (proposed): 23 people per line, 69 in all, from a two-proportion power calculation for 80 against 40 percent; the assumed rates marked `INFERENCE:`. Rejected a handful per line. | [O] `python3` power calculation, 18 Sep; `TAGLINE-AND-FMD.md` section 4.1. | no
ADR-0013 | Inventory, domain row, `UNVERIFIED:` | Who holds the domain registration. | Partly checked: registrar GoDaddy, registered 2026-06-24, expires 2029-06-24. The registrant is redacted, so the holder stays `UNVERIFIED:`, needs the GoDaddy account owner. | [M] `https://rdap.org/domain/frontmatter.in`, 18 Sep. | no
ADR-0013 | Inventory, analytics row, `UNVERIFIED:` | Who holds the analytics and error accounts. | Checked: none exist in the code, so nothing to move; each opens in the company's name when added. | [O] `grep` over `package.json` and `src/`. | no
ADR-0013 | Inventory, IP row, `UNVERIFIED:` | Intellectual property agreement between the founders. | Cannot be checked from here; kept `UNVERIFIED:` with a needs naming the founders. | none, a legal document. | yes, only the founders know whether it exists
ADR-0007 | line 62, `open:` | The one `open:` counted in this file. | Checked: it is the tail of the word "reopen:" in "not as reasons to reopen:", not an open point. Nothing to resolve. | [O] `grep -n "open:" docs/pack/adr/ADR-0007-stack.md`. | no
ADR-0007 | What would reverse it, `UNVERIFIED:` | Two irreproducible readings of the Mumbai Firestore storage price. | Partly checked: the pricing page's embedded data pairs Stored Data $0.165 with Document Writes $0.104 per 100,000, so the $0.104 reading is probably the writes row (`INFERENCE:`). The location of that table could not be read, so the tag stays with a needs (calculator or Billing Catalog API at `asia-south1`). | [M] `https://cloud.google.com/firestore/pricing`, 18 Sep. | no
ADR-0011 | Limits, `UNVERIFIED:` | The roadmap's 18 September batch additions marked unverified. | Checked: `50-ROADMAP.md` now calls them appetites, a budget rather than a measurement, and carries no `UNVERIFIED:` tag. Rewrote the bullet to match. | [O] `grep -c UNVERIFIED docs/pack/50-ROADMAP.md` returned 0; `sed -n 94,96p`. | no
ADR-0012 | Evidence table, `UNVERIFIED:` | Drive revision retention. | Checked and replaced: non-head revisions typically kept 30 days, purgeable earlier past 100; quoted beneath the table. | [M] `https://developers.google.com/workspace/drive/api/guides/manage-revisions`, 18 Sep. | no
ADR-0012 | Limits, `UNVERIFIED:` | Does Drive keep a markdown upload byte for byte. | Cannot be checked without a live Drive call; kept `UNVERIFIED:` with a needs naming `T-67-drive-byte-roundtrip`. | none, needs a test Drive. | no
ADR-0015 | Alternatives, `UNVERIFIED:` | No option of two in the 18 September record. | Checked and confirmed: the plan's section 13 lists only the founders' 1 and the market's 3, and the founder record confirms 1. Replaced the tag with the evidence. | [O] `sed -n` on `docs/mvp0/PRODUCT-PLAN.md` section 13 and `SCREEN-CHANGES-2026-09-18.md:305`. | no
ADR-0015 | What would reverse it, `UNVERIFIED:` | No per-session Durable Object cost measurement. | Confirmed absent (`grep` over the pack finds only this file). Cannot be measured before live sessions exist; kept the tag with a needs naming the pilot's billing read. | [O] `grep -rln -i "per-session cost" docs/pack`. | no
ADR-0016 | Chain table row 5, `UNVERIFIED:` | SambaNova's training sentence not opened. | Checked: the licence limits use to providing the service, "and for no other purposes", so gate B is met; but its Free plan now needs a payment method, so the row reads held out of the chain, matching `27`. | [M] `https://sambanova.ai/cloud-end-user-license-agreement`, `https://cloud.sambanova.ai/plans`, 18 Sep. | no
ADR-0016 | Limits, `UNVERIFIED:` | Cerebras's free terms, two readings. | Checked and settled: one offer, a $5 trial for 30 days with the trial rate limits; its FAQ says there is no permanently free tier. Also corrected the limit that said no provider page was re-opened. | [M] `https://inference-docs.cerebras.ai/support/rate-limits`, 18 Sep. | no
