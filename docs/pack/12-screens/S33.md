---
id: S33
title: S33. Over the cap
mode: reference
tier: canonical
status: specified
spec: billing/over-the-cap
verified_against: 0af3c90
updated: 2026-09-18
owner: sagnik
---

# S33. Over the cap

> **Identifiers.** The registers landed in `docs/pack/` while these files were being written.
> Entitlement and event ids below point at the real entries in `53-PRICING-AND-ENTITLEMENTS.md` and
> `55-MEASUREMENT-AND-EVENTS.md`, and an id with no equivalent there is a proposal. Component, error
> and acceptance ids are proposals reserved above each register's high-water mark, re-derived at
> write time as `C124`, `E117` and `A206`. The scheme is `65-CONVENTIONS.md` section 3 with the two
> digits after the letter as the screen number, so this file reserves `C330` to `C339`, `E330` to
> `E339` and `A330` to `A339`. Copy ids are namespaced `K.s33.*`. Decision ids across S27 to S38
> run `D53` to `D92`, above the `D01` to `D15` in `56-OPEN-DECISIONS.md` and above the `D30` to
> `D47` the other screen writers hold.
>
> **`C900` upward is a shared block** for chrome used by several of S27 to S38.
> `14-COMPONENT-INVENTORY.md` numbers shared components sequentially instead, so that block is the
> one thing here that needs reconciling rather than merging.
>
> **Citations.** `docs/mvp0/PRODUCT-PLAN.md` is being edited in parallel and grew by 12 lines while
> this file was written, so it is cited by **section**, which does not rot. Files that are not being
> edited are cited by line, and the source tree is cited at `0af3c90`.

## Purpose

I have hit a limit, everything I already have still works, and here is exactly how to get going
again.

## Entry and exit

**The shape is three sentences: what happened, what still works, what to do.** That is the plan's
own order in section 5, and the screen keeps it.

how a person arrives | which cap tripped
Creating the 51st cloud document | `limits.docs.cloud`
Publishing the 6th page | `limits.pages.published`
Asking for the 11th AI edit in a month | `limits.ai.edits`
Starting a second blueprint in a month | `limits.ai.blueprints`
Uploading past 1 GB, or a file over 5 MB | `limits.uploads.total`, `limits.uploads.file`
Inviting a second live collaborator | `limits.collab.live`
Connecting a second repository, or the 21st push | `limits.github.repos`, `limits.github.pushes`
A Pro plan lapsing back to Free | Whichever cap the account is now above
A founder lowering a limit on S35 | The same, for every account the change moves over the line
A time-boxed exception from S38 expiring | The same, for that one account

how they leave | what happens
Delete or export something | Back to the workspace, under the cap
Use the desktop app | The desktop has no document cap and is named on the screen
Move to Pro | Razorpay's hosted checkout, then back
Wait for the reset | Nothing to do. Monthly allowances reset on a stated date, shown on S29
Dismiss | Back to the workspace. Everything opens, edits and exports

## Anatomy

region | component | what it holds | notes
Dimmed workspace | `C040` | The editor behind, dimmed, not replaced | The person's documents are visibly still there, which is the point
Cap modal | `C330` | The whole screen, at 520 px on the desktop | Drawn as a modal, and as a bottom sheet on the phone
What happened | `C331` | One heading naming the exact cap and the exact number | Not a generic limit-reached line
What still works | `C332` | A short list, each row marked as allowed or not allowed | Two allowed rows and one not-allowed row in the generator
Ways out | `C333` | Three stacked controls: free space, use the desktop, move to Pro | The paid one is last and is the only primary
Downgrade note | `C334` | The fine line stating that a downgraded account meets this same screen and loses nothing | It is the sentence that makes the screen trustworthy
Tree footer count | `C335` | The standing count in the tree, such as 50 of 50 cloud documents | So the cap is visible before it is hit, not only after
Plan comparison | `C908` | Reached from Move to Pro | Shared with S29, which owns it

**The three rows of what still works, exactly as drawn.**

row | state
Open, edit and export every document | allowed
Share and publish what you have | allowed
Create a new cloud document until you are under the cap | not allowed

**Only the third row changes per cap.** The first two are true of every cap in the table above, and
they are the reason this screen is not a wall.

## Data contract

what | direction | port or store | shape | when
Resolved limits | read | `LimitsReader.forAccount()` | the full entitlement set, plan row plus any exception | Before every create, publish, upload, invite and model call
Current counts | read | `UsageLedgerReader.currentPeriod()` | a count per entitlement id | The same moment
Which cap tripped | read | the refusal itself | the entitlement id and both numbers | On the refusal
Reset date | read | `UsageLedgerReader.periodEnd()` | a date | On render, for the monthly caps only
Exception | read | `LimitsReader.forAccount()` | any exception and its expiry | On render, so a granted exception is visible rather than mysterious

**One read path, and this screen is downstream of it.** Section 30 names a single function,
`limitsFor(account)`, and says nothing else in the product reads a cap. This screen reads the
refusal that function produced. It does not do its own arithmetic, because a second calculation is
a second source of truth.

**No write of any kind.** This screen deletes nothing, prunes nothing, archives nothing and queues
nothing. Its only outward action is starting a checkout, which S29 owns.

## Actions

action | trigger | what happens | failure behaviour | event id
Refuse the action | A create, publish, upload, invite or edit past the cap | The action does not happen. Nothing is written. This screen appears | `E330` names the entitlement id in the log. The person sees the screen, not the code | `cap.tripped`
Show the screen | The refusal | What happened, what still works, what to do | `E331` when the limits cannot be read. The action is refused anyway, because refusing is the safe direction | `cap.tripped`
Free some space | The first control | The workspace, with a view sorted so the largest or oldest are first | None. This is navigation | `cap.exit.chosen`
Use the desktop app | The second control | The download page, or the app if it is installed | `E332` when no build exists for the platform, said on the control | `cap.exit.chosen`
Move to Pro | The third control | S29, then Razorpay's hosted checkout | The failures are S29's | `plan.upgrade.started`
Dismiss | Escape, or the backdrop | The workspace. Everything still opens | None | `cap.dismissed`
Continue in idea mode | Already in a blueprint, over the allowance | The standard question set serves, so the blueprint is not abandoned midway | `E333` when no standard set exists for that template | `ai.fallback.used`

**The idea-mode row is a founders' decision, not a nicety.**
`docs/mvp0/SCREEN-CHANGES-2026-09-18.md:361` records that the standard question set is the fallback
when the model layer is degraded **or the person is over their cap**. It is the only behaviour S32
and S33 share.

## States

state | when | what is shown | what the person can do
first-run | The person's first cap, ever | The same screen. Nothing here assumes prior knowledge | Any of the three ways out
empty | n/a | This screen appears because something is full. It has no empty state | n/a
loading | The counts are being read | **Not this screen.** The action is still in flight | Wait
partial | The count is read, the reset date is not | The cap and the ways out. The reset sentence is omitted rather than guessed | Everything except wait for a date nobody knows
offline | No network | The local document still opens and edits. A create that needs the server is refused with the offline reason, not the cap reason | Write locally
unauthorised | The session expired at the moment of the refusal | Sign in over the modal. The blocked action is retried afterwards, not silently completed | Sign in
over-cap | This is that state | See above | See above
AI-unavailable | The chain is down and the person is also over the cap | **S32 wins.** A person who would have been refused by the cap is not told two different stories | Whatever S32 offers
conflict | n/a | A cap produces no document change, so it cannot conflict | n/a
error | The refusal has no entitlement id attached | A generic line naming the action that could not be completed, and the ways out. `UNVERIFIED:` this state should be unreachable once every refusal carries its id | Retry, or go to S29
degraded | A limit was lowered on S35 while the person was working | The same screen, plus one line saying the plan's limit changed rather than the person's usage | Free space, or move to Pro
lapsed-exception | A time-boxed exception from S38 expired | The same screen again, with one line saying the exception has ended | The same three ways out
downgraded | Pro lapsed to Free | The same screen, with the downgrade note carrying the weight: nothing is removed | Read, edit, export, or pay again

## Copy

id | what it carries
`K.s33.heading.<entitlement>` | One heading per cap, each naming the exact limit and the exact number.
`K.s33.still.open` | Opening, editing and exporting every document is still allowed.
`K.s33.still.share` | Sharing and publishing what already exists is still allowed.
`K.s33.blocked.<entitlement>` | The one not-allowed row, per cap.
`K.s33.exit.free` | Delete or export something.
`K.s33.exit.desktop` | Use the desktop app, which has no document cap.
`K.s33.exit.pro` | Move to Pro, with its price marked GST inclusive.
`K.s33.exit.wait` | Wait for the reset, with the date, shown only for a monthly allowance.
`K.s33.downgrade` | The line that a downgraded account meets this same screen and nothing is removed.
`K.s33.lowered` | The line when a founder lowered the limit rather than the person raising their usage.
`K.s33.exception.lapsed` | The line when a granted exception has expired.
`K.s33.offline` | The different line when the refusal is the network rather than the cap.

**Not one number appears as a literal.** Every figure in every string is interpolated from the
resolved entitlement, so raising a cap on S35 changes this screen with no deploy and no copy edit.

## Keyboard and focus

- Focus moves to the heading, so what happened is read before what can be done.
- Tab order: the three ways out in the order drawn, then dismiss.
- Escape dismisses. A cap screen that cannot be dismissed would be a wall, and it is not one.
- Focus returns to the control the person pressed, so a keyboard user is not dropped at the top of
  the document.
- `UNVERIFIED:` no conflict could be checked against `15-INTERACTION-AND-KEYBOARD.md`.

## Responsive

- The modal becomes a bottom sheet with a grab handle. The generator draws exactly that.
- The three ways out stay full width and stacked, in the same order.
- The what-still-works list is never collapsed on the phone, because it is the part that stops the
  screen reading as a wall.
- `[Z]` the phone view takes the same theme treatment as the desktop,
  `docs/mvp0/SCREEN-CHANGES-2026-09-18.md:19`.

## Desktop differences

- **The desktop app has no document cap**, and section 5 says it is named on this screen. On the
  desktop itself, the row that names it is replaced by a line saying documents on disk are not
  counted.
- The caps that do apply on the desktop are the ones that spend our money or our pools: AI edits,
  blueprints, published pages, live collaborators, repositories and pushes.
- So this screen is reachable on the desktop, but never for the document count.

## Acceptance

id | criterion | test id
`A330` | Nothing is ever deleted, archived or hidden for being over a cap. The document count before and after is identical | `T-S33-nothing-deleted`.
`A331` | Every existing document opens, edits and exports while the account is over any cap | `T-S33-everything-still-opens`.
`A332` | Every publish that already exists keeps serving while the account is over the published-pages cap | `T-S33-published-stays-published`.
`A333` | The refused action writes nothing. The 51st document does not exist in any partial form | `T-S33-refusal-writes-nothing`.
`A334` | A Pro account lapsing to Free with 80 documents meets this screen and keeps all 80 | `T-S33-downgrade-keeps-everything`.
`A335` | Lowering a limit on S35 puts the affected accounts into this state and removes nothing from them | `T-S33-lowered-limit-is-not-a-delete`.
`A336` | The heading names the exact cap and the exact number, both interpolated from the entitlement | `T-S33-no-literal-numbers`.
`A337` | Being over the cap and having the chain down renders S32, not this screen | `T-S33-s32-wins`.
`A338` | Over the allowance in idea mode continues on the standard question set rather than abandoning the blueprint | `T-S33-idea-mode-degrades`.
`A339` | A monthly cap shows the reset date and a standing cap does not, because a standing cap has none | `T-S33-reset-only-where-it-exists`.

**`A334` and `A335` are the two that need a red proof**, because both describe a moment where the
tempting implementation is to tidy up. A test that merely asserts the count is unchanged in the
happy path proves nothing about a downgrade path that was never run.

**None of these tests exist at `0af3c90`**, and there is no `src/modules/billing/` either. Verified
with `ls src/modules/`.

## What this screen must never do

- **Never delete anything.** Not the oldest document, not an unused upload, not a draft, not a
  published page, and not after a grace period. Section 13's downgrade row and section 30 both say
  everything stays readable and exportable.
- **Never hide anything.** Read-only is not the same as invisible, and a document that cannot be
  found has effectively been taken.
- **Never stop an export.** The export is the way out, and a cap that blocks it makes the product
  a trap.
- **Never unpublish a page for being over the cap.** A published page has readers who are not the
  account holder.
- **Never block a person from opening what they already have.** The refusal is on creation only.
- **Never show a cap the account is not actually over.** The numbers are read, not assumed.
- **Never blame the person when the limit moved.** A lowered limit gets its own line, because it is
  our change and not their usage.
- **Never show this screen and S32 together.** If the chain is also down, S32 wins, because being
  told two different stories about one failed action is worse than either.
- **Never make it undismissable.** The workspace behind is dimmed, not disabled.

## Open questions

id | question | why it is open
`D73` | Is there a warning before the cap, or only at it? | The tree footer carries a standing count, which is a soft warning. Whether there is a deliberate notice at, say, the 45th document is not specified anywhere
`D74` | What does the free-space view sort by? | The control exists in the generator and the destination does not. Largest first and oldest first give very different advice
`D75` | Does an upload over the per-file size limit get this screen, or a different refusal? | A 6 MB file on Free is not the same event as a full account, and one screen for both would misdescribe it
`D76` | What happens to the 30-day trash when an account is over the document cap? | Section 18 gives uploads a 30-day trash. Whether a trashed document counts against the cap decides whether deleting actually helps, which is the screen's first advice

**What is not specified here.** The plan comparison this screen links to belongs to S29 and is not
restated. The exception mechanism belongs to S38. The cap values belong to S35 and appear here only
as interpolations. Nothing on this screen was checked against a running enforcement path, because
none exists. What would falsify this file: a decision that some class of content may be removed
after a grace period, which every source above currently forbids.
