// SPAWN-GATE: q1=y q2=y q3=y
export const meta = {
  name: 'frontmatter-round4-editors',
  description: 'Full editor-landscape sweep: office suites, IDEs, writing tools, editor frameworks — what to take',
  phases: [{ title: 'Editor landscape', detail: '5 agents across every editor category' }],
}

const SCHEMA = {
  type: 'object',
  required: ['report'],
  properties: {
    report: { type: 'string', description: 'Full structured markdown report, claims tagged [fetched]/[SS]/[inference] with URLs' },
    features_to_take: { type: 'array', items: { type: 'object', required: ['feature', 'source_tool', 'why', 'frontmatter_form'], properties: { feature: { type: 'string' }, source_tool: { type: 'string' }, why: { type: 'string' }, frontmatter_form: { type: 'string', description: 'the concrete form it takes in frontmatter' } } } },
  },
}

const GUARD = `HARD RULES: research only, RULE 4 — no destructive ops, no writes outside $TMPDIR, no outbound messages, never print secrets. Tag claims [fetched] (primary opened) / [SS] (search summary) / [inference]. Allowlisted curl hosts: api.github.com, github.com, raw.githubusercontent.com, registry.npmjs.org, api.npmjs.org. WebSearch works; WebFetch may be gate-refused — fall back gracefully. CONTEXT: frontmatter — markdown-native editor/renderer/AI-protocol product; splice byte-fidelity + degradation cert + custom renders (kanban/decision/calendar) + AI provenance are the moats; simplest-possible editor is the bar; the four complaint patterns to never commit: plugin fatigue, block friction/exit tax, dialect betrayal, subscription resentment. Your job: mine your assigned editor category for FEATURES AND DECISIONS WORTH TAKING — each with the concrete frontmatter form.`

phase('Editor landscape')
const results = await parallel([
  () => agent(`${GUARD}\n\nCATEGORY A — OFFICE/DOCUMENT SUITES (the review-loop benchmark). Tools: Microsoft Word (incl. Copilot integration), Google Docs (incl. Gemini), Apple Pages, LibreOffice Writer, OnlyOffice, Zoho Writer, Dropbox Paper, Slite, Coda/Superhuman Docs. Mine: (1) THE REVIEW LOOP in depth — Word Track Changes vs Google Docs Suggesting mode: exact interaction grammar (how suggestions display, accept/reject granularity, comment anchoring behavior when text changes, resolved-thread UX, @-mentions, assignment); what 40 years of office software settled that markdown tools never absorbed; (2) version history UX (Docs' named versions, Word's compare-documents); (3) outline/navigation panes; (4) templates galleries and styles systems (styles = the anti-markdown — what did they get right anyway?); (5) offline models (Docs offline, Word autosave-to-cloud tension); (6) what users HATE (Word's paste chaos, Docs' performance at scale, format lock-in) — the anti-features; (7) how Copilot/Gemini surface AI edits IN-DOCUMENT (the AI-provenance prior art in mainstream tools). DELIVER: the feature-extraction table (feature / source / why / frontmatter form) with special depth on the suggesting-mode interaction grammar as the spec for frontmatter's N5 suggest mode. Return via structured output.`, { label: 'office-suites', phase: 'Editor landscape', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nCATEGORY B — CODE EDITORS AND IDEs (the power-feature quarry). Tools: VS Code, JetBrains IDEs, Sublime Text, Zed, Neovim/Vim, Nova, Helix. Mine: (1) the features that made VS Code win (command palette, ubiquitous fuzzy-everything, extensions model AND its costs, settings-as-text, workspaces, integrated terminal, zen mode); (2) JetBrains depth: refactoring (rename-symbol = the #1 unbuilt markdown feature per our research — how do IDEs make rename safe?), LOCAL HISTORY (per-file automatic history independent of VCS — huge for trust), structural search; (3) Sublime: goto-anything, multi-cursor, the speed bar; (4) Zed: performance architecture, native collab (CRDT), AI integration choices; (5) modal editing (vim) adoption reality in prosumer tools; (6) LSP as a pattern: what marksman/markdown-LSP already does, what an editor gets free by speaking LSP; (7) format-on-save — the DANGER pattern (dialect betrayal); how IDEs handle formatter conflicts (editorconfig, prettier opt-in) — the discipline frontmatter's write-path already enforces; (8) diagnostics/problems-panel UX as the model for doc diagnostics (broken links, staleness, cert warnings). DELIVER: feature-extraction table; special depth on rename-safety mechanics and local-history UX. Return via structured output.`, { label: 'ides', phase: 'Editor landscape', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nCATEGORY C — AI-NATIVE EDITORS (the freshest patterns). Tools: Cursor, Windsurf, Zed AI, GitHub Copilot in VS Code, Claude Code's own editor-adjacent UX, Notion AI, Craft AI, Lex.page, Type.ai, Sudowrite, Jasper. Mine: (1) inline-diff review UX — how Cursor/Windsurf present AI edits for accept/reject (per-hunk? per-file? streaming?), what users praise/hate about each; (2) rules files (.cursorrules/.mdc/CLAUDE.md) as USER-FACING feature — how tools surface/edit them; (3) chat-vs-inline-vs-ghost-text: which AI surfaces earn usage, which get disabled (find real usage/complaint signals); (4) Lex/Type (AI writing editors): what an AI-first WRITING editor does differently, their traction reality; (5) AI provenance in these tools — does ANY show which bytes AI wrote after acceptance? (our category-first claim needs this checked in the AI-editor category specifically); (6) pricing patterns for AI editors (Cursor $20, Windsurf, Lex) and what usage limits look like; (7) the failure modes: AI slop complaints, disabled-by-default demands, trust erosion patterns. DELIVER: feature-extraction table; special depth on the accept/reject interaction grammar (feeds our implicit-telemetry design) and the provenance-absence check. Return via structured output.`, { label: 'ai-editors', phase: 'Editor landscape', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nCATEGORY D — WRITING TOOLS + LONG-FORM (the craft quarry). Tools: Scrivener, Ulysses, Highland 2, Drafts, Bear (editor specifics), iA Writer (focus/syntax modes), Typora (WYSIWYG specifics), FocusWriter, Ommwriter, plus Grammarly/ProWritingAid/LanguageTool as the LINTING-UX benchmark. Mine: (1) Scrivener deep: the binder (project-as-tree-of-fragments), corkboard, COMPILE (many fragments -> one output with per-target formatting — this is literally our custom-render/profile thesis in 20-year-old software), snapshots, composition targets/deadlines; what Scrivener users say is irreplaceable vs bloated; (2) iA's focus/syntax/typewriter modes — the minimal-chrome craft; (3) Drafts' capture-first model (actions, append-to); (4) Typora's WYSIWYG mechanics: what breaks, what feels magic (source-view toggle behavior); (5) Grammarly-class linting UX: inline underlines vs sidebar cards, acceptance rates, annoyance patterns — the UX spec for our AEO/quality linter; (6) writing-stats/goals features (streaks, word targets) — engagement without gamification slop; (7) distraction-free modes across all. DELIVER: feature-extraction table; special depth on Scrivener's compile as prior art for profiles and Grammarly's linting UX as the spec for ours. Return via structured output.`, { label: 'writing-tools', phase: 'Editor landscape', schema: SCHEMA }),

  () => agent(`${GUARD}\n\nCATEGORY E — EDITOR FRAMEWORKS + BLOCK EDITORS (the substrate layer). Subjects: ProseMirror, TipTap, Lexical (Meta), Slate, BlockNote, Editor.js, Milkdown, CodeMirror 6 (our engine), Monaco; plus Notion/Craft block models as shipped block-editor UX. Mine: (1) the round-trip problem as THESE frameworks see it: Milkdown/TipTap markdown serialization fidelity (known lossy? issues?), ProseMirror's schema model vs markdown's flatness — evidence for why we stayed CodeMirror-single-engine; (2) what block editors give users that plain text doesn't (drag handles, slash menus, block-level comments) and the cost (block friction — our complaint corpus); which of those affordances are implementable OVER plain markdown without a block model (slash commands: shipped; drag-to-reorder headings/blocks: splice-feasible?); (3) collab plumbing in each (Yjs bindings everywhere — who ships them in production markdown contexts); (4) CodeMirror 6 ecosystem: what exists that we haven't used (decorations patterns, widgets, panels — plugins worth stealing patterns from); (5) mobile editing reality for each framework (the mobile-markdown-editing pain); (6) accessibility state of block vs text editors. DELIVER: feature-extraction table; special depth on which block-editor affordances port to plain-markdown-over-CM6 and which structurally cannot. Return via structured output.`, { label: 'frameworks', phase: 'Editor landscape', schema: SCHEMA }),
])

const ok = results.filter(Boolean)
log(`round 4: ${ok.length}/5 agents returned`)
return { count: ok.length, reports: ok }