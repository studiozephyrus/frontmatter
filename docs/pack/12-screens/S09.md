---
id: S09
title: S09. Flow view
mode: reference
tier: canonical
status: specified
spec: editor/flow-view
verified_against: 0af3c90
updated: 2026-09-18
owner: sagnik
---

# S09. Flow view

> **Deferred on 18 September.** `docs/mvp0/SCREEN-CHANGES-2026-09-18.md:75` records the founders'
> instruction in full: noted, not built now. This file is written so that whoever builds it later
> does not have to rediscover it, and it must not be scheduled into phase A on the strength of
> existing.

> **Where the ids come from.** Every `C`, `E`, `A` and `K` id below lives in its register:
> `14-COMPONENT-INVENTORY.md`, `17-ERROR-AND-REFUSAL-CATALOGUE.md`, `19-ACCEPTANCE-CRITERIA.md` and
> `16-COPY-DECK.md`. The ids this screen first proposed were reconciled into those registers on 18
> September 2026. The old-to-new mapping is in `tools/error-map.json`, `tools/component-map.json`
> and `tools/acceptance-map.json`, and `tools/validate-pack.py` fails if any id here has no home.

## Purpose

Read a long procedural document as a board of phases and steps, without adding one character of
new syntax to the file.

## The one thing that makes this cheap

**Flow view invents no format.** The plan's section 20 lists it with no version field and no unknown
field rule, because its conventions are ordinary headings and lists. A phase is an H2, a step is an
H3, and the tag is a bracketed first word.

That is why the whole screen is a projection and why deferring it costs nothing at the file level.
A document written today already reads correctly in this view whenever it ships.

## Entry and exit

Direction | Route or control | Goes to | Notes
Arrive | View as, set to Flow, in `C097` | this layout, same file | a view, not a route
Arrive | a document whose front matter asks for it | the same | a preference, not a format
Leave, another view | `C097` | Page, Slides, Mind map, Kanban or Outline | all six are projections of the same bytes
Leave, edit a step | click a step card | the caret in that H3's source, in Edit mode | the source is always one click away
Leave, a reference | click the reference line on a step | the linked document, if the link resolves | `E042` when it does not

## Anatomy

Region | Component | What it holds | Notes
View selector | `C097` | Six views, each with one line of what it does | `.pop` with `.mi.row2`; lives on `C077`
Flow board | `C098` | The phases, side by side, scrolling sideways | `.flow`
Phase column | `C098` | A title, a meta line, and one line of what the phase is for | `.col` and `.ch`
Step card | `C098` | A name, an optional tag badge, a body and a reference | `.step`, `.st`, `.sb`, `.sr`
Tag badge | `C064` | The bracketed first word, upper case | `.b` with a per-tag class
Lane legend | `C098` | The colour key, and the convention in one line | `.legend`
Reference line | `C098` | The trailing line of a step, as its source | `.sr`
Counts readout | `C133` | Phases and steps, in the toolbar and the tree foot | the same two numbers in both places
Phone view chips | `C063` | The six views as a scrolling chip row | replaces the menu at phone width
Phone board | `C098` | The same board, fewer columns in view | `.flow` inside the phone frame

### The reading rules

These four rules are the whole parser. A document that follows them reads as a flow; one that does
not still reads as a document.

Element | Becomes | If it is missing
An H2 | a phase column | with no H2 at all, `E505`, and the view offers Page instead
An H3 under it | a step card in that column | a phase with no steps renders as an empty column, which is legible and correct
A bracketed first word in the step body | the tag badge on that step | the step renders with no badge
The step's trailing line | the reference line | the step renders with no reference

- **Nothing here is a new syntax.** A bracketed first word is ordinary text, and a trailing line is
  ordinary text.
- The convention is printed on the screen, in `C098`, so a person can make their document read
  better without opening documentation.
- **An H3 before any H2 is a step with no phase.** It is `E506`, and it renders in a leading column
  named for the document rather than being dropped.

### `[X]` Two colour vocabularies, and they do not agree

Reading `docs/mvp0/screens/gen.mjs` at `0af3c90`:

Where | Values
The legend, `C098` | Data, Signal, Portfolio, Prediction, each with a colour
The badges drawn on steps, `C064` | GATE, ALARM, IO, LOOP, HARD, NEW

The legend keys four lanes. The badges carry six different words. Nothing in the generator maps one
set onto the other, so a reader cannot tell what a badge's colour means from the legend beside it.
See D25. This file specifies the parser and leaves the vocabulary open, because inventing a mapping
here would be exactly the guess the product exists to refuse.

## Data contract

What | Direction | Port or store | Shape | When
Open file bytes | in | `DocumentReader.open(path)` | `{bytes, version, headHash}` | on mount
Phase and step tree | in | derived in the client from the bytes | headings with their byte ranges | on every parse
Counts | in | derived from the same tree | two numbers | on every parse
Reference targets | in | `GraphReader.resolve(link)` | a path, or nothing | on render, per reference
Chosen view | out | account preferences, per document | one of six | on change
An edit | out | `DocumentWriter.splice()` | the step's byte range | only from the editor, never from the board

- **The board is read-only.** Dragging a card does not reorder the document, and this file does not
  specify a drag. See D26.
- Nothing is stored about the board. Close it, reopen it, and it is rebuilt from the bytes.
- No model is called. The view is deterministic.

## Actions

Action | Trigger | What happens | Failure behaviour | Event id
Open the view menu | View as, in `C077` | `C097` | none | `view.menu.opened`
Choose Flow | `C097` | reprojects the document as a board | no H2 headings `E505`; too large to draw `E504`; unavailable at this width `E805` | `view.changed`
Scroll sideways | arrow keys, trackpad, or the scrollbar | moves across the phases | none | `view.flow.scrolled`
Open a step's source | click a step card | Edit mode, caret in that H3 | the range moved under it `E027` | `doc.opened`
Follow a reference | click `C098` | the linked document | the link resolves to nothing `E042` | `doc.opened`
Change view | `C097` again | any of the other five | `E805` at phone width for some | `view.changed`

## States

State | When | What is shown | What the person can do
first-run | the first time Flow is chosen | the board, with the legend's convention line | read, scroll, click into a step
empty | the document has no H2 | `E505` and an offer of Page view | switch view, or add a heading
loading | bytes in flight | the toolbar and a reserved board area | switch view
partial | parsed, references unresolved | the board, with reference lines plain rather than linked | read; links resolve as they land
offline | no network | the board renders from the open bytes | read; unresolved references stay plain
unauthorised | the role can read | the board, read-only, which it is for everyone | read, export
over-cap | not reachable here | n/a; a view costs no credits | n/a
AI-unavailable | the chain is down | unchanged; this screen calls no model | everything works
conflict | the document has an unresolved conflict | a marker on the tab, S31 on opening | resolve on S31
error | a step could not be placed | that step in a leading column, with `E506` beside it | fix the heading levels
degraded | the document is very large | the first phases drawn, the rest on demand, with `E504` naming the limit | read, or switch to Outline

## Copy

`K.s09.view.page`, `K.s09.view.flow`, `K.s09.view.slides`, `K.s09.view.mindmap`,
`K.s09.view.kanban`, `K.s09.view.outline`, and a `.sub` for each, `K.s09.convention`,
`K.s09.legend`, `K.s09.counts`, `K.s09.nophases`, `K.s09.orphanstep`, `K.s09.toolarge`,
`K.s09.badref`, `K.s09.notatthiswidth`.

`K.s09.convention` is the sentence in the legend that states the four reading rules. It is the only
documentation this view needs, and it is on the screen.

## Keyboard and focus

- **Left and right arrows scroll the board sideways.** That is stated in the legend, because a
  sideways board is not obviously keyboard-reachable.
- Tab moves between step cards in reading order: down a column, then to the next column.
- Enter on a focused step opens its source in Edit mode.
- The view menu opens from the toolbar and closes on Escape.
- No shortcut is registered by this screen. The full map is `15-INTERACTION-AND-KEYBOARD.md`.

## Responsive

- The menu becomes a scrolling chip row, `C063`, with the current view first and selected.
- Fewer columns are in view, and the board still scrolls sideways.
- A phase column takes most of the width, so one phase at a time is readable rather than four being
  unreadable.
- **Kanban and Split are the two views that do not work at phone width.** Flow does, because a
  sideways board and a phone are compatible.

## Desktop differences

None.

## Acceptance

Id | What it asserts | Test id
A561 | An H2 becomes a phase and an H3 becomes a step, with no other syntax required | T-S09-01
A044 | The view writes nothing to the file | T-S09-02
A562 | A document with no H2 offers Page view and names the reason | T-S09-03
A563 | An H3 before any H2 renders in a leading column rather than being dropped | T-S09-04
A564 | A bracketed first word becomes a badge, and a step without one renders without a badge | T-S09-05
A565 | Left and right arrows scroll the board | T-S09-06
A566 | The phase and step counts in the toolbar and in the tree foot are the same two numbers | T-S09-07
A567 | An unresolved reference renders as plain text, never as a broken link | T-S09-08
A044 | The same document round-trips byte for byte after being opened in Flow and closed | T-S09-09
A568 | Every badge colour drawn is explained by the legend on the same screen | T-S09-10

A568 fails today against the drawn example, because the legend and the badges use different
vocabularies. That is the point of the criterion, and D25 is the decision that closes it.

## What this screen must never do

- **Never invent a syntax.** The moment this view needs a marker in the file, it stops being free
  and becomes a format with a version, an unknown-field rule and a fixture.
- **Never write to the document from the board.**
- **Never drop a step it cannot place.** Put it in a leading column and say why.
- **Never show a badge whose colour the legend does not explain.**
- **Never persist board state.** The board is a projection and reopening rebuilds it.
- **Never present a heading structure as wrong.** A document that does not read well as a flow is a
  document, not an error.
- **Never use an emoji as a tag badge.**
- **Never be scheduled into phase A on the strength of this file existing.** The founders deferred
  it.

## Open questions

Id | Question | Where it came from
D25 | What is the tag vocabulary, and how do badges map onto the legend's lanes? | `docs/mvp0/screens/gen.mjs` draws four legend lanes and six badge words with no mapping between them
D26 | Is the board ever writable, by dragging a step between phases? | no source says so; this file specifies read-only, and a drag would mean moving a heading and its body as a splice
D27 | Which document, if any, opens in Flow by default? | the entry table allows a front matter preference and no source states the key

## Limits of this file

- **Not assessed:** the other five views. Slides, Mind map, Kanban and Outline are named in the
  selector and specified elsewhere.
- **Not assessed:** the drawing budget past which a large document degrades. `E504` names the state
  without naming a number, because there is no measured one.
- **Could not be verified:** that the founders' flow site, which the plan names as the reference,
  uses the same four reading rules. It was not opened in this session. `UNVERIFIED:`
- **What would falsify this file:** a decision that the board is writable, which would make it a
  second editing surface rather than a projection, and would pull the projection law into it.
