---
title: The final set. Sixteen product questions, and nothing else
date: 2026-09-14
time: 03:01 IST
for: Sagnik and Amit
status: this replaces the fifteen and folds the 67 cards on the site into it. Product only. Answer by number. The final plan is written from these.
---

# The final set

Sixteen questions. Each one folds several cards from the site, and the folded ids are listed so
nothing is lost. Each carries my position in one line and the evidence in one line. Confirm,
overrule, or write your own. `unknown` is allowed and is better than a guess.

The legal clocks, the funding, the name registers and the research process are not here. They
are real and they are on the site. This set is the product.

---

## What it is

**1. The sentence.**
*frontmatter is where you write, review and keep current the files your agents obey.*
Evidence: 60,000 projects use `AGENTS.md`, 77 commits to one file in `openai/codex`, two files
in LibreChat drifted 3x apart, and your own `CLAUDE.md` is 10,033 words. Spec Kit and Kiro
generate and leave.
*Folds `P1`, `P2`.*
**Your answer:**

**2. The buyer.**
The team running coding agents, not the individual. The individual has one file and will not
pay. The team has 88 and cannot say which one governs a path. Above them, the organisation,
because Claude Code documents an org-wide `CLAUDE.md` managed by IT with no tooling around it.
*Folds `MK4`.*
**Your answer:**

## What ships first

**3. Where it opens, and on what surface.**
The web editor at `/`, no account until the first save or share. One core. The shell frozen
where it is, new work into the engine and one surface. The Mac app, whose .dmg was built on 27
May, becomes the desktop surface later from the same tree.
Evidence: dillinger, StackEdit and Excalidraw show zero sign-in words and an editor on arrival;
our home page is the login screen; the gate is three lines and the editor never checks it.
*Folds `FF1`, `FF3`, `FF8`, `AC20`, `PL12`.*
**Your answer:**

**4. The first thirty seconds.**
A paste target and one sample document that turns into a kit. Folder first, sandbox second, the
read path split from the write path. Four templates as files, not prompts.
Evidence: an empty editor demonstrates nothing a text area does not; the sample shows the part
that is different and costs a document rather than a feature.
*Folds `FL21`, `FL12`, `F27`.*
**Your answer:**

**5. The editor chrome, as a block.**
Three modes, Live, Edit and Read, and cut Split. The AI prompt opens at the cursor. Tab indents,
with Escape then Tab to leave. Dark mode ships with dark renders of the nine screens. Tokens
reconciled to the design system before the pilot and gated in CI. Three AI verbs on the rail,
the rest behind the palette. One switch hides every AI surface. Bookmarks and graph demoted to
the palette. The table editor and ghost text deleted this week, because each refutes a claim
the pitch makes. Four concepts, nine controls, displace to fit.
Evidence: each is a craft call with a recommendation on its card. I am asking you to confirm
them as one block rather than ten cards, and to name any single one you want changed.
*Folds `D8`, `D7`, `D15`, `D21`, `D11`, `FL3`, `F8`, `F9`, `F18`, `F21`, `P15`.*
**Your answer, and any exceptions:**

## What makes it different

**6. The instruction-file view is Phase 1, and it is the point.**
Point it at a repository. Every `AGENTS.md`, `CLAUDE.md` and rules file. Which one wins for a
path. Each file's size against the point the vendor says it stops being obeyed. What an agent
changed that no person has read, grouped by path, read-only. Comments live in a separate file
with CriticMarkup export on demand. Writing inside the document body waits until after the
pilot, and only for delimited spans.
Evidence: the strongest signal found all week and the one thing none of the four competitors
does.
*Folds `F35`, `F26`, `F13`, `E21`.*
**Your answer:**

**7. "Does this read like you" ships as a feature.**
The writing gate, measured against 2,710 dictation entries and 196,522 of your own words,
runs on a document and names the tell. It exists. No editor has it.
*Card `F39`, added 2026-09-14.*
**Your answer:**

**8. What ships from the AIOS.**
Three things, as templates: the learned-rule format, the file gate, the skill file shape. The
orchestrator, the 128 skills and the 156 scripts stay yours.
Evidence: 43 of 128 skills are about running the system itself and the preference log it
learns from holds zero pairs. It is the best thing you built and it cannot be installed in a
day. I know this is the one you feel most about; say so if I have it wrong.
*Card `P26`, added 2026-09-14. Touches `FF8`, `P15`.*
**Your answer:**

## What we promise

**9. Nothing leaves the machine in Phase 0.**
The browser fetches the repository tarball and unzips it in the page. The server PDF route goes
and the browser print path that already exists gets wired. Icons inline as SVG, export fonts
embedded, and the build stops fetching Google Sans at build time. The search index is built in
a worker and cached locally. The GitHub App asks for three permissions and never an
organisation one. `firestore.rules` deleted; zero document bytes as an operating rule.
Evidence: two of five exports send the document to us today, the build itself failed inside a
network sandbox on `fonts.googleapis.com`, and the print function at `export-doc.ts:130` is
imported by nothing.
*Folds `AC1`, `AC14`, `D13`, `F25`, `AC3`, `L1`.*
**Your answer:**

**10. Whose key.**
The user's own key, a hard gate before any stranger reaches an AI route. On the web it lives in
session memory; on desktop in the OS keychain. No local model.
Evidence: nothing in the tools checked offers generation on a key the user holds, and it keeps
the privacy claim exact with no metering to build.
*Folds `F10`, `AC13`.*
**Your answer:**

**11. Sharing.**
A link somebody reads and comments on, with suggestions the owner accepts or refuses. Not live
editing. The shipped public-share route disabled in the build and decided in week 2.
Publishing, when it returns, gated to the founder with noindex and system slugs. A small
unlinked wordmark on exports, off by default.
Evidence: live editing reopens the sync decision this repo settled against CRDTs and competes
with Google Docs on its strongest axis; a kit is reviewed asynchronously by nature.
*Folds `F36`, `F19`, `L6`, `G12`.*
**Your answer:**

## The kit

**12. The kit, and its check.**
Generation ships in Phase 2 as "start from here", never as the claim, and only after the
founder test notes are attached and twenty kits have been made by hand. A names-and-claims
pass across the files runs before the link is made, and the user sees the report.
Evidence: any model writes 33 files; a kit whose files disagree is worse than none, because the
agent builds the contradiction silently. The check is the half a competitor cannot copy from a
prompt.
*Folds `F34`, `F5`.*
**Your answer:**

## What it costs

**13. Free and paid.**
Free: the editor, paste, export, local only, uncapped. Pro, around $6 to $8: storage and sync,
share a link, your own key, the kit, templates. Team, around $15 to $20 a seat: the
instruction-file view across a repo, who changed what, review state on the files agents obey.
Enterprise unpriced until somebody asks. Hosted AI, if ever, metered in actions with a daily
dollar breaker. Two tiers on the page at launch, Max deleted until someone asks for it.
Evidence: Obsidian gives the editor away and charges for Sync and Publish; Kiro's Pro row is
$20 per user; nobody in this market charges for an editor.
*Folds `PR24`, `PR1`, `PR21`, `PR7`, `PR10`.*
**Your answer:**

## The floor

**14. What has to be true before a stranger opens a file.**
Patch the branch that refuses a flush-left list, red-proof it first, and pre-commit to the
structural fix if the residual stays above ten. CI lands in week one with a deliberate red run.
Non-English correctness and text scaling in scope. The six fix-first items start now; the rest
split into demo-blockers and ship-blockers.
Evidence: the editor refuses most real vaults today, and no automated check runs on any push.
*Folds `E1`, `E12`, `E33`, `PL3`.*
**Your answer:**

**15. What "the pilot passed" means.**
Three of ten name the problem unprompted. Two still using at day 30. A booked call from
someone who was not asked. Agreed before it starts, not after.
*Folds `PL5`.*
**Your answer:**

## The name

**16. The name.**
"frontmatter" is taken in the VS Code store your buyer uses. Keep it bare, qualify it, or coin
a new one and keep "frontmatter" as the word for the block we edit. It goes on every screen and
every export, so it is due before Phase 0 ships.
My position is coin. It is also the one decision here I would not make for you.
*Folds `N1`.*
**Your answer:**

---

## Not product, still needed, three lines

Who is building and at what rate (`B1`). How the build is funded (`B2`). The twenty names
(`G15`). These were questions 1, 2 and 5 in the previous file and they set every date in the
plan. Answer them there or here.

---

Sixteen answers and three facts, and I write the plan from which the first commit is obvious.
